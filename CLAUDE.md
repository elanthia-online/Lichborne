# CLAUDE.md

AI-facing project guide. Loaded into every Claude session for this repo — keep tight, keep operational, keep current. **If you make a non-trivial architectural change, update this file.** The goal is that a fresh session can pick up the project without re-deriving its conventions from the code.

This file is for *behavior* (what to do / not do, where to look, what not to violate). For *how things work*, link into [DESIGN.md](DESIGN.md). For *what changed*, see [Tracker.md](Tracker.md).

---

## What this is

Lichborne is a DragonRealms (text MUD) game client. Electron + React + TypeScript. Connects via Lich5 (recommended), direct SGE login, or by ATTACHING to an already-running detachable Lich (v0.19.4 — see the Attach mode section). **Cross-platform as of v0.18.0: Windows x64 (stable), Linux x64 AppImage (beta), macOS arm64 (beta, UNSIGNED — deliberate free-project decision: no Apple Developer account, so Mac users need a one-time unquarantine and get NO auto-update; the updater is gated off on darwin in main.ts; revisit the $99/yr cert only if Mac demand shows. **B238 correction — do NOT tell a Mac user to expect "Open Anyway":** on Apple Silicon the real first-launch message is *"Lichborne is damaged and can't be opened. You should move it to the Trash."*, which has no Open Anyway button and instructs them to delete a good download. The app is ad-hoc signed in [build/afterPack.cjs](build/afterPack.cjs) because arm64 refuses an unsigned binary outright, and the documented user step is `xattr -cr /Applications/Lichborne.app`).** Single-developer project with a small, reachable tester pool. Platform rules that flow from this: gate platform-specific behavior on `process.platform` (main) / `window.api.platform` via `IS_MAC`/`IS_WINDOWS` in [lichSettings.ts](src/renderer/lichSettings.ts) (renderer); Linux/Mac Lich paths are `~`-relative and MAIN expands them at every consumption point via `expandHome` ([homePath.ts](src/main/homePath.ts) — launch, lich.db3, lichDirFrom, discovery; a new lichPath/rubyPath consumer in main MUST expandHome); never resolve bare `ruby` from PATH (GUI apps launched from Finder/dock lack shell PATH — rbenv shims must be probed as explicit paths); Mac Desktop probing only behind the explicit Auto-detect (`probeDesktop` — macOS privacy prompt); meta/Cmd chords are additive on Mac and meta chords are never macro-bindable (`formatKeyCombo` bails on metaKey); on a Mac, Option chords are named from the PHYSICAL key (`e.code` → the US character) because Option rewrites `e.key` into a symbol — `formatKeyCombo`/`matchKeyCombo` take a `{ mac }` option rather than reading the platform themselves, so macros.ts stays harness-bundleable (B347, pitfall #143).

**Product position (DESIGN.md §1, §24):** **display and configuration layer over Lich**, not a Lich-replacement. Lich owns automation (scripts, variables, triggers, repository). Lichborne owns rendering, themes, the panel system, profile portability, and surfacing Lich state. **Push back on requests that head toward duplicating Lich functionality** — say so before implementing. **Lich-first nuance (Principle #2):** Lich is the optimal route; the native automation layer (highlights / triggers / macros / aliases / mutes / substitutes) is a deliberate *backwards-compat regression path* for direct-SGE (no-Lich) players, NOT a Frostbite/Genie-style scripting engine — finite and GUI-configured by design.

---

## Principles (rules of the road)

1. **YAML is truth for user state. localStorage is a working copy.** New per-character settings need only a `scopedKey(character, ...)` write + `saveProfile()` call. Shared settings live in `_shared.yaml` and need typed entries in `SharedProfile` + `buildSharedProfile` + `importSharedProfile`.
2. **Lich-first, with a backwards-compatible direct-connect path.** Lich is the *optimal, recommended* way to play — it owns the heavy automation and unlocks the features that need it (maps, spell timers, variables, scripts, repository). But a player can connect **directly to the game (SGE, no Lich)**, and Lichborne must stay genuinely usable that way: every feature either works without Lich or **degrades gracefully** to a native fallback — never a hard "you need Lich" wall (a Lich-optimal feature with a *degraded* no-Lich mode is fine; full parity is NOT required). Lichborne's **native automation (highlights, triggers, macros, aliases, mutes/substitutes) IS that backwards-compat regression layer** — and a GUI convenience for non-scripters. The line we hold: that native layer is **finite and GUI-configured — we do NOT reinvent a scripting engine** (Frostbite's embedded Ruby, Genie's `#command` language); heavy/conditional automation stays Lich's job — *surface* it, don't *host* a script runtime. Lich integration is additive on top. This is the Lich analogue of §32's "AI enhances, never gates" — an optional power tier over a working baseline. (Full articulation: DESIGN §24.1; sharpened 2026-06-09 from the prior "Lich is optional but recommended / every feature must work without Lich" — graceful degradation replaces the parity absolute.)
3. **No silent data loss.** If a file can't be processed (future-version YAML, malformed XML), log it and **preserve it on disk**. Never overwrite content the code doesn't understand.
4. **Theme any new UI — and do a theme pass on every new feature. Dialogs additionally wear the ONE canonical modal chrome (the About Lichborne look) via the `--modal-*` tokens — see UX/UI polish standard #10.** All colors come from CSS custom properties in `themes.ts`; use `color-mix(in srgb, var(--x) N%, transparent)` for tints. When you add UI, audit its CSS: every surface / text / border color must be a `var(--…)`, and **eyeball it on a light theme** (Classic Light, Ivory, Mist, Parchment) — the `--bg-raised/sunken/base/hover` scale is spread out on dark themes but compressed near-white on light ones, so a literal that looks fine in the default dark theme can vanish on a light one (this is the single most-recurring bug class here: pitfalls #34, #55; B108 / B112 / B140 / B141 were ALL this). **The only color literals that are acceptable** (do NOT try to theme these): dark **drop-shadows** under menus/popovers (`rgba(0,0,0,.5)` — dark shadows read fine on light themes too); **text-halos over a *saturated* fill** (white text + `rgba(0,0,0,.9)` stroke on vital / exp bars — works on every theme precisely because the fill is always saturated, e.g. `.vital-text`, `.exp-rxp-bar-text`); and `var(--x, #fallback)` **fallbacks** (the fallback never fires since `darkBase` is the floor every theme merges over). Anything else hardcoded is a light-theme bug waiting to happen — let the cascade do the work. The v0.10.0 top-chrome features (app-bar, More menu, compact vitals, active-glow) were built/audited to this bar; the native Electron menu is the deliberate exception (Win32-native, not themeable) — the About dialog USED to be one too but was converted to a themed modal in v0.17.0.
5. **Stream IDs preserve case end-to-end.** A Lich script that pushes to `LichScripts` lands in a panel called `LichScripts`. The `NEVER_DISCOVER` filter is the only case-insensitive check.
6. **Per-session everything.** Every IPC payload carries `sessionId`; every listener filters by it; every per-character state lives under `lichborne.{character}.{suffix}`. Cross-session bleed is the most common bug class.
7. **Fix root causes, not symptoms.** Past root-cause fixes (B14, B17, B20, B33, B36, B53, etc.) left inline comments explaining the current shape of the code. Read them before patching nearby logic.
8. **Ship reversibly.** Bold changes welcome (v0.6.3 maps rewrite, login redesign). Pair them with migration paths (profile schema versioning, NSIS rescue hook) so a bad rev never costs a tester their data.
9. **Wire every UI-manipulating setting to every surface it should affect — defining it isn't enough, and "new feature" means "make the settings reach it."** This is as load-bearing as theming (Principle #4): a setting that silently doesn't apply to a new surface is a bug. **Font size / line height:** game-area UI scales ONLY if it uses `var(--game-font-size)` / `var(--game-line-height)` — directly, or via `em` inheriting from a container anchored to those vars. **`rem` does NOT scale** — it's anchored to the html root (16px), not the per-character setting; never use `rem` for game-area text (pitfall #45). It's exactly why the icon bar, vitals bar, and Mode button silently stopped tracking the font setting. When you add a game-area strip/panel/button, **anchor its container to `font-size: var(--game-font-size)` and size children (and their heights/paddings) in `em`** so the whole thing scales without clipping — copy how `.panel-frame-body` ([panel-frame.css](src/renderer/styles/panel-frame.css)) and the panel roots in [panels.css](src/renderer/styles/panels.css) do it. **Accessibility:** high-contrast, color-blind, large-print, and epilepsy-safe are applied by `applySettingsToDOM` (re-applied after every theme write via the hook — pitfall #33); new UI inherits them only if it draws from the theme/overlay vars. **The check (do it for every new surface):** toggle each relevant setting — font size, line height, high contrast, color blind, large print, epilepsy-safe — and confirm the surface actually responds. App-chrome modals MAY deliberately opt out of game-font scaling (they have their own sizing), but that's a conscious call, not an accident.

10. **Evidence over guesses — and a guess must be NAMED as one, not slipped in as fact. This is not just for bug-hunting; it governs ALL work** (features, refactors, answering a question, proposing a design, "this is safe because…"). Every claim about how the code behaves comes from reading the actual file *this session* (the Iron rule, generalized) and every decision rests on a chain of code-confirmed links — see "Investigating a reported bug" for the discipline; it applies the same whether or not there's a tester report. When grounding *is* available, get it before acting: grep + read the lines, trace the path end-to-end, simulate the condition (a `tmp-*` repro / a harness), or add a `[diag]` log and reproduce — don't pattern-match a plausible answer onto a symptom (the v0.14.1 macro "fix" did exactly that and shipped a regression). **When grounding is genuinely NOT available** — the behavior is external/unknowable from here, the cost of proving it is prohibitive, or it's a real design judgment with no single right answer — **do NOT silently proceed on a hunch. Say so out loud, label it speculation, and get explicit mutual agreement that we're "going in blind" before acting.** Surface what you can't prove, propose the cheapest way to reduce the uncertainty (read X, run Y, ask Sekmeht), and only take the blind path by shared decision — never as a unilateral guess dressed up as confidence. A fix that is *provably safe regardless of the residual unknown* (B191's `document.hidden` guard) is always preferable to one that's only correct *if the guess holds*.

11. **Every user-facing feature ships with its slash-command surface — and every feature CHANGE updates the registry in lockstep (Sekmeht, 2026-07-04).** The `/` command registry (DESIGN §37, [slashCommands.ts](src/renderer/slashCommands.ts)) is a FIRST-CLASS control surface for Lichborne — as load-bearing as theming (#4) and settings-wiring (#9) — not a bolt-on that covers whatever existed when it shipped. A feature reachable only by mouse is half-wired; a slash command whose hints/summaries/behavior have drifted from the feature it drives is WORSE than none (it teaches users wrong things with confidence). Three obligations:
    - **ADD a feature → ask "what's its `/command`?"** New rule type → the full verb set (`add`/`remove`/`list`/`edit` + the panel's `openRuleId` plumbing); new toggle/setting → an on/off/toggle command (the `/timestamps` shape); new panel/surface → open/close/list coverage; new selectable values → extend the palette's `SlashLiveData` completion chips. The registry is declarative, so one entry buys the parser, the palette, and `/help` at once. **Not every feature fits** (pure-visual polish, one-time setup dialogs, drag-only interactions) — the requirement is to ASK the question explicitly and record the "no command because X" decision in the feature's DESIGN section, never to skip it silently.
    - **CHANGE a feature → grep `slashCommands.ts` for its noun and update in lockstep.** New option on a rule type → add the `key=value` to its `add` entry (+ hint + example); renamed/removed field → fix the executor, the summary formatter, and the hints; changed semantics → re-read every entry touching that subsystem and re-run the harness. The summaries (`hlSummary`, `muteSummary`, …), signature hints, examples, and error messages are USER-FACING documentation — stale ones are bugs, same class as a stale tooltip.
    - **REMOVE/RENAME a feature → its commands fail helpfully, never silently.** Retired command → remove the entry (fail-closed handles the rest with the `/help` hint); renamed concept → keep the old noun as a `nounAliases` entry so muscle memory survives (the `/gag`→mute precedent).
    The enforcement hook is **pre-merge check #5** below; the extension mechanics (registry entry shape, ctx conventions, live-data prop) are in the "Slash commands & toasts" architecture section.

---

## UX/UI polish standards (make a feature look *finished*, not just functional)

A feature that works but looks rough reads as unfinished — and in a single-developer client with a small tester pool, "looks unfinished" is the impression that sticks. **Treat a deliberate polish pass as a named, mandatory step before any new UI goes to testers**, distinct from "does it function." The worked example is the Automation Analytics v0.14.4 pass (the AnalyticsReview banner / per-row badges); the conventions below are extracted from it and apply to every new surface. They sit alongside Principle #4 (theme everything) and #9 (wire settings everywhere) — same bar, the *visual-finish* axis.

1. **Quiet by default — every glyph on screen should mean "look at me."** A clean / empty / zero state must read as clean *at a glance*, never as a wall of zeros. Concretely: **hide** zero-count chips/badges entirely (don't render `0`); collapse "nothing here" to a single affirmative marker (a green **"✓ no issues"** chip) instead of a row of zeros; use a **faint neutral dash** (`var(--text-faint)`, low opacity) for an idle/uneventful row rather than a loud emoji. Noise trains the eye to ignore the surface; absence of noise is what makes the real signal pop.

2. **Consistent shape beats conditional presence.** Render the SAME structure whether or not a section has content — an empty category still shows as its collapsible "report-card" row (muted, body reads "None."), so the layout is predictable and nothing pops in or out as data arrives. **Mute** the empties (≈50% opacity summary + faded count badge) so the populated ones stand out; don't omit them. A user scanning a stable set of rows reads faster than one watching rows appear and vanish.

3. **Order by severity / importance, not declaration or insertion order.** When listing issues, categories, chips, or rows, sort by what the user cares about first (Analytics: broken → conflict → duplicate → obsolete → no-op → quiet). Declaration order is an implementation accident; the user reads top-down and expects the most important thing first.

4. **Stateful/collapsible UI must SURVIVE re-render — HOIST helper components to module scope.** A React component defined *inside* a render function is a brand-new component TYPE on every render, so React unmounts+remounts it and any state it owns — including native `<details>` open/closed — is lost. In a panel that re-renders on every game line (every Automations sub-panel does), this manifests as **"the section I expanded collapses itself the moment new text scrolls in."** Fix: define sub-components at MODULE level, give each rendered section a stable `key`, and lift any user-controlled "is this expanded" state somewhere that persists across renders. (This was the literal v0.14.4 collapse-on-scroll bug — the AnalyticsReview `Section`/`Jump`/`Chip` helpers were inline and got hoisted to `AASection`/`AAJump`/`AAChip` to fix it.) **Any new collapsible/disclosure UI inside a frequently-re-rendering panel: check this first.**

5. **Visual hierarchy comes from MUTING and dividers, not just spacing.** Reduced opacity / faded color on inactive, empty, or secondary elements does more for "looks finished" than adding margins. A labeled divider — uppercase, letter-spaced, `var(--text-faint)`, a thin `color-mix` top border — to separate two *kinds* of content (Analytics: health issues vs. usage counts) reads as deliberate structure rather than a pile of rows.

6. **Polish colors are theme-safe by construction (ties to Principle #4, pitfalls #34/#55/#63).** Success greens, severity reds/ambers, muted/idle backgrounds all come from `color-mix(in srgb, <fixed-hue> N%, var(--bg-*/--text-*))` so the hue stays meaningful while contrast self-corrects per theme — and **eyeball every newly-polished surface on a light theme** (Classic Light / Ivory / Mist / Parchment) before calling it done. A polished-looking chip on the default dark theme that vanishes on white is not polished.

7. **A polished strip is text-height + minimal padding (pitfalls #75b/#77).** Header/footer/chip/badge text in a short container must pin `line-height: 1` (it otherwise inherits the game line-height and balloons the box), action icons stay ≤1em with `line-height: 1`, vertical padding 2–3px. A strip rendered taller than its own text, or bigger than the content it relates to, is the most common "why does this look clunky" smell.

8. **The UI must EXPLAIN ITSELF — a label or icon that names a feature without saying what it does is half-built.** Every category, mode, toggle, badge, status chip, or icon whose meaning isn't obvious from its single word needs an in-UI explanation: a `title=` hover tooltip at minimum, and visible descriptive text (a one-line `desc` under a section, a placeholder, a short caption) where there's room — with a concrete EXAMPLE when the concept is abstract (Automation Analytics' "Obsolete (covered by regex)" reads *“a regex rule already matches everything this rule matches — e.g. `joins the .+` covers `joins the adventure`”*). **A tooltip that repeats the label is not an explanation** — `title={label}` renders a hover that says "Joined" over a chip reading "Joined", which is worse than none because it looks answered (B273; three Overview chips shipped exactly that). The test is whether the tooltip adds a fact the chip does not already show. **And when the real meaning is already written down in a CODE COMMENT, that is the strongest signal it belongs in the UI** — the Joined chip's genuinely non-obvious behaviour (DR's `IconJOINED` marks the FOLLOWER, so a group LEADER shows nothing) had been tester-corrected and recorded in IconBar, where no user could ever reach it. Also prefer WORDS to initialisms in a glance-readable label: `lpm` sat among `up` / `idle` / `ranks` / `rooms` and was the only one nobody could expand. Two specific rules this came from: **(a)** show the explanation in the EMPTY state too — a clean report card whose every row says only "None." teaches the user nothing; render the `desc` even at count 0 so "what is this category?" is answerable without first having an entry in it. **(b)** put the tooltip on the always-visible affordance (a collapsible section's `<summary>`, not its hidden body) so it's reachable while collapsed. The test: a tester opening the feature for the first time, with nothing in it yet, should be able to learn what every word on screen means without asking. "Broken / Conflicts / Obsolete / Quiet" meant nothing until each got a tooltip + visible description (Sekmeht: *"It's not apparent at all through the UI"*) — bake the explanations in **as you build the feature**, not after a tester asks.

9. **An available/unavailable (or on/off, live/stale) pair is ONE artwork in two treatments — never two assets.** Derive the "off" state from the "on" one with CSS (`filter: grayscale() brightness()` + reduced opacity), and TRANSITION between them so the change reads as the thing *waking up* rather than an icon swap. Two hand-made variants inevitably drift as the art evolves, and a hard swap loses the causal link ("this became available"). The worked example is the SimuCoin coin: one SVG, lightly desaturated when nothing's claimable, brightening over 400ms when coins arrive. **It was retuned in v0.19.0 and both halves of that are the lesson.** It shipped as minted GOLD dulled to `grayscale(.85) brightness(.78)` at `opacity: .55`, and measured **2.36** contrast against a dark app bar and **1.34** against Classic Light — invisible, and invisible in the state it wears nearly all the time, since the button renders whenever an account is eligible. Two corrections: **(i) pick a MID-TONE hue.** Gold's value sits close to a light theme's background and its desaturated form close to a dark one's, so it had no reliable separation at either end; copper (`#bd6f38`) is warm against both. **(ii) Carry the quiet/loud distinction in the CHROME, not by dimming the object into nothing** — the border tint, glow, sheen and count badge already make "claimable" unmistakable, so the coin only needs to read as dormant, not to vanish. UX #1 is about removing NOISE, never about hiding a control. The retune lands at 3.58 / 3.04, both clear of the 3:1 non-text floor. **And the rider gained a third clause:** the depicted object keeps its baked palette, the chrome around it comes from theme vars, AND where a baked object must sit on an unknown background, give it a theme-derived OUTLINE (`.sc-coin-ring`, mixed from `--text-primary`) so its edge survives any theme without theming the object itself. Two riders: **(a)** when the object is a depicted THING (a coin, map tiles, the moons' lore colours) its palette is legitimately BAKED — that's the standing exception to Principle #4, but everything AROUND it (the button, borders, badges) still comes from theme vars and must be eyeballed on a light theme; **(b)** motion is decoration, colour is signal — under `:root[data-epilepsy-safe='true']` drop every animation but KEEP the colour distinction, or the accessible path loses the information the effect was carrying.

10. **THERE IS ONE MODAL LOOK — the About Lichborne chrome — and every dialog wears it (Sekmeht, 2026-07-27).** A client whose dialogs each look slightly different reads as assembled rather than designed, and it's the single fastest way to make good features feel unfinished. **The About modal ([about.css](src/renderer/styles/about.css)) is the reference implementation and the house style.** Do NOT hand-roll chrome per dialog and do NOT copy its literals — **use the `--modal-*` tokens in [global.css](src/renderer/styles/global.css)**, which encode the recipe: `--modal-bg` surface, `--modal-border` hairline, `--modal-radius`, `--modal-shadow`, `--modal-scrim` backdrop, an accent-tinted header band (`--modal-head-bg` / `--modal-head-border` / `--modal-head-color`), `--modal-ctl-hover` for header controls, and `--modal-input-bg` for fields. Retuning the house look is then one edit in one file. **The shape:** a title in `--modal-head-color` on the accent band, a rounded ~1.7rem close button that lights on hover, tab bars as accent CHIPS (pill, accent tint + accent hairline when active; transparent + neutral hover when not), and — critically — **only the TOP-LEVEL header is an accent band**; section labels *inside* the body stay small/uppercase/muted, or the hierarchy flattens (the launcher's account panels and the Automations sub-panels both follow this). The same chrome applies to inline PANELS that read as cards (the launcher's account/Favorites blocks), not just floating dialogs.
    **Three obligations when converting or adding a dialog:** **(a) RE-READ ITS INNER ELEMENTS** — their contrast was tuned against the old surface, and `--bg-raised`/`--bg-base`/`--bg-hover`/`--bg-input` collapse toward each other on light themes; this is pitfall #55 and it has now bitten twice (the v0.9.2 wizard tiles, and v0.18.0's Edit Profile fields going white-on-white on Classic Light *in the same changeset that warned about it*). **(b) EYEBALL IT ON A LIGHT THEME** before calling it done — Classic Light is the harshest because `--bg-app`, `--bg-base` and `--bg-input` are all `#ffffff` there, so anything relying on those for separation vanishes. **(c) CHECK THE PREFIX IS UNIQUE** before restyling — two components sharing a class prefix are "compatible by luck" only until someone restyles one of them (the `.tp-*` Triggers/Theme-Picker collision, renamed to `.trg-` in v0.18.0 for exactly this reason). **Converted so far:** About (reference), Edit Profile, the launcher account/Favorites panels, the whole Automations family, Contacts, and the + tab's "Connect a character" modal (v0.19.7, F113 — the panel is the dialog surface with the header band and ✕; the compact Launcher inside it draws no panel or heading of its own). **The CHARACTER TABS also wear the chip treatment as of v0.18.0** ([character-tabs.css](src/renderer/styles/character-tabs.css)) — accent tint + accent hairline on the active tab, neutral INACTIVE-ONLY hover (pitfall #107), and the character NAME deliberately left `--text-primary` because `health-warn` is already `var(--accent)` and an accent label blurs into it. They keep their pre-v0.18.0 rounded-top SILHOUETTE rather than the pill the chip normally wears (Sekmeht) — the chip is a TREATMENT, and a surface may keep its own shape while adopting it. A true attach-to-the-edge folder look was built, measured landing correctly, and REVERTED: the strip is `overflow-x: auto` with a 5px scrollbar, so the attachment breaks precisely when enough characters are open to overflow. Don't re-attempt it without solving the overflow case. **As of v0.19.7 (B400) every dialog wears it.** Settings, the Layout Manager, the Lich Dashboard, the Session Log, the Theme Picker/Editor, the Import Wizard, Profile Transfer, the Add Account wizard / Lich Setup, Quick Send, AI Consent, the Experiences shelf and the Maps overlay joined the list above. The controls inside them come from ui.css (standard #12). A new dialog wears the same chrome from day one; keep the list in global.css current.

11. **TEACH AT THE POINT OF USE, AND MAKE READING ORDER MATCH DOING ORDER (Sekmeht's Team Login review, v0.18.3).** Standard #8 says the UI must explain itself; this is *where* the explanation goes. Three failures, all from one modal:
    - **A preamble is the worst place for an explanation.** Team Login opened with ~100 words across two paragraphs — permanent furniture, re-read on every one of the dozens of times you open it, for information you need once. Moving the "what is a set" copy down onto the SAVE row made it teach at the moment it matters and cost nothing the rest of the time (standard #1: quiet by default).
    - **Copy that says "below" must not sit above its control.** The text read *"choose who you want below, save it under a name"* while the Save button was ABOVE the list. Order the zones by the order the user acts: loading a set belongs on top (it rewrites everything under it), saving belongs after the thing it summarises.
    - **A flat stack of identical rows has no hierarchy at all.** Every element was a sibling `.cne-row` at a uniform gap — paragraphs, controls and list items indistinguishable. Zones need a muted uppercase LABEL and a hairline (standard #5), plus a live count where one applies ("3 of 5 selected") so the user can answer "what will this button do" without reading the button.
    **And the mechanical tell that a component has drifted: a pile of inline `style={{}}` objects.** This one carried ~15, which is precisely why every adjustment had been a one-off patch rather than a change to a rule. When you restructure a surface, move its inline styles into real classes in the same pass — otherwise the next person inherits the same resistance.

12. **ONE CONTROL VOCABULARY — build dialogs from the shared primitives, never from per-dialog copies (v0.19.7, the B367–B410 audit).** Standard #10 unified the dialog *surface*. The v0.19.7 audit found almost everything *inside* the surface still hand-rolled per dialog:
    - about eight primary-button looks and seven tab styles;
    - two input fills;
    - close buttons in six styles and two glyphs;
    - six different "are you sure?" patterns;
    - no editor that knew whether its draft was saved.

    The fix is four shared pieces, specified in DESIGN §43.7:
    - **[ui.css](src/renderer/styles/ui.css):** `ui-btn` (`--primary`, `--danger` [red at rest], `--ghost`, `--sm`), `ui-close`, `ui-field`, `ui-tabs` / `ui-tab`, `ui-section-label`, `ui-hint`, `ui-empty`, the `ui-modal-*` skeleton, and the type, spacing, radius, shadow, transition and code-font tokens. These are rem-sized app chrome, so in-game panels keep their em-sized controls. ui.css is the BASE layer (pitfall #144).
    - **One confirm:** `confirmAction()` / `confirmDelete()` ([confirm.ts](src/renderer/confirm.ts)) for a destructive action on anything not open in an editor, and [InlineConfirm](src/renderer/components/InlineConfirm.tsx) for the item already on screen (an editor's own footer Delete, a script's Kill). Never `window.confirm`.
    - **The unsaved guard:** editors report `dirty` through `useReportUnsaved`, and containers route close, Esc and tab switches through `useUnsavedScope().guard()` ([useUnsaved.ts](src/renderer/hooks/useUnsaved.ts)).
    - **Focus follows the dialog stack:** see pitfall #141(e).

    **The words follow one guide** (the table in DESIGN §43.7):
    - sentence case;
    - "…" only when a control opens something you still have to act in;
    - Delete (gone, confirmed), Remove (can be put back), Clear, Reset and Revert each mean one thing;
    - the ✕ is "Close", and a footer dismiss is Close or Cancel, never Done;
    - "+ New *thing*";
    - footer order [destructive] … [Cancel] [Primary];
    - a disabled button's tooltip says why.

    **When a new dialog needs something the primitives don't cover, add a modifier to ui.css, never a private copy under the dialog's own prefix.** Private copies are exactly how the drift started.

**The pass itself:** before handing a feature to a tester, re-read its UI asking — *does the clean/empty state look intentional? does anything collapse or jump on re-render? is the most important thing on top? does it hold up on a light theme? can a first-time user tell what every label/icon means without asking?* — not just "does it work." That review is the difference between "shipped" and "shipped and looks like we meant it."

---

## Architecture (pointers, not duplication)

### Process split
- **`src/main/`** — Electron main (Node). Sockets, XML parsing, Lich child process, file I/O. Built with esbuild (`build-main.mjs`).
- **`src/renderer/`** — React UI. Built with Vite. Talks to main only via `window.api.*` (preload.ts contextBridge).
- **`src/shared/types.ts`** — single source of truth for IPC payloads and game-event shapes. Imported by both sides.

### Multi-session model (DESIGN.md §13)
N character sessions across one OR MORE windows (v0.11.0). `src/main/main.ts` holds `sessions = Map<SessionId, Session>`; each Session owns its own `ConnectionManager`, `StormFrontParser`, `LichBridge`. Every push channel (`game-event`, `connection-status`, `raw-xml`, `error`) carries `sessionId`. **When adding any IPC, include `sessionId` in the payload** — unless it is genuinely about the WINDOW rather than a character, which so far is only `window-visibility` (see the per-window bullet under Multi-window & decouple). Make that a conscious, documented call, not an omission.

### Multi-window & decouple (v0.11.0)
A tabbed character can be **decoupled into its own OS window** while everything stays in ONE process — so cross-character features (Quick Send) keep working, Lich-launch stays coordinated, and profiles/localStorage aren't raced by a second exe. The model:

- **Main owns the session ROSTER; windows are views.** Pre-v0.11.0 `SessionsContext` ([SessionsContext.tsx](src/renderer/SessionsContext.tsx)) was the renderer-local source of truth for the session list. Now main is authoritative: each `Session` carries `meta` (characterId/account/character/game/useLich) + `ownerWindowId`, and `broadcastRoster()` pushes a `RosterEntry[]` (`session-roster` channel) to EVERY window on any change. [RosterContext.tsx](src/renderer/RosterContext.tsx) mirrors it; `useRoster()` exposes `roster` (all windows' sessions, for Quick Send), `windowId`, `isPrimary`, `myRoster` (owned). `SessionsContext` still drives this window's *own* tabs (rich `SessionStatus` stays local); the **decouple-sync effect in AppShell** ([App.tsx](src/renderer/App.tsx)) keeps it aligned: PULL owned sessions on mount (`get-owned-sessions`) + react to PUSH `session-acquire`/`session-release`.
- **Window registry + owner-targeted routing.** `windows: Map<webContentsId, BrowserWindow>` + `primaryWindowId`. Per-session output routes via `ownerWindow(s)` (falls back to primary if the owner is gone); app-global output goes to primary (auto-update), focused (menu), or sender (flash). `ownerWindowId` is the calling window's `event.sender.id`, set in the LOGIN handler.
- **Decouple = move ownership, NEVER touch the socket.** `session:move-window(sessionId, 'new' | 'main' | windowId)` reassigns `ownerWindowId`; the source window drops the tab (`session-release` → local `removeSession`) and the target mounts a fresh GameWindow. The socket/parser/LichBridge are untouched (verified: GameWindow unmount cleanup doesn't disconnect — pitfall #59). Three entry points: right-click tab, Window menu, Bulk Connect "separate windows" checkbox (persisted in `_shared.yaml` as `bulkConnectSeparateWindows`, default off). **Last-character guard** at 3 levels (main backstop + greyed Window-menu item + disabled right-click) — can't empty a window. The tab right-click menu (v0.11.6) hosts the move options alongside Reconnect/Disconnect and shows only the actionable ones — see pitfall #69.
- **Lifecycle.** Closing a **secondary** window = graceful **logout** of its character(s) (`runSecondaryWindowClose`); **re-home** is explicit via Window → "Move Character to Main Window" (auto-closes the emptied window, no disconnect). Closing the **primary** = app quit (`runAppShutdown` flushes EVERY window's debounced profile saves, then drains all sessions). **Multiple exe instances are deliberately NOT gated** (isolated character sets); the shared-`userData` hardening is a separate deferred item.
- **State replay (seamless takeover) — see pitfall #60.** A window taking over a session gets a render-only replay (scrollback + every vital/room/etc.), gated so it never re-fires triggers or double-logs.
- **Windows remember size, position and maximized state (F109, v0.19.7; DESIGN §13.9).** `createWindow` takes a `stateKey` (`main`, or `char:<characterId>` for a decoupled window) and restores through [windowState.ts](src/main/windowState.ts) → the pure [windowBounds.ts](src/shared/windowBounds.ts) resolver (a title bar that would land off every display ⇒ centred, size kept). The file is machine-local `{userData}/window-state.json`, never a profile. **Two rules if you touch it:** capture bounds at EVENT time, never at close — shutdown and re-home `destroy()` windows and emit no `close`; and read `getNormalBounds()`, never `getBounds()`, or a maximized/minimized window saves the wrong rect. A new `createWindow` caller should pass a `stateKey` or accept an untracked window. **Two platform riders:** capture is SKIPPED while `isFullScreen()` and re-run on `leave-full-screen`, so macOS fullscreen never saves a screen-sized rect (B362); and on Linux a restored window's requested-vs-reported position is compared once after show, and a small non-zero difference (≤ `CREEP_MAX_PX`) is kept as an offset subtracted from every later save, so X11 decoration offsets don't creep the window a few pixels per launch (B360) — a larger difference is a clamp or Wayland's 0,0 and is ignored.
- **Cross-window state — three rules.** (1) **App-wide settings applied to the DOM** (the theme) don't auto-cross windows — each window has its own document. The fix is a `storage`-event listener in App that re-applies on change (`lichborne.theme`/`myThemes` → `initTheme()`); the window that made the change applied it directly and gets no storage event (no double-apply). App-wide settings that are READ FRESH per use (session-log settings, advanced/Lich settings) need NO sync. Per-character settings live in one window (the owner), so no cross-window race. (2) **Anything app-level that acts on "the active character in another window"** must route through main to that window: Profile Transfer remounts a moved-in target via `session:reload` → owner window (pitfall #56); the modal's active-target detection uses the **roster** (all windows), not the local `sessions`, or a cross-window target is mis-treated as inactive and its owner window overwrites the import. (3) **The native menu is one global menu for the focused window** — `refreshMenuState()` scopes Next/Prev/Close/Move enabled-state to the FOCUSED window's owned-session count (re-run on `win.on('focus')`), and `sendMenuAction` routes to the focused window. Don't gate Window-menu items on global counts.
- **Window VISIBILITY is a per-window channel, and it is the one push channel that deliberately carries NO `sessionId` (v0.18.5).** `createWindow` wires `win.on('minimize'/'restore'/'hide'/'show')` → **`window-visibility`** IPC → `App` stamps **`data-window-hidden`** on `<html>` → [global.css](src/renderer/styles/global.css) pauses `animation-play-state`. It is the OS WINDOW that is off screen, not a character, and each `BrowserWindow` has its own document — so each stamps its own root and the §13 "every payload carries `sessionId`" rule genuinely doesn't apply. **Why it exists:** `backgroundThrottling: false` (needed so the room pump and game stream keep running while minimized — pitfall #71) also leaves every CSS animation in the app burning style/paint work nobody can see — the map's ~20, the Experiences' ~24, the highlight text effects, times every mounted character. **Why it can't be `document.hidden`:** Electron's docs state `backgroundThrottling: false` also affects the Page Visibility API, so `hidden` likely never goes true and `visibilitychange` may never fire (pitfall #96 flagged the existing guards as probably inert for this reason). **Deliberately NOT wired to blur/focus** — an unfocused window is usually still fully visible, and freezing it would be a visible regression. **When you add a new animation anywhere, it inherits this for free** (the rule is a root-level wildcard — see the comment there, and pitfall #126 for why a wildcard is correct *here* and wrong on the map: the cost is per TOGGLE, and this one flips only on minimize/restore). Only `animation-play-state` is touched, so animations resume mid-cycle and nothing moves or reflows.

### Lich launch & connect (v0.7.0; GTK-friendly spawn v0.9.x)
`LichConnection.launch()` spawns `rubyw lich.rbw --stormfront --dragonrealms` as a **detached** child (not a service). Lich takes its force-mode path: bind `127.0.0.1:11024`, then `accept` **exactly one** front-end and **close the listener** — one Lich = one session. Readiness is **not** a fixed delay: `connectWithRetry()` retries the real connection (250ms cadence, ≥30s cap) until the port accepts — the first success IS the session socket (no throwaway probes). `launch()` resolves on the child's `spawn` event and watches `exit` so a Lich that dies on startup fails fast (with its launch-log tail). Because each Lich serves one front-end then frees 11024, multi-character logins **must serialize** their spawn→connect window — `serializeLichLaunch()` in [ConnectionManager.ts](src/main/connection/ConnectionManager.ts) is a module-level promise chain shared across all per-session managers. SGE/eaccess auth runs outside that chain so it overlaps the queue wait. A failed launch `killProcess()`es its Lich so it can't squat the port.

**Spawn shape is GTK-friendly (v0.9.x).** Lich runs as a normal Windows GUI-subsystem process so Ruby/GTK scripts get a proper process/desktop context — the community-standard shape (matches how Frostbite / Genie launch Lich). Three things make it so, all in `launch()`: (1) it runs **`rubyw.exe`** (GUI subsystem), not `ruby.exe` (console) — `resolveRubyw()` derives `rubyw.exe` from the configured ruby path (`/ruby\.exe$/i` → `rubyw.exe`), falling back to the given path if the derived file doesn't exist (so a path already pointing at `rubyw.exe` is used verbatim); (2) **no `windowsHide`** (rubyw is windowless by nature — nothing to hide); (3) **no stderr pipe** — Lich's stdout+stderr are redirected to a per-character log file at `{userData}/Logs/lich-launch/{Character}.log` (truncated per launch), and `describeExit()` reads that file's tail for the error banner instead of an in-memory pipe. The redirected-console **pipe** of the old shape was the prime suspect for GTK flakiness (the kind of non-GUI process context that made widget pumps racy), which is why diagnostics moved to a file. `detached` + `unref()` + the child handle are kept (Lich is the proxy, must outlive us; the handle lets `exitInfo` fail a dead launch fast). The settings/default ruby path stays `ruby.exe` ([lichSettings.ts](src/renderer/lichSettings.ts)) — derivation handles rubyw at launch time, no migration.

**Ruby GTK script windows — product stance REVERSED (v0.9.x), VERIFIED WORKING (2026-07-03).** The prior decision was "do NOT support GTK" (2026-05-30/06-01), on the theory that Lichborne's launch shape made GTK irredeemably flaky. **That decision is reversed** (Sekmeht, "I need to revisit the connection stuff to allow for GTK — it's the only way"). The v0.9.x **GTK-friendly spawn shape** (above: `rubyw.exe`, no `windowsHide`, no stderr pipe) was changed specifically to give Ruby/GTK a normal GUI-subsystem process context — the same shape Frostbite/Genie use, under which the same scripts work. **Status: empirically VERIFIED working (Sekmeht, 2026-07-03)** — GTK script windows paint and behave correctly under the new spawn. GTK support is a confirmed capability, not a bet.

The historical root-cause analysis (still useful background): GTK scripts (`Gtk.queue { ... }` + `Gtk::Window.new`) showed (a) windows that failed to paint, (b) windows that painted only **intermittently** (kill-counter often surfaced only after a start→kill→restart cycle — timing-dependent, not categorically "never paints"), and (c) crashes on widget interaction (the `;vars setup` shape — a window that *did* paint could still take Lich down on interaction). The loop itself WAS running (`lich.rbw:135` calls `Gtk.main` unconditionally; `gtk.rb:148-155` yields via `GLib::Idle.add`), so the failure was a subtler process-context problem — which the old console-subsystem (`ruby.exe`) + hidden-window + redirected-stderr-pipe spawn produced, and which the new GUI-subsystem (`rubyw.exe`) plain spawn removed (confirmed by the 2026-07-03 verification). **When a tester reports a GTK script not working:** first confirm they're on a v0.9.x+ build (the new spawn); if it still misbehaves, that's a real bug worth investigating (capture the launch log at `{userData}/Logs/lich-launch/`), not expected behaviour. The `<streamWindow>` / `<pushStream>` XML approach (see `newkill-counter.lic`) remains the more portable recommendation for script authors (works in every front-end with no GTK runtime), and Lichborne-native equivalents are still preferred where they exist (`;vars setup` → the Lich Dashboard → Variables editor, v0.9.0). **The v0.9.0 proactive `--- Lichborne:` GTK advisory (the GTK detector + `client-notice` channel) was REMOVED in v0.9.x** alongside this launch change — it assumed GTK is broken, which is no longer the operating assumption (see the #54 tombstone). Pitfall #52 (the `;vars setup` GTK root-cause analysis) is kept as useful background. Pitfall #53 (write-through-Lich editor pattern) is unaffected.

**Lich version compatibility (verified against Lich 5.18.0, 2026-07-16).** Lichborne presents to Lich as **`--stormfront`** — a deliberate, permanent choice. The whole integration surface was re-verified against the 5.18 diff and every touchpoint holds unchanged: the force-mode loopback bind + one-FE-then-close-listener model (Lich still defaults `bind_address` to `127.0.0.1`; Lichborne passes neither `--bind-address` nor `--reconnect`); the `inventory_boxes_off` downstream-hook + `_flag Display Inventory Boxes 1` workaround (pitfall #80 — the hook now carries `persist: true`, still installed for stormfront/GSL FEs); the `;eq Vars[…]; Vars.save` write path + Marshal reader (pitfalls #53/#91 — `vars.rb` untouched); `cmd\r\n` command sends (the 5.18 CRLF→LF normalization is on `StringProc` script-source, NOT the FE socket); the `;listall` poll + `interceptLine` (pitfalls #22/#76 — output format unchanged); and the parser's existing handling of `<d>` command links, `<output class="mono">`, and an inline-prepended `<dialogData id='AimTimerDialog'>…</dialogData>text` (pitfall #87). Three things to KNOW for tester support: **(1) Lich 5.18 now requires Ruby 4.0** (was 2.6/3.2) — it alerts + refuses to launch on older Ruby, which surfaces in Lichborne's launch-log error banner as a failed launch; a "Lich won't start after updating" report on 5.18+ ⇒ check the tester's Ruby version FIRST. **(2) Lich core now natively injects the room-number + obvious-exits display** (`;display roomlinks`/`roommono`, #1438), replacing the retired dr-script `roomnumbers.lic`; DR defaults are **mono + plain text** (not `<d>` links), which Lichborne's `monoMode` renders correctly — testers may just SEE room-number/exit lines they didn't before (gated on the game room-number flag / `display_uid`/`display_lichid`). **(3) The `(u12345)` subtitle gap** — see pitfall #65. **Do NOT present as the new `--saga` frontend.** Saga is NOT a new XML dialect: in Lich's capability registry ([front-end.rb](file:///c:/temp/lich-dev/lich-5/lib/common/front-end.rb)) `saga = stormfront's exact caps (xml/streams/mono/room_window) + one extra: sentinel`. The `sentinel` capability makes Lich prefix **every** downstream line with `\x1f` (0x1F, `ORIGIN_SENTINEL`) — a whole-stream "this feed came through Lich" marker for Saga's detachable/cloud-profile-sync architecture (attach/detach over LAN/tailscale, keyed on `<playerID>`), NOT a per-line Lich-injected-vs-game discriminator. So adopting saga would give Lichborne the SAME content it already gets as stormfront, plus a sentinel byte to strip from every line (and re-audit every `^`-anchored parser rule against — room-info detection, the hand-glance patterns #78, prompt handling) for ZERO functional gain. Revisit only if a future Lich feature becomes saga/`sentinel`-GATED (nothing in 5.18 is), or if Saga ever ships local config files worth an import parser (today it's cloud-only, no user base). Saga is a **sibling front-end**, not a config format to import — "supporting" it would only ever mean identifying AS it, which is all downside.

**Re-verified against Lich 5.21.0 (2026-09-14, plus unreleased main): the integration surface HOLDS unchanged — no Lichborne change needed.** Every `main.rb` change is on the Lich-launches-the-front-end path, not our force-mode listener; `--stormfront` is still matched with `=~`; `vars.rb`, `;listall`, the assess/percWindow/injury branches of `xmlparser.rb`, and every `drparser.rb` rule we mirror are untouched. Two things to know. **(a) 5.20.1 (#1530/#1532) stops `quiet:` script output from leaving the front-end stuck in mono** — on older Lich a script's quiet command could leave Lichborne's `monoMode` on (tester support: "text went monospaced after a script ran" ⇒ update Lich to 5.20.1+). **(b) 5.21.0 added things a front-end can use:** the `inventoryManager` whole-inventory feed, a `frontends.yml` registry that can launch a custom front-end (trap: a custom front-end's id becomes `$frontend`), an HTTPS login fallback, and ActiveSessions discovery. Facts and citations: Knowledge.md §17. **Re-verified against Lich 5.20.0 (2026-08-16): the integration surface HOLDS — `parse_assess_line` unchanged (#1485 only additionally feeds Lich's own creature tracker), `--stormfront` still `=~` (the 5.19.0 `==` bug stays fixed), map JSON shape and `map-*.json` output unchanged (#1495 dropped OTHER legacy mapdb formats). BUT it surfaced a REAL Lichborne bug and one new parser shape.** **(a) DR now emits `<nav rm='NNNN'/>` on EVERY arrival** (#1491) and Lich adopted it as the authoritative DR room-UID source, no longer forcing the game's ShowRoomID flag — so the room UID is reliably present on every move. That exposed B282: **the Lich Map indexed by Lich's own `r.id` while looking up with the GAME uid**, two different number spaces (ids 1–52,274, uids to 9.8M, ~3% overlap), so the fast path resolved 3% of rooms and silently fell back to title+desc matching. Fixed by indexing `r.uid` too and trying uid → id → title (3.0% → 100% measured). **Both spaces must keep being tried** — the `- NNNN` dash slot holds the GAME number natively but Lich's `display_lichid` puts its OWN id there, and nothing in the text distinguishes them. **(b) New OPT-IN title placements** (#1491/#1500) can emit `[Town Square - 1234 - (u230008)]` with the uid INSIDE the brackets — matched by neither old pattern, so the id was lost and the decorations stayed in the title (B283). Defaults are unchanged, so only opted-in players saw it. **The id-vs-uid distinction is the durable fact: Knowledge.md §17.** **Re-verified against Lich 5.19.1 (2026-07-21, the current release): the integration surface HOLDS unchanged — no Lichborne change needed.** The 5.19.0→5.19.1 delta is all safe: **#1452** (resilient ActiveSessions ownership + a shutdown watchdog, `lib/main/main.rb` + new `shutdown_watchdog.rb`) only hardens Lich's TEARDOWN — it arms a watchdog so a hung `before_dying`/`Vars.save`/socket-close can't leave the process alive holding port 11024; it does NOT touch the force-mode FE listener/accept model, and it's mildly BENEFICIAL for us (faster port release on Lich exit). **#1450** (fix(**gs**)) ingests newly-exposed GS-ONLY fields — mindState `ascension_exp/exp/fashlonae/lumnis/rpa` and `<roommeta weather/inside/water/sanctuary/realm/bonfire>` numeric codes into `XMLData` — which DR never sends, and Lichborne's tokenizer ignores as unknown roommeta attributes regardless (note: if DR ever exposes `<roommeta weather>` too, that'd be a STRUCTURED weather signal worth revisiting the §34.9 "no passive weather feed" conclusion — GS-only today). **The one MAJOR issue was Lich 5.19.0-ONLY, already fixed upstream:** #1443 rewrote the ARGV frontend-flag checks to `a == /regex/`, and `String == Regexp` is ALWAYS false in Ruby, so `--stormfront` (which Lichborne passes) never matched → `$frontend` fell through to `'wizard'` → GSL/Wizard-protocol tokens leaked into the client instead of XML (a tester hit exactly this). **#1456 (shipped in 5.19.1) fixes it** (`==`→`=~`, verified in `lib/main/argv_options.rb:283-332`: `a =~ /^--stormfront$/i` → `$frontend='stormfront'`). **TESTER SUPPORT: a tester on 5.19.x seeing raw protocol garbage / unformatted tokens instead of the normal game feed is on 5.19.0 — have them update to 5.19.1+** (Lich-side bug, Lich-side fix; no Lichborne change). The DR-facing part of the 5.18→5.19 delta was already absorbed in v0.16.1 (assess parser #1413, `(prone)/(sitting)` posture #1442, AimTimerDialog unload #1414, combat-position #1400, roomlinks/roommono #1438). The only changes NEW since that review are **connection-only and never touch the FE socket**: **#1449** lowers the GAME socket's TCP keepalive idle 120s→30s ([games.rb](file:///c:/temp/lich-dev/lich-5/lib/games.rb) `Game.open`; the commit body states "No effect on the [FE] socket"), and **#1437** adds bind-host keywords for `--detachable-client`/`--headless` (Lichborne uses neither; the FE listener still defaults to `127.0.0.1`, main.rb:298/448). The rest of the new XML work is **GemStone-specific or Lich-INTERNAL, so it never reaches a DR front-end**: GS creature XML/templates (#1425/#1427, `lib/gemstone/*`), climate/terrain feeds (#1388, GS weather), a `reserve` pushStream id (#1379, GS), and the REXML→Ox parser migration (#1396, Lich's own state parse — the FE still gets the raw XML passthrough). **Backwards-compat is inherent** (Sekmeht's rule 2026-07-20 — features stay compatible with at least the previous major Lich): the connection model, the `$frontend`-flag lever, and the XML passthrough are all version-agnostic, and **Lichborne's parser degrades gracefully on ANY unknown tag** — the tokenizer strips every `<…>` (tags never leak as text), an unhandled tag falls through the `tagStart` switch ([StormFrontParser.ts](src/main/parser/StormFrontParser.ts) ~355) and is ignored, and its inner text still shows. So a future Lich/DR tag can't break the client; the only thing that does is a whole-protocol swap away from XML (e.g. Lich emitting GSL/Wizard text), because GSL isn't XML and the tokenizer would leak it — but that's a `$frontend`-mode problem (the flag), not a parser gap.

### Attach mode — the third connection mode (PR #2, Kahlen, v0.19.4; DESIGN §48)
Beside Lich-launch and Direct, Lichborne can **attach to a Lich already running and logged in** (`--headless PORT`, sugar for `--without-frontend --detachable-client=PORT`). The session then **outlives the client** — the whole point. `AttachConnection` ([src/main/connection/AttachConnection.ts](src/main/connection/AttachConnection.ts)) is the transport, kept free of electron imports so a plain-node harness can drive it; `ConnectionManager` gains mode `'attach'` + `connectAttach()`; `main.ts` adds `CH.LOGIN_ATTACH` mirroring `CH.LOGIN`'s lifecycle **including hold-for-replay** (pitfall #60 — the attach-time resync would otherwise flush into a window with no GameWindow subscriber). Sessions carry `useLich: true`, so every Lich-only surface stays live.

**Four invariants, each load-bearing.** (1) **Send NOTHING on connect** — the detachable listener takes a bare TCP connection with no auth or handshake, and anything a client writes is dispatched to the game as a command (so no login key, no `FE:` line; harness-verified as zero unprompted bytes). (2) **Disconnect DETACHES — half-close, never QUIT**, for both in-tab Disconnect and app shutdown; typing `exit` stays a real log-out because Lich runs a full orderly shutdown of the *whole session* for a user-exit from a detachable client, which no front-end can soften — so it is documented AT THE USER rather than hidden. (3) **Auto re-attach is deliberately UNCAPPED** (2/4/8/15/30s → 30s heartbeat, reconnecting in place per pitfall #69) — the session outlives the client, so giving up is exactly wrong. (4) **`planReconnect`'s attach branch bypasses the account arithmetic** — those rules protect a *login* from DR's one-per-account law, and an attach starts no login.

The target persists as an optional launcher-owned `CharacterProfile.attach {host, port}` via the read-modify-write path (pitfall #26 — never GameWindow's debounced save). **No slash command, by decision** (one-time setup, not an in-play verb — recorded in DESIGN §48.6 per Principle #11). **Verified Lich-side protocol facts live in Knowledge.md §19** — read those before changing anything here, and note the DR-vs-GS resync shape (pitfall #135) and the flag-vs-message rule (pitfall #134) that this feature surfaced. Known edges (attach-only stub profiles, `--genie` sending no resync, session-file auto-discovery not attempted) are enumerated in DESIGN §48.7. **Open: B310 — auto re-attach REUSES the Session and therefore the parser, so stale `activeStream`/`streamStack`/`monoMode` can survive a mid-block drop; every other path gets a fresh parser because it gets a fresh Session. Unpatched pending the author's call — read B310 before touching `scheduleReattach`.**

### Game-text pipeline
`SGEConnection`/`LichConnection` → `ConnectionManager` (line events) → `LichBridge.interceptLine` → `StormFrontParser.parse` (stateful XML tokenizer) → `GameEvent[]` → batched IPC → `GameWindow`.

`StormFrontParser` is **stateful** (boldDepth, color stack, monoMode, injuryBuf, etc.). Call `parser.reset()` on new login. `<prompt>` is a good boundary for transient state.

Main text rendering uses **react-virtuoso**. Scroll pinning is a delicate state machine: `pinnedRef` snapshots before commit, `suppressUntilRef` gates programmatic scrolls, `onWheel` un-pins synchronously. **As of v0.11.6 (B155), Virtuoso's `followOutput` is OFF — we own pinned auto-scroll ourselves** (`followOutput` under-measures the fractional last row and two-steps; don't re-enable it). **As of v0.11.8 (B158), ONE primitive owns every auto-scroll path: `stickToBottom(reindex?)`** — a self-cancelling rAF **settle loop** that re-asserts the DOM-truth bottom (`scrollTop = scrollHeight − clientHeight`) every frame until `scrollHeight` is **stable for two consecutive frames** (or a 20-frame cap). This replaced the THREE mechanisms that used to race the same target (the old inline sync+1rAF in `totalListHeightChanged`, the `resnapToBottom` settle loop, and the inline `ResizeObserver` write). **Why a loop:** Virtuoso measures the just-appended last row ASYNCHRONOUSLY (its ResizeObserver fires a frame or two late), so `scrollHeight` keeps growing after content renders — any single write (even rAF-deferred) can land on the short pre-measure bottom = the intermittent "rests one line short, wheel down once to fix it" bug. The loop rides out the measurement tail; requiring TWO stable frames is what guarantees the async grow is captured. The per-batch path (`totalListHeightChanged`) does a **synchronous first write before paint** (last line flush in the same frame new content renders) THEN the settle loop; `reindex=true` paths (badge/End, font change, tab show, focus regain, replay) issue ONE `scrollToIndex({index:'LAST'})` first because the target row may be off-screen/unmounted. The loop writes ONLY `scrollTop` (no re-render), so it's safe to call from the scroller's height-gated `ResizeObserver` (viewport resize) without the re-render→re-fire feedback loop a `scrollToIndex`-based resnap caused. Keeping the loop alive across a flood (cancel+restart per batch) keeps the bottom flush every frame, so bursts read as continuous scroll instead of chunky jumps. **During an active flood, suppress stays armed, so only `onWheel` (wheel-up) un-pins — scrollbar drag/arrow un-pins only when the flood pauses; this matches "stay stuck until the user manually scrolls up."** **Don't re-enable `followOutput`, don't add another bespoke one-shot scroll correction, and don't split `stickToBottom` back into multiple mechanisms** (pitfall #68). A story-window smooth-scroll feature existed v0.6.8–v0.6.11 and was **removed in v0.6.12** — marginal, a magnet for false bug reports. **Don't add `useEffect`-driven scroll logic** (or re-add smooth scroll) without reading GameWindow.tsx and DESIGN.md §23. Room-state updates funnel through the rAF pump (`roomQueueRef`), not direct `setRoomState` — see DESIGN.md §23 and pitfall #20. **As of v0.13.4 two more load-bearing structures sit in this pipeline:** main's `scheduleFlush` coalesces event floods (16ms leading-edge throttle — one renderer batch per frame during bursts, immediate when idle; B172, pitfall #82d), and ALL scrollback appends route through `appendTrimmed()` — hysteresis trimming (grow to `MAX_LINES + TRIM_CHUNK`, ONE cut back to `MAX_LINES`), never a per-batch slice to the exact cap (B171, pitfall #81 — that per-batch trim was the years-long "text hops after a while").

### Command-send pipeline (the dual of the game-text pipeline)
How a command reaches the game — traced end-to-end in the v0.14.1 validation pass, recorded so it isn't re-derived. **Boundary:** renderer → `window.api.sendCommand(sessionId, cmd)` → preload `ipcRenderer.send(SEND_COMMAND)` → main `ipcMain.on(SEND_COMMAND)` → `getSession(sessionId)` (**silently `return`s if it misses** — the failure point that makes a wrong sessionId look like a dead send) → `s.connection.send(cmd)` → Lich mode `LichConnection.socket.write(cmd + '\r\n')` / direct mode `gameSocket.write(cmd + '\r\n')` (the `\r\n` is what lets Lich process `;commands` as a complete line). **All ~10 renderer entry points** (typed bar, macros, aliases, repeat-macro tokens, triggers, Genie/Lich map walks, room-exit buttons, in-text command links, Lich-dash run) funnel through three GameWindow functions — `dispatchUserText` (typed/alias/token), `sendCommandSequence` (macro + alias batches, with `delayMs`), and `sendCommand` (map/exit/in-text-link/trigger via `triggerCallbacks`) — and **every one MUST send via `sessionIdRef.current`, never a captured `session.sessionId`** (pitfall #86: a tab Reconnect swaps the id without remounting, so a held closure goes stale and main drops the command). **Text aimed at ANOTHER character rides a second, distinct path (v0.19.0): `window.api.sendUserText(sessionId, text)` → `SEND_USER_TEXT` → main → `ownerWindow(s).webContents.send(USER_TEXT)` → that GameWindow's `onUserText` → `dispatchUserText(text, { pushToHistory: true, clearInput: false })`.** Main is the hop because the target may live in a DECOUPLED window, which a renderer-side DOM event can never reach. Its two users are **Quick Send** (targets the cross-window `roster`, not the active session) and the **Overview's input bar**. Both used to call the raw `sendCommand`, and that produced the two bugs this path exists to kill: a Quick Send `wave` **arrived invisibly** on the receiving character (B199's signature — the raw socket write skips the renderer echo), and a `/command` sent that way was **forwarded to DR as literal text** because it never reached the slash intercept. Consequence to know: text arriving this way now ALSO splits on `;` and resolves aliases, exactly like typing — the pre-v0.19.0 notes saying Quick Send does neither are superseded. **The renderer-side echo (`setLines` `>cmd`) is SEPARATE from the send** — so *"a command echoes but the game never responds and no `>` prompt returns, while typed commands still work"* is the canonical signature of a send-side bug (stale id, or a dropped/edited-out `window.api.sendCommand` call). **As of v0.14.5 all THREE funnels also `logToSession` the `>cmd` echo** (`dispatchUserText` and `sendCommandSequence` always did; the canonical `sendCommand` was the gap — map walks / exit clicks / in-text links / trigger commands echoed on screen but never reached the Session Log). Log↔screen parity is the invariant: any new send path that echoes must also log (or do both via the canonical funnels). **The INVERSE signature — *the game responds but no `>cmd` echoes* — means a send path wired to a RAW `window.api.sendCommand` that skips the `setLines` echo** (B199, Sekmeht: trigger `command` actions did exactly this — `triggerCallbacks.sendCommand` was a raw send, so a trigger that sent `smile` showed only the game's reply). The fix is to route such a path through the canonical echoing `sendCommand` useCallback (echo `>cmd` + send) — for `triggerCallbacks`, via a `sendCommandRef` latest-closure ref because `sendCommand` is defined LATER in the component than the `triggerCallbacks` useMemo. When adding ANY new command path, send through the echoing `sendCommand` (or `dispatchUserText`/`sendCommandSequence`), NOT a bare `window.api.sendCommand`, unless a no-echo send is genuinely intended. The pure resolution core that decides WHICH strings get sent (`resolveMacro`/`resolveAlias`/`parseCursorMarker`/`matchKeyCombo`/`interpolate`/`getMacroToken`) is regression-covered by `tmp-cmd-harness` (see the testing note above); the component send call itself is not (pitfall #86 — re-read the body). **Command history has an app-wide MINIMUM LENGTH as of v0.18.3 (F82, Qij):** `SharedProfile.commandHistory.minLength` (0–5, **default 0 = remember everything**, so no existing behaviour changed), Settings → Behavior + `/history` / `/history min N`. The gate lives at the single push site in `dispatchUserText` — the ONLY writer of history — via `shouldRememberCommand` ([commandHistorySettings.ts](src/renderer/commandHistorySettings.ts)), read FRESH per push (a tiny localStorage read, so a Settings change applies instantly to every open character with no cross-window event — the `loadSessionLogSettings` precedent). Slash commands are ALWAYS remembered regardless of the threshold, length is measured after trimming, and the browse index still resets on Enter even when the command wasn't stored. **Command history is persisted per character as of v0.15.2 (F57):** [commandHistory.ts](src/renderer/commandHistory.ts) load/save on the `commandHistory` scopedKey (≤200 entries, try/catch'd write — background telemetry, never toasts; rides `state:` → YAML, deliberately NOT in `TRANSFER_CATEGORIES` — the automationStats precedent), the ↑-from-live-line draft is stashed in `historyDraftRef` and restored at index −1, and Esc clears the bar (layered under the slash palette's Esc — palette dismiss first, clear second). The push POLICY (which paths push — pitfall #31) is unchanged. Two one-time per-character macro seeds live side by side in GameWindow: `seededRepeatMacros` (v0.8.3, repeat tokens) and `seededNumpadMovement` (v0.15.2, F56 — the Frostbite-default movement pad); both skip already-bound keys and never resurrect deletions. The command input's first-session placeholder (F58) is gated on the initial history load being empty. **Two more v0.15.2 input-layer behaviors (DESIGN §38.6/38.7):** typed lines split on `;` (F59 — `splitTypedCommands` in [macros.ts](src/renderer/macros.ts), harness-covered: a line whose first non-space char is `;` is a LICH command returned VERBATIM — never split a `;line`; `\;` escapes; each part runs the full alias/echo/send/log tail; as of v0.19.0 Quick Send and the Overview input bar arrive through `dispatchUserText` too, so they split as well), and printable keys typed while focus is elsewhere FOCUS the command bar (F60 — end of the global keydown handler; guards: no modifier chords, not in a text field/select/contentEditable, `!anyModalOpenRef.current`, `!e.isComposing`, `!e.defaultPrevented`; always-on by design, no setting). Ctrl+F opens the in-scrollback search (F49, [ScrollbackSearch.tsx](src/renderer/components/ScrollbackSearch.tsx)) — its jumps UN-PIN then `scrollToIndex`, never a new scroll mechanism (pitfall #68).

### Slash commands & toasts (DESIGN §37, Phase 1 v0.14.5, Phases 2–3 v0.14.6)
A `/`-leading line in the command bar is a **CLIENT command** — it executes locally and NEVER reaches the game. Intercepted at the top of `dispatchUserText` ([GameWindow.tsx](src/renderer/components/GameWindow.tsx)), after the history push (↑ recalls), before alias resolution; unknown commands **fail closed** with a hint (a typo can't leak to DR); `//text` escapes to send a literal `/text`. **[slashCommands.ts](src/renderer/slashCommands.ts) is a declarative REGISTRY (the §35 capturer-registry philosophy)** — `SLASH_COMMANDS` entries (noun + aliases + verb + positional args + `key=value` options + flags + description + example + executor) drive the parser, the palette, AND `/help` from one source; **adding a command = one entry, never an if-chain**. Executors receive a `SlashContext` built in GameWindow's `runSlashLine` whose apply functions do exactly what a panel save does (`saveX` → `setX` → `saveProfile()`), and rule creation goes through the `newX()` factories — so a slash-created rule is byte-compatible with an editor-created one (`allGroups: true`, quota-safe saves). Result lines echo as `internal-system` client text + log to `[sys]`. The **palette** ([SlashPalette.tsx](src/renderer/components/SlashPalette.tsx)) mounts only while the input holds a slash line: command-list mode (↑/↓ select, **Tab/click completes, Enter ALWAYS submits as typed** — don't make Enter complete) → argument-hint mode (signature + live validation via `parseSlash` + a live highlight style preview). It's **portaled** (works when the command bar lives in a floating window, §33) at z 130, sized off `var(--game-font-size)` directly (pitfall #45b), and forwards keys via a `useImperativeHandle` ref called FIRST in `handleCommandKey`. **Scope (v0.19.0): the main command bar, Quick Send, and the Overview's input bar** — anything routed through `dispatchUserText`. Quick Send used to be excluded on the grounds that a client command aimed at another character is ambiguous; that was REVERSED at Sekmeht's ask because the alternative was worse: the raw path sent `/highlight add …` to DragonRealms as literal text. A slash command must never leave the client, so every send path that can carry user-typed text has to reach the intercept. It runs in the TARGET character's context (a broadcast therefore runs it once per character — useful for `/highlight add`, and the known wart is that a toggle like `/view` flips once per target). The PALETTE is still the command bar only — the other surfaces get no completion. Syntax rules when extending: noun-first, ONE positional sugar per command (highlight's color, contact's template), everything else `key=value`, flags bare. **Colors resolve via [colors.ts](src/renderer/colors.ts)** (curated 16 > user customs (`/colors add`, `SharedProfile.customColors`) > the ~148 standard web names (Genie's vocabulary) > `#hex`) — **resolve-at-entry: rules/themes store HEX**, palette edits never retro-update; editors' free-text color fields resolve names via `normalizeColorInput` **on BLUR only** (on-change hijacks longer names) and the Theme Editor commits on blur/Enter (theme vars always store hex). A noun may have BOTH a bare form and verbs (`findCommand`: verb wins, bare falls back — the `/colors` shape). **When a noun has a bare form AND verbs, the palette must NOT commit to the bare form on `noun ` — else the verbs never list or Tab-complete (v0.16.0 `/ai` bug).** `resolveCommand` ([SlashPalette.tsx](src/renderer/components/SlashPalette.tsx)) skips the bare (verb `''`) prefix for any noun in `NOUNS_WITH_VERBS`, so `/ai ` / `/colors ` STAY in list mode and their verbs (on/off/catchup, add/remove/list) rank + Tab-complete exactly like a verb-only noun (`/trigger`). The parser's `findCommand` still RUNS the bare form on Enter (`/ai`→status is unchanged); only the palette's *commit-to-arg-hint* is suppressed. This is why a bare+verbs noun must NOT also define a redundant `status`-style verb that duplicates the bare output (two identical rows) — give status to the bare form only. Both such nouns today (`ai`, `colors`) have a NO-ARG bare form, so suppressing the bare commit never hides an argument hint; if you ever add a bare+verbs noun whose bare form takes ARGS, this rule would swallow its arg-hint and needs revisiting. **Completion is RANKED** (`scoreEntry` in SlashPalette: exact part 100 > verb prefix 80 > noun prefix 60 > verb substring 30 > noun substring 20 > description 10; best-first stable sort, selection resets to top on every edit) — Tab completes what the user was typing, never registry order; noun-prefix TIES fall to registry position (bare `/t` → `/template add`; `/ts` is deterministic via the exact alias). **A required arg is required NON-EMPTY** unless its spec sets `allowEmpty` (only `/sub add`'s replacement — `""` = delete the text); without that check `/highlight add ""` mints a dead empty-pattern rule. When adding list-style verbs, pass `listRules`' `where` param if the rules live outside the Automations panel (contacts → Contacts panel). **Phase 2 (v0.14.6):** `/alias` (expansion splits on `;`), the `/trigger` quick-form (`"pattern" do "command"` — the `do` is optional sugar; fills `newTrigger`'s built-in command action; gates/multi-action stay editor-only BY DESIGN, don't grow the quick-form into the full editor), and **`edit` verbs on all five rule nouns** via `SlashContext.openRuleEditor(tab, id)` → GameWindow's `slashOpenRule` state → AutomationsPanel `*OpenId` props → each panel's `openRuleId` find-and-select effect (the TriggersPanel pattern — replicate it verbatim for any new panel; MacrosPanel's is `openAliasId` + `initialTab='aliases'`). The palette takes an optional **`live: SlashLiveData` prop** (template/mode/group/stream/theme names) for value-completion chips — extend THAT prop for new live completions; never import character state into the registry (it stays pure). **/help is TWO views:** bare = one plain-language row per NOUN from the `NOUN_HELP` blurb map (**new noun ⇒ add its blurb line**; a missing noun falls back to the entry description) + a syntax primer; `/help <noun> [verb]` = signatures with every arg/option HINT spelled out — hints are user-facing documentation, keep them current (Principle #11). **Result lines can be RICH** (`SlashLine = string | {rich:[{text,color}]}` — `/colors` renders each name in its color; GameWindow maps `color` → `TextSegment.fg`, `slashLineText()` for the log). **Record lookups on user tokens use the `hasColor`-style `hasOwnProperty.call` guard** — a bare `NAMED_COLORS[t]` walks the prototype chain and `"constructor"` resolves to a FUNCTION (a real shipped-then-caught bug). Slash-added aliases/triggers arm immediately (aliasesRef mirror; `useTriggerEngine(triggers, …)`). **Phase 3 (client control — `/mode`, `/group`, `/panel`, `/theme`, `/log search`, `/clear`):** these executors drive UI STATE, so the integration lives in GameWindow's `SlashContext` implementations, with the registry entries as dumb matchers — keep that split. Load-bearing details: `/panel` MUST branch on `layoutMode` (pitfall #79 — `slashOpenPanel`/`slashClosePanel` generalize `toggleFreeDebugWindow`'s shape in free mode, `addToZone(id,'bottom')`/`removeTab` in panels mode) and matches stream names case-insensitively but opens the CANONICAL id (Principle #5); `/theme` resolves id-FIRST then display name (two built-ins share "Classic", pitfall #35 — a shared name that isn't an id errors with the ids); `/mode`/`/group` ride `useGroups()` so rule gating updates through `activeGroupStates` automatically; `/clear` = the context menu's `clearLines`, deliberately no confirm. **Contacts/templates are in the registry too** (`/contact add "Bob" Friends` updates an existing contact rather than erroring; template names must EXIST — no silent minting; `/template remove` mirrors ContactsPanel.deleteTemplate, leaving members' `templateId` dangling): contact applies MUST use the pitfall-#36 blessed path (`saveContacts` + `setContacts` together), which the `SlashContext` built in `runSlashLine` does — don't hand a future executor a raw contacts write. **Toasts (§37.6):** `showToast()` in [toasts.ts](src/renderer/toasts.ts) (no-dep, CustomEvent) → [ToastHost.tsx](src/renderer/components/ToastHost.tsx) mounted once per window in App. Use toasts for app-level/background notices (quota failures, transfer results); use in-flow `internal-system` lines for command feedback. Never `window.alert` from the renderer (it freezes the whole window mid-play; `safeSetItem`'s quota warning is the precedent conversion). **Never `window.confirm` either (v0.19.7, B377).** A destructive action asks through `confirmAction()` / `confirmDelete()` ([confirm.ts](src/renderer/confirm.ts)), which ConfirmHost renders once per window beside ToastHost at the confirm tier (2080). An editor's own footer Delete uses `InlineConfirm` instead (DESIGN §43.7, UX #12).

### Profile system (DESIGN.md §20)
YAML files in `app.getPath('userData')/profiles/` — `%APPDATA%\lichborne\profiles\` in production (lowercase — `app.getName()` reads the package.json `name` field, NOT `build.productName`). `ensureProfilesDir()` in [profiles.ts](src/main/profiles.ts) is the canonical accessor. Two-stage migration (NSIS `preInit` + runtime fallback) rescues pre-v0.6.4 install-dir profiles.

- **Per-character setting** — write to `scopedKey(character, 'myThing')` and call `saveProfile()`. The dynamic `state:` pipeline picks it up automatically. No type changes needed.
- **Shared setting** — must be added to `SharedProfile` type + `buildSharedProfile` + `importSharedProfile`. (v0.15.2 additions: `lastSessionCharacters` — the F62 "Reconnect Last" roster snapshot, written non-empty-only by the primary window; and the four F37 `shared*` global-rule lists, which bridge the `lichborne._global.*` localStorage keys — see the Automations global-rules bullet.)
- **Schema versioning** — both files declare `profileVersion`. Migrations registered in `profile-migrations.ts` by SOURCE version. Adding optional fields is NOT a breaking change. Future-version YAMLs are skipped (preserved on disk).
- **Backups** — timestamped `{name}.yaml.{ISO-ish}.bak`, rolling retention of 5.

### Profile Transfer (F38, v0.10.0)
Platform-wide character setup Export/Import. As of v0.10.0 this is the **single Lichborne↔Lichborne path** — it superseded the old Automations export (that Export button was removed, and the ImportWizard's "Lichborne" source card was removed; the Automations panel's Import button is now legacy-client only, "Import from another client…" — see the Import wizard section). Launcher top-bar **"Transfer"** button → one modal, Export / Import tabs ([ProfileTransferModal.tsx](src/renderer/components/ProfileTransferModal.tsx), core logic in [profileTransfer.ts](src/renderer/profileTransfer.ts)). Export picks ANY source character (reads its YAML `state`, works disconnected) and writes a `.lb.yaml` bundle into a new **`Exports/` folder** (sibling of `profiles/`, `getExportsDir`/`ensureExportsDir` in [profiles.ts](src/main/profiles.ts)). Import lists files from `Exports/` (+ Browse… dialog defaulting there) and fans out to multiple already-added characters via checkboxes. Files: `profile-transfer:*` IPC in [main.ts](src/main/main.ts).

- **A Transfer category needs THREE registrations, not one: the `TRANSFER_CATEGORIES` entry, a slot in `applyProfileImport`'s `order` array, AND a switch case** — a category with only the entry exports fine and shows as a checkbox but silently applies NOTHING on import (the v0.14.0 Experiences category shipped exactly that; caught + fixed v0.14.6 while adding the Named Colors category). **Shared-data categories** (Theme's `myThemes`, Named Colors' `customColors`) are target-character-independent — they merge into the SHARED stores and rely on the modal's post-import `_shared.yaml` flush; don't route them through the per-character `store`.
- **What transfers is an explicit allowlist** — `TRANSFER_CATEGORIES` in [profileTransfer.ts](src/renderer/profileTransfer.ts). Ten categories grouping exact `state` suffixes: Display & Accessibility (`settings` minus `panelFontSizes`), Panel Layout (zone added-flags / tabs / activeIds / heights / `panelWidth` / `panelFontSizes`), Panel View Preferences (`mapViewMode`, `lichMapScale`, `streamTimestamps`, `scriptPalette`, exp prefs), Theme (top-level `theme` + shared custom theme), and Highlights / Triggers / Macros / Aliases / Groups & Modes / Contacts. **Deliberately excluded**: `seededRepeatMacros` / `seededNumpadMovement` / `mainTopMigrated` (internal one-time flags), `discoveredStreams` (ephemeral), `commandHistory` (character history, the `automationStats` precedent), `activeGroupStates` / `activeModeId` (only on Replace). **When you add a new per-character setting, decide if it belongs in a category here and add it** — settings default to NOT transferred until listed (safe default; a forgotten key is a papercut, never a target-breaker).
- **Non-destructive by construction** — import writes ONLY into the target's `state` map (per-key) + top-level `theme`. Identity/launcher top-level fields (`account`, `character`, `game`, `useLich`, `hidden`, `favorite`, `guild`, `circle`, `notes`, `profileVersion`) are never in `state`, so they're structurally unreachable.
- **Active vs inactive targets** — see pitfall #56. Inactive → YAML read-merge-write. Active (focused OR backgrounded session) → write localStorage working copy + remount the session via App's per-session reload nonce. **LB** is the project's short branding abbreviation (used in the `.lb.yaml` extension).

### Map system (DESIGN.md §19)
Two views: **Lich Map** (Lich image tiles) and **Genie Maps** (Genie XML-driven graph). Genie XML is the spatial source of truth for the graph view — coordinates come directly from the hand-curated XML files; no auto-layout, no BFS placement, no zone stitching. One zone visible at a time, picked by the dropdown or auto-switched when the player's room title matches a room in another loaded zone.

**Two prior approaches have been deleted; do not reintroduce.** The per-zone Genie Graph (`MapGraphView.tsx`, deleted v0.6.3) tried to render zones individually with the old augmentation matcher. The Lich-native BFS view (`LichGraphView.tsx` + `lichLayout.ts`, deleted v0.6.6) tried to make every Lich room renderable by inferring layout from `wayto` cardinals — but Genie's hand-curated coords and Lich's directional walks disagree in dense clusters, producing "type west, marker goes north" misrenders.

**Rendering matches Genie's `MapForm.cs` exactly.** 8×8 node rects are CENTERED on the XML position because Genie's `ConvertPoint(pos, 4)` SUBTRACTS the offset (MapForm.cs:187–193). Arc endpoints go through the XML position directly. Labels at `(pos + 1, pos + 1)` matching `DrawString(r.X + 1, r.Y + 1)`. Getting any of this wrong visibly misaligns label clusters; verify against the reference source instead of guessing.

**Arcs render in two passes** for dense-cluster legibility: under-pass at full opacity (drawn before nodes; lines disappear into rect fills inside clusters) + over-pass at 0.35 opacity drawn on top (faint trace across rects so you can follow a line all the way to its endpoint). Each pass collapses N arcs into 3 `<path>` elements (one per cardinal/climb/go category) via concatenated `M x,y L x,y` segments; same data, render twice, ~6 SVG elements total.

**Cross-zone stubs** are 1-room boundary markers with `note="MapXX_Name.xml"`. `isStubNode(n)` detects `.xml` in note aliases. Title lookup MUST prefer non-stub matches (otherwise the marker in zone A wins over the real room in zone B) — and it merges `byTitle ∪ byNormalized` *before* the non-stub filter, so a `byTitle`-only stub can't trap the match when the real room is reachable through the normalized form (B78).

**Ambiguous-title disambiguation — the Genie matcher (v0.7.0; exits + inline-desc capture + pure-resolver rewrite v0.11.7 — see pitfall #70 for the full story).** Same-titled rooms (a Barrows zone has 40+ "Whistling Wood, Barrows" nodes; the Crossing has 7 "Moonstone Street") are resolved by a **module-level PURE function `resolveGenieRoom(pool, normDesc, liveExits, prev, sourceFileToZoneId, staleCount)`** ([GenieMapView.tsx](src/renderer/components/panels/GenieMapView.tsx)), driven from a SINGLE effect that owns the breadcrumb (`prevLocationRef`) and the cross-zone hold counter (`staleCountRef`). **It is NOT a `useMemo`** — the old impure memo mutated those refs as it computed and double-counted under React StrictMode (dev), so it was rewritten; `pool` (the title-matched candidate list) stays a pure memo, but the stateful resolve lives in the effect (`currentLocation` is `useState`). This mirrors how GenieMaps' own client matches (NAME + EXIT-SET + DESCRIPTION — `Node.Compare`/`CardinalCount`, NodeList.cs). Resolution ladder: (1) single candidate; (2) **description-EXACT** unique; (3) **exit-set EQUALITY** unique; (4) **description-SUBSTRING** unique (≥24 chars, B148/B150); (5) **graph adjacency** over the FULL pool — the candidate joined by a Genie arc to `prev` (the last resolved room), preferring an exit-matched neighbour; (5b) cross-zone stub adjacency; (6) **exit-aware conservative cross-zone hold** (B110) — commits to the new-zone guess when it's exit-corroborated OR `prev`'s exits no longer match the live exits, else holds `prev` (3-strike escape); (7) file order last resort. **Two signals carry it:** the inline `<preset id='roomDesc'>` description is now captured into `roomState.desc` every look/move (pitfall #70) so the desc tiers actually fire, and the **exit set** (always-fresh, the game sends the compass every room) tracks the marker *while running* when DR hasn't re-sent a description. **Exits are ADDITIVE — never a pre-filter that removes candidates before adjacency** (that excludes the correct neighbour when Genie exit data is imperfect → marker strands; this was the v0.11.7 regression). The Lich Map's `findRoom` ([mapTypes.ts](src/renderer/components/panels/mapTypes.ts)) carries the same exact-then-substring desc logic (B150) and benefits from the inline-desc capture too. **The two maps differ in their PRIMARY signal:** the Lich Map matches by **room id first** (`lichDb.get(roomId)`, exact/robust — the parser reads the Simu room-id-flag `[Title] (id)` format, pitfall #65) and falls to title+desc when the id is absent; the Genie Map has **no room ids in its data**, so it is **title + description + exits** (exits make it track reliably while running, not "always degrading"). Matching is per-session/state-driven (MapPanel gets each GameWindow's own `roomState.{title,desc,exits,roomId}`, gated on the per-character `showMapOverlay`, NOT `isActive`) so it tracks for active AND backgrounded characters; only the camera/layout is active-gated (pitfall #24).

**Click model (v0.6.8): left-click pins, right-click walks.** Left-click a regular room → toggles a pinned gold BFS path (no walking). Left-click a stub → switches the displayed zone to its target XML. Right-click anything → walks (BFS in-zone, `sendWalkPath`). The map only auto-switches zones when the player's `roomTitle` actually lands in the new zone — never on walk completion (the game can block any command; racing the map ahead strands the user).

**Camera follow-the-player** uses `useLayoutEffect`, not `useEffect`, so the transform update lands in the same paint frame as the indicator's new world position. Default ON; manual pan/zoom turns it off; the ◆ button re-enables. Always-center model (not margin-snap). The follow effect gates on `followNode` — the *same* `visibleById.get(...)` lookup the indicator uses — so "marker visible" and "camera following" can't desync.

**Smooth camera motion.** Pan group is positioned with the CSS `transform` *property* (not the SVG attribute) so `.genie-pan-smooth` can `transition` it (`150ms linear` — linear, not ease-out, because a follow camera re-targets every step and ease-out's velocity reset pulses visibly). `snapTransform` (delta > 600px or > 20% scale vs last-painted transform) drops the transition for big jumps so zone switches / ◆ / fit cut instantly. The indicator carries the *same* transition + snap flag so it stays screen-centred during walks instead of bouncing. Gated on the `mapAnimations` setting (default ON): pan group + indicator only get `.genie-pan-smooth` when `!isDragging && mapAnimations`. (v0.6.8–v0.6.11 gated this on a separate `smoothScroll` setting; v0.6.12 removed that and folded the map glide under `mapAnimations`.)

**Current/selected/hover/hover-path/pinned-path indicators are hoisted OUT of `nodeRects`** as single overlay elements. Pre-hoist, walking re-built the entire `nodeRects` JSX every step. The current-room indicator is a sonar locator: two `genie-here-ping` expanding rings + the solid `INDICATOR_R` ring; the pings are exempt from the animation-pause via a higher-specificity rule.

**Per-color effect system (v0.6.7).** Every COLOR_LEGEND category has its own visual signature (heartbeat for Healer, coin glint for Shop, ripples for Water, leaves for Lumberjacking, dirt for Mining + Trailhead, vortex/drift/rise sparkles for the magical 4, etc.). The structure is uniform — adding a new effect is a 4-step pattern: (1) declare a `*_COLORS` set or extend `MOTE_EFFECTS`, (2) add a memo at the appropriate place in the component that builds the SVG elements per visible-node match, (3) add a CSS keyframe in `map-panel.css`, (4) render the memo at the correct layer in the SVG tree. Stub `↗` glyph always wins over category glyphs (cross-zone identity > resource category). `getMoteContrastColor(hex)` picks mote fills based on room luminance (light bg → dark motes, dark bg → light motes). `normalizeNodeColor()` in `parseGenieZone` converts CSS color names (`"Blue"`, `"Red"`, etc.) to canonical hex so downstream effect lookups don't silently miss rooms.

**Animation pause — two freeze classes (v0.6.7, extended v0.6.9).** The pan group `<g>` carries ONE of two mutually-exclusive freeze classes, both cascading `animation-play-state: paused` to descendants:
- `genie-pan-dragging` — *transient* freeze when `isDragging || inMotion`. `isDragging` flips on mousedown/up; `inMotion` flips true on any `currentLocation` change and resets a `MOTION_QUIET_MS = 800` timer. The locator sonar ping is **exempt** (`.genie-pan-dragging .genie-here-ping` keeps it running) — you still want to see yourself mid-walk.
- `genie-anim-off` — *permanent* freeze when `settings.mapAnimations` is off (default ON). **No ping exemption** — "off" stops the ping too.
The className picks one or the other (`!mapAnimations ? 'genie-anim-off' : (isDragging||inMotion) ? 'genie-pan-dragging' : ''`) so the ping-pause and ping-exemption rules never collide.

**Category effects are OMITTED (not paused) and CAPPED (v0.7.0).** Two layers of defense, both because SVG CSS animations are expensive — each animated element forces Recalculate Style every frame and is a compositor-layer candidate:
- *Omitted during motion.* `animation-play-state: paused` stops an animation advancing but leaves the element layer-promoted — profiling during travel showed residual Layerize ~21% even with effects "frozen". So the 10 animated per-category effect groups (sparkles, heartbeats, coin glints, ripples, bubbles, caution/implode rings, leaf/dirt falls, XP rises) render only when `showEffects = mapAnimations && !isDragging && !inMotion`. During travel/pan/off they're not in the DOM at all. They re-mount once the player has been still for `MOTION_QUIET_MS` (1500ms — long enough that normal sub-second running cadence never re-mounts mid-run; a short window would tear them up and down every step).
- *Capped to the nearest N rooms.* Idle in a dense zone (the Crossing) all effects on was ~29% Recalculate Style. The effect memos iterate `nearbyNodes`, not `visibleNodes` — `EFFECT_CAP` (30) rooms nearest the player (or zone-centre when viewing a zone you're not in), or the stable `EMPTY_NODES` ref when `!showEffects`. So at most ~30 rooms animate regardless of zone density, and the effect memos don't recompute during travel (dep is the unchanging `EMPTY_NODES`). **`auras` deliberately still uses `visibleNodes`** — they're static (no animation, no per-frame cost) and they ARE the colour map.

The freeze classes still cover what *stays* rendered (the sonar ping, the static/fire auras). Node **hover** is likewise suppressed during motion (`onNodeHoverEnter` checks `inMotionRef`) — the map scrolling under a stationary cursor otherwise fires a storm of pointer enter/leave → re-renders.

**`mapAnimations` setting (v0.6.9; sole map-motion switch as of v0.6.12).** `mapAnimations` (default ON) is the single toggle for ALL Genie map motion — the per-color category effects AND the camera glide. Off → `genie-anim-off` freezes effects and the glide is disabled (camera snaps). Per-character `AppSettings` field, persists via the dynamic profile `state:` pipeline, threads `GameWindow → PanelFrame → MapPanel → GenieMapView` (the `autoLinkUrls` path). (v0.6.9 also had a `smoothScroll` setting; removed v0.6.12 with the story-window smooth scroll.)

**Genie parse cache (v0.6.7).** First-time parse of a 122-XML maps folder takes several seconds (DOMParser is synchronous and chunky). `genie-cache:load` / `genie-cache:save` IPC handlers in `main.ts` serialize the parsed `Map<zoneId, GenieZone>` to `userData/genie-cache.json`. Subsequent launches verify a fingerprint (sorted `filename:mtimeMs:size`) against current folder state and skip re-parsing if it matches — load drops from seconds to ~50ms. Version field (`GENIE_CACHE_VERSION`) lets us invalidate every user's cache when the GenieZone shape changes without manual cleanup. Cache write is fire-and-forget after parse.

**Map render perf — two invariants (B231, v0.18.0).** The node layer is the expensive thing on screen (~1057 `<g>` groups in the biggest zone), so **(1) every callback in the `nodeRects` dep array MUST keep a stable identity** — `onNodeContextMenu` reads its inputs from `walkCtxRef` for exactly this reason; putting `currentLocation` (or anything else that ticks per room) back into a handler's deps re-creates the whole layer on every step (8.92ms → 0.09ms when fixed; pitfall #106). And **(2) anything derived per render must be memoized all the way to the ELEMENT** — the arc paths memoize both the segments (`arcSegs`) and the joined `<path>` elements, because the `.join()` was the expensive half. The sonar ping and healer heartbeats were profiled during this pass and are NOT a bottleneck (60fps, zero dropped frames at real density with them on) — don't "optimize" them away.

Files: [MapPanel.tsx](src/renderer/components/panels/MapPanel.tsx), [MapImageView.tsx](src/renderer/components/panels/MapImageView.tsx), [GenieMapView.tsx](src/renderer/components/panels/GenieMapView.tsx), [mapTypes.ts](src/renderer/components/panels/mapTypes.ts).

### Theme system (DESIGN.md §6)
~150 CSS custom properties in `themes.ts`. `darkBase` is the canonical full set; every other theme is a partial override merged over `darkBase` in `applyTheme` / `applyCustomTheme`. Theme Editor (`ThemeEditor.tsx`) uses a `TABS` declarative schema — new variables surface there (it exposes ONLY the general vars, not specialty ones like `--map-*`). **The structural automap colors (`--map-bg/chrome-bg/text/text-muted/border/border-subtle/btn-bg/btn-border/select-bg/node-fill/node-stroke/dot`) are NOT per-theme — they live in `MAP_STRUCTURAL_CASCADE` (one block wired to the general palette: `--map-bg`→`var(--bg-app)`, `--map-text`→`var(--text-secondary)`, …) which is spread into `darkBase` AND re-applied LAST in both `applyTheme`/`applyCustomTheme`, so the map always follows App Background / Game Text / borders on every theme and live-updates in the editor (B161). Only the SEMANTIC map cues stay per-theme: `--map-arc-*` (directional path lines), `--map-current-color` (the "you-are-here" marker), `--map-select-color`. Don't pin a structural `--map-*` in a theme — it's inert (the force-apply overrides it); see pitfall #75.**

### Automations, groups, modes (DESIGN.md §8)
Highlights, triggers, macros, aliases, contact templates, **and mutes** share a `groupIds: string[]` + `allGroups: boolean` shape. `isRuleActive(rule, activeGroups)` gates them. New rule types should follow this pattern and default to `allGroups: true`.

- **Global cross-character rules (F37, v0.15.2 — DESIGN §39).** Highlights/triggers/macros/aliases/**mutes/substitutes** (all six rule types; mutes/subs joined at Sekmeht's ask, same release) can also live in a GLOBAL store that applies to every character: the **virtual `_global` character scope** (`GLOBAL_RULES_SCOPE` + `asGlobalRules` in [characterScope.ts](src/renderer/characterScope.ts)). The rule stores/panels work on it UNCHANGED (they only see `scopedKey`); the disk bridge is **`_shared.yaml`** (`SharedProfile.sharedHighlights/Triggers/Macros/Aliases`, raw-key read/write in profile.ts), NOT a character YAML. GameWindow merges globals AFTER the character's lists at each engine input (**character-first** — ties/key/prefix conflicts resolve to the character's rule), normalized **always-active** (groups are per-character; the editors take a `hideGroups` prop in Global scope). Live sync = `lichborne:global-rules-changed` custom event (same window) + `storage` events on the `lichborne._global.` prefix (cross-window). The AutomationsPanel header's "This Character | All Characters" switch re-points a `CharacterProvider` at `_global`; the analytics prune counts global ids as live (fires record under each character). **When adding a rule-consuming feature, decide whether it reads the merged list (engines do) or the character list (editors/saves do) — merged arrays are DERIVED, never saved.** Global contacts are deliberately NOT built (presence tracking mutates contacts at runtime — a shared store races across windows; needs its own design). No slash `global` flag yet (recorded, §39.5). **F63 (same release, DESIGN §39.6–39.7):** each of the four editors has an **"Applies to" scope-MOVE control** (rendered only when AutomationsPanel passes `onMoveScope` — the storage stays two stores, the toggle is a deliberate move; content-identical targets → source removed + toast, never a duplicate); rule content-identity lives in [ruleIdentity.ts](src/renderer/ruleIdentity.ts) — **ONE definition shared by the move, Transfer's append-dedup, and Transfer's global-awareness; change identity semantics there, nowhere else.** Transfer has a merge-only "Global Rules" shared category (`globalRules` — runs FIRST among rule categories so the per-character global-dup filter sees the merged store), and all six per-character rule categories skip incoming rules already present in the destination's global store (both merge modes). **The header scope switch renders on EVERY Automations tab** — greyed + tooltip on Groups (the only non-capable tab) — so header buttons never shift position between tabs (Sekmeht's disabled-in-place-beats-disappearing call; apply it to future header controls).

- **Automation Analytics — usage stats + health, off by default (v0.14.4, DESIGN §36).** A self-tuning layer over the user's OWN native automations (display/config — not a Lich concern). One **app-wide master toggle** (`SharedProfile.automationAnalytics`, mirrors `bulkConnectSeparateWindows` — localStorage working copy `lichborne.automationAnalytics` + a same-window `lichborne:analytics-changed` custom event the GameWindow's `analyticsEnabledRef` listens for, because a cross-window `storage` event never fires in the writing window). **Two layers:** (a) **static health** — pure analyzers in [automationHealth.ts](src/renderer/automationHealth.ts) (`analyzeHighlights/Triggers/Macros/Aliases/Mutes/Substitutes` → broken / duplicate / no-op / **key-or-name conflict**), reusing the existing `buildXRegex` so "broken" means exactly "won't compile"; (b) **runtime usage** — per-rule fire counts + lastFired in [automationStats.ts](src/renderer/automationStats.ts) (`recordFire`, in-memory cache + 800ms debounced flush, per-character `scopedKey('automationStats')` → rides `state.*` into the profile YAML for free, **excluded from Profile Transfer** as character history). **Storage is bounded — two rules.** The flush is **try/catch'd, NOT `safeSetItem`** (background telemetry must never throw or alert the user — a quota-failed stats write just skips; the live cache keeps the count). And the only unbounded vector — `recordFire` keys by `ruleId` and never deletes an entry, so a DELETED rule's stats would linger forever (worse on re-imports, which mint new nanoid ids) — is closed by **`pruneStats(character, liveIds)`**, called from AutomationsPanel's mount when analytics is on: it drops every stats entry whose id isn't in the union of all six current rule lists, so the map is bounded by the live rule count (Sekmeht: "I don't want this to bloat anywhere"). The map is ONE flat id→stat record across all six types, which is why the prune needs the union of all six `loadX(character)` id sets, not one panel's. **Reset + prune persist to YAML, not just localStorage.** `resetStats` writes an empty map and `pruneStats` shrinks it in localStorage — but YAML is the truth that re-seeds localStorage on next connect (`importCharacterProfile` copies every `state` key back), so a clear that never reached YAML would re-appear next launch. Both therefore trigger the panel's `onSaved` (→ scheduled profile save, flushed on shutdown): `useRuleAnalytics(…, onSaved)`'s `reset` calls it after `resetStats`; AutomationsPanel calls it when `pruneStats` returns `>0`. The stats flush is `try/catch'd` not `safeSetItem` (background telemetry never alerts/throws). **The runtime hooks ride EXISTING per-line work (pitfall #82) — gated on `analyticsEnabledRef.current`:** triggers piggyback `triggerCallbacks.onFire` (already per-rule); highlights ride the Debug fire-scan (`logHighlightFiresRef` gate widened to `showDebug || analytics`); mutes/subs report via an optional `onFired(ruleId)` added to `applyMutesToSegments`/`applySubstitutesToSegments`; macros/aliases count at resolve time (`resolveMacro`/`resolveAlias` now return `ruleId`). **OFF → zero added cost.** UI: shared [AutomationAnalytics.tsx](src/renderer/components/AutomationAnalytics.tsx) (`useRuleAnalytics` hook + `RuleBadges` per-row + `AnalyticsReview` banner) — the master toggle is in the AutomationsPanel header, each sub-panel wraps its body in an `.aa-host` column with a **full-width** Review banner above the list (NOT in the ~210px sidebar — it was unreadable there), the Review is **collapsible per-category** (each issue type a `<details>`, capped at 200 rendered entries — a real ruleset has 1000s of duplicates), jump links call the panel's real `selectRule`/`selectAlias`/`selectMacro` (NOT bare `setSelectedId` — the detail pane renders off the `draft` that the selector sets), and the **"Remove duplicate copies"** bulk action (keep one of each set) lives in the Duplicate section. `useRuleAnalytics` live-refreshes every 2s while open (recordFire MUTATES the cached stats object in place, so the panel needs a periodic re-read or counts never update / Reset looks dead). **When adding a new rule type, add an `analyzeX` + thread the runtime hook; keep all six in sync.**

- **Mutes & Substitutes — two display-layer rule types (DESIGN.md §31, v0.12.0).** **Two separate Automations tabs** ("Mutes" → [MutePanel.tsx](src/renderer/components/MutePanel.tsx), "Substitutes" → [SubstitutesPanel.tsx](src/renderer/components/SubstitutesPanel.tsx); Sekmeht chose separate top-level tabs over one nested "Text Modification" tab) that act on incoming main-window game text. Both are **native, NOT delegated to Lich `textsubs.lic`** (display concern; must work without Lich per Principle #2; portable via Profile Transfer; clean import target). Same shape as highlights (`scopedKey('mutes')` / `scopedKey('substitutes')`, ride the `state.*` map, **no profile-shape change**); compiled+group-gated into `activeMutesRef`/`activeSubsRef`; applied by **GameWindow's `applyTextMods` — mute → substitute → (highlight at render)** — DISPLAY ONLY (the Session Log captured the raw line per-event upstream, so neither loses history). **Stream scope = GLOBAL by default** (the pass runs over `newMain` AND every `newStream[key]` buffer; Sekmeht's call = Genie's global model), with an **optional per-rule `stream` restrict** (Frostbite's `target`): a rule applies everywhere unless `rule.stream` is set, then only to that stream (`!rule.stream || rule.stream === streamId`). The editors' **"Apply to"** dropdown (`STREAM_OPTIONS` in mutes.ts) sets it; we do NOT auto-skip Thoughts (unlike Genie). **On IMPORT, substitutes come in GLOBAL** (no per-rule stream — "substitute wherever the text shows up", the Genie model; Sekmeht 2026-06-08); the per-rule restrict exists for users who want it, but nothing auto-scopes on import. (An earlier experiment scoped Frostbite's `target` to streams and special-cased an `exp` pseudo-stream so mind-state subs could fire on the skill readout — **all reverted**: see the mind-state note in the import bullet for why those are unsupported instead.) Transferable via **separate `mutes` + `substitutes` categories** in `TRANSFER_CATEGORIES`. **Right-click menu** ([ContextMenu.tsx](src/renderer/components/ContextMenu.tsx) now supports **nested submenus** — hover-expand, viewport-flipping + the root-menu viewport clamp): the game-window menu groups display-changing actions under **`Modify Text ▸`** (Highlight / Mute / Substitute, each ×"word"/"this line") and automation under **`Trigger ▸`**, then Show in Log / Clear — four short root rows instead of ten flat ones (Sekmeht's framing: "modify the text" vs "build something from the text").
  - **Mute** (renamed "Gag/Ignore" — friendlier) removes matching text, with a **`scope` like highlights**: `line` (default — drop the whole line, the typical gag) or `match` (strip only the matched text — e.g. mute a name without nuking the room you're in). `applyMutesToSegments` ([mutes.ts](src/renderer/mutes.ts), global regex): `line`→`null` (drop TextLine), `match`→`replace(re,'')` per-segment **+ tidies the whitespace gap** (collapse double-space, drop space-before-punct, trim seams) so `…healer Quentin.`→`…healer.`. **Right-click** (like Highlight/Trigger): `Mute "word"` (new `match` mute) / `Mute this line` (new `line` mute) → `openMuteEditor` → `mutePrefill` → AutomationsPanel `mutes` tab opens an unsaved draft.
  - **Substitute** rewrites matched text → `replacement` (native JS `$N`/`$&` capture refs). `applySubstitutesToSegments` ([substitutes.ts](src/renderer/substitutes.ts)), per-segment, after mutes/before highlights. Game-state vars NOT interpolated (capture groups only — what the source clients use). **Right-click** `Substitute "word"` / `Substitute this line` → `openSubstituteEditor` → `substitutePrefill` → Substitutes tab opens an unsaved draft (empty replacement to fill in).
  - **Import** (the count-only "belongs in Lich" notices are gone; terminology: **Mute = Wrayth `<ignores>` = Genie `gags.cfg` = Frostbite `ignores.ini`**; **Substitute = Genie `substitutes.cfg` = Frostbite `substitutes.ini`**, Wrayth has none): Mutes from all three (Genie `#gag`/`#ignore`, Frostbite `[ignore]`, Wrayth `<h>` inside `<ignores>`); Substitutes from Genie `#sub`/`#subs`/`#substitute` (regex, `$N` kept) and Frostbite `[substitution]` (regex, `\N`/`\0`→`$N`/`$&`, via `unescapeQtRegex` — NOT `unescapeQtIniValue`, whose punctuation-collapse would mangle regex escapes like `\(`). Each gets its own preview tab. **Imported substitutes are GLOBAL** (no stream scope — Genie model). **EXCEPTION: Frostbite `target=Experience` subs are flagged `unsupported`** (shown greyed in the preview with a note, NOT imported by default): these are the mind-state numbering rules (`(clear)`→` 0 $1`, `(very rapt)`→` 31 $1`, …) for Frostbite's dedicated Experience WINDOW. Lichborne shows the skill readout in the **main window already space-aligned**, so substituting a variable-width number (1-digit `0` vs 2-digit `31`) into it **breaks the table's column alignment** — and the patterns are bare words (`(clear)` has no boundary) so applying them globally also mauls normal prose. Flagging unsupported (detected by `target === 'Experience'` in `parseSubstitutes`) was Sekmeht's call after we tried & **reverted** an `exp`-pseudo-stream approach (storm-gray/frostbite: 89 subs → 54 ready + 35 unsupported). The precise `target=Main` damage/protection subs (lookahead-anchored, `poor → poor (2/27)`) import normally as global and fire fine. **Frostbite array sections iterate actual indices, not `size=N`** (stale-size, pitfall #73). Imported rules **persist exactly like highlights** — `saveMutes`/`saveSubstitutes` are byte-identical to `saveHighlights`, and the wizard's `onSaved` chains to GameWindow's `setMutes`/`setSubstitutes` reload + `scheduleProfileSave` → YAML. **Each AutomationsPanel tab panel loads its rule list once on mount (`useState(() => loadX(character))`), so after an import it would show STALE/empty until a tab switch remounts it** — fixed by an `importNonce` in [AutomationsPanel.tsx](src/renderer/components/AutomationsPanel.tsx) that bumps on the wizard's `onSaved` and is part of every tab panel's `key`, forcing a remount+reload on import ONLY (a panel's own edits don't bump it, so editing never resets its UI). **The key MUST be tab-unique (`` key={`${panelType}-${importNonce}`} ``), NOT a bare `importNonce`:** Macros and Aliases are the SAME component (`MacrosPanel`, differing only by `initialTab`, which `useState` reads once on mount), so a shared `key={importNonce}` let React reuse the one instance across those two tabs and the Macros tab rendered Alias content. Any new AutomationsPanel tab needs `` key={`<type>-${importNonce}`} `` too.

- **Highlight overlap resolution = SPECIFICITY + per-property compositing (v0.11.3, the Profanity model).** In [renderSegmentFull.tsx](src/renderer/utils/renderSegmentFull.tsx): contacts still beat highlights (B116); among match-scope highlights covering a run, each visual property (text color / background / bold / glow) is taken INDEPENDENTLY from the **smallest (most-specific) covering highlight that sets it** — so a word-highlight's color punches through a broad phrase highlight while the phrase's background still shows, and a bold-only/bg-only highlight layers onto another's fg. Equal-length ties go to the first-encountered (top-of-list) highlight (deterministic, unlike Profanity's arbitrary unstable sort). **This replaced v0.11.2's "first/top-of-list wins, all-or-nothing" model** after the cross-client research below (Sekmeht's call: specificity is more dynamic and doesn't force users to manage list order at scale). Consequences: the `HighlightRule.priority` field stays **vestigial**, and there is **deliberately NO reorder UI** in HighlightsPanel — a v0.11.2 ▲/▼ reorder control was added then **removed in v0.11.3** because list order no longer determines precedence (it would falsely imply a priority mechanism; specificity does the work, and ties fall to first-in-array silently). Line-scope highlights use a separate path (`getLineHighlightStyle`, first-match) but are NOT just a container tint: **a line-scope highlight's TEXT COLOR must override preset/fg segment colors** (B200, Cherisse — otherwise a "Line — entire line is styled" highlight is invisible on thoughts/speech/lnet/substituted lines, because `[data-preset]` colors the segment and a child's explicit color beats the container's inherited one). The line color is threaded down as an `overrideColor` param to `renderSegment`/`renderSegmentFull` (applied at the 2 line-render sites — `TextLineRow`, which covers the main window + all stream panels, and `RoomPanel`); the non-match runs render it inline (beating the no-`!important` `[data-preset]` CSS), keeping the preset's bg/italic + bold/links; match-scope runs still punch through with their own color; contacts keep their template color (B116). bg-only line highlights leave text colors alone (only `color` is threaded). If you change the property-resolution, keep the merge `key` in sync (adjacent runs merge only when the composite key matches).
- **Highlight overlap resolution across front-ends (research, v0.11.3 — TheTargonian's question "the higher in the list wins? is that how Genie does it?").** There is **no single convention**; the four DR/GS front-ends use three different models, along TWO axes. *Axis 1 — what decides precedence:* **Wrayth & Frostbite = list position, BOTTOM wins** (last/lowest in the list supersedes; Frostbite verified in [highlighter.cpp](file:///c:/temp/Frostbite-Dev/frostbite/gui/text/highlight/highlighter.cpp) — it walks the list top→bottom inserting nested HTML `<span>`s, so the innermost/last color wins; user-reorderable). **Genie = pattern text, sorted** (its `HighlightList`/`HighlightRegExpList` are `SortedList`s keyed by the pattern string — [Globals.cs](file:///c:/temp/Genie4-Dev/Genie4/Lists/Globals.cs), applied in [ComponentRichTextBox.cs](file:///c:/temp/Genie4-Dev/Genie4/Forms/Components/ComponentRichTextBox.cs) via `SelectionColor=`; last-applied in sorted order wins, NOT user-orderable). **Profanity = specificity, smallest match wins** ([highlight_processor.rb](file:///c:/temp/Profanity/ProfanityFE/lib/highlight_processor.rb): per segment, `color_list.sort_by { |h| h[:end]-h[:start] }` then takes the first non-nil — smallest/most-specific range wins; ties arbitrary via Ruby's unstable sort; NOT user-orderable). **Lichborne v0.11.2 was list position, TOP wins** (the outlier direction). *Axis 2 — all-or-nothing vs per-property compositing:* Profanity, Frostbite, and Genie all resolve **fg / bg / underline INDEPENDENTLY** (a small fg-only highlight punches its color through a broad bg-only highlight; the two compose — red text on the broad highlight's yellow bg). v0.11.2 Lichborne was the only all-or-nothing one. **DECISION (v0.11.3): Lichborne adopted Profanity's model — specificity (smallest match wins) + per-property compositing** (the bullet above is the live behavior). Rationale: it's the most dynamic, handles overlaps well WITHOUT forcing users to maintain a precedence order (which Sekmeht judged unmanageable at scale). We diverge from Profanity in ONE detail: equal-length ties are deterministic (first/top-of-list) rather than arbitrary. We did NOT adopt Wrayth/Frostbite bottom-wins or Genie pattern-sort.
- **Built-in variable catalog (v0.11.2 expansion).** Trigger/macro/alias `$var` interpolation resolves from `buildVars` ([useTriggerEngine.ts](src/renderer/hooks/useTriggerEngine.ts)) and `buildMacroVars` ([GameWindow.tsx](src/renderer/components/GameWindow.tsx)) — these MUST stay in sync, and the editor display lists (`INTERPOLATABLE_VARS` in [triggers.ts](src/renderer/triggers.ts), `MACRO_VARS`/`ALIAS_VARS` in [macros.ts](src/renderer/macros.ts)) MUST be updated when you add one. Current set: regex groups (`$0/$match`, numbered `$1..`, **named** `(?<who>…)→$who`), `$line`, `$characterName`, `$date`/`$time`/`$timestamp`, vitals (`$health/$mana/$stamina/$spirit/$concentration`), `$rt`, `$ct`/`$casttime`, `$stance`, `$spell`/`$preparedspell`, `$left`/`$right`, `$room`/`$roomname`, `$roomid`, `$exits`, and indicator booleans (`$bleeding/$poisoned/$diseased/$stunned/$webbed/$joined/$hidden/$invisible/$dead`, plus **`$unconscious`**, which has NO indicator tag — it is decoded from the `U` in the status prompt, so it stays `false` for a player without `set statusprompt`, B426). Sources are live per-session state (`triggerCtxRef`), so adding one = thread the value into `TriggerGameState` + the GameWindow event handler that populates `triggerCtxRef` + both var-builders + the display lists. Unknown `$foo` interpolates to the literal `$foo` (working-as-intended). No profile-shape change — these are read-only resolution sources.

### Login & character-selection flow (v0.8.0, DESIGN.md §13.6)
Empty state and "+" tab open `Launcher.tsx` + `AddCharacterWizard.tsx`, not `LoginScreen.tsx` (kept only as type-host for `SessionInfo`). `LichSetupFields.tsx` is the single source of truth for Lich path/port/frontend editing — its render sites are `LichSetupDialog.tsx` (shared by the Launcher's ⚙ button **and** the Settings panel as of v0.9.2 — Settings launches the dialog via an "Open Lich Setup…" button rather than embedding the fields) and the legacy `LoginScreen`. All wrap it in `.login-form` for CSS scoping. The XML-stream-mode dropdown is labeled **"Lich frontend"** (v0.9.2; sentence case since v0.19.7) — Lich's own term: each `--stormfront`/`--wizard`/`--avalon`/`--frostbite`/`--genie` flag sets `$frontend` in Lich's `argv_options.rb`. The stored field is still `lichMode`; only the label changed.

- **Launcher rendering rules** are documented in DESIGN.md §13.6. Key invariants: **Favorites is collapsible and defaults to EXPANDED** (v0.18.0, Sekmeht — supersedes the former "always-open"; the localStorage key `lichborne.launcher.favCollapsed` stores the **COLLAPSED** flag precisely so an absent key = expanded, i.e. no existing install folds shut on upgrade — use that inverted-flag shape for any future default-ON toggle); per-account sections default collapsed but **single-account is always expanded** and **freshly-added accounts auto-expand once** (wizard writes the account name to `lichborne.launcher.expandedAccounts` before `onCompleted` bumps `refreshKey`, and Launcher's refresh effect re-reads it). **The account/Favorites sections render in the canonical About-modal chrome as of v0.18.0** — see the modal-chrome tokens in [global.css](src/renderer/styles/global.css) — and "+ Add account" is a full-width dashed row the height of an account header (v0.19.7, F111; it used to be a grid tile). **The compact launcher (the + modal) offers the same ways in as the full one since v0.19.7 (F110)** — Teams, pinned teams, Reconnect Last / Team Login / Attach; only account Remove, Transfer and Lich Setup stay full-screen — and it seeds its list from the last one this window loaded (`lastLoadedCards`, B323), because the modal mounts a fresh Launcher on every open.
- **AddCharacterWizard is account-driven, not character-driven** (v0.8.0). Step 1 collects account/password/game; step 2 runs `eaccessFetchCharacters` (mode-agnostic SimuCo auth, no Lich needed) and bulk-writes one stub `CharacterProfile` per checked discovered character. Existing profiles are listed but disabled. No connection happens in the wizard — tiles appear, user clicks Connect when ready.
- **Per-tile state is launcher-owned and writes immediately** via small read-modify-write helpers (`setCharacterFavorite`, `setCharacterGame`, `setCharacterUseLich`, `setCharacterHidden`, `patchCharacterProfile` for the notes editor). Never go through the GameWindow's debounced `scheduleProfileSave` for these — that's a different ownership track (see Pitfall #26 below).
- **Team Login** (renamed from "Bulk Connect" in v0.18.3 — DISPLAY ONLY; the filename, the `bulk-connect` menu action, `bulkConnectSeparateWindows` and `bulkSets` are all unchanged, because they are contracts rather than labels) ([BulkConnectPicker.tsx](src/renderer/components/BulkConnectPicker.tsx) + `runBulkConnect` in App.tsx) runs sequential per-account connects. DR's one-character-per-account rule forces this; sequential is also the simpler error-isolation model.
**F85 (v0.18.3, Binu): per-account include checkboxes + NAMED SETS.** A set is a TEMPLATE of character names (`SharedProfile.bulkSets`, app-wide — a set spans accounts by definition), one per account by construction. Loading one replaces the whole selection; connect-time drops whoever is already on. **Saving a set uses a SEPARATE roster function from the connect list** — the connect list excludes already-connected accounts, so reusing it silently saved sets without any character who happened to be logged in while you built it. **The ▦ Sets… dropdown it originally shipped with is GONE (v0.18.4, F97)** — it named a noun and explained nothing, so a collapsible **Teams** section on the launcher replaced it, where the members ARE the explanation. Teams sit between Favorites and the accounts, and Favorites is now the quick-select block for pinned TEAMS as well as characters. Launching still routes through `handleReconnectLast`, inheriting `planReconnect`'s already-on skips, one-per-account dedup and keep/switch chooser. `BulkSet` gained optional `favorite` and `notes`; **every function that REBUILDS a BulkSet must copy them** — `coerce` runs on the way OUT of `saveBulkSets`, so an omission is data loss, and `upsertBulkSet` merges rather than overwrites because Team Login supplies only name+roster (pitfall #121). Saving in the picker is an opt-in checkbox, not a zone: unticked it is honestly a multi-character login. A run can be **stopped** part-way (F98) — Stop, not Cancel, because the login in flight cannot be aborted, so it lands and is kept while the rest are skipped. 
- **Same-account conflict** raises a confirmation modal (Cancel / Disconnect-and-continue) instead of refusing. The disconnect uses the awaitable `disconnect-await` IPC; the conflicting tab stays in the bar in disconnected state.
- **Shutdown** uses `gracefulDisconnect({ quickClose: true })` which calls `socket.end()` (guaranteed flush of QUIT) + force-close without waiting for server ack. ~300ms total regardless of session count. In-tab Disconnect and the conflict-modal auto-disconnect keep the full 5s ack-wait — they need the account slot confirmed released for the next action.

### Top chrome: app-bar, native menu & the menu-action bridge (v0.10.0, top-chrome redesign)
A tester-driven space-reclaim pass (TheTargonian, F39): Lichborne was showing ~3 fewer lines of game text than Frostbite because of stacked full-width chrome rows. Two reclaims shipped, plus a menu build-out.

- **Unified app-bar** ([AppBar.tsx](src/renderer/components/AppBar.tsx)) — one **app-level** row (rendered by AppShell when `!isEmpty`, replacing the old bare `CharacterTabBar` row) holding the **Lichborne brand + a connection status dot** (from the active session's `useSessions().status.connected`; **the wordmark also wears the active character's `settings.brandEffect`** as of v0.19.7 — a per-character text effect delivered through `SessionStatus.brandEffect` and painted by the shared [brandMark.tsx](src/renderer/utils/brandMark.tsx), DESIGN §8.3 and pitfall #147), the **character tabs** (composes `CharacterTabBar`), the **action buttons** (Debug/Logs/Panels/Maps/Contacts/Automations/Lich/Theme/Settings), and **Disconnect/Reconnect** (F108). The per-session `game-toolbar` that used to live inside every GameWindow is **gone** — that's the reclaimed row. This realizes DESIGN.md §13.3's original single-row intent (which had drifted to two rows). CSS in [app-bar.css](src/renderer/styles/app-bar.css); button/brand classes are shared from `game.css` (globally available in the bundle).
- **The menu-action / session-action bridge** is how app-level UI drives the **active** session. Native menu clicks (main process) → `menu-action` IPC → preload `onMenuAction` → `App`'s subscriber, which **splits**: session actions (`isSessionAction`, [src/shared/menuActions.ts](src/shared/menuActions.ts)) re-dispatch as a `lichborne:session-action` **DOM event**; app actions (login-character, bulk-connect, profile-export/import, quick-send, close/next/prev-character, check-updates) run in App via a latest-closure ref (so they see live `sessions`/`activeId`). The **app-bar buttons skip the IPC** and dispatch the DOM event directly (they're already in the renderer). The **active** GameWindow handles `lichborne:session-action` via a latest-closure ref (pitfall #31 pattern) guarded on `isActiveRef` — every mounted GameWindow hears it, only the active one acts. Each case mirrors what the old toolbar button did. Adding a new app-bar/menu action = add the string to `SESSION_ACTIONS`/`APP_ACTIONS` + a case in GameWindow's `runSessionActionRef` (session) or App's `runAppActionRef` (app).
- **ModeSwitcher moved to the Icon Bar** (its `trailing` slot) — it reads `useGroups()`, and `GroupsProvider` is **per-session inside GameWindow** (pitfall #6 isolation), so it can't live in the app-level bar. See pitfall #57. Its portal dropdown right-aligns / flips up / clamps to the viewport (it now sits at the row's right edge, possibly a bottom Icon Bar).
- **Native menu** ([main.ts](src/main/main.ts) `setupMenu`) is the full tree **File · Edit · View · Tools · Lich · Window · Help**; Lichborne items are click-only (hotkey policy in Operational guardrails). The Window menu also hosts the **decouple** items (Move Character to New Window / to Main Window, v0.11.0). `refreshMenuState()` scopes every Window-menu item to the **FOCUSED window's** owned-tab count (re-run from `sendStatus`/`createSession`/`destroySession` AND `win.on('focus')`): Next/Previous + Move-to-New need 2+ tabs in that window, Close needs 1+, Move-to-Main only enables in a secondary (decoupled) window — NOT global counts (that mis-shows items as enabled in separate-window mode). Disconnect/Reconnect on the app-bar is driven by the active session's `connected` flag. **Reconnect is App's `handleReconnectTab` — the same IN-PLACE reconnect as the tab and card menus** (F108, v0.19.7). It replaced a Login button that destroyed the tab and opened the picker, throwing away the scrollback, while the good path sat hidden in a right-click menu. Logging in a different character is the + tab.
- **Compact vitals** (opt-in, `settings.compactVitals`) — dense half-height strip with acronym labels (DESIGN.md §5.1).
- **Done in v0.10.0 deferred-polish:** **(a)** per-button **active-state glow** — `SessionStatus` gained a `panel*` flag for EVERY toggle button (`panelDebug/panelLogs/panelMap/panelLich/panelManager/panelAutomations/panelSettings/panelContacts/panelTheme`, pushed by a GameWindow `updateStatus` effect), and AppBar applies the matching `btn-*--active` for the **active** session, so every panel/overlay button glows while its panel is open (inline ones + the four in the More menu; the ⋯ button glows when a hidden one is open). **As of v0.19.7 (B345) every toggle button's open-state rule lives in ONE file, [game.css](src/renderer/styles/game.css)** — app-bar.css used to restate the Panels/Automations/Settings ones, and at equal specificity bundle order decided which file won. An open button reads as accent text + accent border + an inset accent ring and KEEPS it on hover, while hovering a CLOSED button is neutral (the old open state was a 10% tint, nearly indistinguishable from hover — pitfall #107(b)). app-bar.css keeps only the More-menu items' open state (an accent bar). Add a new toggle button's `--active` rule to game.css's group, not a second file. **And a button is lit only while the thing IT toggles is open (B346)** — Experiences was lit whenever any Experience was showing, and a docked Experience tab is permanent layout, so it never went dark; it now lights for its shelf only. A "something is live" hint is a different state and would need a different, quieter marker. **(b)** a static **"More ⋯" dropdown** in AppBar holding the less-used buttons (Debug/Logs/Contacts/Theme) — inline buttons are Panels·Maps·Automations·Lich·Settings — so the bar stays usable on narrow windows with no width-measurement. **v0.13.4 (B178) extends this into a degradation ladder:** window `minWidth` is now **480** (users tile multiple windows), and em-based container-query tiers on `.app-bar` hide the wordmark + compact the buttons (≤ 71em), then fold the five inline buttons into the ⋯ menu (≤ 58em) via always-rendered CSS-gated twins (`app-bar-collapsible` / `app-bar-more-item--overflow`) — Disconnect/Reconnect never collapses. The thresholds are em (container queries resolve em against the bar's game-font anchor) so they track the font setting; don't convert them to px media queries. **`container-type` makes the bar a STACKING CONTEXT** (layout containment), which buried the More dropdown under the game area (B179) — the bar carries `position: relative; z-index: 70` (content/WindowLayer ≤ 60, overlays ≥ 100) to lift its whole context; don't portal the menu instead (the `--overflow` items are gated by the bar's own `@container` query and a portal breaks descendance), and audit popovers any time you make an element a query container. **(c)** the vestigial GameWindow `status`/`scriptPalette` state was pruned (AppBar derives connection state from `useSessions`).
- **Still deferred:** Phase 3 (logo into the brand slot — Sekmeht parked it) and the **script palette** as a real future feature (its dead stub was removed, so build it fresh when wanted).

### Views — Session & Overview (v0.19.0, DESIGN §47)
A top-level **view** concept, app-level and orthogonal to everything else: **Session** (today's behaviour — one character at a time, switched by tabs) and **Overview** (a dashboard of every character in this window at once). Multi-boxing is the whole reason: DR is one character per account, so "is anyone dying / idle / being talked to?" previously meant tabbing through everyone and getting a stale answer.

- **The card is rendered BY its own GameWindow, through a `createPortal` into an app-level grid.** This is the load-bearing idea: a portal moves the child into a different DOM subtree while **keeping the React tree**, so the card escapes the `display:none` session shell (becoming visible) while being written inside GameWindow's scope — it reads that character's `vitals`/`roomState`/`lines` as ordinary local values, and its per-session `HighlightsContext`/`ContactsContext` remain reachable (v1 deliberately does NOT use them: the feed passes empty rule arrays so it skips the ruleset pass — the capability is what makes a styled feed cheap to add later). That dissolves pitfall #57 rather than working around it — **zero new data plumbing**. When you need another all-characters surface, reach for this shape before inventing a data channel. **The converse bites too:** context flows down the REACT tree, so a provider on the app-level shell does NOT reach a portaled card — the shared 1 Hz clock had to live in the store for exactly this reason (it was a context first, and every card read the default forever).
- **`SessionStatus` is deliberately NOT touched.** Its update path is guarded by a hand-written 16-term equality chain whose whole purpose is to stop the character tab strip re-rendering on vital ticks; dashboard fields there would defeat it (and add a term every future field must remember — see pitfall #130). The cross-character reduction lives in **[overviewStore.ts](src/renderer/overviewStore.ts)** instead: a module store of thin scalar `CharacterDigest`s, coalesced at 500ms, consumed by LEAF subscribers only (`ViewToggle`, the summary strip) so a publish never reaches the tab strip. **The tab strip DOES subscribe as of v0.19.7** (F112: in the Overview it highlights the input bar's TARGETS, not `activeId` — the one selected character, or every connected one under All characters; cards follow the same rule), but only to `useViewMode` / `useOverviewTarget`, which return primitives — `useSyncExternalStore` bails on an unchanged snapshot, so a digest publish still never re-renders it. Never subscribe the strip to the digests themselves. It is the codebase's first `useSyncExternalStore` — pitfall #129.
- **It is an OVERLAY, not a third render branch.** `OverviewShell` is `position:absolute; inset:0` over App's content region (which gained `position: relative`); the session shells keep their existing visibility, so the active character's Virtuoso stays measured and returning to Session view needs **no scroll re-snap**. Hiding all the shells instead would reintroduce the pitfall #24/#68 0×0 class — and note `GameWindow`'s `stickToBottom` re-snap keys on `isActive`, which does NOT change on a view flip, so that variant would need its own dep.
- **The grid host is ALWAYS mounted** (CSS-hidden in Session view) so every card's portal target has a stable identity and there is no null-target first frame. **Card order is CSS `order`**, computed in-card from `attentionOrder(score, tabIndex)` — portal children append in commit order, so DOM order drifts from session order; `order` also buys attention-sorting for free.
- **The attention model is pure and harnessed.** [attention.ts](src/renderer/attention.ts) (`computeAttention`, `ATTENTION_DEFS`, `attentionOrder`) is React-free, severity-ordered so chip order == reading order == sort order (UX #3), and covered in `tmp-rules-harness` §H. Two traps it encodes: `healthPct === null` means "no vital yet" and must read CALM (never `0 < 25` critical), and `free-to-act` is an invitation suppressed whenever anything real is wrong.
- **`idle` is DERIVED by the consumer, never pushed.** An idle character stops receiving events, so it stops re-rendering, so a push-computed idle flag would never arrive. The digest carries `lastInboundAt` **quantised to 5s** (`IDLE_QUANTUM_MS`) so it doesn't churn, and the card + summary strip derive idle against the shell's shared 1 Hz clock. The app-bar badge deliberately counts push-driven problems only — a badge that lights for a deliberately-parked character is one you learn to ignore. **`spoken-to` is the OTHER time-windowed flag and takes the OPPOSITE mechanism (B286, v0.19.2):** push-computed, with a bounded per-speech `setTimeout` (`spokeTick` in useSessionStats — the hook's one deliberate piece of React state) re-running the attention memo just past the window's end. Consumer-derivation can't serve it because the badge must clear while the Overview is CLOSED — exactly when the 1 Hz clock isn't running. Any future time-windowed flag picks one of these two shapes; a bare memo with no time driver expires never (the shipped bug).
- **The card's stream dropdown selects WHAT IT SHOWS, not a stream (F103).** `exp` is offered and renders the COMPACT EXPERIENCE VIEW rather than a feed. It was excluded in v0.19.0 for a correct reason — experience is a state map built from `exp-component` events, so a feed of it could only say "nothing yet" — and the exclusion expired when the card learned to render state. Still excluded: `map` (graphical), `injuries` (already the wound chip), `room` (structured sub-streams), `lichScripts` (the `;listall` poll), raw/debug. **The filter and the ORDER are shared with ExpPanel** (`compactExpRows` in expParse.ts) so the two cannot disagree; the MARKUP is not, because the panel is interactive and a card is read-only — share the rule, not the copy. Memoize on the skills map (it parses every entry, and a card re-renders per game line), anchor it to the TOP (a table's first row is its most important, unlike a feed's newest line), and note that `sortDesc` INVERTS `alpha` — `false` is Z→A — a shipped quirk the harness pins rather than renames.
- **Two card rendering contracts, both from Binu's v0.19.1 pass.** **(a) The feed clips the OLDEST content, never the newest** — lines are uncapped and the feed is bottom-anchored, so overflow scrolls off the top and the newest line is always whole. It shipped capped at three rows and failed twice: it truncated the TAIL of a long room description (the half you are reading), and never cut on a row boundary because `.text-line` re-declares `line-height: var(--game-line-height)` while the cap was written in `1.3em` — 3.9em against ~1.5em rows is 2.6 rows, two rows and a sliver, with the count sliding as the user changed a prose setting (B277; pitfall #116's rider). **Before doing arithmetic in rows, check what the CHILD declares.** The rule is scoped `.ov-card-feed > .text-line` so it beats game.css deterministically rather than tying (pitfall #111). **(b) The input bar's history belongs to the BAR, not the target character** — it can broadcast, so "the last thing I sent" is the only unambiguous phrasing; in-memory and per-window, and each character's own persisted history is untouched because the send still goes through that character's `dispatchUserText` (B278). **"Per-window" is real only because the ARRAY lives in overviewStore (B303, v0.19.2)** — the bar unmounts with the view (`{open && …}`), so a component ref made it per-VISIT; the browse index/draft stay component-local on purpose.
- **Cost when closed is two instructions per event batch**, and nothing else: the portal isn't rendered, so the §35.6 "free until used" principle applies without main's help. The accumulators ([useSessionStats.ts](src/renderer/hooks/useSessionStats.ts)) run ALWAYS on purpose — one `+=` per already-coalesced batch is free, and it means opening the view shows real numbers instead of zeros. **The one option with real per-line cost is `watchSpeech`** (the "spoken to" flag), which extends the §35.6 `sceneActiveToggle` gate to every open character — hence default OFF and a hint that says so.
- **The global keyboard handler early-returns while the Overview is open.** The command bar and main text are still in the DOM underneath, so without it a printable key would focus an invisible input (F60 type-anywhere) and a macro would fire at an unseen character.
- **Extracted in the same change** so the card and the panels can't drift: [expParse.ts](src/renderer/expParse.ts) (`MINDSTATES`/`parseExp`/`dotBucket`/`summarizeExp`, 34 = mind lock) out of ExpPanel, and [injuryParse.ts](src/renderer/injuryParse.ts) (`parseInjury`/`summarizeInjuries`, **carrying the B224 scar-vs-wound reasoning with the code**) out of InjuriesPanel.
- Slash surface `/view` (bare toggle · `status` · `sort` · `set`); app action `toggle-view` in `APP_ACTIONS` (it acts on the WINDOW, not a character); View menu item, click-only. Options are app-wide (`SharedProfile.overview`, optional → non-breaking) and carry a **cross-window rule worth knowing beyond this feature: every OTHER field of `SharedProfile` is read FRESH from localStorage inside `buildSharedProfile`** (`loadMyThemes()`, `loadAIConfig()`, `getItem(…)`), and localStorage is shared across windows — so a flush from ANY window is automatically current, and that is the invariant the whole shared profile quietly rests on. `overview` is the ONE entry that reads MODULE MEMORY (`getOverviewPersisted()`, deliberately, so an unflushed working copy cannot overwrite a live change), which made it the one shared setting a second window could silently revert (B271). It is kept safe by a `storage` listener in App beside the theme one. **If you add a shared setting held in module state rather than re-read per save, it needs that listener too** — and if you add one read fresh, it does not; **the view MODE is per-window ephemeral** so a decoupled window is never dragged into the main window's view.
- **Scope today is this window's characters.** Cross-window remote tiles are the planned Phase 2 and would come from main's existing per-session `stateSnapshot` ([main.ts:77](src/main/main.ts#L77)) — already maintained free for the handoff replay — throttled behind an `overview-active-toggle`; a remote tile can carry condition and situation but **not** the text feed (main's `historyBuffer` holds raw `GameEvent`s, pre-mute/substitute and pre-`TextLine` assembly).

### Import wizard (DESIGN.md §15)
Wrayth XML / Genie .cfg / Frostbite .ini parse into a neutral `ImportCandidate` intermediate, then `mapper.ts` converts to native rules with `allGroups: true` and blank names. Per-client quirks under `src/renderer/import/`. Substitutions counted but not imported (F12). **Wrayth coverage was completed in v0.11.1 (B146, Thanator's real export):** highlights live in `<strings>` (NOT `<highlights>` — that block doesn't exist in Wrayth; the old parser silently imported zero), so `parseStrings` reads them as match-scope highlights colored via the `<palette>` (`@NN`→hex through `resolveWraythColor`). `<names>` → contacts AND per-color **contact templates** named `colorNN` (by palette index, stamped on `ImportHighlight.templateName`; the wizard's contacts-apply find-or-creates the template by name and assigns `templateId`). `<presets>` → `themeVars` via `parsePresets` (bold/speech/whisper/thought/roomName/command/link → CSS preset vars; `watching`/`selectedLink` skipped; `"skin"`/empty bg → none) feeding the existing "Imported from {source}" custom-theme path. Macros import from **all 10 sets** (was set 0 only) with cross-set key collisions flagged `partial` + an apply-time first-wins key dedup in `doImport` (Lichborne has one keybinding set). `<ignores>`(gags)/`<vars>` are count-only notes. Two layout/dedup traps fixed in the same pass — see pitfall #61. **Frostbite coverage was completed in v0.11.9 (B159, Rakkor's real `highlights.ini`):** highlights live in `[TextHighlight]` keyed `N\value`/`N\color`/`N\bgColor`/`N\options`/`N\alert`/`N\alertValue`/`N\timer`/`N\group`. Colours are Qt `@Variant(...)` QColor blobs (spec byte at index 4 must be `1`=RGB; channels are 16-bit, take the high byte; spec `0`=invalid=no bg). The **`options` field is a 5-bit QBitArray** whose meaning is taken verbatim from Frostbite's `highlighter.cpp`: bit0=entire-row (→ `scope:'line'`), bit1=partial-words (set→`phrase` substring, clear→`text` word-boundary), bit2=starting-with (whole-line submodifier, no LB equivalent), bit3=match-groups (per-capture-group colouring → flagged `partial`), bit4=case-insensitive (deliberately **ignored** — imports forced case-insensitive, Sekmeht's call). **`|` in a value is alternation** — `buildPattern` splits and emits a `regex` (each alt regex-escaped, wrapped `\b…\b` unless partial-match) so `A|B|C` matches; passing it as literal `text` matched nothing (the pre-v0.11.9 silent failure on ~15 of Rakkor's entries). Values are unescaped in **two layers** — Qt INI (`\\`→`\`) THEN QRegExp punctuation-escapes (`\'`→`'`), because a Frostbite value is a QRegExp pattern: `Redeemer\\'s` → (Qt) `Redeemer\'s` → (QRegExp) `Redeemer's` (a single-pass unescaper leaves the stray `\` and the regex then hunts a literal backslash — caught only by the end-to-end match test, not the parse). Highlights dedup on full visual identity (pitfall #61a). `[GeneralHighlight]` (roomname/speech/whisper/thought/script → preset theme vars) and `[AlertHighlight]` (health/bleeding/death → "belongs in Lich" count) are **keyed by role/alert NAME with NO `size=` key** — both prior parsers used a numeric `size`-indexed loop and silently returned nothing; fixed to iterate the named keys. Frostbite **groups** (Critters/Combat/Travel/…) stay informational (`sourceClass`), NOT recreated as LB groups (Sekmeht's call) — **EXCEPT `group=Names`** (v0.12.0): a `[TextHighlight]` with `group=Names` is a player name, routed to `result.names` (Contacts) instead of highlights, each with a per-colour `templateName` (`color<HEX>` — hex-derived since Frostbite has no Wrayth-style palette index). `parseHighlights` returns `{ highlights, names }`; the wizard's **source-agnostic** contacts-apply (`ensureTemplate(n.templateName, n.textColor)`) creates one template per colour and assigns each contact's `templateId` — identical to the Wrayth `<names>` and **Genie `names.cfg`** (`#name {#hex} {Name}` → `parseNames`, v0.12.1) paths. **All three clients** import player names as Contacts with per-colour templates (`color<HEX>`; Wrayth prefers its palette index `colorNN`). (Rakkor's Frostbite file: 151 names → 9 templates; Genie names.cfg: 96 → 7. Genie's `parseNames` now also stamps `templateName` + dedups by name — before v0.12.1 it produced contacts with NO template.) Timers (`timer`/`timerValue`/`timerAction`) aren't imported (no LB timer feature) → flagged `partial`. The colour/options decode shares `parseQtEscapes` ([colorUtils.ts](src/renderer/import/colorUtils.ts)) — see pitfall #72 for the single-digit-hex bug this rework surfaced. The wizard's highlight preview renders each pattern in its real fg/bg on a fixed dark game-like chip (`.iw-hl-preview`) + text/bg swatches — shared by all three legacy importers. **`macros.ini`** (`[keypad]`/`[alt]`/`[ctrl]`/`[function]`, keyed by Qt modifier+key integer codes decoded in `normalizeFrostbiteKey`) parses via the **shared `parseImportedMacroAction`** ([macroAction.ts](src/renderer/import/macroAction.ts)) — the SINGLE macro-action parser used by **all three** legacy clients (Frostbite, Wrayth, Genie) so they import macros identically. It has **two split modes**: a **send-token** model (Frostbite `sendToken: /\$[ns]/gi` from `macrothread.cpp` — BOTH `$n`/`$s` send, `$s` adds a sequence pause we don't model, + `stripOuterQuotes`; Wrayth `sendToken: /\\r/g` + `cleanSegment` to drop the leading `\x`) where a token sends the preceding command and a TRAILING no-token command is type-and-wait (gets `@` appended at the end); and a **separator** model (Genie `separator: /;/g` + `cleanSegment` dropping leading `\x`/trailing `\r`) where every command AUTO-SENDS and nothing is appended — a command is type-and-wait only if it already has an `@`. In BOTH modes a command with `@` is a cursor (the runtime `parseCursorMarker` drops the cursor where the `@` is, incl. **mid-string** — `take @ from my backpack` / Genie `first @` → cursor in the gap), so `@` is NEVER flagged "move to a Lich alias" (the pre-v0.11.9 mistake — fixed for Frostbite/Wrayth, then Genie). **Segments are NOT trimmed** so an intentional trailing space survives and positions the cursor (`advance ` → `advance @`; trimming gave `advance@` → "advancesword"). `allBuiltin` is reported so every client surfaces a nothing-but-built-ins macro as `unsupported` instead of silently dropping it; Genie additionally filters `#`-prefixed internal scripting (`isGenieInternal`, e.g. `#if`/`#put`/`#class`) so a `#if`-conditional macro reduces to `unsupported`. Do NOT re-add a per-client copy; change macro-import behavior in the one helper so the clients can't drift. **Genie count-only notices fixed (v0.11.9):** Genie writes the SHORT command forms, so `parseGenieFiles` counts `#sub`/`#var`/`#gag`|`#ignore` prefixes (covering `#subs`→substitutes, `#var`→variables, etc.) — matching only the long `#substitute`/`#variable` reported 0 on real Genie configs and silently suppressed the "belongs in Lich" notices. (Genie **classes** → Lichborne groups remains an unbuilt opportunity: the 4th `{class}` arg on highlights/triggers is still informational-only `sourceClass`.) **Genie highlight/trigger PATTERN handling (v0.11.9 bug-check fixes):** Genie's `/pattern/`, `/pattern/i` wrapping marks an EXPLICIT regex regardless of the declared type (`stripGenieSlashWrap`, matching Globals.cs:1007-1019 — strip leading `/`, trailing `/i`→case-insensitive or `/`); without it a slash-wrapped pattern imported with the slashes baked in as literal text and **never matched** (`/The taipan…/i`). And **`beginswith`** highlights (Genie's `HighlightBeginsWithList`, "line STARTS WITH") are imported as a `^`-anchored regex (`'^' + escapeRegex(pattern)`, line scope), NOT a plain substring — the old `phrase` mapping over-matched (fired on the pattern appearing anywhere in the line). Highlight type → mode/scope otherwise: `regexp`→regex/match, `line`→phrase/line, `string`→text/match, `beginswith`→regex/line. Triggers are always regex (also slash-stripped). **Known minor gaps (accepted):** `string` (6) maps to word-boundary `text` vs Genie's raw substring; the `;` command separator is hardcoded (Genie's `separatorchar` is configurable but defaults to `;`). **Trigger sound actions (v0.11.9):** the `sound` `TriggerAction` supports BOTH a `soundFile` (a real WAV/audio path — the engine plays it via `playWavFile`, taking priority) and a `soundPreset` (one of 4 built-ins). `mapTrigger` ([mapper.ts](src/renderer/import/mapper.ts)) preserves a **real audio file** verbatim as `soundFile` (`isAudioFile`: a path separator or audio extension) and only falls back to a `soundPreset` (`mapSoundPreset`) for a built-in sound NAME (Genie's `#play MiniFanfare1`). A sound NEVER makes a trigger `partial` — it's a supported action (uniform with how Frostbite imports highlight sounds as `ready`); a library-name fallback gets an informational note (a tooltip on the `ready` badge), a real file gets none. Don't re-add the old "preset only" mapping that discarded WAV paths, or the sound-→partial downgrade. **`general.ini`**: `[GameWindow]/[DockWindow]/[Commandline] background`→`--bg-app`, `[GameWindow] fontColor`→`--text-primary` (the story-window text colour), `[QuickButton]`→ a surfaced count (no LB command-button feature → "recreate as macros/aliases"). **`substitutes.ini`** (`[substitution]`, regex find/replace) **NOW imports natively** (v0.12.0 — superseded the old "count-only → use textsubs.lic" stance) → `SubstituteRule`s, GLOBAL scope; the `target=Experience` mind-state numbering subs are flagged `unsupported` (see the Mutes & Substitutes bullet's import note). **The wizard is legacy-client only as of v0.10.0** — the "Lichborne" source card (F29 self-import) was removed and Lichborne→Lichborne moves through the platform-wide **Transfer** feature instead (the Automations panel's Export button was removed too; its Import button is now labeled "Import from another client…"). The `parseLichborneYaml` parser ([lichborne.ts](src/renderer/import/parsers/lichborne.ts)) and the `nativeRules`/`nativeLayout`/`nativeGroups`/… apply branches in [ImportWizard.tsx](src/renderer/components/ImportWizard.tsx) are now unreferenced (the legacy parsers never populate those fields); left in place as harmless dead code rather than ripped out. `'lichborne'` stays in the `ImportSource` union so the residual `source === 'lichborne'` guards compile.

### Session Log (DESIGN.md §28, shipped v0.7.0)
Per-character daily plain-text logs. **Capture** happens in `GameWindow.onGameEvent` → `logToSession()` → `session-log:append` IPC → buffered per-character writer in [sessionLog.ts](src/main/sessionLog.ts). Files: `{userData}/Logs/{Character}/{Character}_YYYY-MM-DD.log`, line format `[HH:MM:SS][stream] text` (the date is in the filename; parsers also accept the legacy `[YYYY-MM-DD HH:MM:SS.mmm]` form). Buffered (1s / 100-record flush, force-flush at 5000); `flushAllSessionLogs()` runs on graceful close next to `backupAllProfiles()`.

- **Config is app-wide, NOT per-character.** All Session Log preferences (capture gates, retention, compression, raw-size cap, the Recent-tail filter, the Export-builder format prefs) live in one `SessionLogSettings` object — [sessionLogSettings.ts](src/renderer/sessionLogSettings.ts), stored in `_shared.yaml` via `SharedProfile.sessionLog`. `logToSession` calls `loadSessionLogSettings()` fresh per batch (it's a tiny localStorage read — no stale-closure ref needed, and a Settings change applies instantly to every open character). The capture config also rides along in `SessionLogAppendPayload` (`retentionDays`/`compress`/`maxRawMB`) so main can run maintenance without its own copy. Both the SettingsPanel section and the modal read-modify-write the shared object so neither clobbers the other's fields.
- **What's skipped:** `raw`, room sub-streams (`ROOM_STREAMS`), blank lines — state, not history. Stream IDs `cmd`/`sys`/`main` are the capture-category keys; everything else counts as "streams".
- **Size management (v0.7.0):** closed (non-today) day-files are gzip-compressed to `.log.gz` (~85-90% smaller) by background `maintain()` — runs at session start + day-rollover, gated by `sessionLogCompress`. All readers route through `readLogFile()` (gunzips transparently) and `resolveDayFile()`. Two retention limits: `pruneOldLogs` (age, `.log` + `.log.gz`) and `enforceRawSizeCap` (caps *uncompressed* `.log` bytes only — never `.gz`, never today's file). If you change the line format or add a maintenance limit, keep both parsers (`LINE_RE` in the modal, `EXPORT_LINE_RE` in main) accepting the legacy form.
- **UI:** [SessionLogModal.tsx](src/renderer/components/SessionLogModal.tsx) — one modal, three views (Recent Tail + Quick Search + Export), opened by the toolbar **Logs** button or right-click → "Show in Log" (which opens straight into Search). Tail is paginated (`PAGE`/`JUMP_RADIUS`); never loads a whole day. The Recent-tail filter (`filterHidden`/`filterDedup`) and the Export format prefs persist in the app-wide `SessionLogSettings`, not per-character.
- **Export builder ("Create Log File"):** the renderer gathers a `SessionLogExportSpec` (date range, stream layers, include-timestamps/tags, dedup, summary, split-per-stream, target) and main does all the reading/filtering/formatting/writing in `session-log:build-export`. Big data never crosses IPC — only the spec and a small `SessionLogExportResult`. `target: 'file'` runs a save (or folder, when `splitPerStream`) dialog; `'clipboard'` writes combined text via Electron `clipboard`.
- **Read IPC:** `session-log:list-days`, `read-day` (tail + `beforeLine` pagination), `list-streams` (single day, or a range when `toDate` is passed), `search` (returns `SessionLogSearchHit` with `lineNo`/`total` for jump-to-tail), `build-export`, `disk-usage`, `open-folder`. All read handlers flush the character's buffer first so reads reflect the live session.

### AI features — BYOK, capability-routed (DESIGN §10, shipped v0.16.0)
**Three guardrails, all load-bearing (DESIGN §32):** (1) AI **advises / composes / summarizes / decorates and NEVER issues game commands** — the single rule that keeps every AI feature inside Simutronics' scripting policy and Principle #1; (2) **BYOK, opt-in, privacy-disclosed** — nothing leaves the machine unless a feature is enabled AND its one-time consent gate accepted; (3) **AI ENHANCES, never GATES** — every feature has a working non-AI baseline (for Catch Me Up the baseline is the scrollback + Session Log: the player can read what they missed themselves; the AI just summarizes it).

- **The adapter is CAPABILITY-routed, not provider-routed** ([src/main/ai/](src/main/ai/)) — the correction to the old §10 "one OpenAI key does everything" assumption. **Claude has no image generation and no embeddings endpoint**, so `text` / `embeddings` / `image` are three independently-BYOK capabilities. v0.16.0 implements **text (Claude)** only; `embed()`/`generateImage()` are **declared but throw a clear "capability not configured"** so the image (G9/AI9) and RAG (AI3/AI6/AI10) tracks slot in later with no redesign — a feature reaching a dark capability must **degrade with a message, never crash**.
- **Keys live in main's `safeStorage` (`ai-keys.json`, the `passwords.json` pattern) and NEVER cross IPC** — only a "is a key present" boolean does ([aiKeys.ts](src/main/ai/aiKeys.ts)). All AI calls run in MAIN, never the renderer (keeps the key off every game-text path; a renderer `fetch` to the provider would be CSP-blocked anyway).
- **The Claude client is a ~100-line raw-`fetch` SSE reader** ([claudeProvider.ts](src/main/ai/claudeProvider.ts)), **deliberately NOT `@anthropic-ai/sdk`** — zero new npm packages (v0.15.0 packaging hygiene: `dependencies` = true runtime externals only) and it matches this codebase's hand-rolled-parser ethos. Don't "upgrade" it to the SDK without a reason.
- **Config split:** the **secret key** → `safeStorage`. The **non-secret config** (master enable, model tier, per-feature consent flags) → [aiConfig.ts](src/renderer/aiConfig.ts) (`lichborne.ai` working copy) → `SharedProfile.ai` → `_shared.yaml`. Deliberately NOT in `TRANSFER_CATEGORIES` (machine-local — the `automationStats` precedent). Default model **Haiku 4.5** (cheap, the workhorse); Sonnet 5 / Opus 4.8 selectable.
- - **Log-backed Catch Me Up (v0.17.1, Sekmeht — reverses the v0.16.x screen-only decision above).** `/ai catchup 2.5h` now reads THIS CHARACTER's session log from `[now − 2.5h, now]` instead of only the screen buffer — fixing the "I did a 2.5h catchup but it only returned recent items" report (the screen buffer is bounded by `MAX_LINES`, so it can't reach past a few thousand lines). **Why it works now when the v0.16.x log build was reverted for scale:** the whole pipeline runs in MAIN (`buildCatchupDigest` in [sessionLog.ts](src/main/sessionLog.ts)) and returns a COMPACT digest — never raw rows over IPC (a year is ~29M lines). It walks **day by day, yielding between days** (`setImmediate`) so a long window can't block main and freeze every session, and pushes PROGRESS on `session-log:catchup-progress` for every phase (reading day N/M → deduping → extracting → summarizing) so the UI shows "Working on it…". **The model analyses EVERYTHING (Sekmeht), not a sample:** the payload is the FULL deduped log body (up to the tier's `maxChars`) + a fact-sheet of EXACT whole-window tallies as anchors. **DEDUP is what makes "everything" affordable** — grinding's repetition collapses, so a realistic window's UNIQUE content is a fraction of its raw size; if the deduped body still overflows the biggest tier, the most-recent fits and the tallies still cover the whole period (main trims, header says so). **Six TIME-SCOPED tiers** (`CATCHUP_TIERS` in GameWindow: recent ≤45m → session → extended → day → period → historical ≤1y) each carry their own prompt guidance (narrative at 30m, retrospective at 1y), `maxTokens`, and `maxChars` (45k→240k). **Extractors are EMPHASIS, verified against real log lines Sekmeht supplied** — ranks (`You've gained a new rank in …`), combat damage taken (`lands a light hit (1/23) to your chest` → attacker + part + severity — the severity ladder is the **VERIFIED 22-level GM-Kodius scale** `light hit`→`apocalyptic strike`, which **alternates `hit` and `strike`**, so the damage regex MUST match both (`(?:hit|strike)`) or every *strike*-level hit silently drops — a real bug shipped-then-fixed with a made-up `moderate`/`severe` ladder; attackers are counted ONLY on damage lines, never on arrivals/misses), deaths (`Your death cry echoes…`), directed speech (say/whisper/`<to you>` telepathy), work orders (`You hand X your logbook … N Dokoras in return`), and banking with Lich's VERIFIED coin ratios (plat=10000…copper=1, from `drbanking.rb`) → real net money flow. **THREE load-bearing rules for any future extractor:** (1) **tally counts BEFORE dedup** — two identical rank/hit lines are two REAL events; deduping first silently halves them. (2) **NEVER invent DR game math** (coin ratios, xp curves) — mine it from Lich's `drinfomon` (`drbanking`/`drexpmonitor`/`drstats`, each with SPEC files = verified inputs) or omit the category. (3) The `DRExpMonitor: Skill(+N)` line is the **mindstate/learning-rate ticker, NOT ranks** — counting it as ranks inflates the number wildly (nearly shipped exactly that); ranks come from the game's own message, and the ticker is filtered out as churn. **Per-character** (each GameWindow passes its own `session.character`; progress is `requestId`-filtered — pitfall #6). **Fallback (the safeguard, "AI enhances never gates"):** if session logging is OFF or the log read throws, it degrades to the screen buffer and SAYS SO in the header (`session log off, screen only`); the header also reports real coverage (`log reaches back 40m`, `N repeats collapsed`, `analysed the most recent portion`). **NOT YET DONE / KNOWN GAPS:** never run against a live game (all verification is pattern-level against pasted lines); banking currently keys on the `DRBanking:` SCRIPT line, not the game-native `The clerk slides a small metal box…` messages Lich parses (script-independent — a documented port); no injury-message extractor; docs/live-test/pattern-registry-port are the next pass. **`CATCHUP_MAX_MINUTES` (slashCommands.ts) is the executor's window ceiling — raised 24h → 1 YEAR in the same change**, because the tiers/pipeline supporting a year is USELESS if the executor still rejects `7d` before running (it did — a leftover screen-only guard one function away from the tier code). **Lesson: when you widen a feature's range, trace the WHOLE command path — the `parseDuration` unit, the tier table, AND the executor's cap + its error text + the arg hint (Principle #11) — not just the piece you're editing.** Keep `CATCHUP_MAX_MINUTES` aligned with the largest `CATCHUP_TIERS` window.
- **Catch Me Up (AI4) — the window is ALWAYS a TIME RANGE `[from, now]`, from exactly TWO sources: (1) an explicit duration (`/ai catchup 27m` — `parseDuration` ([slashCommands.ts](src/renderer/slashCommands.ts)) takes `27m`/`90`/`2h`/`2.5h`/`1.5h`/`1h30m` AND unit suffixes `m`/`h`/`d`/`mo`/`y` — `2mo`=2 months, `1y`=1 year; `mo` MUST be alternated before `m` or `2mo` reads as 2 minutes), else (2) the default 30m.** The header ALWAYS states which was used — the feature must never be a black box. **Two auto-window mechanisms were BOTH removed — do NOT reintroduce either:** a *silent idle-detector* (watched keypress gaps, made `/ai catchup` behave differently based on state the user couldn't see — Sekmeht: *"I don't understand what data /ai catchup looks at"*), and a **`/afk` toggle** (marked an explicit away-span; removed as not-worth-it, Sekmeht 2026-07-14: *"it's on the player to know how to catch up"* — the duration argument covers it). If you wandered off, the answer is `/ai catchup 2h`, not a mode or a heuristic.
- **GATHERING IS AN INVARIANT — read main + EVERY OPEN STREAM PANEL, never `lines` alone.** A **watched** stream (thoughts/deaths/arrivals) routes to `streamLines` and **never reaches `lines`** — `STREAM_FALLBACK` only spills a stream into main when *nobody is watching it*. Gathering from `lines` alone made the summary blind to exactly the conversation a returning player cares about, and it got **WORSE the better the user's panel layout was** (that inversion is how the bug was found). `collectHistory()` merges both, **timestamp-merged + source-tagged** (`[thoughts] Rakkor says …`), skipping our own `internal-system` chatter, the `lbAI` stream, and `CATCHUP_SKIP_STREAMS` (state readouts that clear+rewrite — `exp`/`inv`/`activespells`/`moonwindow`/… — which would feed the model a stale TABLE instead of events). **Any future AI feature that reads "what happened" must gather this way.**
- **⚠️ SUPERSEDED v0.17.1 — Catch Me Up NOW READS THE SESSION LOG (per-character), tier-scoped. The screen-only scope described in THIS bullet was the v0.16.0–v0.17.0 behaviour; see the "Log-backed Catch Me Up (v0.17.1)" bullet below for the current architecture. The history here is kept because its LESSON still governs: the FIRST log attempt (v0.16.x) failed on SCALE, and the v0.17.1 build only works because it solves scale differently (extract-then-summarize + dedup + per-tier budget), NOT by dumping raw log text with filters. Read the rest of this bullet as "why the naive version was reverted", not "why it can't read the log".** SCOPE (historical) — Catch Me Up summarizes WHAT IS ON YOUR SCREEN. It does NOT read the session log. Purpose, in Sekmeht's words (2026-07-14): *"I walked away, I came back — what did I miss in the last 20 minutes?"* Source is `collectHistory()` (live scrollback + open stream panels), filtered to the window. **A log-reading build was written, tester-shipped, and REVERTED** — it worked, but log scale (a busy character logs **80k lines/day**: 49k `main` + 38k `inv` + 17k `spells`) forced in priority tiers, a verbatim block, realm-ticker gating, NPC-vendor filters and template collapsing, and it *still* produced walls of vendor chatter. None of that is needed for the few thousand on-screen lines (already bounded by `MAX_LINES`). **LOG ANALYSIS IS A DIFFERENT FEATURE** (DESIGN §10.4) — do NOT merge it back in. The transferable lesson: **the scale of the input decides the architecture** — the same feature over 3k screen lines and 80k logged lines is two different features. Groundwork left behind: the `session-log:read-window` IPC (filters a time range in MAIN; **never bound a TIME window with a LINE count** — pitfall #92).
- **AI output NEVER enters a future prompt — INVARIANT (three guards, v0.17.2).** Feeding a recap back makes the model summarize its own summaries. `collectHistory` enforces it by skipping (a) `internal-system`-led lines (the `[sys]` category — slash results, notices), (b) the `lbAI` STREAM (`id === AI_STREAM`), AND (c) **any line carrying an `ai`-preset segment** — the format-independent guard added v0.17.2 to catch a recap routed to the MAIN window (when no lbAI panel is open, the recap lands in `lines`, not a stream). Before (c) it was only prevented by the *coincidence* that the recap's header segment is `internal-system`; the `ai`-preset check is the durable rule and covers any future `ai`-preset output. **The LOG path is safe by construction** — Catch Me Up output is never written to the session log (`appendLine`/`updateLive` call `setLines`/`setStreamLines`, never `logToSession`), and `DIGEST_SKIP_STREAMS` lowercases + skips `lbai` as belt-and-suspenders. **Any new AI feature whose output could land in `lines` must use the `ai` preset** (it already should — that's the AI-content color) so this guard covers it.
- **`gatherWindow` dedups on TEXT ALONE, not (stream, text) — deliberate.** DR double-emits speech (pitfall #49: once inside the pushStream block, once to main), so with a conversation/whispers panel OPEN the two copies land under **different streams** at the same timestamp. A stream-aware dedup misses that and feeds the model every spoken line twice.
- **The budget is CHARACTERS, not lines** (screen path: `CATCHUP_MAX_CHARS` 40k; log path: per-tier `maxChars` 45k–240k — chars ≈ tokens/4). A flat tail is fine on the SCREEN path precisely because the source is the screen buffer, not a log; the LOG path sends the full deduped body (main-trimmed to the tier budget, most-recent kept) plus the exact tallies. The header says so honestly when it can't fit everything. **Naming the character in the prompt** (`catchupSystem(character)`) is the highest value-per-token line in it — the first version said "the player" and made the model *guess* which name in raw game text was them.
- **`lbAI` is the AI OUTPUT STREAM** (`AI_STREAM` in aiConfig.ts) — a first-class named stream seeded into `discoveredStreams`, so it's always addable in Panel Manager / `/panel open lbai`. **DECIDED default (Sekmeht, 2026-07-14): output goes to the MAIN game window; when an `lbAI` panel is open it routes THERE instead — and as of v0.19.3 (B307) "open" means SHOWING: `watchedStreamsRef.has(AI_STREAM) && activeIdsRef.has(AI_STREAM)`, the tab exists in a rendered surface AND is that surface's active tab. Tab-existence alone let a background lbAI tab swallow the recap invisibly (unread dot only) and read as "closed but nothing came out".** Making `lbAI` the DEFAULT was considered and DECLINED — the summary should land where the player is looking; `lbAI` is the opt-in "keep it out of my scroll" surface. Routing is decided ONCE per run and the streaming updates follow it. Never emitted by the game → **no `STREAM_FALLBACK` entry**. **Every future AI feature routes its output through `lbAI`.** The two main-window-path edges (live line trimmable in a 2000+-line flood; per-token full-`lines` re-map, pitfall #82) are accepted-low-severity — opening the `lbAI` panel fixes both. **Its display label is `lbAI` (not the mangled `LbAI`):** client-seeded streams have no game title, so `streamLabel(id, title?)` (aiConfig.ts) special-cases `AI_STREAM` and is used at every tab/add-menu label site (makeCustomTab, both add-menus, slashPanelTab, addToZone) — a game title still wins, else capitalize-id as before (behavior-preserving for non-AI streams).
- **Rendering:** the summary is ONE live line, updated per delta, with TWO segments — a quiet `internal-system` **header** and the body on the **`ai` preset**. **`ai` follows the GAME TEXT color** (`color-mix(in srgb, var(--accent) 25%, var(--text-primary))` in panels.css), NOT `--text-dim` — AI output is *content the user reads*, not a throwaway notice like "Connection closed." (Sekmeht: *"it's dark grey when I have white text"*). It ends with a **trailing `\n`** for separation from the next paragraph — **never a separate spacer row**, which picks up its own empty `[HH:MM]` when timestamps are on. Errors stay on `internal-system` (a notice, not content).
- **Header is CONCISE — start–end clock + compact counts, model, voice; empty responses auto-retry (v0.17.2).** The recap header leads with the window via **`fmtWindow(from, now)`** (actual start–end times, Sekmeht's ask — the old "the last 6h" relative label was cluttered): **clock times** under a day (`14:05–16:05 (2h)`), **compact dates** for multi-day windows (`Jul 16–Jul 23 (7d)`, +2-digit year when the span crosses years) since clock times mislead across days, and `fmtDuration` is day-aware ≥48h (a 1-year window is `365d`, not `8760h`). Then compact metrics via **`fmtCount`** (58405→`58.4k`, 51034→`51k`; `N lines, M collapsed`), then only the caveats that apply as SHORT tokens (`log from HH:MM` when the log doesn't reach back as asked, `recent portion` when the body was trimmed, `use sparingly` when heavy — replacing the long sentences), then `· via ${modelName}` plus `· voice: ${persona}` when set. Both the log and screen paths build it the same way; `modelLabel(loadAIConfig().textModel)` + the trimmed persona are read fresh per run so a tester report says which model AND voice produced it. **Keep it concise — don't re-expand the caveats into sentences.** The streaming call is wrapped in an `attempt(tries)` inner fn: on an EMPTY completion (`onDone` fires with `!acc.trim()` — a stream that ended cleanly with no text, i.e. a transient dropped/empty response) it **retries ONCE automatically**, then on a second empty settles with a diagnostic (`the model returned an empty response (N tokens in, M out) … try again`) instead of the old cryptic `(no summary returned)`. This is safe against `/ai stop` because abort tears down the listeners so `onDone` never fires post-cancel (the retry can't fight a cancel). Diagnosed from a tester's "24h failed but 1d worked" — those parse to the SAME 1440 minutes (`parseDuration`: `24h`=24×60, `1d`=1×1440), so identical requests, so the difference was purely transient. **Any server-streamed AI call should treat a clean-but-empty completion as retryable, distinct from an `onError`.**
- **Response voice / persona (v0.17.2).** `AIConfig.persona` (free text, blank = default; Settings → AI → "Response voice", capped 120 chars) flavours the VOICE of AI output — e.g. "a 90s TV news anchor". `catchupSystem(character, tier, label, persona)` swaps the default warm-companion voice line for a persona line when set, **explicitly subordinated to the facts** ("changes only HOW you speak, never WHAT is true — keep every statement grounded in the log"), and the plain-prose formatting rule still binds so a silly persona can't break the output. Read fresh per run (same `loadAIConfig()` as the model), applied on BOTH paths, and **surfaced in the recap header** (`· voice: …`, only when set — the model precedent). Rides `SharedProfile.ai` → `_shared.yaml` like the rest of the AI config (machine-local, non-breaking — `loadAIConfig` coerces a missing/retyped value to `''`). No prompt-injection risk that matters: AI never issues commands (architecture guardrail), it's the user's own key, worst case is a quirky summary. The persona IS sent to the provider (user-authored, not PII) — reflected in AINOTICE.md.
- **Slash surface:** `/ai` (bare status · on · off · key · catchup · stop). It's a **bare+verbs noun**, so it depends on the `NOUNS_WITH_VERBS` palette rule (see the slash section) — without it the verbs never list or Tab-complete. `/ai key` deliberately does NOT accept a key inline (it would land in command history and the palette); it points at Settings. **There is deliberately NO `/ai model` command** (Sekmeht, 2026-07-23 — model selection stays in Settings, consistent with `/ai key`); `/ai` status DOES show the current model. (A `/afk` command existed briefly and was removed — see the window-source bullet above.)
- **Error handling — SSE `error` frames MUST be surfaced.** `claudeProvider.handleFrame` THROWS on an Anthropic `error` frame (e.g. `overloaded_error`/529 arriving AFTER `message_start`) so it propagates out of `claudeChatStream` → main's try/catch → the renderer's error line. Without that branch a mid-stream failure hit no case, was ignored, and the caller's `onDone` fired with a partial/empty summary and NO error (tokens still counted). A pre-stream failure is already handled by the `!res.ok` check. Any future SSE consumer must handle the `error` frame, not just the deltas.
- **When adding an AI feature:** route output through `lbAI` (main-default, lbAI-if-open); gather via `collectHistory()` (never `lines` alone); add a consent key to `aiConfig.consent` + a disclosure modal that discloses BOTH the data sent AND that it's billed to the user's own key; call the adapter, never an SDK; give it a `/ai <verb>` + `NOUN_HELP` line; pre-check the key so consent isn't shown for an unfireable request; and **build its non-AI baseline first**.

### SimuCoin claim (F71, v0.18.0 — DESIGN §42)
Claims the **monthly free SimuCoins** Simutronics gives subscribers (they expire unclaimed). An authenticated **HTML scrape of store.play.net** — sign in → read balance/offer → optionally `POST /Store/ClaimReward` (`game=DR`) → sign out. Mechanism verified against [Thires' SimuCoins](https://github.com/Thires/SimuCoins) Genie plugin. Not a Lich concern (Lich has no web-store surface) and works identically direct-SGE.
- **All network work in MAIN** ([src/main/simucoin/](src/main/simucoin/)) — the AI-adapter rule: the renderer names an ACCOUNT, main pulls the password from `passwords.ts`/safeStorage itself, and only `SimuCoinStatus` crosses IPC. Cookies ride an **in-memory** partition (`fromPartition('simucoin', { cache: false })`, no `persist:`) reset via `resetJar()` — cookies **AND `clearCache()`** — before every attempt and after every run; a per-account `inFlight` guard stops two windows (or a click racing the startup pass) doubling a claim POST; multi-account runs are **sequential**.
- **`cache: false` + no-cache headers are LOAD-BEARING (B229).** ASP.NET pairs the sign-in form's `__RequestVerificationToken` FIELD with a matching COOKIE; a partition's HTTP cache is storage `clearStorageData({storages:['cookies']})` does NOT clear, so a cached sign-in page served a stale token against a fresh cookie and the SECOND account of a session was rejected. Never make the token GET cacheable. Backed by **one automatic retry on `auth-failed`** against a fully reset jar (bounded to one — a wrong password must not become a retry storm; it cannot double-claim because `auth-failed` returns before any claim POST).
- **[constants.ts](src/main/simucoin/constants.ts) is the ONLY file that knows store HTML** — a store redesign is a one-file fix. **Failure posture: go QUIET, never guess** — every failure resolves to a status (never throws), an unconfirmed claim reports still-claimable rather than false success, and `parseNextAt` only honors clear day/hour counts (a wrong "next claim" would hide the icon while coins wait — the worst failure this feature has).
- **Cadence: once per launch, then on demand. NO background polling** (repeated automated logins are the abuse-looking posture). **Claiming is opt-in per account** (`autoClaim`, default ask). **The startup pass runs ONLY in the primary window** (`isPrimary`, the F62 pattern) and is latched once-per-launch — secondary windows (and re-enumerations after the wizard adds an account) seed from `simucoin:cached` instead of re-hitting the store. **Runs are serialized GLOBALLY** (a module-level promise chain), not just deduped per account: every run shares one cookie jar that it clears on entry and exit, so two different accounts overlapping would claim under the wrong name — pitfall #101.
- **Consent is re-read FRESH at the choke point** (`App.runSimucoin`), never trusted from component state: the coin popover loads config at mount, so a second window's copy goes stale the moment another window revokes consent.
- **Consent is per account and gates ALL network access**, shown on the surface that enables it (AI-consent precedent) — it names exactly what is sent (the saved password, over HTTPS, to store.play.net). **That surface is Settings → SimuCoins as of v0.18.1** (F75): the disclosure renders ONCE, directly above the per-account toggles that act on it, from the single `SIMUCOIN_DISCLOSURE` string in [simucoinConfig.ts](src/renderer/simucoinConfig.ts) — one string so no two surfaces can promise different things about the user's password. An account with no saved password is never offered. Config `{consented, autoClaim, lastBalance?, lastCheckedAt?}` → `SharedProfile.simucoin` → `_shared.yaml` (optional, no migration); NOT a Transfer category (machine-local + credential-gated). Both the loader and `importSharedProfile` coerce entries so a hand-edited YAML can't read as consented — and **the loader's coerce REBUILDS each entry, so a new field must be added there too or it is destroyed on the next save** (pitfall #121; the same edit must fix any restated inline `Partial<{…}>` at a write path — Settings' `setSc` had one). **Last-known balance (F100, v0.18.6, DESIGN §42.9):** already scraped, now persisted and shown — Settings gets a quiet per-account line, the coin popover ONE aggregated line that never grows with the roster (pitfall #109). Recorded at the single `App.runSimucoin` choke point via `rememberBalance`, which **no-ops on a failed check** so an outage keeps the old figure and its real age; the age is never optional (a bare number reads as "now"), unknown renders nothing, and revoking consent drops the cache. One formatter (`simucoinBalanceText`) serves both surfaces so they can't drift (B234).
- **UI is app-level** ([SimuCoinButton.tsx](src/renderer/components/SimuCoinButton.tsx) in the AppBar) — an allotment belongs to an ACCOUNT, not a character (pitfall #57's converse). **Renders NOTHING when no account is opted in or offerable** (UX standard #1). **The surface is SPLIT as of v0.18.1 (F75), and the split is the point:** *Settings → SimuCoins* owns the LIST (per-account enable / auto-claim / Check now / status, plus the disclosure), because a modal is built for lists; the *coin popover* owns the ACTION and is deliberately **roster-INDEPENDENT** — one summary line plus full-width **Collect available coins** / **Check now** / "Set up in Settings…", so it renders at the same size with two accounts or twenty (pitfall #109 — a popover that grows with user data was the B235 defect). It shows the soonest `nextAt` only when the store supplied one, never a guessed date. Settings is per-session and the coin is app chrome, so "Set up in Settings…" crosses that gap with a `lichborne:open-settings` DOM event carrying a section name, answered by the ACTIVE GameWindow (the `lichborne:session-action` shape, pitfall #57); Settings scrolls to it via its F61 `sectionRefs`. Settings is now the only config WRITER, and it must do all three of `saveSimuCoinConfig` + `scheduleSharedProfileSave()` + dispatch `SIMUCOIN_CHANGED_EVENT` — the last because a `storage` event never fires in the writing window, so without it the coin keeps offering to set up an account you just enabled (the `lichborne:ai-key-changed` precedent). `/simucoin` (bare status · check · claim, aliases `/sc`) is a bare+verbs noun. Executors are synchronous while a store run is a network round-trip, so an executor returns only whether a run STARTED — but the OUTCOME is reported: `runSimucoin` resolves with the account's `SimuCoinStatus` and GameWindow appends a result line per account as each settles (`emitClientLines`, the `internal-system` + `[sys]` path slash results use), alongside the toast and coin popover. **Take the status from the RESOLVED VALUE, never re-read `sc.statuses`** — that `setState` has not committed when the promise resolves, so a read-back formats the previous run's row. One formatter (`simucoinRowText`) serves both the async report and the bare `/simucoin` status so they cannot drift (B234).

### Windowed Panels / Free Layout (DESIGN.md §33, shipped v0.13.0)
A per-character toggle (`layoutMode: 'panels' | 'free'`, scopedKey) between **Static Panels** (the docked 4-zone skeleton) and **Windowed Panels** (free-floating windows). **Terminology:** *Static Panels* = docked; *Windowed Panels* = floating; *streams* are the content placed inside either — those are the user-facing terms. Internal code keeps the older name "Free Layout" (`freeLayout.ts`, `fl-*` CSS, `freeWindows`/`layoutMode`/`freeLayoutLocked`); don't bulk-rename.

**Direction (Sekmeht, 2026-06-11): Windowed Panels is the end state — Static Panels WILL be discontinued** (stage 1: flip the default to `'free'` for new characters; stage 2: remove Static entirely with a one-time `buildWindowsFromCurrentLayout` conversion so no layout is lost — neither stage scheduled yet; full plan in DESIGN §33). Operational consequences NOW: don't build static-only features; until stage 2, every new feature must still work in BOTH modes (the pitfall-#79 per-mode aggregation pattern is the interim cost — those branches collapse when Static goes away); prefer designs that get simpler without the zone skeleton; and treat the zone scopedKeys as future legacy state to MIGRATE at stage 2, never silently delete (Principles #3/#8).

- **Model:** in free mode GameWindow renders a pointer-through `WindowLayer` ([WindowLayer.tsx](src/renderer/components/WindowLayer.tsx)) of `FloatingWindow`s ([FloatingWindow.tsx](src/renderer/components/FloatingWindow.tsx)) INSTEAD of the skeleton (the skeleton's chrome strips / main-top zone / right column are gated on `layoutMode === 'panels'`). Each `FloatWindow` ([freeLayout.ts](src/renderer/freeLayout.ts)) has a **fractional** rect (0..1 of the game area, so windows scale on OS-window resize / §13.9 decouple), `z`, `showTitle`, and a `kind`: `panel` (a PanelFrame on `sharedFrameProps`) or decoupled chrome — `main` (the extracted `textAreaNode`), `command` (`commandBarNode`), `vitals`, `icon`. Persisted via the scopedKeys above → YAML (Principle #1; carried by Profile Transfer's Panel Layout category).
- **Conversion = CASCADE, NOT measure-and-mint.** Switching to windowed (or "Rebuild from panels") runs `buildWindowsFromCurrentLayout` — clean default SIZE per surface at a staggered position (only chrome HEIGHTS are measured). The pixel-for-pixel "pop your exact layout free" approach was tried and **abandoned**: titled windows are bigger than edge-to-edge panels strips, so it always overlapped / "scaled weirdly" (esp. the bottom command bar constricting everything). Don't revive it.
- **Snapping** (§33.5): magnetic edge + window-to-window on drag AND resize; live guide lines; **Alt** disables; arrow-keys nudge the focused window. Snap targets measured LIVE from the DOM (`getSnapTargets`).
- **Add/lock UI lives in the Layout Manager** (renamed from Panel Manager in v0.18.2 — DISPLAY ONLY; every persisted key, the `toggle-panels` action and the `panelManager` status flag are unchanged, and the app-bar button reads **Layout**), NOT floating on the overlay (a floating button had no good home): an **Add Window** section (unlimited new panels + re-add any missing singleton so a closed bar can't be lost) and a **Lock windows** toggle (`freeLayoutLocked` → drag/resize/nudge become no-ops and the handles aren't rendered. **As of v0.18.1 locking also drops the window header ENTIRELY and quiets the frame to a panel-weight `--border-subtle` hairline with no lift-shadow (F74)** — locked is a DISPLAY mode that reads like docked panels; this supersedes the F44-era rules that merely softened the locked header (`cursor: default`, hiding the grip's drag dash) and the "keep the 11px grip strip so geometry doesn't shift on lock" workaround. **That is only lossless because the header is an absolute OVERLAY on the content, never a flow row above it** (v0.18.1) — it reserves no height, so lock/unlock is a pure visibility change with ZERO layout effect and the stored rect is never touched, satisfying pitfall #74 outright instead of working around it. **Do NOT put the header back in flow:** as a flow row it reserved ~10px per window, and because snapping aligns OUTER edges, tiled windows met flush while their CONTENT stayed ~10px apart — hiding the header on lock then merely RELOCATED that space (a fixed-height chrome window re-centred its bar and the gap survived; Sekmeht). Z-order inside a window is body → header (1) → resize handles (2) → title buttons (3) so the top and corner grips stay grabbable *through* the overlay, and the header is quiet at 55% opacity until window hover/focus since it now sits over content. **Tab drag-reorder is NOT gated on the lock as of v0.18.2** — F46 v0.13.5 originally passed `!freeLayoutLocked` for windowed, but Sekmeht reversed it: `reorderTabs` is now unconditionally `true` in BOTH modes. **The line the lock draws is CONTAINER vs CONTENT — it freezes window position/size (the things you knock askew by accident), never what lives inside a window.** **ONE window is exempt from even that: DEBUG (v0.18.4, F96).** It is not part of the layout you froze — it is a transient diagnostic surface you open, read and close — and because a locked window renders NO header, locking it left nothing to grab. It also renders above every ordinary window (`DEBUG_Z`, derived from the exported `Z_RENORMALIZE_CEILING` so no amount of click-to-front can outgrow it — pitfall #124). `isDebugWindow` in freeLayout.ts is the SINGLE definition of which window that is, shared by the toggle, the lock exemption and the z-order so they cannot disagree. Right-click → Close on a stream circumvents the lock for the same reason (and chrome bars are excluded from that menu entirely — closing the command bar is a footgun, not a content op). Users run locked most of the time, so gating content operations on it made lock read as "read-only", which it isn't. Apply that split to any future lock-adjacent feature. Reorder flows through the normal `onTabsChange` path, so persistence is the existing one in both modes). The static-panel **zone manager is hidden in free mode** (windows aren't bound to zones).
- **Chrome windows (command/vitals/icon) are FIXED-height** (deterministic), resizable both axes, bar centered in the body. **Never auto-height, and never auto-*hug* either** — see pitfall #74 (the deterministic-height rule) and pitfall #93 (a measure→persist auto-hug was tried in v0.17.0 and REVERTED — it corrupted saved heights + clipped the vitals bar).
- **Window chrome (title bar / grip) — the v0.17.0 polish + its invariants ([free-layout.css](src/renderer/styles/free-layout.css)).** The `.fl-titlebar` (name shown) and `.fl-grip` (name hidden) are a slim ~8px header; **their heights MUST stay matched** (both derive from `1.4em × 0.5em` / `0.7em`) or showing/hiding the name jumps the header (the bug: a shrunken titlebar was thinner than the grip). Title text needs box > glyph height to centre (too tight → reads top-aligned). The **top resize handle is 4px** (was 6px) so the slim header keeps a drag-to-move zone below it — and because that handle is a FIXED 4px while the header is em-scaled, both `.fl-titlebar` and `.fl-grip` carry a **`min-height: 9px` floor** (kept EQUAL) so the move band stays grabbable at the minimum game font (8px, where the em height would be ~5.6px). The **T (hide-name) button is a chevron `⌃`**, and both controls (collapse/close) are **hidden until window hover/focus** (`.fl-window:hover`/`--focused .fl-tb-btn`) — quiet by default. Visuals are a soft top-lighter **gradient + hairline** (not a hard grey block) with a 1px inset **accent focus line**, all `color-mix` (theme-safe, eyeballed light — Principle #4). When touching the header, keep titlebar==grip height and re-verify the show/hide toggle + drag both work.
- **Cross-feature invariants (verified in the v0.13.0 sweep — hold these when touching windowed mode):** (1) **Adding streams in windowed mode goes through the per-window PanelFrame `+`** (it lists discovered + builtin + custom streams), NOT the Panel Manager's Available-Streams list (that's hidden in free mode) — so don't assume the zone-add path is reachable. (2) **All scroll-resnap paths are `scrollRef`-based** (`stickToBottom`), and `scrollRef` is set by the Virtuoso `scrollerRef` whether the main text renders in the skeleton or a `kind:'main'` window — so replay/tab-show/font-change/resize resnap cover free mode automatically; keep it that way (don't gate resnap on `layoutMode`). (3) **Modals/overlays must out-z the `WindowLayer` (z-index 60)** — the Panel Manager / Settings / Maps / context menus all do (portaled or higher z); a new overlay under z 60 would hide behind floating windows. **Corollary (B166): a DOCKED strip (a flex child of the shell, like the Debug strip) can never work in free mode** — the layer is absolute inset-0 over the whole shell, so the strip renders UNDER the windows; don't reserve a layer inset for it either (tried, replaced). The pattern is to open such surfaces AS a floating window in free mode: the Debug button toggles a panel window seeded with the `debug` tab (`toggleFreeDebugWindow`), the strip renders only in panels mode, and collection is presence-based (`debugOpen` memo, the pitfall-#76 `lichScriptsOpen` shape) so the window ✕ / tab ✕ turn telemetry off with no extra bookkeeping. The collection effect also keys on `session.sessionId` so a reconnect-in-place (pitfall #69) re-arms main's `debugPanelOpen` raw-XML gate. (4) Session-log capture, highlights/contacts/triggers, and the map all work unchanged because windows host the *same* PanelFrame content as zones — only the container changed. **(B193 caveat) That "same content" guarantee only holds for props that ride `sharedFrameProps`** — a callback wired ONLY to a panel's docked panels-mode strip (the Debug panel's `onGotoFireRule`, which drives the Fires-tab "Edit" button) is absent from the PanelFrame-hosted copy, so it silently degrades (greyed button) in a floating window or a zone tab. When a panel is hosted BOTH as a docked strip and via PanelFrame, every prop/callback the strip gets must also be in `sharedFrameProps` → PanelFrame → `renderPanel`, or the PanelFrame copy is missing it. (5) **Every "which tabs are visible/watched" aggregation must branch on `layoutMode`** — the zone arrays are deliberately-preserved STALE state in free mode (they restore the layout on switch-back); aggregating them in free mode caused B167 (unread dots never cleared) and B168 (stream fallback double-routing/blocking). Current per-mode sites: `lichScriptsOpen`, `debugOpen`, `activeIdsRef`, `watchedStreamsRef` — mirror them for any new per-tab feature (full write-up: pitfall #79). (6) **The WindowLayer's ResizeObserver IGNORES 0×0 measurements** (B174 — a hidden character tab is display:none = 0×0; letting it through unmounted every floating window on every tab switch). The `size > 0` render gate means "never measured yet," nothing more — don't re-introduce a 0×0 bail (pitfall #83). **Known minor edge (not a bug):** closing the Game/Command window leaves no main text or input until they're re-added via **Add Window**, which is recoverable by design. ("Reset panels" used to sit in the header and act invisibly in windowed mode. Since v0.19.7 it renders only in the Layout Manager's static-only section, and the windowed reset is "Rebuild from panels".)

### Lichborne Experiences (DESIGN.md §34 — SHIPPED v0.14.0: scaffold + Living Tableau [Beta])

**Profiled clean (v0.18.0) — don't re-investigate; the numbers are in DESIGN §45.7.** Both components were measured against PRODUCTION React (bundle the real module, time `flushSync`; `<Profiler>` is a no-op in a production build): a Moons render costs 0.38ms, a CROWDED Tableau (14 players / 9 creatures / 8 bubbles) 0.15ms, the heaviest Moons sky holds 60fps with zero dropped frames, and the scene capturers cost 0.37µs/line (~0.3ms per MINUTE). Three known-but-unfixed items are recorded there with the reason each was left alone — the important one: **do NOT add a custom `memo` comparator to an Experience** (it would render silently stale the day the component starts reading a prop the comparator ignores; 0.38ms is not worth that). The one UNMEASURED area is the combat facet — readiness rings, gauges, assess arena.
The §32 G-series/X-series ("graphics for text players": Combat HUD, Wound Paper-Doll, Tactical Radar, Living Tableau, …) ship as **Lichborne Experiences** — a new first-class layout object: registered graphical **floating surfaces** over the game layout, hosted by the v0.13.0 `FloatingWindow` engine via a new `ExperienceLayer` rendered in BOTH layout modes (the Maps overlay is the shipped precedent). **The panel/stream system stays byte-identical**: Experiences have their own registry (`experiences.ts`), own id space (never in `discoveredStreams` or tab arrays — stream-collision-safe by construction), own scopedKey persistence (rides `state:` → YAML, no profile-shape change) + a `TRANSFER_CATEGORIES` category, and an app-bar **"Experiences"** shelf. `map`/`exp`/`lichScripts` stay panels forever. **Two models were explicitly REJECTED — don't re-propose them** (full reasoning in §34.2): per-panel "Interactive Mode" view toggles (Simu splits combat text between the combat stream and main, so the combat window is optional equipment; the HUD reads parsed *state*, not stream text) and "Interactive Streams" as a new panel category (wrong chrome for instruments; pointless namespace bookkeeping). A third draft — the "v2" panel→Experience conversion/dual-hosting revision — was **deliberately discarded** (2026-06-12), but its **DUAL-HOSTING half was SUPERSEDED and SHIPPED v0.15.1 at Sekmeht's fresh explicit ask** (screen estate): every PanelFrame `+` menu offers Experiences below a separator with an [e] badge; hosted tabs are `type: 'experience'` with namespaced `exp:<id>` ids (NEVER bare — §34.1 collision safety; NOT in ALL_PANEL_TYPES), rendered by the same `renderExperienceContent` builder via `renderExperienceTab` on sharedFrameProps (B193), sharing the floating instance's ⚙ prefs — **and hosting their OWN ⚙ in the F31 view-controls corner as of v0.15.2** (`getExperienceHidden`/`onToggleExperienceOption` on sharedFrameProps; find-or-create mints a CLOSED instance as the prefs record for tab-only experiences — one `hidden` map, window and tab can never disagree), taking the panel's F31 A+/A− via an inline `--game-font-size` re-map, counted into the §35.6 scene gate by the pitfall-#79 per-mode `expTabIds` aggregation, and EXCLUDED from `watchedStreamsRef`. **The panel→Experience CONVERSION half stays discarded** — `map`/`exp`/`lichScripts` remain panels forever (full split: §34.2 item 3). First Experience: **Living Tableau (X1, "Gather Mode")** — decided 2026-06-12, superseding the Combat-HUD-first call. **G1 (Combat HUD) is now a combat FACET of X1, NOT a separate Experience** (2026-07-16, Sekmeht — full decision + risks in DESIGN §32.1's G1 entry): its instruments render as auto-revealing `ExperienceDef.options` LAYERS on the Tableau's scene (readiness ring / range gauge / threat+condition drawn ON the creature figures — the SceneParser already tracks `cast-creatures`), which subsumes a hard Social⇄Combat mode toggle (all combat layers off = today's pure social Tableau; social off + combat on = a combat cockpit rendered on the scene). The `CombatParser` work is unchanged and co-locates in the same Experience. Watch at spec time: visual-grammar collision (tactical layers must take hierarchy when combat is live), combat-flood perf under the §35.6 gate, and G1 now riding X1's Beta stability. NOT a §34.2 rejected model (those rejected giving *panels* interactive modes; this layers onto an *Experience*). **G1 combat facet — Phase 1 BUILT (v0.16.1, 2026-07-17) — the cockpit renders AROUND the player avatar, NOT on the creature figures (Sekmeht refinement: "the combat flavor should sit around the player's bubble").** On the Tableau: **readiness RINGS** hug the avatar (three cooldown sweeps inner→outer — Roundtime / Cast / Aim, each in its command-bar timer colour `var(--rt/ct/aim-end)`, only while that timer runs; a `CombatRings` SVG); a **danger pulse** on the avatar for wound conditions; **threat/engaged flare** on creature figures (v0.17.0/B220 — driven ONLY by ASSESS, which knows who's at melee; the old BLANKET `inCombat`→flare-every-creature marker was RETIRED because it flagged harmless NPCs that wandered in mid-fight, and no non-assess signal can tell friend from foe); and a **gauge panel UNDER the figure** ([TableauExperience.tsx](src/renderer/components/experiences/TableauExperience.tsx) + [experiences.css](src/renderer/styles/experiences.css)) with **BAL / POS bipolar gauges** (a red→yellow→green spectrum track + a fixed **0/neutral tick** + a moving value needle; balance is anchored at **"solidly" = 0 baseline**, green above / red below) and an **RNG** row (M/P/R cells, threatened band lit). All gate on the `readiness`/`threat`/`danger`/`position` ⚙ options; gauges FADE with combat (v0.17.0: hold through the 8s `inCombat` sticky, then fade out via asymmetric CSS — quick in, slow out; NOT the old persistent-HUD "leave them on"), timers/rings only while live. The state (`rtExpires/ctExpires/aimExpires/stance/hands/position/balance/range/assess`) rides ONE `useMemo`'d `experienceCombat` object through the single `renderExperienceContent` props source (pitfall #82c — a fresh literal each render would defeat the memo'd Tableau). **THREE gotchas this build cost:** (a) **a scene root that hosts A−/A+ zoom MUST anchor `font-size: var(--game-font-size)`** — `.tableau-scene` lacked it, so the em-based figures/gauges never scaled and "the +/- buttons did nothing" (the moons-scene had it; B211). A−/A+ is effectively a whole-scene ZOOM (element-scaling, fit-to-window, crisp) — the right model for a graphical Experience; a transform camera-zoom (crop-in) is for large navigable spaces like the map. (b) **The scene is a flex column: a fixed-height `.tableau-header` (title+desc, desc line-clamped) + a `.tableau-stage` (flex:1, holds EVERY figure + bubble, `sceneRef` measures it).** Header/stage separation stops figures rendering over the title at close zoom, and the FIXED header height stops figures hopping when description length changes. (c) Combat position/balance/range are parsed by **mirroring Lich's verified regexes VERBATIM** (combatExtract, DESIGN §32.1) — `DRStats` is NOT front-end-readable (in-memory Ruby `@@` vars, no client push; verified), so we run Lich's exact parse in the FE (identical value, live, works direct-SGE). **ASSESS (combat arena, layout B) — BUILT (v0.16.1):** `parseAssessLine` ([combatExtract.ts](src/shared/combatExtract.ts)) MIRRORS Lich's `parse_assess_line` VERBATIM (xmlparser.rb #1413) → typed per-creature records (relation facing/flank/behind/advancing + range + status + id). GameWindow accumulates the `assess` stream per block (`clearStream 'assess'` resets `assessAccumRef`; each line's creature id is scraped from its `<d cmd='look #id'>` segment `cmd`; routing untouched — assess text flows to its own `assess` stream, which as of v0.17.3 (B227, Illiahanna) is DISCOVERABLE (removed from `NEVER_DISCOVER`) so it can be opened as its own panel, and falls back to main when no Assess panel is open (`STREAM_FALLBACK['assess']='main'`); the Tableau's accumulation is independent of discovery/watching, so making it discoverable is purely additive. NEVER_DISCOVER rule: only INTERNAL streams (main/raw/room sub-streams) and ALIASES to a built-in panel belong there — never a real DR stream that has no built-in panel home (assess was the lone violation)), snapshots to `assessCast` + `assessAt`, threads into `ExperienceCombatState`. **The snapshot is guarded on a NON-EMPTY accumulator** (`batchAssessTouched && assessAccumRef.current.length > 0`): DR emits `clearStream 'assess'` THEN the lines, and at the 16ms flush coalescing (B172) they almost always land in one batch — but a block straddling a flush boundary would apply the just-cleared empty accum and flicker the arena blank for a frame. A completed assess always carries ≥1 entity (the self line), so the guard never suppresses a real update; a genuinely-empty assess (left combat) ages out via the 30s TTL instead. Same clear-then-lines batch-boundary shape applies to any future stream-repaint accumulator — guard the snapshot on non-empty. The Tableau renders id'd creatures with a relation tag + range chip + targeting-number badge, **your target ringed gold** (from the self row's `target`), **melee = engaged** (red glow — the honest "who's attacking", all-melee-attack, no faked per-attack attribution), **reeling** (stunned/imbalanced) dimmed, **click a creature → `face #id`** via the new `onCommand` prop (user action, echoes+logs, NOT automation — like map-walk buttons); ages out after `ASSESS_TTL_MS` (30s — assess is on-demand/script-driven). **v0.17.0 lifecycle fix:** the arena RECONCILES against the live creature cast — assess is a snapshot, so a killed/decayed creature would linger as "engaged"; reconcile by **name-count** vs the SceneParser's dead-aware cast (`normAssessName` normalizes article/case both sides; safe fallback to the raw snapshot if a name-mismatch would blank it), and render the cast's **dead creatures as ✕ corpses** in a row above the tactical row until they DECAY out (they don't vanish on death — Sekmeht). **v0.17.0 layout:** the scene font **fits the container** (width-dominant `fitScale` off the measured stage — a wide-short panel tab was fitting to ~6px "way too tiny"; height only a gentle floor), the **BAL/POS/RNG gauge panel is pinned to the stage BOTTOM** (not hung under the self, so it can't clip in a short tab), the **self figure clamps up clear of the gauge panel** in a short stage (measured gauge height), and the **self renders above seated figures** (z-index) so an overlap reads as depth. **The assess snapshot is ROOM-SCOPED** (B219): it has no inherent room binding (on-demand, cleared only by `clearStream 'assess'`), so an effect drops it on any room-identity change (title OR nav id, pitfall #46) — else retreating drags the old room's creatures into the new one (the name-mismatch fallback surfaces the stale snapshot when the new room has creatures). **Any future per-room SNAPSHOT state must clear on room change the same way** — the live cast auto-updates (B121 sub-stream clear), but a snapshot doesn't. **INVARIANTS from the assess design:** the **id is identity, the NUMBER is a reusable targeting slot** (a dead creature's slot recycles — corpus 2026-07-17: slot #1 went 48407408→48430408; key figures on id); **combat narration is ANONYMOUS** (attacks/closes/advances carry NO creature id — only assess/look/face do — so per-attack→figure attribution is impossible; don't fake it); creatures have POSITIVE ids, PCs NEGATIVE (`pc`). Corpus: `corpus/2026-07-17-assess-lavadrakes.xml` (gitignored). Verified by a focused bundle harness against the real captures + Lich's spec cases. The `CombatParser`'s range/facing/target is now partially realized through assess; the memory backlog's "Tactical Radar" idea is this same data if we ever want a top-down view. X1's Phase 1 kernel (cast + stable seating + procedural avatars + Say bubbles + focus-the-speaker) needs the NEW **`SceneParser`** (name pinned 2026-06-12; G1's later range/target/facing sibling = `CombatParser`) — typed speaker/channel/arrival *scene events* (speech/emotes/arrivals are only styled text lines today, no typed "who said what" exists). **The SceneParser is a CAPTURER REGISTRY (DESIGN §35, Sekmeht's design rule):** every scene event = one registry entry with `gate` (cheap substring pre-check, pitfall #82a) + `match` + `provenance` + `status: verified|unverified` — never a hardcoded if-chain; patterns are mined from existing parsers (Lich `drinfomon` drdefs.rb = the cast-extraction rules; Frostbite xmlparserthread.cpp = the channel/stream routing) and corpus capture (`corpus/`, gitignored) VALIDATES them (replay the real bundled code against captures, flip to verified, fix GAPs). **Phases 1+2 are BUILT (2026-06-12):** [sceneCapturers.ts](src/main/parser/sceneCapturers.ts) (registry; only `verified` capturers run live), [SceneParser.ts](src/main/parser/SceneParser.ts) (cast + arrive/depart diffing per-session, transition-suppressed so our own moves never mass-fire), [sceneExtract.ts](src/shared/sceneExtract.ts) (Lich-rule extraction), `scene-cast` snapshotted for replay (pitfall #60b); the Tableau is a pure view over the typed cast — don't re-add renderer-side text parsing. **Speech (say/ask/exclaim, yell, whisper, thought) is VERIFIED against the real captured session `Frostbite-Dev/frostbite/support/mock.xml`** (the B165 corpus) — harness `tmp-scene-harness/run.mjs` (~115 cases) replays verbatim corpus lines. **The harness is MACHINE-LOCAL, deliberately gitignored under `tmp-*/`** (Sekmeht 2026-06-12: it embeds verbatim corpus lines with real player names — same privacy stance as `corpus/`); it is NOT in the repo or git history — if it's missing, rebuild it from §35.4's recipe (bundle the REAL parsers via esbuild, feed corpus lines, assert events) rather than hunting for it; key facts: speaker+verb ride INSIDE the preset with the quote outside, yells are `<b>`-wrapped (no preset), whispers alias into `conversation`, thoughts have 3 shapes (one rides the TALK stream), DR double-emits to main (deduped by home-stream gating), and DR reuses speech/thought presets for STAT TABLES — the verb shape, never the preset, is the signal. Bubbles are replay-gated (#60a); thoughts render as wisps, NEVER a seated body (§32.2). **v0.17.0: the wisps live in a quiet BOTTOM-LEFT "thought log"** (was top-right, where they obscured the scene — Sekmeht): `.tableau-wisps` anchored `bottom/left`, `flex-direction: column` so the array (oldest→newest) puts the NEWEST at the fixed bottom spot and older ones stack UP + recede (`nth-last-child` opacity fade, `transition: opacity` for a smooth age-up). Bottom-LEFT clears the centre avatar, the bottom-centre gauges, AND the right-edge A−/A+/⚙ controls; `max-width: 42%` keeps it short of centre. **PERF CONTRACT (§35.6, v0.14.0 — INVARIANT): all scene work is OFF until the session has an open Experience** — `parser.sceneCapturersEnabled` (default false) gates the per-line capturer call, `SceneParser.setActive(false)` silences ALL emission (cast still silently tracked so activation backfills via `snapshotCast()` — never inject a LOOK), toggled by GameWindow's `scene-active-toggle` effect (re-arms on sessionId change, pitfall #69); don't add scene work outside this gate. Movement/stealth/emote/logon capturers + conversation gravity (§35.7: chattiness circle, directed-speech `target` pulls pairs together) all shipped 2026-06-12, every shape corpus-verified; procedural avatars are the permanent no-AI baseline (AI backdrop/portrait tier is Phase 3, behind the `AIProvider` adapter). **An Experience may declare its own RESIZE FLOOR — `ExperienceDef.minSize` (B318, v0.19.6):** a floating Experience is `kind: 'panel'`, so it inherits `MIN_WIN_PX` (180×110), a floor sized for a panel of TEXT. An instrument whose natural shape is a STRIP (the Spell Monitor: a grid of short cells, ~84px for one row) hits that floor well above its content and reads as refusing to shrink — the SAME failure the chrome bars already have per-kind floors for (`minSizeFor` in freeLayout.ts, whose comment calls it "unshrinkable vertically"). `ExperienceLayer` passes `def.minSize` to `FloatingWindow`, which prefers it over `minSizeFor(win.kind)`. **Declare it per EXPERIENCE, never lower the global floor** — "how short can this usefully be?" is a property of the scene, and a sky or a tableau genuinely needs height. **Per-window view controls (v0.14.7):** every Experience window hosts hover-quiet A−/A+ (per-instance `fontSize` override, applied by shadowing `--game-font-size` on the window subtree) and a ⚙ popover of content-layer checkboxes driven by `ExperienceDef.options` — a new Experience declares its option list in the registry and gates on the `hidden` prop at its single prop entry point (filtered arrays MUST be useMemo'd — effects key on their identities); both persist as optional `ExperienceInstance` fields (old saves load unchanged, ride YAML + Transfer for free). No slash command by design (the window is the surface — recorded §34.6). Phased plan in §34.9; checklist + guardrails in §34.7–34.8. **Experience #2 — Moons [Beta] (v0.15.1, §34.9 item 3; renamed from "Weather & Moons" — id stays `moons`; the [e] badge in the + menu AND tab strip distinguishes it from moonwatch's "Moons" STREAM, and experience-tab labels resolve LIVE from the registry so renames reach already-placed tabs):** a sky-dial instrument over the community **moonwatch.lic** stream — the proof an Experience can ride a Lich script's feed. `parseMoonLine` (**TRIAGE — "the moons look wrong" on moonwatch 4.2+ is almost always THEIR side: v4.2 re-anchored the moon epochs and requires `;moonwatch reset` ONCE PER CHARACTER after updating, or their timers drift and ours drift with them. Ask that first.** The `moonWindow` stream format is UNCHANGED through v4.5.0 — still the three `[K]+(12)` shorts — so the upgrade cannot break our parser; everything v4.3–v4.5 added (lunar phase, arc position `t`, next-phase, sun/calendar) is **UserVars-only**, which reaches disk on Lich's ~5-min save cycle. Lunar phase turns out to be a PURE function of the DR client's own constants that we could compute ourselves with no Lich at all — spec'd and DEFERRED as DESIGN §34.9 **F64a**, constants recorded in Knowledge.md §16; don't re-derive it.) `parseMoonLine` accepts the script's NEGATIVE overdue timers `[x]-(-2)` — a digit-only regex made moons vanish pre-rise) + orbital constants + `computeSunPhase` + the sun-prose regexes (mirrored VERBATIM from the script's own detection) live in [experiences.ts](src/renderer/experiences.ts); GameWindow parses `moonWindow` lines at the generic stream branch (read-only id match — routing/discovery untouched, the stream stays addable as a plain panel = the text equivalent; reports MERGE into state, never replace — a moon can't vanish) and catches sunrise/sunset in the main branch behind a pitfall-#82a substring pre-gate. **Sun anchors come from three sources, priority-ordered:** locally-observed prose transitions (exact, provenance-flagged) > the **dr-scripts Firebase** (`moon_data_v2.json` — the same public feed moonwatch polls; `moons:fetch-sun-data` in main, 10-min cache — Lichborne's ONLY direct community-feed read, remove-by-one-gate if ever unwanted) > a lich.db3 `UserVars.sun` synth (≈-flagged, only after the fetch fails; reading it exposed + fixed the pitfall-#91 Marshal link bug). Day/night therefore works direct-SGE (Principle #2; the moon feed itself degrades to an empty state teaching `;moonwatch window`). The footer always shows data age — the feed is crowd-sourced; stale must never read as live. Rendering: viewBox width tracks the measured aspect (0×0-guarded, pitfall #83) — its width CEILING is a **degenerate-measurement guard only** (5000; B204: the original 1100 triggered on ordinary maximized panels, and `xMidYMax meet` then letterboxed the SVG ground/ridges apart from the full-width HTML sky layers — the "sky fills, ground inset" split is the diagnostic signature of a mixed HTML+SVG surface whose viewBox clamp is too low; size any such clamp so normal use can never hit it); SVG text sizes via `calc(var(--game-font-size) * k)` (CSS px = SVG user units — tracks A−/A+, the B201 map-LABEL approach); sky = elevation-blended gradient layers + horizon silhouette + rise/set rings, ALL motion gated on `!settings.epilepsySafe` AND its ⚙ layer. **⚙ options are ONE PER VISUAL LAYER and accurate about exactly what each hides** (The Sun / Living sky / Countdowns / Names / Horizon / Effects — the original combined "Sun & sky" conflated hiding the sun with flattening the backdrop; Sekmeht's split, 2026-07-08; `sunPhase` computes regardless of the sun toggle because the SKY needs it) — apply the same one-option-per-layer rule to future Experiences. **Moons stay STREAM-driven by moonwatch — a Firebase moon fallback was proposed and DECLINED; don't re-add it without a fresh ask** (only the sun reads the feed). **Tier 1 + Tier 2 SHIPPED v0.17.0 (Sekmeht) — do NOT call any of this "Almanac"** (Simutronics' later, separate feature; the term is banned in code/UI/docs — it's the "Moons" experience, full stop): **(Tier 1) a time-of-day WORD on the sky** (DAY/NIGHT/DAWN/DUSK, `skyPhaseLabel` off the SAME sun elevation the gradient uses so it can never disagree; centered just above the horizon, clear of the moons/sun that rise & set at the left/right ends; follows the ⚙ "Living sky" layer — and once TIME is captured the fine Elanthian **daypart** is appended in parens (`calendar.timeOfDay`), e.g. `Night (late evening)`, so it moved OFF the footer date line, which now reads month · year · season). **(Tier 2) weather + the Elanthian calendar, both from COMMAND PULLS** (DR exposes NEITHER passively — verified via drinfomon/DRStats/Vars/XML: the ONLY sources are the `WEATHER` and `TIME` commands; **no community script stores them** — `almanac.lic` is a skill grinder, unrelated). **Weather** (`WEATHER_GLANCE_RE` in experiences.ts): the glance marker — BOTH `You glance up at the sky.` (outdoors) AND `You glance outside.` (indoors w/ window/door) — is followed on the NEXT main line by the weather prose, captured + shown VERBATIM. This is PASSIVE (any natural sky-glance updates the chip). The enclosed refusal `That's a bit hard to do while inside.` is a GENERIC refusal (never matched passively) → read as "indoors" ONLY inside a ⟳ sync. **Calendar** (`parseTimeLine`): parsed ONLY from a ⟳ sync's TIME reply (line 3's `It is currently X and it is Y` isn't unique enough to trust from room prose) — day-of-YEAR (0-indexed "N days since the Victory") → day-of-MONTH via [src/shared/elanthianTime.ts](src/shared/elanthianTime.ts) (`dayOfMonth`, months are a uniform 40 days) so it reads `4 Ka'len the Sea Drake · 457 A.V. · winter · evening`; season/daypart verbatim from TIME line 3. **THE ⟳ IS SILENT (Sekmeht — "don't want players to see the command go through"):** it sends `TIME` + `WEATHER` via RAW `window.api.sendCommand` (NOT the echoing one — pitfall #53's silent-send pattern, no `>time`/`>weather` echo) and their replies are CONSUMED — never shown, logged, or fed to triggers. Suppression is **per-REPLY-BLOCK, NOT a time window** (`silentSyncRef {time,weather,at}` armed by the click) so a `TIME`/`WEATHER` **you type yourself always shows** — the crucial fix (an 8s window ate typed commands). Each flag clears at its reply block's **LAST line** (weather = the desc/refusal; TIME = L4 roisaen/Anlas) IN-LOOP, **NOT post-batch** — a post-batch clear dropped TIME's month/season/daypart when its 4 lines split across flush boundaries (the "Day 44 · 457 A.V." with no month bug); block-end clearing is batch-agnostic, `at`+`SKY_SYNC_WINDOW_MS` backstop a reply that never completes. Suppression is computed at the TOP of the main `stream-text` case (before the log push) via `suppressSync`, which gates the log record AND the `newMain` push AND processLine/highlights; capture happens regardless. **LAYOUT: a HEADER strip + a FOOTER strip** (`.moons-header` / `.moons-footer`, both full-width bands, siblings in the `.moons-scene` flex column) — the **header** holds "what's happening in the sky now" (**sky phase · moons · weather**), the **footer** holds the **Elanthian date**; a ⟳ ends BOTH strips (header + footer — the footer one alone was easy to miss, Sekmeht; same silent TIME+WEATHER pull, same tooltip). Each strip's row is **centered** inline-flow (`.moons-foot-row` under `text-align:center`, so a long weather sentence wraps) with consistent `KEY value age` segments (`.moons-foot-key/v/age`) divided by a faint `|` bar. (Iterated HARD — don't re-litigate: footer-only run-on → too-tall 4-row grid → 2-row footer → a floating top pill (rejected "weird") → these two strips.) **CONTRAST is SKY-ADAPTIVE, not fixed** — a `sceneInk` IIFE lerps three things off sky brightness (`wDay`): the text `color` (dark by day → light by night), a `--moons-halo` (OPPOSITE lightness, an SVG `paint-order:stroke` behind the scene text so names/chips/the DAY word read on ANY sky — day was the weak case, dark ink on a bright sky), and a `--moons-band` (OPPOSITE the text, so the strips are a frosted-LIGHT band by day behind dark ink / a DARK band by night behind light ink — a fixed dark tint was low-contrast by day). Scene text fades via `fill-opacity` (NOT element opacity) so the halo stays solid. The ONE themeable cue is the arc guide (`--moons-guide` → `var(--accent)` in darkBase, blended toward the sky-ink) — everything else in the scene (sky gradients, moon lore colors, shadow landscape) is FIXED realistic/lore data, the Principle #4 exception like the map's baked tiles. **Weather-effect sky animations + season icons (v0.17.0):** `detectWeather(prose)` regexes the captured weather line into `WeatherFx` flags (snow/rain/clouds/fog/wind/storm/clear/heavy — `\b`-anchored keyword sets; **`gale` is WIND not storm**; storm ⇒ rain+heavy+clouds; any precip ⇒ clouds) driving CSS-keyframed SVG layers over the sky: drifting clouds behind the bodies, wind-slanted snow/rain, a horizon fog gradient, an occasional storm lightning flash. Particle arrays are DETERMINISTIC (index-hashed, no `Math.random`); motion gated by the ⚙ "Weather effects" layer AND `epilepsySafe`; the SVG helpers are module-hoisted (pitfall #4, no hooks). A **season icon** (❄️/🌱/🌻/🍂) prefixes the season on the date line; indoors the weather line shows a **⌂** glyph + "sky not visible — step outside". **`wx` is a PLAIN const (NOT a hook), placed after `if (!moons) return`** — a `useMemo` there changed the hook count when moon data arrived → Rules-of-Hooks mount crash (blank screen after login). Three ⚙ layers ("Weather", "Weather effects", "Calendar"). **No slash command (§34.6 — the window/⚙ is the surface).** **Living-sky visual pass (v0.17.0, full detail in DESIGN §34.9):** the day glow is **sun-centric** (a userSpace `lb-sunglow` radial follows the sun's arc position — warm-white at noon, gold near the horizon, NONE below it so night stays night; the base day/zenith gradients were flattened so the horizon no longer dominates); **F69 sun-lit moons** shade each up-moon from the sun's on-screen direction (per-moon dynamic `radialGradient` + `MoonStyle.tones`, sun-tint via `color-mix` applied through **`style` NOT the SVG attribute** — color-mix is unreliable as an SVG presentation attribute); **F67/F68** ambient (twinkling stars, shooting star, winter aurora, summer fireflies) gate on the ⚙ Effects layer + `!epilepsySafe` via `anim` AND need `calendar.season`; **F70** winter snow + the sun glow are STATIC (always on when data supports). The **`Moons feature backlog` F64–F73** (F67–F70 shipped, F64–F66/F71–F73 planned) is enumerated in DESIGN §34.9. **General gotcha worth keeping: `color-mix()` works as a CSS *property* (via `style`) but is unreliable as an SVG *attribute* (`stop-color=`/`fill=`) — route dynamic color-mix through `style`.** **Bug-checked (comprehensive, 2026-07-20) — no blocking bugs; residuals all minor:** TIME line 4 (skill-dependent fine clock) is consumed-not-parsed via `roisaen`/`Anlas` tokens (an exotic low-skill variant could leak a line); within `SKY_SYNC_WINDOW_MS` (8s) of a ⟳ whose reply is DROPPED/slow, a MANUALLY-typed TIME/WEATHER can be over-suppressed (the 8s backstop for a slow reply is required; needs a dropped cmd + you redundantly typing it). `silentSyncRef`/`awaitingWeatherRef` are RESET on a reconnect-in-place (pitfall #69) so a mid-sync/mid-glance flag can't carry over and mis-capture the new connection's first line; the orphaned-`>` risk (pitfall #88) is handled by the existing prompt-collapse (suppressed lines never enter `newMain`). **elanthianTime.ts is the platform-wide Elanthian date/time reference** (units, month names, seasons, 7-year cycle, dayparts, `dayOfMonth`/`monthIndex`) — reuse it for any future Earth↔Elanthia conversion; see [[elanthian-time-reference]]. **Living landscape below the horizon (v0.17.0, Sekmeht — "I like nature"):** the ground band hosts a persistent, data-free NATURE scene (`MoonsLandscape`, a module-hoisted no-hooks sub-component like `MoonsClouds`/`MoonsPrecip`) — a distant forest edge, mid + large foreground trees (mixed pine + round), a winding **stream** flowing into a reflective **lake** — gated by a new ⚙ **"Trees & water"** option (id `landscape`, default on; the id is stable so an earlier "Village & landscape" save carries over — the village was the first draft, REPLACED by nature at Sekmeht's ask; don't reintroduce buildings without a fresh ask). **PERSPECTIVE is the core model:** every element's size scales with its baseline y (`persp(baseY)` 0.5 near the horizon → ~1.45 near the bottom), distant pieces fade toward a pale haze (atmospheric perspective), and everything LIGHTENS by day / DARKENS at night via a `landNight` factor derived from `sunElev` (works with NO TIME check, like `groundShade`). Colours lerp through **`mixHex(a,b,t)`** (a module-level hex-lerp — the Principle #4 realistic-colour exception; use it, NOT color-mix-as-SVG-attribute, pitfall). **Daytime contrast (the trees/lake read as objects, not ground):** saturated foliage vs a desaturated neutral ground, a dark **edge outline** on every canopy/tier, **contact shadows** under trees, and a **dark rim** on the lake. **Sun-DIRECTIONAL shadows (`shadow()`):** cast along the SAME radial fan as the crepuscular rays — from the sun's horizon point `(sun.x, horizonY)` THROUGH the object base, so a shadow points straight down (6 o'clock) under the sun, angles to ~4–5 o'clock toward the far side, lengthens as the sun sinks, and (because `baseY − horizonY` grows with depth) near-horizon objects cast flatter shadows than foreground ones; a rotated ellipse renders the angle; dusk/night falls back to a soft ambient blob. **Lake REFLECTIONS:** shimmering vertical columns for the sun (by day) + the LIT moons (Yavash/Xibar) where they pass above the pool (clipped to the lake's height at that x, no clipPath); **Katamba is excluded** (emits no light) and reflections are **skipped when the lake is iced** (winter). **SEASONAL dressing** rides the EXISTING ⚙ "Seasonal touches" toggle (`landSeason` = 'none' unless that's on AND the season is known): a per-season foliage palette (`SEASON_CFG`) + winter snow-caps on trees & an **iced-over lake/stream**, spring **blossoms**, summer lushness (+ the existing fireflies), and autumn recolour + **falling leaves** (a `LEAVES` particle array + `.moons-leaf` keyframe, gated on `anim`/epilepsy-safe like snow/rain). **Moon-body colour/glow corrections (v0.17.0):** **KATAMBA HAS NO GLOW as of v0.19.6 (Sekmeht) — don't reinstate it without a fresh ask; `MoonStyle.glow` is OPTIONAL and its ABSENCE is the switch, read by BOTH the gradient defs and the disc layer so they can't disagree (both used to name-check `'katamba'`).** The removed treatment, kept for its still-live lesson: Katamba = soot-black disc + **grey** rim + an **ominous MIASMATIC dark-violet glow** (`#2a123f`) that darkens a bright day sky AND reads as a shadowy purple on a dark one — it HOLDS at full through day+dusk and fades only into true night (`clamp01((sunElev+0.35)/0.3)`); **a pure-black glow was tried and is INVISIBLE on a dark sky (black only darkens) — a "shadow" glow that must read at night has to be a coloured haze, not black** (the lesson). Yavash = ruby/crimson (glow `#e01430`); Xibar = vivid ice-blue disc + a darker **blue** rim (`#88bce6`) + a **silvery-white** glow (`#dbe9f5` — no atmosphere, so the halo is ice-field shine). `MoonStyle` gained optional **`glowStrength`** (>1 = more intense) + **`glowR`** (halo radius × disc r); overlapping bodies paint in **`MOON_DEPTH` z-order** (furthest→closest: Sun, Yavash, Katamba, Xibar — the disc passes sort by it). Pill dot colours mirror the corrected hues. All new gradients (`water`, the moon glows) are per-instance via `gref`/`uid` (pitfall #95). **Sky-motion + night-sky polish (v0.17.0, Sekmeht):** the scene's `setTick` interval was dropped **30s → 2s** so the bodies AND everything derived from the sun's position — shadows, crepuscular rays, lake reflections — advance together in sub-pixel steps (smooth + in-sync), instead of a jerky 30s redraw; a CSS-transition ("glide") on the disc `cx`/`cy` was tried first and REVERTED because it smoothed the discs ONLY, desyncing them from the recomputed shadows/rays (the correct fix is one smoothly-advancing clock, not per-element transitions). **Star field**: grew from 9 to **70 deterministic stars**, each with a brightness `b` and a `reveal` threshold — bright stars show at dusk, fainter ones reveal the closer it gets to **true midnight** (a "light-pollution-clearing" effect) via a new **`nightDepth = clamp01(-sunElev)`** that PEAKS at midnight (unlike `wNight`, which saturates just past dusk); per-star `opacity = clamp01((nightDepth − reveal)/0.28) * b`, culled when invisible; the twinkle animation moved to **`fill-opacity`** so it multiplies with (rather than overrides) the per-star reveal opacity. **Shooting stars**: from ONE fixed rare streak to **6 scattered across the sky** (varied x/y, one down-left, staggered timing via inline CSS custom props `--sx/--sy/--ex/--ey/--dur`), still gated on `anim && isNight && clearSky` (clouds correctly hide them). **FX draw order** was reorganized to: sky + stars (background) → bodies → ground/landscape → **day/night FX (shooting stars, fireflies, leaves) in the FOREGROUND** → **weather FX (clouds, then precip/fog) FRONTMOST** (Sekmeht). **TDZ lesson (a real startup crash this session):** a module-level `const` array built at import time (`STARS = Array.from(...)`) called `clamp01`, a `const` arrow declared LATER in the file → temporal-dead-zone `ReferenceError` at load, "nothing renders." Build + tsc pass (valid code, runtime-only) — same blind spot as a deleted side-effecting statement. **Rule: a module-level array/object built at load must only call HOISTED `function`s (like `mixHex`) or `const`s declared ABOVE it — never a later `const` arrow; inline the helper if in doubt.**

**Experience #3 — Spell Monitor [Beta] (v0.19.5, DESIGN §34.9 item 4; id `spellmonitor`, NOT `spells` — a distinct id so it can never read as the Active Spells PANEL in a tab strip, the Moons stream-vs-experience lesson):** DR's active-spell readout as a grid of live countdowns, soonest-expiring first. **It cost ZERO panel-system files** (§34.8 item 5) — one registry entry buys the `+` menu, the shelf and the ⚙, which is the clearest demonstration yet of why the Experiences model exists. **Read this before touching the §34.2 rejection above:** a "right-click Active Spells → Classic/Advanced" panel view toggle was proposed for this and correctly checked against that rejection — and **both prongs INVERT for `percWindow`**, which is why an Experience was built instead. (a) The rejection turns on the combat panel being *optional equipment* (Simu splits combat text, so players skip the window); but `spells` has **no main duplicate and no `STREAM_FALLBACK` entry**, so its panel is the only place that content ever appears. (b) Its deeper point is that the HUD reads parsed *state*, not stream text; for spells the opposite holds — the active list exists ONLY as text on `percWindow` (the `spell` event is the spell being *prepared*, a different thing). **So the §34.2 rejection is not a blanket ban on ever reading a stream into an instrument — it is an argument about those two properties. Check them against the case in front of you rather than refusing on the label.** The Experience route still won on its own merits (byte-identical panel system, dual hosting for free). **Data path — no new plumbing:** `percWindow` aliases to `spells`; that stream is clear-and-rewrite with no fallback and its clear is applied in the batch commit, so **`streamLines.spells` is ALREADY an exact mirror of the current block** — no accumulator, no assess-style batch-boundary guard. `parseSpellLine` / `deriveSpellState` ([experiences.ts](src/renderer/experiences.ts)) are pure and harness-locked (`tmp-rules-harness` §L, 149 cases over the verbatim capture — run it with `iso.mjs`, not `run.mjs`); ONE effect in GameWindow owns the refs (pitfall #70). **Five things to preserve if you touch it:** (1) **parsing is TOLERANT** — the singular `roisan` as well as plural `roisaen` (a plural-only pattern silently drops every effect in its final minute), a non-numeric parenthetical kept as an untimed effect with its note (`Trabe Chalice (intact, fading)`), a bare name kept — never a dropped line, because the display must not hide something the game says is on you; (2) **expiries anchor on each LINE's own `timestamp` (its receipt time), not one `Date.now()` per block** — NOT the B192 skew case, since DR gives a duration rather than a server absolute time, so a local anchor is correct. **It is NOT replay-correct, and a first draft of these docs wrongly said it was:** `mkLine` stamps `Date.now()` and IGNORES the `timestamp` its `StreamTextEvent` carries, so a pitfall-#60 replay re-dates the block and inflates every remaining time — bounded and self-correcting, because the delta gate re-anchors once predicted exceeds reported by a roisan (DR's next repaint). Fixing it properly means `mkLine` preferring `evt.timestamp`, which changes every stream's timestamp display — a shared-path change deliberately not taken here; (3) **the DELTA GATE** re-commits state only on a real change (effect set / note / max / a timer diverging from prediction by >1 roisan) — a repaint confirming 29→28 tells us nothing our own clock doesn't know, and committing it would mint a fresh prop identity on the shared props bag and re-render every mounted Experience (pitfall #82c) plus re-anchor every countdown as visible jitter; (4) **the bar's denominator is LEARNED** (DR never states a full duration) — the highest roisaen seen per effect, in a **GameWindow ref** because the component unmounts on every tab switch and would otherwise forget instantly; (5) **an empty block clears the grid on a 400ms DEFERRAL** — meaningful (everything dropped, and each effect's own timer could otherwise leave it up for another 29 minutes) but deferred, because a clear and its lines can straddle a flush boundary and committing the gap blinks the grid empty. **Two HONESTY rules in the display, both deliberate:** whole minutes only, never a seconds countdown (DR reports whole roisaen, so `29:00` claims a precision the game never gave us; the last minute reads `<1m`, while the BAR may move smoothly since a proportion isn't a precision claim), and an unparsed effect is still SHOWN with whatever the game put in its parentheses. **The TRAFFIC LIGHT (green full → yellow midway → red near the end) is `spellBand()`, and it takes the MORE URGENT of a PROPORTIONAL and an ABSOLUTE reading — do NOT simplify it to proportion alone.** The proportion denominator is LEARNED, so on the first block after connecting every effect has `max === current` (proportion 1.0) and a pure-proportional band paints the WHOLE GRID GREEN including a buff with two minutes left — the normal startup state, not an edge case. The absolute floor (≤1 roisan crit / ≤5 mid) cannot be fooled by a guessed denominator; the proportion (≤0.2 / ≤0.5) supplies the gradient so 10% of a 4h buff reads red with 24 minutes left. **Colours are three dedicated `--spell-band-{ok,mid,crit}` vars — NOT the vitals health ramp, and do NOT "simplify" them back to it.** That reuse was the first implementation and was WRONG in the sharpest form of the pitfall #34/#75 family: **the cascade must follow MEANING, not hue.** `--vital-health-*` means "this theme's health bar" — `classic` takes its vitals verbatim from Genie where health is **RED AT FULL** (`ok-end: #dd0000`), so the light ran red→orange-red→dark-red and a full spell screamed "expiring"; `terminal` is monochrome so its mid stop is green and the amber band vanished. The replacements live ONCE in `darkBase` in the pitfall-#63 `--syntax-*` shape (fixed hue mixed 75% toward `var(--text-primary)`, resolved per theme at use time) so meaning is fixed while contrast self-corrects, with no per-theme work. Hues are MID-TONE by necessity (UX #9): a true yellow is ~1.5:1 on a white theme, so the middle band is an AMBER — no yellow survives both ends. **Colour-blind is EXPLICIT in `COLORBLIND_VARS`, and the vitals answer does not transfer:** protanopia turns health-crit AMBER, which is exactly our MID band, so it would collide — we move green→teal and LIGHTEN red instead. Unverified with a real colour-blind tester (Principle #10), safe only because colour is secondary: bar LENGTH + printed time carry the same information (§34.7). **Colour lives in the BAR + BORDER, not the numerals** (3:1 vs 4.5:1 — the yellow stop cannot clear text contrast on white without ceasing to look yellow; `crit` is the exception at ~6:1). **The percWindow SHAPE CATALOGUE is mirrored from LICH'S OWN PARSER** (`lib/common/xmlparser.rb`, the `@dr_active_spell_tracking` branch — its comments list the real lines verbatim), and a first version written from ONE captured block got four of them wrong: **`anlaen` is a real unit** (1 anlas = 30 roisaen — `Stellar Collector (0%, 4 anlaen)` lost its countdown), **`Fading` is the OPPOSITE of untimed** (Lich reads duration 0 = lapsing NOW; rendering it as a quiet note showed the most urgent thing on screen as background), **`Indefinite`/`OM` are permanent**, and a stated **percentage** is a proportion not a time. Hence `SpellKind` = timed/fading/permanent/percent/unknown — **do NOT collapse these back into one "untimed" bucket**, because fading is the most urgent state and permanent the calmest; a duration outranks a percentage in a compound reading; a stated percentage beats the learned ceiling (true proportion vs "the most we've seen"); `spellSortRank` orders lapsing→counting-down→no-countdown→permanent; the `untimed` layer hides the quiet kinds but NEVER fading; the delta gate treats a KIND change as always meaningful; and a missing/NaN percentage resolves to NO band rather than the calmest one. **SKILL BADGES + ABBREVIATIONS** come from Lich's `base-spells.yaml`, snapshotted by [tools/gen-spell-data.mjs](tools/gen-spell-data.mjs) into a COMMITTED [spellData.ts](src/renderer/spellData.ts) (430 entries, ~29KB) — committed, not read from a Lich install, because this Experience works without Lich (Principle #2); re-run the generator when Lich adds spells, and note an unknown name gets NO badge, never a wrong one. Data facts: percWindow names match the YAML keys VERBATIM (8/8 on a real capture), the three sections have ZERO name collisions (one flat map is safe), and all twelve values have distinct first letters (A U T D W C X / F B M R S). **Metamagic is X, not M** — M is Meditation, and no character has both, but a letter unambiguous only by luck is the pitfall #55 trap. **Two source-data gotchas: 33 entries are `metamagic: true` with NO `skill`, and `See the Wind` has `Skill:` with a CAPITAL S — a typo in Lich's own file** that a case-exact read drops silently; the generator tolerates both and REPORTS skips rather than discarding (that report found the typo). **Known gap: Thief Khri are absent from base-spells.yaml but DO appear in percWindow**, so Thieves get no badges. Badge colours are twelve `--spell-badge-*` vars, **all exposed in the Theme Editor's HUD tab** (genuinely user-editable, not just themeable); the chip is a quiet low-alpha tint because it IDENTIFIES while the traffic light ALARMS. **The ⚙ popover is BOUNDED as of v0.19.5 — `.exp-inst-options` gets `max-height: calc(100% - 40px)` + `overflow-y: auto`, and that is a PRE-EXISTING fix that matters most to MOONS (17 options), not to the Spell Monitor.** `.fl-body` and `.panel-frame-body` are both `overflow: hidden`, so an unbounded popover in a short window was CLIPPED — top options unreachable, no scrollbar, nothing on screen saying they existed. A popover whose height is a COUNT (options, accounts, rules) inside a freely-resizable window is pitfall #109 regardless of whether the count comes from user data or the registry; bound every one. **THEME-EDITOR ROWS FOR A CASCADING VAR (B314) — `<input type="color">` FALLS BACK TO BLACK on any value that isn't `#rrggbb`, and `createCustomThemeFrom` copies the WHOLE of darkBase into a new custom theme, so every var stored as `var(--x)` or `color-mix(…)` reached the editor verbatim and rendered a BLACK swatch for a colour that actually paints teal/rose/slate.** Pre-existing for the `var()`-valued rows; the Spell Monitor's 15 `color-mix` rows made it conspicuous. `resolveDisplayHex` (ThemeEditor.tsx) resolves non-hex values by asking the BROWSER what they evaluate to (hidden probe + `getComputedStyle().color`) — the only correct answer, since it depends on the live theme; a literal hex takes exactly the old path and the COMMIT path is untouched (an edit still writes plain hex, so a theme never depends on the expression form). **When you expose a new var in the Theme Editor, check what darkBase actually stores for it** — an expression is normal and correct there, but it is not a value a colour input can hold. **PROFILED CLEAN — don't re-investigate; numbers in DESIGN §45.10 (the v0.19.6 counter) and below (the v0.19.5 derivation).** The 1 Hz tick was re-measured on BOTH axes after v0.19.6 made the clock run continuously: render+commit is **0.087ms at 8 effects / 0.143ms at 25** (cheaper than a Moons render), and a `MutationObserver` over real ticks shows the ONLY DOM mutation is **one `.sm-bar-fill[style]` transform write per cell, plus one text node for the feed readout — and ZERO childList**. `transform` is compositor-only, so a tick invalidates no layout (the v0.18.0 scaleX-not-width decision paying off; with `width` those same 25 writes/sec would each have reflowed). The always-on clock's genuinely NEW cost is the empty-grid case: **0.007ms and one text node per second**. A tick cannot reach the other Experiences — `setNow` is local state, so React re-renders this subtree only. **Not measured, so not claimed:** Blink style recalc, paint, and the compositing of N permanently-transitioning bars — unchanged by v0.19.6, but that is the axis to measure if a big buff stack is ever reported as heavy (§45.8's lesson).  The 1 Hz clock's whole derivation chain is **0.24–0.94 µs/tick** (8 → 25 effects, grouped) and the always-on parse is **4.4–20.5 µs per repaint** — 0.006% of a core even at one repaint per prompt. **Zero per-line cost** (the derive is an effect on `streamLines.spells`, never a branch in the stream-text loop) and a background TAB is unmounted so its clock stops; a background CHARACTER's floating window does tick, left alone deliberately (gating on `isActive` buys nothing measurable and costs a stale label on return — the §45.7 trade). Note the delta gate does NOT save parse work (a gated repaint measures slightly SLOWER — it parses then compares); its value is purely the avoided re-render. **THE ONE REAL FIND: the bar animates `transform: scaleX()`, NEVER `width`.** It shipped as `transition: width`, and since the bar drains every tick it is in a CONTINUOUS transition whenever the window is open — so width invalidated layout every animation frame, per bar, per window, per character. scaleX is compositor-only. **Do not "restore" a width transition, and do not give the fill a border-radius** (scaling squashes it; the track's `overflow: hidden` rounds the left end and a square leading edge is right for a progress bar). Same family as pitfall #126. **ENDED EFFECTS ARE KEPT, GREYED (v0.19.6) — the ⚙ `expired` layer, ON by default — AND THE SIGNAL IS ABSENCE FROM THE NEXT percWindow BLOCK, NEVER OUR OWN CLOCK (Sekmeht's correction; it inverted the first build).** Our anchor FLOORS DR's whole roisaen, so the clock reaches zero up to a minute EARLY — treating that as "ended" lied AND flickered (a repaint re-anchors a spell still up), and worse, the first build DELETED the cell at the exact moment the game finally told us the truth. `deriveSpellState` now DIFFS the new block against the previous: **anything the old block listed that the new one doesn't has ended, because the game just said so.** That lives in `SpellState.ended` (a `kind:'ended'` copy with `endedAt`), carries forward, is cleared by a RECAST (the name returns — it must not sit in both lists), and ages out at `SPELL_ENDED_TTL_MS` = **one roisan** (Sekmeht; derived from `ROISAN_SECONDS`, not a bare 60_000, so it stays tied to the unit it means). **A departure ALWAYS commits — the delta gate must never swallow it.** **The converse matters as much: a timed effect past its anchor is NO LONGER dropped** — the game listing it is evidence it's live, only the game dropping it is evidence otherwise, and neither is ours to invent; it reads **"expired"** (see the two-stage note at the end of this bullet), not `<1m` and not `ended`. Three display couplings: `spellBand` returns `none` for `ended` (grey and red must never both fire), `spellSortRank` returns 5 (below even permanents), and the CSS is distinct from `--untimed` (quiet-but-LIVE) — spent is **desaturated as well as dimmed** so the badge greys too, plus a dashed border. **ONE LINE PER CELL, ALWAYS (v0.19.7, B411). Do NOT bring back a note row.**
    - A cell used to grow a second row for its note. A grid row takes the height of its TALLEST cell, so one note (a spent cell's leftover "Fading") stretched every cell beside it.
    - `spellSlotText` now puts the note IN the time slot when it says more than the label. That happens for fading, percent and unknown readings, where the note is the fuller statement of the same reading.
    - A timed effect keeps its ticking minutes, with its charge level in `spellSlotAside`.
    - A spent cell keeps "ended" plus its clear-out countdown, and its stale reading moves to the tooltip.
    - The slot is capped and ellipsized. The bar slot is always reserved (`.sm-bar--none`, `visibility: hidden`), so a barless permanent is as tall as its neighbours.

    **The general rule: an optional element inside a grid cell makes the whole row's height depend on data.** Give every cell the same structure and let the content fill it. What survives of the older rule below is that `spellNoteText` still decides whether the note ADDS anything. **THE NOTE ADDS WHAT THE LABEL DOESN'T SAY (`spellNoteText`)** — rendering the parenthetical unconditionally printed "fading" TWICE for a `fading` reading and paid an extra ROW for it, which is what made those cells look oversized (Sekmeht's `Tenebrous Sense (Fading)` shot). The test is a **case-insensitive comparison against the label, NOT a switch on `kind`** — that is load-bearing, because a bare `(Fading)`/`(94%)` must collapse while a COMPOUND reading survives (`0%, fading` under a `0%` label adds a word; `0%, 4 anlaen` under `120m` adds a charge level). **`spellRemainingLabel` + `spellNoteText` live in experiences.ts, NOT the component, so the PAIR is harnessable** — neither reads correctly alone and it was their pairing that failed. **GROUP BY SKILL (`groupSpells`, v0.19.5)** partitions the grid under a labelled hairline heading per skill/type — a Barbarian gets exactly four blocks (Form/Berserk/Roar/Meditation), casters 5–7. **It COMPOSES with `sortByTime` instead of competing** (it preserves the caller's within-group order, so sort-then-group leaves each group internally sorted) — which is the ONLY reason two booleans still suffice; a third ORDERING mode (alphabetical) genuinely conflicts and is the natural trigger for the typed-control registry extension. Group order is **FIXED** (`SPELL_GROUP_ORDER`), never derived from what's up — a self-reordering list reshuffles the grid mid-read, the same churn that made `sortByTime` opt-in; an unlisted label sorts LAST alphabetically so a future Lich category appears predictably at the end. The heading is a **labelled hairline divider (UX #5), never a padded header** — a Warrior Mage would otherwise spend 7 rows on chrome in a strip-shaped window. Unknown names bucket into `Other` (last), so grouping is **inert for a Thief** whose Khri aren't in Lich's data at all. `groupSpells` takes the lookup as a PARAMETER rather than importing `spellData`, keeping it pure/harnessable and `experiences.ts` free of that dependency. Eleven ⚙ layers (bars / urgency / expired / untimed / pulse / badges / header / updated ON; **`abbrev`, `sortByTime` and `groupBySkill` are `defaultHidden` / opt-in** — Sekmeht 2026-09-06, so DR's OWN order is the default: the game's ordering is stable as timers tick where ours re-arranges under the reader, and the urgency colour still marks a lapsing effect without moving it — note `spellSortRank`'s fading-first priority therefore applies only when that layer is on). **⚙ PREFS ALREADY PERSIST per character with no extra plumbing** — `ExperienceInstance.hidden` rides `scopedKey('experiences')` → `state:` → YAML, and `experiences` is a `TRANSFER_CATEGORIES` id so they move with a Transfer; `setExperienceOption` find-or-creates an `open:false` prefs record for a tab-only user and seeds `defaultHiddenMap(def)` so a default-off layer persists explicitly. **`loadExperiences` filters with a type GUARD, never a rebuild — do NOT "tidy" it into one**, or every field it forgets to copy (`hidden`, `fontSize`) is destroyed on load (pitfall #121). Harness-locked: round trip + default resolution + a `check-options.mjs` guard asserting the registry ids and the component's `shown()` ids are the same set, because `shown()` falls back to TRUE for an unknown id and a typo would silently force a default-off layer permanently ON (pitfall #127). epilepsy-safe drops the pulse but KEEPS the colour (UX #9b), and the bar LENGTH + printed time mean meaning is never colour-alone (§34.7). Bounded + scrollable (pitfall #109 — effect count is user data), `tabular-nums` (#103), em chain anchored to `--panel-font-size`/`--game-font-size` (#58a), `.sm-` prefix verified unique (#55c). Works direct-SGE — `percWindow` is a game stream, no Lich needed (Principle #2). **No slash command by design** (§34.6 — the `+` menu / shelf / ⚙ is the surface), recorded rather than skipped (Principle #11). **Open:** DR's repaint CADENCE is still unmeasured, but the v0.19.6 **feed readout now SHOWS it** (`updated 3s ago · every ~6s`) — so a tester screenshot answers the question that used to need a Debug raw-XML capture, and the delta gate is correct either way. There is still **no ⟳ button** because no *verified* command forces a `percWindow` repaint and one was not guessed (Principle #10) — the strip at least tells you when the next one is due. The parsed state rides `ExperienceProps`, so a Tableau strip, an Overview card field or `$activespells` trigger vars are cheap follow-ons. **ENDING IS TWO STAGES AND THE CODE MUST KEEP THEM APART (v0.19.6, Sekmeht — this area was mis-implemented twice, in opposite directions, so read this before changing it).** *Stage one, "expired"* = OUR countdown reached zero (`spellExpired(e, now)`, timed effects only) — the moment you act, and NOT authoritative. *Stage two, "ended"* = the game STOPPED LISTING the effect in `percWindow` (`kind: 'ended'`, produced by diffing the new block against the previous one) — the only thing that settles it. **They are different facts and both are shown**: DR FLOORS its roisaen, so our clock reaches zero with up to 59 seconds still to run, and a repaint can legitimately hand an "expired" cell more time and send it back to counting down. The first build greyed on our own clock (early and flickering) and DELETED the cell at the exact moment absence proved it had ended — throwing away the only reliable evidence it ever gets. The second showed only stage two, so the expiry moment had no appearance at all and read as an ordinary `<1m`. **Do not collapse them, in either direction.** Three details that are easy to undo: `.sm-cell--expired` is applied INDEPENDENTLY of the `urgency` layer (an expiry is a STATE, not a colour band — turning the traffic light off must not hide it), its ring is a `box-shadow` rather than a border because the pulse ANIMATES `border-color` and an animation beats a normal declaration (pitfall #111), and the crit-coloured variant is the two-class `.sm-cell--crit.sm-cell--expired` so SPECIFICITY decides, never bundle order. **The 1 Hz clock bids on THE NEXT MOMENT THE DISPLAY CHANGES, not on the next expiry** — the union of live anchors AND `endedAt + SPELL_ENDED_TTL_MS` (the latter only while the layer is on). Bidding only on expiries let it retire with a greyed cell on screen; `now` froze, the one-roisan grace is measured against `now`, and the cell stranded until DR happened to repaint — which in the common case (your last buff drops) nothing was left to cause (B315). **And `spellPrevRef` MUST reset on a sessionId change** (pitfall #69): "ended" is a diff, so carrying a dead session's list across a reconnect-in-place stamps every effect that lapsed while you were away as having ended *this instant* (B316). `spellMaxRef` deliberately does NOT reset — a learned ceiling is a fact about the spell, not the connection. **A SPENT CELL COUNTS DOWN TO ITS OWN REMOVAL, IN SECONDS (v0.19.6, Sekmeht)** — `ended 45s`, plus a BAR that drains the roisan instead of sitting empty. Seconds are correct here and are NOT a violation of the whole-minutes honesty rule: that rule exists because DR reports whole roisaen, whereas this grace is `endedAt` (our stamp) against `SPELL_ENDED_TTL_MS` (our constant), known to the millisecond. Don't "align" it. The bar is what disambiguates the number (`45s` beside "ended" reads as easily as *elapsed since*; a shrinking bar is unmistakably *remaining*), and the countdown is a SEPARATE element so `spellRemainingLabel` keeps returning one fact and stays harnessable. **FEED STATUS — the ⚙ `updated` readout (`updated 3s ago · every ~6s`), ON by default, living INSIDE THE HEADER BAR and gated on that layer too** (Sekmeht: it is metadata about the window, so it belongs with the window's identity — and it then costs no row, which matters for a surface meant to shrink to a strip). It reports the block's ARRIVAL, never `reportedAt`: the delta gate means `reportedAt` can sit still for many minutes while DR repaints constantly, so a strip built on it would say "30m ago" about a live feed. `SpellPulse` records every non-empty block from the TOP of the derive effect, before the gate. **It rides a REF, and that is load-bearing — see pitfall #140** (a per-pulse prop VALUE would re-render Moons and the Tableau, undoing the gate). Consequence: the component must own the clock, so **the 1 Hz interval runs continuously while the strip is shown** — bounded by mount, and the reason the strip is switchable. **The cadence is MEASURED, never claimed** (median of the last 8 gaps, silent under 3 — one gap is not a rate; median not mean, or one quiet stretch reports "every ~155s" about a 6s feed). It RESETS on a reconnect-in-place, because cadence belongs to the connection. **`formatAgo` is SHARED with the Lich Scripts footer** ([utils/formatAgo.ts](src/renderer/utils/formatAgo.ts)) — the phrasing is user-visible and two copies drift (pitfall #127); it takes `now` as a parameter so a caller with its own clock formats against the instant it rendered. **LIVENESS IS RECORDED FOR EVERY ARRIVAL — ahead of the delta gate AND of the empty-block branch, and both halves are load-bearing.** (a) Recording inside the non-empty branch FROZE the strip in the exact case it exists for: the empty branch early-returns once `effects` is already empty, so when every spell dropped and DR kept repainting an empty list the readout sat still beside a live feed. `undefined` (stream never touched) records nothing; `[]` (DR sent a clear) counts, because a clear IS an arrival. (b) DR sends a clear THEN its lines, and main's flush coalescing is LEADING-EDGE on 16ms (pitfall #82d) — so when idle the clear flushes alone and the lines follow next batch: one repaint, two arrivals. `recordSpellPulse` advances the timestamp but DISCARDS any gap under `SPELL_PULSE_COALESCE_MS` (250ms), or the median cadence would collapse toward zero precisely when someone is sitting still reading it. **Also `.sm-name` carries `flex: 1 1 auto`** — `.sm-cell-top` is `space-between`, so without it a third child (the clear-out countdown) splits the free space into two gaps and floats the time chip into the middle of the cell.

---

## Sibling clients — feature reference (Genie · Frostbite · Profanity)

How the three popular DR/GS front-ends implement the features Lichborne also has — read from their source (`C:\temp\Genie4-Dev`, `C:\temp\Frostbite-Dev`, `C:\temp\Profanity\ProfanityFE`). Captured so we have grounded context when a tester asks "how does X do this?" or we're deciding Lichborne's approach. **The big axis is HOW MUCH the front-end does itself vs delegates to Lich:** Genie = everything native (Lich optional); Frostbite = lots native + its own Ruby scripting; **Profanity = thin presentation layer, ALL automation/scripting/mapping is Lich** (the model closest to Lichborne's stated philosophy). Lichborne sits between Profanity and Frostbite: a GUI config layer that implements *some* front-end automation (highlights/triggers/macros/aliases) for users who don't write Lich scripts, while pushing the heavy automation to Lich.

### The three at a glance
- **Genie** — C#/.NET, WinForms GUI. **Full-featured do-everything client**, Lich optional. One config panel per feature ([Forms/ConfigPanels/](file:///c:/temp/Genie4-Dev/Genie4/Forms/ConfigPanels/) — `UCHighlightStrings/UCTriggers/UCAliases/UCSubs/UCIgnore/UCMacros/UCVariables/UCClasses/UCNames/UCPreset/UCWindows`). Its OWN scripting language (`.cmd` scripts + ~100 `#commands`, variables, `#if/#math/#goto`, classes) — [Core/Command.cs](file:///c:/temp/Genie4-Dev/Genie4/Core/Command.cs) (the `#command` catalog is pitfall #48). Own StormFront XML parser ([Core/Game.cs](file:///c:/temp/Genie4-Dev/Genie4/Core/Game.cs)). Genie also owns the **map XML format Lichborne's Genie view reads**.
- **Frostbite** — C++/Qt GUI. **Full GUI client + its own RUBY scripting** (scripts run as an external `QProcess`, talking back over TCP via `scriptapiserver`/`scriptstreamserver`; [gui/script.cpp](file:///c:/temp/Frostbite-Dev/frostbite/gui/script.cpp), [scripts/lib/](file:///c:/temp/Frostbite-Dev/frostbite/scripts/lib/) `ruby_goto.rb` etc. — a Lich-like Ruby model embedded in the client). Has a Lich integration dir ([gui/lich/](file:///c:/temp/Frostbite-Dev/frostbite/gui/lich/)) AND its own SGE login (eauth/connectwizard), like Lichborne. Genie-format maps ([gui/maps/](file:///c:/temp/Frostbite-Dev/frostbite/gui/maps/)).
- **Profanity (ProfanityFE)** — **Ruby, curses TUI, NO GUI, Lich-forward.** Connects to a *port* (`--port=8000`, i.e. Lich's port) — it has **no own login; Lich does connection/login**. It implements ONLY presentation/config: highlights, gags, key macros, stream→window routing, colors, layouts. It has **NO native triggers, aliases, general substitutes, variables, scripting, or mapping** — every bit of that is **Lich's job** (Lich scripts, `;alias`, vars, `;go2`/mapper). Clean Ruby lib ([lib/](file:///c:/temp/Profanity/ProfanityFE/lib/) — `highlight_processor.rb`, `gag_patterns.rb`, `xml_tokenizer.rb`, `game_text_processor.rb`, `window_manager.rb`) + a hand-edited XML settings file, hot-reloaded with `.reload`. **This is the purest expression of Lichborne's "display & configuration layer over Lich" stance — when in doubt about whether to build automation in the client, Profanity is the "delegate to Lich" reference point.**

### Per-feature
- **Highlights.** *Genie:* three lists (string / line-begins-with / regex), stored as `SortedList`s keyed by the pattern text, applied to a RichTextBox via `SelectionColor`/`SelectionBackColor` — **per-property, last-in-(alphabetical)-order wins, NOT user-orderable.** *Frostbite:* HTML — wraps matches in nested `<span>`s walking the list top→bottom, so the **innermost/bottom-of-list color wins per-property**; a highlight can also carry **alert (sound) + timer actions** (so a Frostbite highlight doubles as a trigger). *Profanity:* `<highlight fg bg ul>regex` applied after XML strip; **specificity — smallest match wins — per-property** ([highlight_processor.rb](file:///c:/temp/Profanity/ProfanityFE/lib/highlight_processor.rb)). *Lichborne (v0.11.3):* adopted **Profanity's model** (specificity + per-property compositing) — see the Automations note above for the full write-up + why.
- **Triggers.** *Genie:* dedicated `UCTriggers` — regex line → run command(s)/script, gated by classes. *Frostbite:* **no separate trigger system** — uses highlights-with-actions (sound/timer) for simple cases and Ruby scripts for real logic. *Profanity:* **none — Lich does triggers.** *Lichborne:* native trigger engine (WHEN→THEN, gates, cooldown, variable-watch) — richer than Genie's, the one place we're more "client-automation" than the Profanity ideal (deliberate: GUI users want triggers without scripting).
- **Macros / key bindings.** All three map a key → a "typed" string with a **cursor marker + escapes**. *Genie & Wrayth:* `@` = cursor, `\x` = clear, `\r`-less = wait. *Profanity:* `<key macro='...'>` with `\r` (send), `\x` (clear), `\@` (literal @), `@` (cursor), `\?` (backfill) + a rich **action** set (cursor/word-edit/kill-ring/window/tab/history) bound separately from macros. Lichborne already implements the `@`/`\@` cursor convention (pitfall #51); the kill-ring/word-edit actions are a Profanity idea we don't have.
- **Aliases.** *Genie:* `UCAliases` (`#alias`), `$1/$rest` args. *Frostbite:* via scripts/macros. *Profanity:* **none — Lich `;alias`.** *Lichborne:* native aliases with `$1/$rest`.
- **Gags (suppress lines).** *Genie:* `UCIgnore` (`#ignore`/`#gag`). *Frostbite:* `gui/text/alter/ignoresettings` (an "ignore" tab in its alter subsystem). *Profanity:* `<gag>` (all text) + `<combat_gag>` (combat stream only), regex, dropped before routing ([gag_patterns.rb](file:///c:/temp/Profanity/ProfanityFE/lib/gag_patterns.rb)). *Lichborne:* **SHIPPED v0.12.0 as "Mutes"** (DESIGN §31; the Automations "Mutes & Substitutes" note above) — global by default with an optional per-rule stream restrict (a superset of Profanity's general/combat split), `line` or `match` scope, imported from all three legacy clients.
- **Substitutes (rewrite displayed text).** *Genie:* `UCSubs` (`#sub`/`#substitute`). *Frostbite:* `gui/text/alter/substitutionsettings` + `alter` (recolor/replace). *Profanity:* only a narrow **`perc-transform`** (rewrites spell-window text) — general subs are Lich's job. *Lichborne:* **SHIPPED v0.12.0 as "Substitutes"** (DESIGN §31) — native display-layer rewrite with `$N` capture refs, global by default, imported from Genie/Frostbite (Frostbite `target=Experience` mind-state rules flagged unsupported).
- **Variables.** *Genie:* `UCVariables` — user `$vars` + built-in game vars, used in scripts/triggers/macros. *Frostbite:* script/shared-data vars. *Profanity:* **none — Lich `Vars`.** *Lichborne:* built-in game-state vars (v0.11.3 catalog) + trigger-set vars; we deliberately do NOT persist a Lich-style var store (that's Lich).
- **Groups / classes / modes.** *Genie:* `UCClasses` — named on/off "classes" that gate highlights/triggers/aliases/macros. This is **exactly Lichborne's groups+modes** (DESIGN.md §8). Frostbite/Profanity have no direct equivalent.
- **Mapping.** *Genie:* its own map editor + the XML map format (Lichborne's Genie view consumes it). *Frostbite:* Genie-format maps in `gui/maps`. *Profanity:* **none — Lich's mapper/`;go2`** (it just shows a room window). Lichborne does both a Lich-image map and a Genie-XML map (DESIGN.md §19).
- **Scripting.** *Genie:* its OWN interpreted language (`.cmd` + `#commands`). *Frostbite:* **Ruby** subprocess + TCP API (Lich-like). *Profanity:* **none native — Lich Ruby scripts** are the whole scripting story. *Lichborne:* none native, and per Principle (display layer, not Lich-replacement) we **surface Lich scripts** rather than ship a script engine — the Profanity stance (see DESIGN.md §30 Lich-integration opportunities).
- **Parsers.** All four parse the same StormFront/SGE XML stream. *Genie:* `Core/Game.cs`. *Frostbite:* `gui/xml`. *Profanity:* a streaming tokenizer ([xml_tokenizer.rb](file:///c:/temp/Profanity/ProfanityFE/lib/xml_tokenizer.rb) + `tag_handlers.rb` + `game_text_processor.rb`). *Lichborne:* `StormFrontParser` (stateful tokenizer, DESIGN.md §-game-text-pipeline). Useful when DR sends an oddly-shaped tag: cross-check how Profanity's `tag_handlers.rb` (closest in spirit — also Lich-fed) treats it.

### Takeaways for Lichborne
1. **Profanity is the philosophical north star** — when deciding whether a feature belongs in the client or in Lich, "Profanity delegates this to Lich" is a strong signal to surface-not-rebuild (esp. scripting, mapping/go2, persistent vars).
2. **Highlights specificity + per-property compositing (Profanity) beat list-order** for UX at scale — already adopted (v0.11.3).
3. **Gags and substitutes shipped in v0.12.0** (Mutes & Substitutes, DESIGN §31) — built native (display concern, must work without Lich), with Genie's global-scope model + an optional per-rule stream restrict, closing the former gap vs all three siblings.
4. **Frostbite proves a GUI client can host Ruby scripting** via a subprocess+TCP API — relevant if we ever want richer script *integration* (but our stance is surface Lich's, not host our own).
5. **Macro kill-ring / word-edit actions** (Profanity) are a low-cost input-ergonomics win we lack.

---

## Pitfalls (read before touching adjacent code)

> **Adding a new pitfall — read this first.** Pitfalls are numbered sequentially and the numbers ARE referenced from other comments / commit messages / docs (e.g. "see CLAUDE.md pitfall #33"). When adding a new one, **find the highest-numbered existing pitfall and add the new one AFTER it, with the next number** — do NOT insert before an existing pitfall or renumber an existing one. The natural editing instinct (using Edit with an existing pitfall as the anchor `old_string` and putting the new content first in `new_string`) puts the new pitfall in the wrong position; the correct anchor is the `---` separator at the END of the pitfalls list, with the new pitfall inserted between the last existing pitfall and that separator. Sekmeht caught me making this exact mistake three times across v0.8.7 and v0.8.8 (B121, B124, B126) — each time renumbering an older pitfall instead of just appending. The fix path when caught: revert the renumbered older pitfall to its original number, then append the new one at the end. The numbering is `1, 2, 3, ..., 31, 33, ...` (32 has been missing for ages; don't try to fix the gap, it's referenced from elsewhere by its current number).

1. **`app.getName()` returns `name`, not `productName`.** userData is `%APPDATA%\lichborne\` (lowercase). The `productName: "Lichborne"` under `build:` only affects installer / Start menu display. Mixing the cases caused a v0.6.4 hotfix to silently no-op.
2. **electron-builder NSIS `customInit` runs AFTER the previous uninstaller.** Use `preInit` and read the previous install location from `HKCU\Software\${UNINSTALL_APP_KEY}` (`$INSTDIR` isn't set that early).
3. **NEVER put user data inside the install directory.** NSIS `RMDir /r $INSTDIR` on every upgrade. Pre-v0.6.4 lost everyone's profiles this way.
4. **`StormFrontParser` is stateful and lives for the session lifetime.** Reset on new login. Stream-mode state can bleed across `<prompt>` boundaries.
5. **Scroll pinning fights you if you add naive useEffect logic.** Read GameWindow.tsx + DESIGN.md §23 before adding any programmatic scroll.
6. **Stream IDs are case-sensitive everywhere except `NEVER_DISCOVER`.** Lowercasing them anywhere will break Lich scripts. See B34.
7. **`lichPort` is the Lich front-end port, not an SGE shard port.** Don't look it up from a per-game table at credential build-time (v0.6.3 bug).
8. **`setState` in a mousemove handler must be gated on a hover sentinel** or you re-render on every pixel. See `GenieMapView.tsx` `onMouseMove` (gates on `dragRef.current`).
9. **Genie node rects are CENTERED on the XML position, not top-left anchored.** `ConvertPoint(pos, 4 * scale)` SUBTRACTS the offset (MapForm.cs:187–193). Anchoring top-left shifts every node 4px down-right and misaligns label clusters. Verify against Genie source for any map-rendering math change.
10. **`pointer-events: none` on the SVG pan group breaks click-to-walk.** `isDragging` flips true on mousedown BEFORE click fires — toggling `pointer-events` on the parent at that point makes the click target the SVG root, not the inner node `<g>`. Gate hover at the React layer (check `dragRef.current` in the hover handler) instead.
11. **Use `useLayoutEffect` for camera-follow.** The map's follow-the-player effect must update the transform in the same paint frame as the indicator's new world position. Plain `useEffect` produces a 1-frame "indicator at new spot, camera still old" flash at high walk rates.
12. **`useRef(initialValue)` initialized to a meaningful prop is a footgun.** The "did this change since last render" pattern requires `useRef(null)` (or whatever sentinel ≠ the first real value) so the first non-null render is treated as a change. See `lastLocationRef` in GenieMapView for the cautionary tale.
13. **Genie XML colors can be CSS color names, not just hex.** Some maps use `color="Blue"`, `"Red"`, `"Aqua"`, `"Lime"`, `"White"`. SVG `fill` accepts both, but `COLOR_LEGEND` and every effect lookup keys by hex. `normalizeNodeColor()` in `parseGenieZone` converts these to canonical hex at parse time so downstream code sees a single form. If a new room mysteriously has no aura or animation, check whether its color attribute is a named color we don't normalize.
14. **`opacity` SVG attribute conflicts with CSS animation on `opacity`.** Per SVG2 spec CSS wins, but having both set creates ambiguity that browsers can handle inconsistently. When applying an animated class to an SVG element, omit the `opacity` attribute (set to `undefined`) so CSS owns it cleanly. See the aura render's `opacityAttr` ternary.
15. **`indicator.id` is case-normalized to lowercase by the parser** (`raw.replace(/^Icon/i, '').toLowerCase()`) for the typed-flag checks AND the generic event payload. Renderer keys (`indicators.poisoned`, `indicators.bleeding`, etc.) are lowercase. Don't switch on `IconBLEEDING` directly downstream — match the lowercase form.
16. **If you change the `GenieZone` / `GenieNode` / `GenieArc` shape, bump `GENIE_CACHE_VERSION`** in `main.ts`. The parse cache stores the entire serialized zone graph; a shape change without a version bump means old caches deserialize into wrongly-shaped objects and produce silent runtime errors deep in the renderer. The version bump invalidates every user's cache on next launch with no manual cleanup.
17. **Map animations pause via the parent `<g>`'s class, not per-element.** The freeze classes (`genie-pan-dragging` / `genie-anim-off`) go on the pan group and the rules target the animated classes as DESCENDANTS. **They used to do this with a `*` wildcard; that was the v0.18.5 perf bug — see pitfall #126 before touching these rules, and never reintroduce `.genie-pan-dragging *`.** Don't add `pointer-events: none` to that group when toggling drag/motion either — it would shift the click target off the inner node `<g>` and silently break click-to-walk (the mousedown sets the class before click fires).
18. **The map pan group and the current-room indicator must transition in LOCKSTEP.** Both carry `.genie-pan-smooth` (same `150ms linear`) and share the `snapTransform` flag. The indicator lives inside the pan group, so its screen position is `pan ∘ indicator`; if only the pan transitions while the indicator's world position jumps instantly, the halo bounces off-centre every walk step. Matched transitions make the interpolations cancel. Don't change one easing/duration without the other.
19. **No inline `height: 100%` on the `GenieMapView` outer wrap.** It's a flex child (`.map-canvas-wrap` → `flex: 1; min-height: 0`). An inline `height: 100%` overrides flex sizing and, evaluated against the parent's full height before the parent subtracts its own toolbar, pushes the MapPanel's view-selector toolbar off-screen at narrow window heights. Keep it `display: flex; flex-direction: column; minHeight: 0` and let CSS own the height.
20. **Room-state updates go through the rAF pump, not direct `setRoomState`.** Fast running emits multiple `room-title` events per frame; React 18 auto-batches them so only the last room survives and the map skips rooms. `roomQueueRef` + a `requestAnimationFrame` loop apply one per frame (cap 8). Don't bypass it — **except when `document.hidden`** (minimized/backgrounded), where the pump drains synchronously because rAF is throttled and would otherwise freeze room state (pitfall #71).
21. **`Home`/`End` are native (command-box cursor) when the command input is focused.** Only `Ctrl+Home`/`Ctrl+End` and the focus-elsewhere case scroll the story window. `PageUp`/`PageDown` always scroll. Testers expect text-editing keys to edit text (B77). Don't re-claim plain `Home`/`End` for scrolling.
22. **`LichBridge.interceptLine` suppression is poll-aware — don't make it match on output format alone.** The auto-poll's `;listall` response is consumed (hidden) so it doesn't spam the game window, but a player-typed `;list`/`;listall` produces the same format. `LichBridge.pollScriptList()` arms a 4s window; `interceptLine` consumes a matching line only while armed, otherwise lets it through (B79). Route the auto-poll through `pollScriptList()`, never `injector.pollScriptList()` directly.
23. **Never put CSS `contain` on `.text-line` / `.text-line-wrap` (the react-virtuoso rows).** Layout containment isolates each row from Virtuoso's own item-measurement / scroll-offset bookkeeping, so the scroll position can't be held stable when the list updates while the user is scrolled up — what they're reading gets dragged off-screen (B84). It was a v0.6.8 per-row perf hint, removed v0.6.12. The CSS carries DO-NOT-RE-ADD comments.
24. **An inactive character's GameWindow is `display:none` — its DOM measures 0×0.** Every inactive tab stays mounted and keeps processing game events (room titles, etc.), but `clientWidth`/`clientHeight`/`getBoundingClientRect` all read **0** until the tab is shown. Any layout-dependent logic must (a) guard against 0 dimensions so it doesn't write garbage (`centerOnCurrent`, `fitToView`, the follow `useLayoutEffect` all bail on `!w||!h`), and (b) re-run when the tab becomes visible again — GenieMapView uses a `ResizeObserver` on the SVG to recenter the camera on the 0→size transition. Without (b), a character that travels while its tab is hidden tabs back with the camera stranded rooms away. This is the canonical multi-character map pitfall: hidden ≠ unmounted, but hidden = unmeasurable.
25. **`LoginCredentials` must carry per-shard fields; main has no other source for which DragonRealms shard a connect is for.** Lich and SGE both need to know the shard (DR/DRX/DRT/DRF) — Lich because the launch flags (`--dragonrealms` vs `--test --dragonrealms` vs `--platinum --dragonrealms` vs `--fallen`) decide which front-end port it opens, SGE because the gameCode third arg to `authenticate()` decides which character list / login key it returns. Before v0.8.0, `LoginCredentials` had no `game` field, `App.tsx#runConnect` used the *global* `adv.lichPort` instead of the per-character port, `LichConnection.launch` hardcoded `'--dragonrealms'`, and `ConnectionManager` never passed gameCode to SGE — a 4-layer chain (B95) where DRT/DRX/DRF characters silently routed to DR at every layer. Rule now: every per-shard connection parameter rides in `LoginCredentials` — `game`, `lichArguments`, and `lichPort` (derived per-character from the canonical `GAMES` table in [lichSettings.ts](src/renderer/lichSettings.ts) via `gameOptionByCode()`, NOT taken from the global advanced settings). When adding a new shard or a new per-shard parameter, update both `LoginCredentials` and `GAMES`; don't keep a duplicate table in main.
26. **`CharacterProfile` has two ownership tracks; the GameWindow's debounced save must NOT clobber launcher-owned fields.** GameWindow owns `theme` + `state` (the dynamic localStorage-mirror map). Launcher owns `game`, `useLich`, `hidden`, `favorite`, `guild`, `circle`, `notes` — all written via the small read-modify-write helpers in [Launcher.tsx](src/renderer/components/Launcher.tsx) (`setCharacterFavorite`, `setCharacterHidden`, etc.) directly via `window.api.writeCharacterProfile`. Before v0.8.0, `exportCharacterProfile` ([profile.ts](src/renderer/profile.ts)) called `buildCharacterProfile` and wrote the result verbatim — which only knew the GameWindow's fields. Every debounced save from an active GameWindow (firing 2.5s after any per-character settings change) silently stripped favorite/hidden/notes/guild/circle AND reverted game/useLich to the stale session.* values, un-favoriting connected characters and bouncing back launcher-side pill changes (B97). `exportCharacterProfile` now does a **read-merge-write**: spread `built` first, then override launcher-owned fields from the existing YAML so they persist. When adding a new launcher-owned profile field: (a) add it to the `CharacterProfile` type as optional; (b) add it to the merge in `exportCharacterProfile`; (c) write it via a launcher helper, not through any GameWindow path. Don't try to extend `buildCharacterProfile` to know about launcher fields — they're authored from a different surface and the GameWindow doesn't have authoritative values for them.
27. **`discoveredStreams` must never contain a builtin `PanelType` id.** A "discovered" stream is one the game emits via `stream-declare` / `stream-push` events; the renderer adds it to the `discoveredStreams` array so the Panel Manager can surface it as a Custom row in Available Streams. But every builtin PanelType (`combat`, `room`, `exp`, `thoughts`, …) is ALSO offered as a built-in row from `ALL_PANEL_TYPES`. If the same id appears in both lists, PanelManager shows the stream twice — once as a builtin (correctly hidden after add via the `openTypes` filter) and once as a "custom" discovered row (the old `availableCustom` filter only checked `openCustomIds`, and a builtin tab has `type: 'combat'`, not `'custom'`, so the discovered row never hid). Clicking the second row added a duplicate tab with the same id (B100, combat). Root fix is at the discovery filter in [GameWindow.tsx](src/renderer/components/GameWindow.tsx): reject any id matching an entry in `ALL_PANEL_TYPES` in addition to the hand-curated `NEVER_DISCOVER` list — adding a new builtin PanelType is then automatically covered, no `NEVER_DISCOVER` bookkeeping needed. PanelManager's `availableCustom` filter also defensively excludes builtin-type ids and ids matching an open builtin tab, so a stale discoveredStreams from an older session can't reintroduce the duplicate. **When adding a new builtin PanelType, you don't need to touch `NEVER_DISCOVER`** — only add aliases the server might use (e.g. `experience` → `exp`) if any.
28. **Never call `navigator.clipboard.writeText` from the renderer. Always use `window.api.writeClipboard`.** The renderer's `setPermissionRequestHandler` / `setPermissionCheckHandler` (set up originally for the font picker's `queryLocalFonts`) refuse `navigator.clipboard.writeText` silently — Electron's internal permission name doesn't match a whitelist and the call returns a swallowed promise rejection. No error, no log, nothing reaches the system clipboard. This was B18 (auto-copy) in v0.1.9 and re-bit us as B102 (Debug Copy All) in v0.8.2 because DebugPanel was the one site that never got migrated. The fix every time is the same: route through `window.api.writeClipboard` → `write-clipboard` IPC → main's `clipboard.writeText` (which has no permission guard). When adding any new "copy to clipboard" UI, use the IPC channel from the start. Grep `navigator.clipboard` periodically — if a hit is in renderer code, it's a bug.
29. **Highlight / trigger regexes are compiled with `/g`; loops using `regex.exec()` must `lastIndex++` after a zero-width match.** Highlights compile via `new RegExp(...source, rule.caseSensitive ? 'g' : 'gi')` in [highlights.ts](src/renderer/highlights.ts). When walking matches with `while ((m = regex.exec(text)) !== null)`, a zero-width match (e.g. user pattern with an empty alternative like `(word1|word2|)`) leaves `lastIndex` at the same position and the loop spins forever. Both the renderer (`renderSegmentFull.tsx`) and the Fires log (`logHighlightFiresRef` in GameWindow) have this guard — copy the pattern when adding any new exec-loop. Also: don't use `regex.test()` if you want "does it match a NON-empty string" — `test()` returns true for empty matches. The Fires log used to use `test()` and logged phantom fires for malformed patterns until B101 (v0.8.2) replaced it with `exec()` + length check.
30. **Don't `encodeURIComponent` the `URL=` param of `https://www.play.net/bounce/redirect.asp`.** play.net's bounce takes the value after `URL=` *literally* — Genie's `FormMain.cs` LinkClicked path does raw string concatenation for exactly this reason. Encoding `://` to `%3A%2F%2F` makes the bounce page hand the browser a malformed destination ("site doesn't exist"). The F23 wrapper in [renderSegment.tsx](src/renderer/utils/renderSegment.tsx) ships intentionally raw (B105). The instinct on review is "URLs in query strings should be encoded" — that's the right instinct in general but wrong for this specific endpoint. Comment in the source explains why; preserve it. If the bounce's behaviour ever changes (it accepts encoded input), test by clicking a real external link, not by reading the resulting string.

31. **`dispatchUserText` is the canonical "process text as if the user typed it" path; don't bypass it with raw `window.api.sendCommand` when you want alias resolution + echo + history + log.** Extracted from `handleCommand` in [GameWindow.tsx](src/renderer/components/GameWindow.tsx) as part of v0.8.3 F07 (repeat-command macros). The two callers are (a) the command-bar Enter handler — `{ pushToHistory: true, clearInput: true }`, and (b) the macro token expansion site (`{RepeatLast}` / `{RepeatSecondToLast}` / `{ReturnOrRepeatLast}`) — `pushToHistory` and `clearInput` vary by token. Macro tokens are resolved at **fire time**, not interpolation time, because they reference live state (`historyRef.current` and `commandRef.current`) that doesn't fit into the string-substitution `interpolate(template, vars)` shape. The global keydown handler mounts once with empty deps, so it reaches `dispatchUserText` via `dispatchUserTextRef.current` (latest-instance mirror updated in a no-dep `useEffect`) and reaches the live command bar via `commandRef.current` (mirror of the `command` state, updated when state changes). If you add a new flow that programmatically sends user-equivalent input — a hotkey, a context menu "send", a script-driven replay — route it through `dispatchUserText` so it picks up aliases, the command echo, the session log, and the proper history-push policy automatically. `historyRef` push rule: only the bar's normal Enter and `{ReturnOrRepeatLast}` (typed case) push to history — RepeatLast/SecondToLast must NOT push when replaying, otherwise two presses of RepeatLast would re-pin the previous command and break the next RepeatSecondToLast. Two-press idempotence is part of the Stormfront/Wrayth contract testers expect.

33. **Accessibility overlays (high contrast, color blind) live on top of theme vars — never write theme vars without re-applying overlays.** `applySettingsToDOM` in [settings.ts](src/renderer/settings.ts) writes the active character's accessibility overlay vars *after* the theme vars (e.g. deuteranopia rewrites `--vital-health-ok-start` to a teal). The two writes share keys, so any `applyTheme` / `applyCustomTheme` call from any caller would silently erase the overlay if not followed by an overlay re-apply (B114). The fix is a `registerThemeAppliedHook(fn)` export in [themes.ts](src/renderer/themes.ts): `applyTheme` and `applyCustomTheme` call the hook after writing their vars. GameWindow's theme-apply effect registers a callback that closes over the active session's `settings` and re-runs `applySettingsToDOM(settings)` — so theme writes from ThemeEditor live editing, ThemePicker preview paths, and theme switching all get overlays re-applied automatically. Cleanup on effect teardown clears the hook so an unmounting GameWindow doesn't apply the wrong character's overlays after the next mount. Settings.ts can't import themes.ts directly without a circular dep, hence the registration pattern. When adding a new accessibility-style overlay (epilepsy-safe shapes, dyslexia-friendly font, etc.), add it to `applySettingsToDOM` — the hook will re-apply it after every theme write for free. Don't write overlay vars from outside `applySettingsToDOM` or you'll miss the post-theme re-apply path.

34. **Theme vars: panel-specific colors should cascade from the closest general `--*` var via `var(...)` in `darkBase`, not literal hex.** Established by B108 (`--map-bg` → `var(--bg-app)`) and broadened in B112 (8 panel text vars → `var(--text-secondary/muted/dim)`). The Theme Editor only exposes the *general* surface vars (App background, Game text, Labels, Muted text, etc.); custom themes built via the editor never set the specialty vars. With literal hex defaults in `darkBase`, custom themes were stuck with whatever value `darkBase` baked in — Rakkor's "Game Text" black on a light theme left Room / Hands / Exp panels stranded grey. Pattern when adding any new panel-specific text/bg/border var: pick the closest general var that matches the role (body text → `--text-primary` or `--text-muted`; section labels → `--text-secondary` or `--text-dim`; panel bg → `--bg-base`; panel surface bg → `--bg-app`) and default to `var(--general)`. Built-in themes (Parchment, Mist, Ruby, …) that need a distinct color *still* set the specialty var explicitly — `applyTheme` merges over `darkBase`, so an override always wins. Held / active accent colors (`--hand-held-color`, `--spell-active-color`, etc.) are the exception — they ARE the visual cue and should stay specialty-tinted. If you find yourself writing `'--new-thing-color': '#888888'` in `darkBase` and `#888` is also the value of `--text-muted`, that's the smell: write `'var(--text-muted)'` instead. If you find yourself adding a new specialty var to the Theme Editor schema *just so* light-theme users can override it, that's also the smell — make the cascade do the work.

35. **Built-in themes can share a display `name`; persistence + lookup keys on `id` only.** Established in v0.8.6 F35: both `{ id: 'classic', ... }` (dark) and `{ id: 'classic-light', ... }` (light) ship with `name: 'Classic'`. The two rows in the Theme Picker differentiate visually via the swatch dot (`item.swatches[0]` = black for the dark Classic, white for the light one). ThemePicker uses `item.id` for React keys ([ThemePicker.tsx](src/renderer/components/ThemePicker.tsx) `key={item.id}`) so no double-key warning. `initTheme` reads `localStorage.getItem('lichborne.theme')` (a saved id) and resolves via `THEMES.find(t => t.id === savedId)` — the display-name collision is never load-bearing. If a future contributor sees "two Classic" in the catalog and tries to rename one, they'll break the muscle-memory of testers who picked "Classic" expecting the white-bubble light theme. Keep the duplicate display name when the visual differentiation (swatch dot) is the intended affordance. Apply the same pattern when adding any future paired-theme set (e.g. a Slate Light, Mist Dark).

36. **Contact tracking has TWO long-running effects (last-seen + time-spent polling) that BOTH share the same `pendingContactsRef` buffer — every read AND every state-change must respect it.** Established in B119 + F34. The last-seen effect (fires on `roomState.players` change) builds a 2-second debounced buffer in `pendingContactsRef` to batch lastSeen / lastRoom / encounterCount updates. The 60s `timeSpentMs` polling tick reads contacts to add 60s to anyone in the room. Both effects MUST start from `pendingContactsRef.current ?? contactsRef.current` — using `contactsRef.current` alone misses in-flight buffer updates and the next `saveContacts` clobbers them. The buffer also gets cleared whenever `contacts` state changes externally (ContactsPanel save, import, polling-tick flush): a no-deps `useEffect([contacts])` nulls `pendingContactsRef` + `clearTimeout(lastSeenTimerRef)` — without this clearing, an in-flight buffer would write a stale snapshot back to localStorage and wipe whatever the user just added (B119: Rakkor's "save 12th contact, it disappears"). When adding a new contact-tracking effect, always: (a) base on `pendingContactsRef.current ?? contactsRef.current`, (b) write through `saveContacts` + `setContacts` + `scheduleProfileSave` together, (c) trust the B119 cleanup useEffect to drop your buffer when state changes externally. Don't write to `contactsRef.current` directly — it's a downstream mirror, not a source of truth.

37. **Encounter / presence detection needs TWO gates, not one — absence-threshold AND cooldown.** F34's `encounterCount` increments only when a contact is detected AND `(now - lastSeen) > ABSENCE_THRESHOLD_MS` AND `(now - lastEncounterAt) > ENCOUNTER_COOLDOWN_MS`. The absence-threshold (90s, > polling-tick 60s with a margin) gates "they actually LEFT since last seen" — without it, a contact who stays in your room for 10+ minutes ticks the counter every time `room.players` changes (someone else enters / leaves) because the last-encounter timestamp is old. The cooldown (10 min) gates "even if they left, don't double-count a quick exit-then-re-enter." `lastEncounterAt` updates only on actual increments; `lastSeen` updates on every detection. Both timestamps are needed and serve different purposes — don't fold them. When adding any new contact-state-transition feature (e.g. "first seen on day X" badges), think in terms of presence-edge detection rather than just "is this contact here right now," and use the same absence-threshold + cooldown shape so we don't end up with multiple half-correct definitions of "encountered."

38. **`lichborne:open-quick-send` custom DOM event is the canonical way to open QuickSend from anywhere outside AppShell.** Established in F33 v0.8.6. AppShell owns the QuickSend modal state and listens for the event; on receipt it queries the visible `.session-shell:not(.session-shell--hidden) .command-input` for the value and opens QuickSend prefilled — the same DOM read the keyboard handler does for Ctrl+Shift+Enter. Use the event rather than prop-drilling an `onOpenQuickSend` callback through every GameWindow / panel — AppShell is already DOM-aware for this path, and the event keeps the source-of-input logic in one place. If you add a new "send a command somewhere else" entry point (a context menu item, a panel button, etc.), dispatch `document.dispatchEvent(new CustomEvent('lichborne:open-quick-send'))`. Don't dispatch the event with a payload — the event handler's contract is "open with whatever's in the active command bar" and adding payload-based behavior would split that contract.

39. **Panel Manager has two ownership dimensions; don't conflate "slot added" with "slot has streams".** v0.8.1 introduced explicit add/remove of panel locations — the 4 slots (Main-Top, Top-Right, Middle-Right, Bottom-Right) each carry an `*Added` boolean (`mainTopAdded` / `topAdded` / `midAdded` / `bottomAdded`) that controls whether the slot renders in the game window. The slot's `tabs` array controls which streams live in it. The two are independent. An added slot with empty tabs renders an `EmptyPanelSlot` placeholder; a removed slot can still have tabs in the array — *that's not necessarily a bug*. `removePanelZone` clears the tabs when the user explicitly removes, but the **migration default for `mainTopTabs` was `[]` as of v0.8.3** (was `[room, combat]` in v0.8.1–v0.8.2) precisely because the default + `mainTopAdded=false` produced "phantom tabs in an un-added zone" — invisible to the user but visible to any code that aggregates tabs across zones. **Anywhere code aggregates tabs across the four zones, it MUST gate on the `*Added` flag.** Two sites currently do this (both fixed in B106/B107): (a) `watchedStreamsRef` in [GameWindow.tsx](src/renderer/components/GameWindow.tsx) — used by `STREAM_FALLBACK` routing; without the gate, phantom tabs claim their streams as "watched" and lines like combat / conversations silently buffer instead of falling back to main. (b) `allTabs` in [PanelManager.tsx](src/renderer/components/PanelManager.tsx) — used by `openCustomIds` and `openTypes`; without the gate, phantom tabs hide discovered streams from Available Streams, leaving the user with no path to re-add a stream to a visible slot (this was Legiro's moonwatch / Moons bug). New code that mutates tabs (`addToZone`, `moveTabToZone`) auto-flips the `*Added` flag on as a safety so adding a stream to a removed slot doesn't leave it invisible. Migration defaults when the flag is missing: **Main-Top → false** (new zone, explicit opt-in), other three → **true** (preserve v0.8.0 always-visible). When adding a fifth panel location in the future, follow the same pattern: new `*Added` state + scopedKey persistence + migration default + render-gate in GameWindow + `addPanelZone`/`removePanelZone` handlers + PanelManager UI entry + **audit every cross-zone aggregation site to gate on the new flag**.

40. **`newDiscovered` in GameWindow's event batch must dedupe WITHIN the batch, not just against existing state.** Established in B123 (v0.8.7). The event loop in [GameWindow.tsx](src/renderer/components/GameWindow.tsx) translates every incoming `stream-declare` AND `stream-push` event into a push to a per-batch `newDiscovered: string[]`. The dedup filter at the `setDiscoveredStreams` site checks `existing.has(id)` (against pre-batch state) but originally did NOT track ids it had already added in the same pass. A Lich script that emits `<streamWindow id='X'/>` immediately followed by `<pushStream id='X'/>` produces both events in a single batch; both push `'X'` into `newDiscovered`; both pass the `existing` check (since `X` isn't in `discoveredStreams` yet); both get appended → array becomes `['X', 'X']`. PanelManager renders one row per array entry, so the user sees two identical "Available Streams" rows; adding either makes `openCustomIds.has('X')` true and filters out BOTH rows (this is also how to recognize the bug — adding "one" of two identical-name rows hiding both is a duplicate-id signature, not a display bug). The fix is a `seenInBatch: Set<string>` local to the filter; first entry in a batch passes and gets recorded, duplicates within the same batch are rejected. When adding a new stream-discovery event type in the future (a hypothetical third tag that should also surface a stream), route it through the same `newDiscovered` push site so the existing dedup catches it for free. If you add a separate write site, replicate the `seenInBatch` pattern. The corresponding parser invariant: `stream-declare` (from `<streamWindow>`) and `stream-push` (from `<pushStream>`) deliberately emit independently — script writers' intent is "make sure the UI knows this stream exists" with multiple safety nets, and stripping one of them at the parser layer would break legitimate scripts. Dedup is the renderer's job.

41. **Room sub-stream stale-clear lives at the parser, not the GameWindow event batch.** Established in B121 (v0.8.7). The Room panel's structured sections (`room`, `room-objects`, `room-players`, `room-creatures`, `room-extra`, `room-exits`) get populated from `<component id='room ...'/>` tags. When DR doesn't re-emit a component for a room transition (common — DR is sparse with re-emits unless `LOOK` forces them), the previous room's data carries over and surfaces as ghost players / objects in the Room panel until a manual `LOOK`. **The wrong place to fix this is GameWindow's event batch loop** (which is where I tried first): track which sub-streams "were seen" in this batch, clear the rest on title change. The fatal failure: IPC batches can split title-change from component updates across multiple batches. A batch carrying only the new title with no component re-emits would falsely "see no players this batch" and wrongly clear data the next batch was about to populate. **The right place is the parser** ([StormFrontParser.ts](src/main/parser/StormFrontParser.ts)). DR's XML protocol orders `<streamWindow>` (which carries the subtitle = room title) BEFORE any `<component id='room ...'/>` for the new room — deterministic, single-batch-or-not. New `lastRoomTitle: string` field on the parser; in the streamWindow handler, when `cleanTitle !== lastRoomTitle` emit `clear-stream` for all six room sub-streams BEFORE the typed `room-title` event. Components that DO arrive then re-populate via their own clear+stream-text flow; sections that DON'T arrive stay correctly empty. The `lastRoomTitle` gate prevents a no-op streamWindow re-emit (same subtitle, DR sends these for non-transition repaints) from clobbering freshly-populated sub-stream data. **B117 compatibility**: the parser's `captureSegments` work for monsterbold lives inside the `<component>` handler which fires AFTER the streamWindow pre-emit clear-streams — bold capture is unaffected. **Downstream beneficiary — contacts tracking**: the F34 encounter / `timeSpentMs` effects iterate `roomState.players`, so before B121 they were ingesting carryover ghost players from the previous room and crediting time-spent against the wrong room. B121's clear at the parser cascades to a clean `roomState.players: []` during the transition gap, so contacts stats are now accurate by construction (no separate fix needed for F34). **Don't move this logic back into GameWindow** — every fix at that layer has to know about batch boundaries, which makes it brittle. The parser sees the raw tag stream and knows the ordering. **Reset on session boundary**: `lastRoomTitle` is cleared in `parser.reset()` alongside other per-session state, so the first room of a new session always triggers clear-streams.

42. **Lichborne-source imports bypass the `ImportCandidate` intermediate; new rule types must update the bypass, not just the intermediate.** Established in B124 (v0.8.7). The import wizard's neutral intermediate at [types.ts](src/renderer/import/types.ts) (`ImportHighlight` / `ImportTrigger` / `ImportMacro` / `ImportAlias`) was designed to model the union of Wrayth / Genie / Frostbite source formats — it does NOT model the full native Lichborne rule shape. `ImportHighlight` has no `bold` / `glow` / `glowColor` / `groupIds` / `allGroups` / `name` / `enabled`; `ImportTrigger` has no `gates` / `oneShot` / `cooldownSeconds` / `watchStream` / nested action ordering / `groupIds` / `allGroups` / `name` / `enabled`; `ImportMacro` / `ImportAlias` are similarly trimmed. When F29 (Lichborne→Lichborne) shipped in v0.8.4, it routed everything through this intermediate, and the mapper at [mapper.ts](src/renderer/import/mapper.ts) hardcoded defaults for every field the intermediate didn't carry — silent data loss on round-trip across every rule type. **The fix shape**: `ImportResult` carries an optional `nativeRules?: { highlights, triggers, macros, aliases }` block (loose `unknown[]` typing to avoid a circular import with the rule modules). The lichborne.ts parser populates it in lockstep with the ImportCandidate arrays, spreading the source rule object and overriding `id: nanoid()` (and for triggers, every nested `TriggerAction.id` regenerates too). [ImportWizard.tsx](src/renderer/components/ImportWizard.tsx)'s `doImport` checks `result.nativeRules` and prefers it over `mapImportResult(...)` output when present; non-Lichborne sources leave it undefined and fall through to the legacy mapper path. **When adding a new rule type** (e.g. a future "scene", "snippet", "schedule"…), the new type needs: (a) full shape in the native rule module with `id` + `name` + `enabled` + `groupIds` + `allGroups` like every existing one; (b) an entry in `LichborneExportFile` and the parser's `nativeRules` block at [lichborne.ts](src/renderer/import/parsers/lichborne.ts) that spreads the source and regenerates ids; (c) an apply branch in `doImport` mirroring the existing highlights/triggers/macros/aliases shape — content-based dedup key, append-merge filter, save call. The ImportCandidate intermediate is **only** for surfacing the rule in the wizard preview UI; if you don't need preview support for the new type, you don't need an ImportCandidate at all (just `nativeRules`). The cardinal sin is the reverse — adding the field to ImportCandidate + mapper but forgetting to extend `LichborneExportFile`'s parser shape, which would re-introduce the B124 loss pattern. **Don't trust loaded rules to have default fields** — the runtime stores assume rules saved by Lichborne have all required fields, but a hand-edited YAML or a malformed export could be missing them. Defensive normalization belongs at the `loadHighlights`/`loadTriggers`/etc. layer, not the import boundary; we don't have it today because exports always come from `loadHighlights(character)` which returns runtime-complete shapes. Add it only when a tester actually hits a malformed-import crash.

43. **When the user can manually scroll past where the scroll API thinks the bottom is, the bug is in LAYOUT, not scroll math.** Established in B122 across v0.8.7 → v0.8.8. The half-row clip at font 13+ ate three failed hypotheses before landing on a real fix: (a) v0.8.7 lowered a `totalListHeightChanged` threshold from 2px → 0.5px — didn't help; (b) v0.8.8 first attempt replaced raw `el.scrollTop = el.scrollHeight - el.clientHeight` with `virtuosoRef.current?.scrollToIndex({ align: 'end' })` on the theory Virtuoso's internal accounting differed — didn't help; (c) Sekmeht's diagnostic — "I can manually scroll down ~half a row past where the snap lands" — proved BOTH APIs agreed on the wrong target. When two independent scroll APIs converge on the same position but the DOM has scrollable content past that position, the issue is in CSS / layout, not in the math. The fix that worked: a `components.Footer` on Virtuoso (`<div style={{ height: 14 }} />`, fixed 14px, gated to `settings.fontSize >= 13`) — defensive layout slack that doesn't require knowing why the scroll APIs land short. **General principle**: scroll-snap math problems are usually solvable by adding layout slack rather than by chasing what the math is doing. If you're tempted to write a third hypothesis-based fix to scroll math, stop — add invisible padding below the content instead. Specific to this case: the 14px footer is **gated on font ≥ 13** because at font 12 and below row heights round to clean integer pixels and no clip occurs; the slack would just be empty space below the last row (visible gap above the vitals strip). **Pair fix shipped in same release**: a separate `useEffect([settings.fontSize, settings.lineHeight, settings.largePrint])` re-snaps via `scrollToIndex({ align: 'end' })` ONLY when `pinnedRef.current` was already true — font / line-height / large-print changes reshape every row's height, growing scrollHeight, leaving the previously-pinned scrollTop short of the new bottom. Bail on `!pinnedRef.current` so scrolled-up readers aren't yanked back. rAF before the snap so Virtuoso has a frame to re-measure rows at the new dimensions. The threshold check in `totalListHeightChanged` is left at 0.5 (responsive) but doesn't carry the load — the footer does. **SUPERSEDED (v0.11.4, B153 — Rakkor reported the clip back at font ≥ 13; the footer was REMOVED.)** The real diagnosis (after several wrong turns): at font ≥ 13 the pinned view rests exactly **one line short** of the bottom — the decisive tester clue was "I can always wheel down one notch to reveal it," i.e. the true DOM bottom *is* reachable, the auto-follow just wasn't getting there. Cause: Virtuoso's `followOutput` lands at "last item at viewport bottom" but **under-measures the last row at fractional heights** (a row is `min-height: 1.4em` + `0.15em` wrap padding ≈ 1.55em, non-integer at font ≥ 13), so the last line lands clipped — AND `followOutput` runs *after* the synchronous bottom-correction in `totalListHeightChanged`, overriding it. **Two documented dead ends — do not retry:** *(1)* **integer per-row pixel heights** (`ceil(font × line-height)` on `.text-window .text-line`) — did NOT help; the short-landing is followOutput under-measurement + override, not row-height rounding. *(2)* a **font-scaled `components.Footer`** (slack to scroll into) — once the correction below reached the true bottom, any footer just became a one-line **gap** (last line a row ABOVE the vitals bar instead of flush), and it under/over-shot non-monotonically right around font 13–14 (so the bug looked "font-13-specific" — it was just the footer math crossing over there). **The actual fix: defer the raw bottom scroll into a `requestAnimationFrame`** in `totalListHeightChanged` ([GameWindow.tsx](src/renderer/components/GameWindow.tsx)) — `el.scrollTop = el.scrollHeight - el.clientHeight` inside a rAF runs AFTER `followOutput` and wins, landing at the genuine DOM bottom (DOM `scrollHeight` is immune to Virtuoso's internal under-measurement). Last line sits flush at every font, **no footer, no clip, no gap.** `scrollToBottom` (End key) and the font re-snap do the same rAF raw correction after their `scrollToIndex({align:'end'})`. The re-snap effect also arms `suppressUntilRef` **synchronously before** the rAF (not only inside it), because the relayout on a font change (rows AND the command bar both grow — the command bar scales with `--game-font-size`, pitfall #45) balloons `scrollHeight` while `scrollTop` holds, so `dist` crosses the 40px deadband and `handleVirtuosoScroll` un-pinned BEFORE the rAF ran → the re-snap bailed, the "N new lines" badge appeared, and the view stranded ~1 line up. **Lessons:** (a) when a pinned virtual list rests one line short but the bottom is wheel-reachable, the auto-follow (`followOutput`) is under-measuring and the fix is a DOM-truth `scrollTop = scrollHeight - clientHeight` correction — but it must be DEFERRED to a rAF to win the race against `followOutput`; (b) a "slack footer" only masks a short-landing and turns into a visible gap the moment the scroll actually reaches bottom — fix the scroll, don't pad the content; (c) integer rows are NOT the lever for a followOutput short-landing.

44. **The Room panel must mirror the main scroll's styling for the same content.** Established in B117 (v0.8.5, monsterbold creatures), reinforced in v0.8.8 (line-mode highlights for treasures). DR's `<component id='room players'/>` / `room objs` / `room creatures` / `room extra` sections are the exact same content that appears in the main scroll's "You also see ...", "Also here: ...", and (for desc) the room paragraph. Anything that styles that content in the main scroll — server-pushed bold/color, user contact name highlights, user match-scope highlights, user line-mode highlights — should style the structured Room panel sections the same way. Two things flow from this: (1) **the parser's `captureSegments` must carry the same styling fields as `pushSegment`** (currently: `bold` from B117; future styling sources will need to be added to both). When you add a new styling field to TextSegment (a `class`, `linkType`, whatever), update both the main-scroll `pushSegment` AND the capture path in `text()`'s `captureSegments.push`. (2) **RoomPanel's `renderSegments` must pass every kind of rule that TextLineRow passes** — `matchRules` (B111), `lineRules` (Rakkor v0.8.8), contacts, contact templates, nameRegex. The line-style is computed PER-SECTION (objects/players/creatures/extra each compute their own `getLineHighlightStyle` off their own joined text) so a rule that matches a player name paints only the Players section, not the others. `desc` deliberately stays out of line-style scope because it's multi-sentence prose where a single match would over-paint a large block. **B111's original "lineRules excluded on purpose" exclusion was overcautious and got overturned in v0.8.8** — the documented "lobster paints the whole section" objection turned out to be the wrong call: in the main scroll the same content gets that same paint when the user authored a line-mode rule, and consistency between surfaces beat the theoretical objection. Future "should the Room panel apply X?" questions should default to YES if the main scroll applies X for the same content. **(v0.14.7, F52: the panel now renders these sections as PROSE lines — the game's own sentences verbatim, no labels, exits as a clickable "Obvious paths:" text line last, matching all three sibling clients. The rule above is unchanged — "per-section" is now per-prose-line; the paint pipeline is identical. Exit links send FULL direction words — the compass token `dn` is not a valid DR command. Don't reintroduce buttons/labels without a fresh tester ask.)**

45. **UI that should scale with `Settings → Font Size` must use `em` (inheriting from `.game-toolbar`) or `var(--game-font-size)` directly. Never `rem` for game-area UI.** Established in B113 (text weight pass), reinforced in v0.8.8 Mode button fix. `rem` is anchored to the browser root font-size (~16px) and ignores the user's per-character `settings.fontSize`. So a button declared as `font-size: 0.75rem` renders at a fixed 12px regardless of whether the user picked 8 or 24 in Settings. Three sizing categories in the codebase: **(a) toolbar buttons** like `.btn-debug` / `.btn-theme` / `.btn-settings` use `font-size: 1.0em` and inherit from `.game-toolbar` (which itself scales). New toolbar buttons should match this pattern — the Mode button shipped with `0.75rem` and visibly drifted smaller as users bumped their font size; v0.8.8 fixed it to `1.0em`. **(b) Portaled popovers** (Mode switcher menu, contact popover) can't inherit from `.game-toolbar` because they're mounted to `document.body` via React portal. Use `var(--game-font-size)` directly with a multiplier (e.g. `calc(var(--game-font-size) * 0.85)` for smaller sub-text). **(c) Text-line content** uses `var(--panel-font-size, var(--game-font-size))` to support the F31 per-panel override with a fallback to the global. When adding any new UI surface in the game area, grep for `rem` in the new CSS before committing — every `rem` is a fixed pixel size that won't track user preference. App chrome (settings panels, modal headers, etc.) can use `rem` deliberately if it should NOT scale with game font, but that's the exception, not the default.

46. **DR's room-transition data arrives in TWO independent XML tags; the parser must read both, not just one.** Established in B126 (v0.8.8). When the player moves to a new room, DR usually emits both: (a) `<streamWindow id='main' subtitle='[Title - rmId]'/>` — carries the room title and room id together inside the subtitle bracket, parsed in [StormFrontParser.ts:441-484](src/main/parser/StormFrontParser.ts#L441-L484), and (b) `<nav rm='X'/>` — carries the new room id as an attribute, parsed in [StormFrontParser.ts:523-548](src/main/parser/StormFrontParser.ts#L523-L548). For most transitions both arrive together so reading just `<streamWindow>` is sufficient. **But some transition shapes (teleports, NPC-induced moves, certain scripted moves) emit ONLY `<nav rm>` and skip `<streamWindow>` entirely.** For those, a front-end that reads only `<streamWindow>` will have `roomState.title` and `roomState.roomId` frozen on the prior room until a manual `LOOK` forces a re-emit. Lich's `$room` variable is correct the whole time because Lich's scripting reads `attrs.rm` from `<nav>` independently — that's the canary: when `;e $room` shows the right room but the Lichborne map shows the wrong one, we missed a `<nav rm>` somewhere. **The fix shape**: a separate `RoomIdEvent` (not an overloaded `RoomTitleEvent`) — emit `{ type: 'room-id', roomId: N }` from the `<nav>` handler when `attrs.rm` is numeric. The handler in GameWindow routes it into `roomUpdates.roomId` ONLY — leaves title / desc / sub-streams alone, because forging an empty string for fields we don't have would wipe the only data we have (the prior room's title is at least *something* to display until a real `<streamWindow>` arrives). **Don't overload RoomTitleEvent with an empty title for the `<nav>` case** — `GameWindow`'s `case 'room-title'` does `roomUpdates.title = evt.title` unconditionally; an empty title would clear the prior title and the Room panel would go blank. Two events with different invariants is the right separation. **Scope of the Lich Map fix**: it's not a fix for the Room panel's title-display (only `<streamWindow>` carries the title string), and not a fix for Genie Map (its resolver is title+desc+exits-based, not roomId-based — the Genie data has no room ids; the exit-aware cross-zone hold in `resolveGenieRoom`, pitfall #70, handles Genie's variant of the symptom). **General rule**: when extracting data from a stateful XML stream, audit every place the same data could appear. Room id is in two places (streamWindow subtitle + nav rm); both need handlers. The same audit should apply for any future "data appears in multiple tags" case.

47. **Save calls on multi-category data sources must be gated per-type on `incoming.length > 0`.** Established in B127 (v0.8.9). The import wizard's `doImport` writes to four independent per-character stores: highlights, triggers, macros, aliases. Plus four F29-native stores: groups, modes, contacts, contactTemplates. Different source types fill different subsets of these — Wrayth's parser returns `[]` for triggers and aliases (Wrayth's XML doesn't carry them); Frostbite returns `[]` for triggers, aliases, and contacts; Genie returns all five; F29 returns whatever the source character had. Pre-v0.8.9, BOTH the Append and Replace branches in [ImportWizard.tsx](src/renderer/components/ImportWizard.tsx) `doImport` called `saveX(character, mapped.X)` (or `[...existing, ...keepX]` in Append) for ALL four rule types regardless of whether `mapped.X` was empty. The catastrophic failure mode was Replace: `saveTriggers(character, [])` wipes the user's existing triggers when the current import has no triggers. Append was mathematically a no-op for empty arrays but indistinguishable in correctness from a future regression — the right shape is to gate the save itself, not rely on save-array math being benign. **The fix shape**: every save call (rules AND F29 native data) gated on `mapped[type].length > 0` (or `result.nativeX.length > 0` for native data — the outer `result.nativeX && Array.isArray(...)` check passes for empty arrays too because `[]` is truthy and `Array.isArray([])` is `true`, so a Lichborne F29 export from a character with no groups would have wiped the recipient's groups on Replace pre-fix). **When adding a new rule type** to the import system (e.g. a future "scene", "snippet", "schedule"…), follow the same pattern: gate every `saveX(...)` call on the corresponding `mapped[type].length > 0`. The Append-branch dedup math (`[...existing, ...keepX]`) is still correct for non-empty incoming and should run normally. **When adding a new parser** that doesn't carry some rule types (like Wrayth doesn't carry triggers), explicitly return `[]` arrays for the missing types — don't omit the field from the result object, because every consumer assumes the field exists. The per-type guards mean the empty array just no-ops the save; no special handling needed at the parser level.

48. **Genie's complete `#command` catalog lives at [Genie4 source Command.cs:270](C:\temp\Genie4-Dev\Genie4\Core\Command.cs#L270) (`switchExpr = oArgs[0].Substring(1).ToLower()` then ~100 case branches). When adding a new import-side recognizer, check the full catalog first to identify aliases — Genie has multiple `#commands` for the same operation.** Established in B133 (v0.8.9). The clean mappings to Lichborne action types are deliberately narrow because most Genie commands have no front-end equivalent: **(a) Game commands** — `#send`, `#put`, `#q`, `#que`, `#queue` → `command` action; **(b) Variables** — `#var`, `#variable`, `#setvar`, `#setvariable`, `#tvar`, `#tempvar`, `#tempvariable`, `#svar` → `variable` action (Lichborne doesn't distinguish var scope at the action layer — temp/saved/string/server vars all collapse to the same action); **(c) Sounds** — `#play`, `#playsound`, `#playwave` → `sound` action; **(d) Beep** — `#beep`, `#bell` → `beep` action; **(e) Flash** — `#flash` → `flash` action; **(f) Echo** — `#echo` → `echo` action; **(g) Log** — `#log` → `log` action. **Everything else gets dropped with a "Unsupported actions skipped: #X, #Y" note in the import preview** via the catch-all `else { dropped.push(part) }` at the end of `parseActionParts` in [genie.ts](src/renderer/import/parsers/genie.ts). The non-mappable categories: **rule manipulation** (`#trigger`, `#untrigger`, `#alias`, `#unalias`, `#macro`, `#unmacro`, `#highlight`, `#unhighlight`, `#class`, `#unclass`, `#name`, `#unname`, `#gag`, `#ungag`, `#sub`, `#unsub`, `#substitute`, `#unsubstitute`, `#ignore`, `#unignore`, `#squelch`, `#unsquelch`, `#unvar`, `#unvariable`, `#unsetvar`, `#unsetvariable` — modify rules at runtime, which Lichborne's static rule model doesn't support); **UI/window control** (`#statusbar`, `#window`, `#position`, `#layout`, `#show`, `#hide`, `#close`, `#edit` — Genie-specific UI); **connection control** (`#connect`, `#disconnect`, `#exit`, `#reconnect`, `#lc`, `#lconnect`, `#lichconnect`, `#ls`, `#lichsettings` — would let a trigger forcibly disconnect; user should do this from the UI); **system/config control** (`#plugin`, `#load`, `#save`, `#reload`, `#unload`, `#config`, `#set`, `#setting`, `#settings`, `#profile`); **movement/mapping** (`#mapper`, `#map`, `#walk`, `#walkto`, `#go`, `#goto`, `#path` — could partially map to Lich `;go2` but routing differs); **scripting flow** (`#if`, `#parse`, `#regex`, `#regexp`, `#do`, `#goto`, `#true`, `#false`, `#wait`, `#waitfor`, `#pause`, `#abort`, `#resume`); **math/random/eval** (`#math`, `#evalmath`, `#random`, `#eval` — Lichborne variable actions only store, don't compute); **custom Genie events** (`#event`, `#events`). **`##escape` syntax** — Genie uses `##text` to send a literal `#text` to the game (escape for the `#` prefix); the parser strips the leading `#` and treats as a plain command. **Explicit no-ops** — `#nop` and `#comment` are silently dropped without "Unsupported" warning since the user never intended them to do anything; flagging them would scare users with a useless note. **`#send #X` / `#put #X` recursive shape** — see pitfall #X (TODO if separate, otherwise documented inline in [genie.ts](src/renderer/import/parsers/genie.ts) at the COMMAND_PREFIX_RE branch). When the inner content of a send/put wrapper itself starts with `#`, recursively process via `parseActionParts` instead of treating as a DR command — this handles user-error shapes like `#send #flash` (Jaded's B130 case).

49. **`STREAM_FALLBACK` and DR's native protocol duplication can double-count.** Established in B136 (v0.8.10). DR's XML protocol sends some streams TWICE per server event — once inside a `<pushStream id="X"/>` block (which routes to the X panel), and once OUTSIDE the block to main directly. **Speech and whispers do this; thoughts / arrivals / deaths / etc. do not.** A `say hi` produces literally: `<pushStream id="talk"/><preset id='speech'>You say</preset>, "Hi."<popStream/><preset id='speech'>You say</preset>, "Hi."` — two complete copies, the second going to whichever stream is active outside the pushStream block (typically `main`). Lichborne's [`STREAM_FALLBACK`](src/renderer/components/GameWindow.tsx) routes unwatched stream content to a fallback (typically `main`) so important text doesn't get silently buffered. **Adding a STREAM_FALLBACK entry for a stream that DR already natively duplicates to main produces double-counting** — the inside-pushStream copy gets fallback-routed to main on top of DR's outside-pushStream copy. Symptom: "You say, Hi." appears twice in main when no panel watches that stream. **Rule**: when adding a new stream to STREAM_FALLBACK, FIRST check whether DR natively emits an outside-pushStream copy for that stream. If yes (speech / whispers — and any future stream that follows that pattern), DO NOT add a fallback entry. If no (thoughts, arrivals, deaths, custom Lich script streams that emit only inside the pushStream block), add the fallback. The B136 fix removed `conversation: 'main'` from STREAM_FALLBACK for exactly this reason; the comment block at the STREAM_FALLBACK declaration documents the principle inline. **How to check**: open Debug → Raw XML, do a `say test`, look for two consecutive copies of the speech in the XML capture — one inside `<pushStream id="talk"/>...<popStream/>`, one immediately after. If you see that pattern, the stream is natively duplicated and shouldn't have a fallback.

50. **Stream id aliases live in [`src/shared/streamAliases.ts`](src/shared/streamAliases.ts); use `normalizeStreamId(id)` everywhere a stream id crosses a boundary.** Established in B135 (v0.8.10). Pre-v0.8.10, the parser had its own inline `STREAM_MAP` constant for translating server / legacy / sibling-client stream names to Lichborne panel ids (e.g. `talk` → `conversation`, `death` → `deaths`). When we needed the same translation in the renderer's `echoToStream` (so imported Genie triggers with `#echo >talk` would land in the right panel) and in the import parsers (so legacy plural references on imported triggers got rewritten), the choices were either (a) duplicate the map in each module — error-prone drift risk — or (b) extract to shared. The shared module is `STREAM_ID_ALIASES` (the bare map) + `normalizeStreamId(id)` (helper that returns id unchanged if not aliased). Used at: parser's `<pushstream>` / `<streamwindow>` handlers, parser's `<clearstream>` handler (uses `STREAM_ID_ALIASES[id]` directly to preserve COMPONENT_STREAM fallback chain), renderer's `echoToStream` in GameWindow, Genie parser's `parseEchoAction` (normalizes `>stream` tokens at import time), F29 `lichborne.ts` parser (normalizes legacy `watchStream` / `echoStream` on imported triggers). **When adding a new alias** (e.g. a future Lich script emits a new variant id you want to merge into an existing panel), add ONE entry to `STREAM_ID_ALIASES`. All five call sites pick it up automatically. **When adding a new builtin panel type**: add the canonical id to [`PanelFrame.tsx`](src/renderer/components/PanelFrame.tsx) `PanelType` / `PANEL_LABELS` / `ALL_PANEL_TYPES` AND add a switch case for it. If multiple ids should route to it, add aliases too. **When renaming an existing panel id** (B135's `conversations` → `conversation` precedent): write a localStorage migration in [`src/renderer/localStorageMigrations.ts`](src/renderer/localStorageMigrations.ts) that walks per-character tabs / activeIds / panelFontSizes keys / trigger watchStream values. **Case-insensitive matching** in the migration is important — user data may include custom-typed tabs created from a discovered stream id with arbitrary casing (e.g. `'Conversations'` if Lich emitted `<streamWindow id='Conversations'/>` at some point). **Dedupe pass after rename** is also important — if a user had both the builtin tab AND a custom variant, both collapse to the new id and the duplicate should be dropped, not left as two visible duplicate panels. **Migration flag bumping** (e.g. `conversationRename` → `conversationRename2` → `conversationRename3`) is the recovery mechanism if a draft migration ships with a bug — bumping the flag re-runs the (now-fixed) migration on every user's next launch. **When writing a rename migration, walk NESTED fields too — not just top-level**. The v0.8.10 retroactive sweep caught a real example: the trigger handler in `localStorageMigrations.ts` originally only renamed `t.watchStream` but a `TriggerRule` also has `t.actions[]` with `echoStream` on echo actions. A pre-v0.8.10 user with a trigger that echoed to the legacy panel id had stale `echoStream` left after the migration — functionally OK via runtime alias normalization but the trigger editor showed the legacy value outside its dropdown options. Fix: extend the handler to walk `actions[].echoStream` too, bump flag to `conversationRename3`. **General rule**: when a rename touches a domain object (Contact / Trigger / Macro / etc.), enumerate ALL fields where the renamed string could appear — including nested arrays — and rename in all of them. The runtime alias map in `STREAM_ID_ALIASES` is a safety net, NOT a substitute for clean data; users editing rules through the UI need the stored value to match the displayed options.

51. **Macro `@` is the universal cursor-position marker; `\@` escapes to literal `@`.** Established in B137 (v0.8.10). Genie ([`FormMain.cs:1140`](C:\temp\Genie4-Dev\Genie4\Forms\FormMain.cs#L1140) `ParseInputBox` + [`FormMain.cs:4079`](C:\temp\Genie4-Dev\Genie4\Forms\FormMain.cs#L4079) `ClassCommand_SendText`) and Wrayth both use `@` as the cursor-position marker in macro commands. The presence of an unescaped `@` switches the macro from "send immediately" to "type into the input box and wait for the user to finish typing" mode. **Lichborne's canonical helper** is [`parseCursorMarker(command)`](src/renderer/macros.ts) in `macros.ts` — walks the string char by char, treats `\@` as a literal `@` in the output (escape), strips all unescaped `@` chars from the typed text, returns `{ text, cursorPos }` for the first unescaped `@`'s position. Returns `null` when no cursor marker is present (caller sends normally). **Runtime in the macro fire path** ([GameWindow.tsx](src/renderer/components/GameWindow.tsx)): for each command, check cursor marker FIRST (before token check). If found: flush any pending send-mode commands, `setCommand(text)` + sync `commandRef.current`, wait one rAF for React to commit the input value, then `inputRef.focus()` + `setSelectionRange(cursorPos, cursorPos)`. Stop iterating remaining macro commands (the user must hit Enter to send what's in the bar; anything after the cursor command would never fire). **Wrayth `\r` translation at import time** ([wrayth.ts](src/renderer/import/parsers/wrayth.ts) `parseWraythAction`): Wrayth's `\r` is the explicit "send / Enter" marker; absence of `\r` means "type and wait." To map Wrayth's signal into Lichborne's universal `@` convention, the importer tracks whether the raw action ended with `\r` — if NOT, and the last command doesn't already have an unescaped `@`, append `@` so the runtime treats it as type-and-wait. Jaded's `<k action="&apos;}"/>` (decoded `'}`, no `\r`, no `@`) imports as `'}@` and works correctly. **Genie imports**: Genie macros use `@` natively, but Genie's file format ALSO uses Level-1 escape sequences (`\\` → `\`, `\}` → `}`, `\{` → `{`) for content that contains the argument-delimiter characters. Pre-v0.8.10 our `parseArgs` used `line.indexOf('}')` and terminated arguments at the first `}` even when escaped — a Genie compound macro like `{\\x;'\}sekmeht @}` parsed as `\\x;'\` (the `\}` was treated as the terminator) and the rest got dropped. v0.8.10 rewrites parseArgs to walk char-by-char and resolve Level-1 escapes during extraction. **`\x` handling — deliberate simplification (Sekmeht 2026-06-01)**: Genie's runtime has a two-mode model where `\x` triggers "clear + type" and `@`-without-`\x` triggers "insert at cursor in current input." Lichborne picks ONE mode — "always clear and type when there's an `@`" — because (a) it's simpler and more predictable for testers and (b) every real-world `\x` macro from Jaded's file also has `@`, so the simplification covers the common case. So in Lichborne: **`\x` is purely import-time noise** — `splitAction` strips it from each part and lets empty parts get filtered out. **Only `@` triggers wait-mode**, not `\x` alone. `\\x;hide` (hypothetical) imports as just `hide` and auto-sends; `\\x;whisper group @` imports as `whisper group @` and waits (because of `@`). **The "insert at cursor" mode SHIPPED in v0.13.3 (B170, JadedSoul) as a composition-aware rule, not a per-macro flag:** when a cursor-marker macro fires while the command input is FOCUSED and NON-EMPTY (the user is mid-composition — e.g. sitting in the gap a previous `get @ from my pack` template left), the macro's text is INSERTED at the caret (replacing any selection, caret landing at the inserted text's `@` offset) — Wrayth's type-into-the-entry-box model, enabling macro-within-macro composition (`Alt-T` template + `Ctrl-2` `second @` → `get second from my pack`). An EMPTY or unfocused bar keeps the v0.8.10 replace behavior (template fire starts fresh). Consequence: re-firing a template macro while still focused in its own non-empty output INSERTS again (Wrayth-faithful) — clear the bar first if you want a fresh template. Git-verified: the fire path had been replace-only and byte-identical from v0.8.10 through v0.13.2 — the v0.11.9/v0.12.0 Genie/macro import work never touched it, so any earlier "it worked" memory was Wrayth itself or different macro data. **Genie variables** (`$speak`, `$whisper`, `$emote`, etc.) are NOT expanded by Lichborne — they pass through `interpolate` (which only handles names that match Lichborne's reserved variable list) and stay as literal `$varname` text in the imported command. Future feature. **Multi-command behavior**: if a macro has both send-mode and wait-mode commands, the send-modes fire first, then the wait-mode command deposits text and stops; commands AFTER the wait-mode command are silently skipped (matches Genie's behavior — once the cursor is in the input box, the macro is "done" until the user hits Enter, which sends only the box contents). **Backward compat for manually-created Lichborne macros** with literal `@` in the command text (uncommon — usually for DR's `SAY @person message` syntax, but people typically type that directly): post-v0.8.10 the `@` is a cursor marker; users with such macros need to change `@` to `\@` to preserve literal `@` send-mode. **When adding a new macro feature** that interacts with command text (variable interpolation, command chaining, etc.), be aware that `parseCursorMarker` runs AFTER interpolation (`resolveMacro` interpolates `$varname` before returning) — so a variable that expands to text containing `@` would trigger cursor-marker handling. Could be a feature (variable-driven cursor positioning) or a bug depending on intent.

52. **`;vars setup` (and Lich GTK setup-window scripts in general) crash Lich on user interaction; route testers to Lichborne's SQLite vars viewer instead.** Investigated 2026-06-01 (v0.8.11). The `;vars setup` script lives at `Ruby4Lich5/Lich5/scripts/vars.lic` (it's a core Lich script, not a dr-script — `dr-scripts/` has no `vars.lic`, only `setupaliases.lic`). It opens a GTK window via `Gtk.queue { Gtk::Window.new ... window.show_all }` and blocks on `wait_until { done }` until the `delete_event` handler fires. The window DOES paint under Lichborne (which contradicts the older "kill-counter shape" assumption that all such windows fail to render — see updated architecture note above). **What crashes Lich:** [`vars.lic:136-141`](C:\Ruby4Lich5\Lich5\scripts\vars.lic#L136-L141) spawns a `Thread.new { ... sw.vadjustment.value = ... }` block that mutates a GTK widget (`sw.vadjustment`) from a non-main thread inside the "focus-in" event handler that fires when the user clicks the "(new var name)" textbox. This directly violates the warning at [`gtk.rb:4`](C:\temp\lich-dev\lich-5\lib\common\gtk.rb#L4) — "Calling Gtk API in a thread other than the main thread may cause random segfaults." Under Lichborne's launch (`ruby lich.rbw --stormfront --dragonrealms`) the race appears to manifest reliably; under Genie's launch it apparently doesn't (GTK runtime timing differs between front-end environments — both run the same `vars.lic`, so the difference is in how the GTK loop is hosted). The failure shape: user focuses new-var textbox → `Thread.new` launches and mutates the scrolled-window adjustment from off-thread → GTK widget state race → Ruby/GTK segfault → Lich process exits → Lichborne's [LichConnection](src/main/connection/LichConnection.ts) sees the socket close and emits `disconnect`. **This is a Lich-script bug, not a Lichborne bug.** It cannot be fixed from Lichborne's side without forking Lich's `scripts/vars.lic`, which is out of scope (Principle: not a Lich-replacement; we don't ship script forks). **Lichborne-native replacement SHIPPED in v0.9.0** — the Lich Dashboard → Variables tab is now a full add / edit / delete editor (see pitfall #53). It deliberately did NOT take the approach this pitfall originally speculated (a Marshal *serializer* + direct `lich.db3` write): a direct DB write is unsafe while Lich is running because Lich's in-memory `@@vars` is authoritative and its next auto-save would clobber it (vars.rb). Instead writes go THROUGH Lich's public `Vars` API via `;eq Vars['name'] = value; Vars.save`, which mutates Lich's memory AND forces an immediate flush — no Marshal serializer needed, no race with Lich's auto-save. Read still uses the SQLite/Marshal path (`lich:get-vars`). **When a tester reports the `;vars setup` disconnect:** confirm root cause matches (window opens fine, disconnects only on adding/editing), explain it's a Lich-script GTK race that manifests under the `--stormfront` launch shape, and route them to the existing vars viewer for read access plus the script's NON-GTK sub-commands `;vars set NAME=VALUE` / `;vars delete NAME` / `;vars list` (vars.lic:41-75 — these don't touch GTK at all and work fine). **Related discovery, correcting CLAUDE.md drift:** the older "Known limitation — Ruby GTK script windows don't render" architecture note claimed "force-mode (`--stormfront`) skips Lich's entry GUI, which is what starts the GTK main loop" — that's factually wrong. [`lich.rbw:135`](C:\temp\lich-dev\lich-5\lich.rbw#L135) calls `Gtk.main` UNCONDITIONALLY when `defined?(Gtk)` (i.e., whenever the `gtk3` gem loads), and [`gtk.rb:148-155`](C:\temp\lich-dev\lich-5\lib\common\gtk.rb#L148-L155) registers a `GLib::Idle.add { sleep 0.01 }` yield so Ruby's I/O threads can run while the GTK loop spins. So the loop IS running under `--stormfront`. The kill-counter family does NOT "never appear" either — Sekmeht confirmed (v0.9.0 testing) that `kill-counter.lic`'s window surfaces **intermittently** (often after a start→kill→restart cycle), so the shared root cause is GTK-runtime timing/threading flakiness under `--stormfront`, not a missing pump and not a categorical no-paint. A painted kill-counter window is still unsafe — interacting with it can crash Lich the same way `;vars setup` does. Don't repeat either the "no main loop" or the "windows never appear" claim without re-verifying. The architecture-section paragraph above has been corrected in the same v0.8.11 update. **General principle for future Lich-GTK-script disconnect reports:** read the actual `.lic` source first (the user's live install at `Ruby4Lich5/Lich5/scripts/` is the canonical location; `dr-scripts/` does NOT contain core scripts like vars/log/repository), look for `Thread.new` blocks that touch GTK widgets from off-thread, and check `gtk.rb` for the thread-safety contract before blaming the front-end launch shape. The disconnect signature in Lichborne is the same regardless of cause (Lich exits → socket closes → `LichConnection` emits `disconnect`); the diagnostic value is in the .lic source, not the front-end.

53. **Editing Lich state writes THROUGH Lich's runtime API, never into `lich.db3` directly — and force a save so the read view doesn't lag.** Established by the v0.9.0 editable-variables feature in [LichDashboard.tsx](src/renderer/components/LichDashboard.tsx) `VarsTab`. The shape generalizes to any future "edit a thing Lich owns" feature (vars, settings, the repository list, etc.). **Why not write the DB:** while Lich is connected, its in-memory copy is authoritative (for vars, the `@@vars` hash in [vars.rb](C:\temp\lich-dev\lich-5\lib\common\vars.rb)); Lich persists to `lich.db3` only on its own schedule (vars: a 5-min auto-save, and only when the md5 changed) and rewrites the WHOLE blob via `INSERT OR REPLACE`. A direct Lichborne DB write would be silently clobbered the next time any Lich script touches a var. So Lich's memory is truth and the DB is just its persistence — same inversion as Principle #1's "YAML is truth, localStorage is a working copy," but here Lich owns the truth. **The write path:** send a single atomic `;eq <ruby>` (ExecScript — `;e`/`;eq` is dispatched at [global_defs.rb:2319](C:\temp\lich-dev\lich-5\lib\global_defs.rb#L2319), `;eq` = the quiet form) that BOTH mutates via Lich's public API AND forces the flush in one statement list: `;eq Vars['name'] = value; Vars.save` (delete = `... = nil; Vars.save`). One `;eq` (not two separate commands) because two commands race — a standalone `Vars.save` could run before a separate `;vars delete` finished, persisting the pre-delete state and leaving the DB stale until the 5-min timer. `Vars.save` is mutex-guarded (vars.rb:78) and rewrites the whole blob, so a deleted key actually leaves storage and the SQLite read view is correct on the next refresh. `Vars[]=`/`Vars.save` are Lich's documented public API (as stable as the `;vars` command, and what `;vars` calls internally) — this is NOT "running arbitrary Ruby," it's the same API surface. **Escaping:** values/names are injected into Ruby single-quoted literals via `rubyLit(s)` (escape `\` and `'` only — those are the only specials in a Ruby `'...'` string) so values with quotes/spaces/backslashes survive. Mirror Lich's own coercions (vars.lic coerces literal `true`/`false` to booleans — so do we, for display parity). **Silent send:** the `;eq` runs via a no-echo path (`onRunCommand` wired to `window.api.sendCommand(sessionId, cmd)` directly, NOT the echoing `sendCommand`) so the raw Ruby doesn't spam the game window — the optimistic UI update is the user-facing feedback. **Read stays SQLite** (`lich:get-vars` + Marshal) — structured display of lists/hashes/times, cross-scope browse, graceful per-row parse-error degradation. The read/write asymmetry is deliberate and correct: read wants structure + any-scope browsing (the Marshal storage format is a far more stable contract than the cosmetic `;vars list` output, and command-read would need a stream-capture/suppress mechanism like the `;listall` poll); write must mutate live memory. **Two gates on edit controls**: (a) `session.useLich` AND (b) the selected scope equals the connected character's own scope (`${game}:${character}`) — `;eq Vars[...]` only touches the CONNECTED session's memory, so editing while viewing another character's scope would silently write the wrong character's vars. Viewing other scopes is read-only with a note. **Optimistic overlay** (`pendingRef`, keyed by scope → name → change) bridges the sub-second gap between the write and the next DB read so ↺ / reopen never flicker an edit back to its old value; it survives a refresh by re-applying over the DB read, and is discarded on modal close (the forced save makes the DB authoritative-fresh by then, so this is belt-and-suspenders). **When adding a new "edit Lich state" surface**: follow this exact shape — write through Lich's public Ruby API + force its save in one `;eq`, read through whatever structured path exists, gate on connected-own-scope, send silently, overlay optimistically. Don't reach for a direct DB writer or a Marshal serializer; the race with Lich's in-memory authority is the trap.

54. **(Removed v0.9.x — number kept as a tombstone.)** This pitfall documented the v0.9.0 GTK-script advisory: a `client-notice` IPC channel that injected `--- Lichborne:` lines into the stream, plus a `maybeWarnGtkScript` / `GTK_CODE_RE` / `resolveLichScriptFile` detector in [main.ts](src/main/main.ts) that scanned a started script's `.lic` for GTK code and warned the user. **All of it was removed** when the GTK-friendly launch shape shipped (see the "Ruby GTK script windows" architecture note + the launch section) — the advisory assumed GTK was broken, which is no longer the operating assumption. The `client-notice` channel (its only consumer was this advisory), the `onClientNotice` preload/`global.d.ts` binding, the GameWindow subscription, and the per-session `gtkChecked`/`useLich`/`lichPath` Session fields are all gone. **Transferable lesson worth keeping** (this is why the number isn't just deleted): *any* future feature that reacts to "the user started script X" — a per-script setting, a script-specific panel, a fresh advisory — must resolve the script the way Lich's `Script.start` does ([script.rb:65-84](C:\temp\lich-dev\lich-5\lib\common\script.rb#L82)): build the candidate list (custom/ root + subdirs, then scripts/ root, each sorted by basename) and match **exact first, then prefix** — because users abbreviate script names constantly (`;kill-cou` starts `kill-counter.lic`). Don't assume `;typed` maps to `typed.lic`. **And if you ever need to inject a client-authored line into the stream again** (the old `client-notice` pattern): a segment with `bold: true` and NO `preset` renders as monsterbold yellow, because `renderSegment.tsx` resolves `preset = seg.preset ?? (seg.bold ? 'bold' : undefined)` — an explicit preset (e.g. `internal-system`) WINS over the bold fallback and gives you a dim gray-bold line instead. Rebuild the channel from scratch if needed rather than expecting the old wiring to still exist.

55. **A themed dialog must use the SAME background vars as the canonical `.sp-modal` / `.sp-header` chrome — `--bg-raised` and `--bg-sunken` can collapse to near-identical values on light themes.** Established v0.9.2 (Sekmeht, Classic Light). The wizard chrome ([wizard.css](src/renderer/styles/wizard.css)) styled `.wiz-modal` body with `var(--bg-raised)` and `.wiz-header` with `var(--bg-sunken)`, while the Settings modal ([settings.css](src/renderer/styles/settings.css)) used `var(--bg-base)` body + `var(--bg-hover)` header band + `var(--accent)` title. On the dark theme this looked fine (the four vars are visibly distinct), so it shipped — but Classic Light defines `--bg-base #fff`, `--bg-raised #efefef`, `--bg-sunken #fafafa`, `--bg-hover #e8e8e8`: the wizard body went grey instead of white and the header band (`#fafafa`) was indistinguishable from it, so the Lich Setup dialog had **no visible title bar and a "wrong" greyed body** while the Settings window next to it looked right. Fix was to align the wizard chrome to the exact vars the Settings modal uses (`--bg-base` body, `--bg-hover` header, `--accent` title). **General rule**: there is one canonical modal-chrome var recipe (body `--bg-base`, header/footer band `--bg-hover`/`--bg-sunken`, title `--accent`); reuse it for every dialog rather than picking "semantically nice" raised/sunken vars per-dialog. **And always eyeball new dialog chrome on a light theme (Classic Light, Ivory, Parchment, Mist)** — the raised/sunken/base/hover scale is spread out on dark themes but compressed near white on light ones, so a var choice that looks distinct in the default theme can vanish on a light one. Related: this same release converted [login.css](src/renderer/styles/login.css) (the shared `.login-form` / `.advanced-panel` chrome behind LichSetupFields) from hardcoded dark hex to theme vars — same lesson as pitfall #34 (write `var(--x)`, not a literal hex that happens to equal the dark-theme value), but for form/dialog chrome rather than panel body text. Semantic status banners (the discovery ok/warn/error chips) use `color-mix(in srgb, <fixed-hue> N%, var(--bg-base/--text-primary))` so the hue stays meaningful while the background + text contrast self-correct per theme — the right pattern when a color must stay a fixed semantic hue but still adapt to light/dark. **Crucially — when you change a SHARED chrome var, audit the inner elements that referenced the OLD value, because their contrast was relative to it.** `.wiz-modal` is shared by the Add Character wizard, and flipping its body `--bg-raised → --bg-base` collided with two inner rules keyed to the old relationship: the selectable tiles (`.wiz-radio`/`.wiz-game`/`.wiz-char`) were `var(--bg-base)` (raised *above* a `--bg-raised` body) and went flat once the body was also `--bg-base` — fixed by moving the tiles to `--bg-raised` (now raised above a `--bg-base` body); and `.wiz-close:hover { background: var(--bg-hover) }` became invisible once the header band was `--bg-hover` — fixed with a `color-mix(... var(--text-primary) 14%, transparent)` overlay (a theme-agnostic darken/lighten that's always visible regardless of the surface under it; note `--bg-active` was NOT an option — classicLight defines it but `darkBase` does not, so it'd be `transparent` on the default theme). Both regressions were silent on the dark theme and only surfaced in a cross-feature read during the v0.9.2 bug check — the canonical example of why changing a shared var means re-reading every consumer of it, not just the dialog you were fixing. **Modal SIZE standardization (v0.11.2, separate from chrome):** the big management windows (Panel Manager, Automations + standalone Highlights/Triggers/Macros editors, Lich Dashboard/Profile/Scripts, Settings, Session Log, Contacts, Theme Picker/Editor, **Import Wizard** `.iw-modal` — added v0.11.9 because the old fixed 780px squished the Genie highlights table's columns) all set `width: var(--modal-w-standard)` / `height: var(--modal-h-standard)` (88vw × 86vh, matching the Maps overlay `.map-overlay-window`) — two layout tokens in [global.css](src/renderer/styles/global.css) `:root`, NOT theme colors. Retune all modal sizes in one place. This is the SAFE slice of the deferred modal-chrome unification (size only — no bg/header/backdrop/close-button changes, so no pitfall-#55 light-theme risk). **Heads-up — `.tp-` is a shared prefix collision (a real, live trap):** BOTH `theme-picker.css` and `triggers.css` declare `.tp-modal`/`.tp-body`/`.tp-backdrop`/`.tp-close`/`.tp-header`/`.tp-list`/`.tp-list-item`/etc., and BOTH components render those classes — so since CSS is global, every `.tp-*` rule from either file applies to the OTHER panel too, with conflicts resolved by bundle load order. v0.11.2 made them agree on `--modal-w-standard`/`--modal-h-standard` so SIZE is harmless. But the resize work exposed a layout bug: theme-picker's `.tp-list { width: 210px }` was leaking onto the Triggers rule list (only theme-picker sets `width` on the shared class, so it applies regardless of order) — it coincidentally fit when the triggers sidebar was also 210px, but broke once the sidebar widened. **Fix technique (when two components share a class and you can't rename yet): scope by a structural ancestor with the child combinator.** theme-picker's list is `.tp-body > .tp-list` (direct child); triggers' is `.tp-body > .tp-sidebar > .tp-list` (grandchild). Scoping theme-picker's rule to `.tp-body > .tp-list` makes it match ONLY theme-picker's list (the `>` excludes the nested triggers list) AND raises its specificity (0,1,1) so neither leaks onto the other. **RESOLVED in v0.18.0 — the real fix was done: the Triggers panel's prefix was renamed `.tp-` → `.trg-`** (126 rules in triggers.css, 164 refs in TriggersPanel.tsx, plus the `.at-body .trg-body` hooks in automations.css and the shared `.trg-list-item:hover .list-item-delete` rule in global.css, which belongs to the rule-editor family — `hp-`/`ma-`/`cp-` — not the Theme Picker). `.tp-*` is now **exclusively** the Theme Picker's, so its rules can be edited freely. The rename was forced by the Automations theming pass: both files declared `.tp-modal`/`.tp-header`/`.tp-title` with the SAME properties, so restyling triggers would have silently restyled the Theme Picker, with bundle load order deciding. **Lesson worth keeping: two components sharing a class prefix are 'compatible by luck' only until someone restyles one of them — rename at the first sign of a real conflict rather than scoping around it forever.** The other still-colliding classes (`.tp-body`/`.tp-backdrop`/`.tp-close`/`.tp-header`/`.tp-list-item`) are currently compatible by luck; don't trust that.

56. **Platform-wide profile import has TWO write paths — YAML read-merge-write for inactive targets, localStorage + remount for active ones — and writing only the YAML for an ACTIVE target gets erased on logout.** Established by F38 (v0.10.0, JadedSoul) in [profileTransfer.ts](src/renderer/profileTransfer.ts) `applyProfileImport`. The feature fans an import out to many characters at once; each target is either *inactive* (no open session — only a `{Character}.yaml` on disk) or *active* (has an open session, focused OR backgrounded). The two need different writes:
- **Inactive → YAML read-merge-write** (the `setCharacterGame` pattern, [Launcher.tsx](src/renderer/components/Launcher.tsx)): `readCharacterProfile` → merge selected categories into `state` (+ `theme`) → `writeCharacterProfile`. **NEVER** call `buildCharacterProfile`/`exportCharacterProfile` for a non-active character — they rebuild `state` from localStorage, which for an unopened char is empty, so they'd wipe its settings (Launcher already documents this).
- **Active → localStorage working copy + remount.** Write each selected category into `lichborne.{target}.*` scoped keys, then bump that session's reload nonce in App.tsx (`reloadSession(characterId)` → it's part of the GameWindow `key`) so the GameWindow remounts and re-reads everything from localStorage. **Why not just write the YAML:** the live GameWindow is still holding the *pre-import* state, and `exportCharacterProfile` (debounced save + logout flush) rebuilds the YAML from localStorage — so a YAML-only import would be silently overwritten on the next save/logout (JadedSoul's exact concern). Writing the working copy makes the imported state the live state; logout then persists it correctly. **"Active" includes backgrounded tabs** (pitfall #24: hidden ≠ unmounted) — they hold their own stale localStorage-seeded state, so the import loop must `reloadSession` EVERY target with an open session, not just the focused one. Remounting a hidden tab is safe (its 0×0 DOM re-measures on show via the GenieMapView ResizeObserver). ~~Tradeoff: the remount clears in-memory game-text scrollback~~ **Fixed v0.13.3 (B165 hole #2):** main's `session:reload` handler arms a pitfall-#60 replay (`replayTarget`/`holdingForReplay`) before triggering the remount, so the remounted GameWindow restores scrollback AND every sticky state — hands included (a long-parked held item never re-sends a hand tag, so pre-fix it could never self-heal post-remount; found while chasing B165, see pitfall #78 for the attribution nuance).

**Non-destructive guarantee:** import writes ONLY into `state` (per-key, never a whole-`state` replace) + the top-level `theme`. Every identity/launcher field (`account`, `character`, `game`, `useLich`, `hidden`, `favorite`, `guild`, `circle`, `notes`, `profileVersion`) is a top-level YAML field outside `state`, so it's structurally unreachable — the spread `{ ...existingProfile, state: merged }` preserves them all. **Theme is the one quirk:** Lichborne's current theme is a single global `lichborne.theme` key, and `exportCharacterProfile` rewrites an active char's YAML `theme` from that global on every save — so you CAN'T independently pin an active target's theme (it reverts to global). Theme applies cleanly to *inactive* targets (YAML `theme` field, applied on their next connect via `importCharacterProfile`); for active targets it CANNOT be pinned (an active char's YAML theme is rewritten from the global on the next save), so it's flagged "app-wide — not changed for a connected character; pick it from the theme menu." The custom-theme *definition* always goes into shared `myThemes` regardless (so it's available in the picker everywhere), and the import flushes `_shared.yaml` so it persists. **The `settings`/`panelFontSizes` split:** Display category owns `settings` minus `panelFontSizes`; Layout owns `panelFontSizes`. On apply, Display preserves the target's existing `panelFontSizes` and Layout merges its own into `settings` — Display runs before Layout so both can touch the `settings` key without clobbering. When adding a new transferable setting, register it in `TRANSFER_CATEGORIES` (the single allowlist); a key not listed there is simply not transferred (safe default).

57. **Per-session React contexts and per-session UI state live INSIDE GameWindow — app-level chrome (the app-bar, the native menu) cannot consume them, and must drive per-session behavior through the action bridge instead.** Established by the Phase 2c app-bar (v0.10.0). `GroupsProvider`, `CharacterProvider`, and the `HighlightsContext`/`ContactsContext` providers are wrapped **per-session** (`GroupsProvider`/`CharacterProvider` around each GameWindow in [App.tsx](src/renderer/App.tsx); the Highlights/Contacts providers *inside* GameWindow's render) — that's the per-character isolation model (pitfall #6). So anything rendered at **app level** (the [AppBar](src/renderer/components/AppBar.tsx), App's modals) **cannot call `useGroups`/`useCharacter`/`useHighlights`/`useContacts`** — there's no provider above it. This is why **ModeSwitcher had to stay in the per-session Icon Bar** (it reads `useGroups()`) rather than move into the app-bar with the other buttons, and why the app-bar's buttons are **plain triggers that dispatch `lichborne:session-action`** to the active GameWindow rather than reading/showing per-session state directly. The **per-button active-state highlight** (e.g. "Maps is open" glow) is the canonical example of the *reflect-via-snapshot* rule below: the app-bar can't read the active session's `showMapOverlay` etc. directly (those are GameWindow `useState`), so GameWindow **reports them up through `useSessions`** — a `SessionStatus.panel*` flag per toggle button, pushed by a GameWindow `updateStatus` effect and read by AppBar to apply `btn-*--active` for the active session (shipped in v0.10.0 deferred-polish; this is exactly the "surface a minimal snapshot through `useSessions`" pattern, not lifting GameWindow state to app level). **Rule:** when you need app-level chrome to act on the active session, route an action through the bridge (`menu-action` IPC for the native menu / `lichborne:session-action` DOM event for renderer UI) and add a case to GameWindow's `runSessionActionRef`; do NOT try to lift a per-session context to app level (it breaks the isolation model) unless you genuinely intend every session to share that state. When you need app-level chrome to *reflect* active-session state, surface a minimal snapshot through `useSessions` (as the connection dot does via `status.connected`) rather than reaching into GameWindow.

58. **Game-area font scaling has two traps: the panel-root anchor and the own-font `em`.** Established fixing the icon bar / vitals / Mode button / Lich Scripts panel (v0.10.0). Settings → Font Size sets `--game-font-size` ([settings.ts](src/renderer/settings.ts)); to follow it a surface must use `var(--game-font-size)` / `em`-off-an-anchored-container, never `rem` (Principle #9). Two specific traps bit during this work:
    - **(a) Panel content scales only if that panel-type's OWN root anchors to `var(--panel-font-size, var(--game-font-size))`.** `.panel-frame-tabs` carries the game-font anchor — but that only sizes the **tab labels**; `.panel-frame-body` (the content wrapper) has **no** font-size, so each panel's root must set the var itself (as `.exp-panel` / `.stream-panel` do). The Lich Scripts panel (`.sl-panel`) shipped without it and silently ignored *both* the global font and the per-panel +/− (its `.sl-*` children were `rem`). **New panel type ⇒ anchor its root to `var(--panel-font-size, var(--game-font-size))` and size children in `em`,** or it won't scale. **The v0.14.7 audit (B201) caught two more variants of this class:** the Genie map's SVG landmark labels read `var(--game-font-size)` DIRECTLY — the global font worked but the A+/A− override didn't (reading the game var without the panel var IS this pitfall, even when it looks font-wired); and the Debug panel was `rem` throughout (ignored both). Both fixed (map labels/tooltip/legend on the chain via a `PANEL_FONT` anchor — SVG node GLYPHS stay geometry-sized on purpose; debug.css rem→chain+em). Every other panel root verified chained in the same audit.
    - **(b) `em` on `width`/`height` is relative to the element's OWN `font-size`, not its parent's.** A chip that sets `font-size: 0.84em` and wants "72px at default" needs `width: 7.14em` (72 ÷ its own 10.08px), NOT `6em` (= 6 × its own font). The icon bar's `.status-bar` hit exactly this. Elements with no own font-size (`.vital-bar`, `.row-sep`) inherit the container's game font, so their `em` dims are relative to that — simpler. Conversion to preserve the default look: `em = rem × 16/12` (rem is the 16px html root; the default `--game-font-size` is 12px).
    - **(c) Per-panel font overrides (F31) PIN a panel.** Once `A−/A+` writes `settings.panelFontSizes[tabId]`, that absolute size wins over the global Settings font, and there's currently **no reset-to-global** (`onAdjustPanelFontSize` always writes, never deletes the key). So a panel you've adjusted stops tracking the Settings slider — **by design (override wins) but surprising.** If a user reports "panel X ignores the font setting," check for an override before assuming a bug (that was the exp confusion). A `next === settings.fontSize ⇒ delete the key` reset (or a relative-delta model) would let adjusted panels rejoin the global font — but Sekmeht **decided to leave it as-is** (2026-06-04): an override pins the panel, full stop. Don't "fix" this without a fresh ask.
    - **(d) Two surfaces share `lich-panels.css`.** The Lich Scripts **panel** (`.sl-*`, a zone panel) scales with the game font; the Lich **Dashboard modal** (`.ld-*`, opened by the Lich button) is deliberately fixed `rem` like every other modal (Settings, Automations, …). Don't confuse them — converting the modal to scale was explicitly declined (modals are the chrome-sizing exception; Principle #9).

59. **Decouple moves a session's WINDOW, never its socket — and that only works because GameWindow unmount doesn't disconnect.** Established by the multi-window feature (v0.11.0). `session:move-window` ([main.ts](src/main/main.ts)) reassigns `ownerWindowId`; the source window drops the tab via `session-release` → renderer `removeSession` (local-only — it does NOT call `destroySession`), which unmounts that GameWindow. The session's socket/parser/LichBridge in main are untouched, so the target window mounts a fresh GameWindow for the SAME `sessionId` and main re-routes its events there. **This is load-bearing on GameWindow's unmount cleanup containing no teardown of the session** — verified: the IPC-subscription cleanup only unsubscribes listeners + cancels rAF/timers ([GameWindow.tsx](src/renderer/components/GameWindow.tsx)); `window.api.disconnect` lives only in `handleDisconnect` (a button), never an unmount path. **If you ever add session teardown to a GameWindow unmount effect, decouple/re-home will silently kill the character on every move.** Don't. Closing a *secondary window* IS a logout (`runSecondaryWindowClose` graceful-disconnects then destroys), but that's the window 'close' handler in main, not the GameWindow unmount. Per-BrowserWindow renderers are fully isolated JS contexts (separate heaps) — only localStorage is shared (same partition) — so module-level renderer state (theme hook, etc.) can't bleed across windows; that isolation is why the move is clean. The roster (`session-roster`) is the cross-window awareness mechanism; the render source stays `SessionsContext`-local driven by acquire/release/pull (NOT roster-driven) to avoid the connect-flicker that a fully roster-derived render loop would cause.

60. **A render-only "replay" reseeds a window taking over a session — gate it at the side-effect choke points, snapshot sticky state, and HOLD live delivery during the handoff.** Established by the multi-window state-transfer (v0.11.0), refined across three tester-found bugs. A freshly-mounted GameWindow starts blank; to make decouple/re-home seamless, main replays the session's recent history to the window that took it over. Three things make this correct, each from a real bug:
    - **(a) Gate side effects, don't reprocess them.** The replay batch carries `replay: true` ([GameEventBatch](src/shared/types.ts)). The renderer rebuilds display + game state from it but MUST NOT re-fire triggers (would re-send commands!), re-log the session, or re-count fires. All trigger firing funnels through `processLineRef`/`processVariableChangeRef` and logging through `logToSession`/`logHighlightFiresRef`, so gating those FOUR choke points on a `replayingRef` (set true at the batch top in `onGameEvent`, reset false at the bottom) covers every call site at once — far safer than threading a flag through the ~14 individual call sites. The room/vital STATE setters (setVitals etc.) are NOT gated (rebuilding state is the point).
    - **(b) Snapshot sticky state — a ring buffer alone loses unchanged vitals.** Main keeps per-session `stateSnapshot: Map<key, GameEvent>` (latest of each vital/indicator/RT/CT/stance/spell/hand/room/exp/injury/exits, via `snapshotKey`) PLUS a bounded `historyBuffer` (scrollback text). The first design was a single ring buffer of recent events — but a vital sitting at 100% hadn't emitted a `vital-update` recently, so it fell off the end and restored BLANK (only actively-changing bars survived). The snapshot keeps the latest value of every bar regardless of age; replay sends snapshot FIRST then history.
    - **(c) Only replay to a MOVED-into window, and HOLD live during the handoff.** Replaying on EVERY GameWindow mount doubled the login stream on a fresh connect (the replay overlapped the live stream still filling the buffer). Fix: a session only earns a replay when DELIBERATELY REMOUNTED — `replayTarget` is set on `session:move-window` (decouple/re-home) and, since v0.13.3 (B165 root cause), on `session:reload` (the profile-import remount) — one-shot, checked in `session:request-replay`; a fresh connect never gets one. AND because a session can still be actively streaming *during* a move (bulk-connect logs in then decouples), `holdingForReplay` stops live `game-event` sends to the window from the moment of the move until the replay is delivered (events accumulate into the buffer, the replay delivers them once, then live resumes) — otherwise the in-flight login lines show live AND in the replay. A 5s `scheduleReplayHoldRelease` safety net resumes live if the window never requests its replay. The buffer is filled at FLUSH time (events actually sent), kept disjoint from the pending `eventQueue`, so a replay can't double a not-yet-flushed event either. **When adding a new "rebuild a window from history" path, reuse this shape: gate side effects via a replay flag, snapshot sticky state separately from scrollback, and never let live + replay overlap.**

61. **Two import-wizard traps, both from completing the Wrayth importer (B146, v0.11.1). (a) Don't copy `parseNames`' pattern-only dedup onto a HIGHLIGHT parser — it's data loss.** `parseNames` dedups by pattern because two contacts with the same name make no sense (one contact). But HIGHLIGHTS legitimately repeat a pattern with DIFFERENT colors (Wrayth had `shirt` @54 and `shirt` @26), and Lichborne supports multiple same-pattern highlights (priority-ordered). When `parseStrings` was first written by copying `parseNames`, its pattern-only dedup silently dropped the color variant (147 strings → 146). Fix: dedup highlights by **full visual identity** (`pattern + textColor + bgColor`), so only truly-identical rows collapse. **Any new highlight-producing parser must dedup on visual identity, not pattern alone.** **(b) `max-width` + `text-overflow: ellipsis` is a NO-OP on an inline element.** `.iw-pattern` ([import-wizard.css](src/renderer/styles/import-wizard.css)) is a `<span>` in a table cell with `max-width: 320px` + ellipsis to keep long patterns from blowing out the table. That silently did nothing for years because Genie/Frostbite highlights are short words — but Wrayth `<strings>` are full sentences, so the Pattern cell expanded to the full string and pushed the Color/Status columns off-screen (the symptom looked like "colors didn't import" — they had, just scrolled off behind a horizontal scrollbar). Fix: `display: inline-block` so `max-width`/ellipsis apply. **When truncating text with `max-width`+ellipsis, the element must be block/inline-block** — and when a tester says "X didn't import," verify against the parsed data (a quick node script replicating the parse) before assuming a parse bug; here the data was correct and the bug was layout. General Wrayth-format reminders for future import work: highlights are in `<strings>` (NOT `<highlights>`); colors are `@NN` palette refs resolved through `<palette>`; `&apos;`/`&quot;` are decoded by `getAttr`→`decodeXmlEntities` (don't add a second decode); `bgcolor="skin"`/empty means "inherit" → treat as null.

62. **A line-number gutter and its content MUST share an ABSOLUTE per-line height, never a unitless `line-height` — different font-sizes silently drift them apart (B147, v0.11.2).** The Lich Dashboard YAML editor renders line numbers in a separate gutter column (`.ld-line-num`) beside the content (`.ld-yaml-preview` view / `.ld-yaml-editor` textarea). The gutter font is intentionally smaller (`0.75rem`) than the content (`0.79rem`) — fine — but all three used a **unitless** `line-height: 1.65`, which multiplies each element's OWN font-size. So the gutter's per-line height (12px×1.65 = 19.8px) ≠ the content's (12.64px×1.65 = 20.86px): ~1px/line of drift that ACCUMULATES, leaving the numbers ~2.5 lines off the content they label by line ~50. The tester's phrasing ("line spacing bleeding through") was exactly this. It also made *search* feel wrong: `scrollElementToLine`/the overlay read `getComputedStyle(pre).lineHeight` (the content's 20.86px) so they scrolled to the correct *content* line, but the gutter number next to it was misaligned, so the user read the wrong number. **Fix:** pin the gutter, the preview pre, AND the editor textarea to ONE absolute `line-height: 1.3rem` — per-line height is now font-size-independent and identical across all three, and the computed value the search/overlay reads matches the gutter. **Rule for any future gutter/aligned-column UI:** the gutter and the content it indexes must use the same ABSOLUTE line-height (px/rem), kept in lockstep with a comment on each rule; a unitless `line-height` shared between two different font-sizes is a drift bug waiting to happen. Same trap would bite any future "minimap", "diff gutter", or aligned two-column code view.

63. **A self-correcting `--syntax-*` palette: define a color ONCE in `darkBase` as `color-mix(fixed-hue, var(--text-primary))` and every theme inherits a per-theme-correct value with no override (v0.11.2).** Syntax-highlight palettes (the Lich Dashboard YAML/var preview, the Debug panel event types, the Fires log) need ~8 distinct token hues. Hardcoding VS-Code-dark hex (`#9cdcfe` etc.) is the pitfall-#34 light-theme bug at scale — light-blue keys vanish on white. The fix is NOT to add 8 vars × 20 themes; it's to add the 8 `--syntax-key/string/number/literal/comment/meta/type/symbol` vars to `darkBase` ONLY, each as `color-mix(in srgb, <fixed-hue> N%, var(--text-primary))`. Because the nested `var(--text-primary)` resolves in each element's (theme's) context at use time, one definition yields a readable, hue-distinct value on every theme automatically. Consumers just say `color: var(--syntax-key)`. **When you need a palette of fixed-meaning hues that must stay legible across themes, define them once in `darkBase` as color-mix-toward-`--text-primary`, not per-theme.** (Status/semantic single colors follow the same idea via `color-mix(fixed-hue, var(--bg-base/--text-primary))` — pitfall #55.) Related v0.11.2 work: the per-theme `--text-dim`/`--text-faint` ramp was compressed toward `--text-muted` (the dim tail was below readable contrast on several surfaces), and `classicLight` had an inverted ramp (`dim`/`faint` = pure black, more prominent than body text) — a reminder that the 5-level text ramp must be monotonic toward the bg on light themes too.

64. **Per-window state delivered by PUSH-only IPC is race-prone for a freshly-opened window — pull it on mount too (B149, v0.11.2).** Multi-window state (the session roster) is broadcast from main on every change (`broadcastRoster` → `session-roster`), including on a window's `did-finish-load`. But a brand-new window's renderer registers its `onSessionRoster` listener only AFTER React mounts (a `useEffect`), so it LOSES the `did-finish-load` broadcast — and if no further change fires (a stable connected session emits no status events; `session:set-name` only broadcasts on a name *change*), the empty value never self-heals. Symptom: Quick Send (which renders nothing when the cross-window roster is empty) did nothing via Ctrl+Shift+Enter in a decoupled window — the hotkey fired, but `RosterContext.roster` was `[]`. `SessionsContext` never had this bug because it **pulls** owned sessions on mount (`get-owned-sessions`) in addition to reacting to push (`session-acquire/release`); `RosterContext` was left push-only — the oversight. **Fix shape (the rule):** every per-window context that mirrors main-owned state must **PULL the current value on mount AND subscribe to future pushes** — never push-only. Added `get-roster` (returns `buildRoster()`) called in RosterContext's mount effect next to the subscribe. When you add a new cross-window broadcast channel, add its pull-on-mount handler at the same time. (Pull-resolves-after-subscribe ordering is fine: a push arriving between subscribe and pull isn't lost, and a late pull just reasserts the same authoritative list.)

65. **DR carries the room id in the `<streamWindow>` subtitle in TWO formats — parse BOTH, and treat its absence as normal (B151, v0.11.2).** The subtitle (which also carries the room title) can be either `[Whistling Wood, Barrows - 9479]` (id INSIDE the brackets after ` - `) OR `[Arthelun Ruins, Courtyard] (56107)` (title in brackets, id in PARENS after them). The parens form is the StormFront-standard `[Title] (uid)` — it's DR's **Simutronics room-id flag** display, an account setting the player can toggle; `(**)` means "room has no id" (unmapped, e.g. `[The Heavens] (**)` during a Moon Mage telescope flip). Pre-v0.11.2 the parser ([StormFrontParser](src/main/parser/StormFrontParser.ts) streamWindow handler) only parsed the dash form, so flag-on players got `roomId: undefined` → the Lich Map's instant `lichDb.get(roomId)` lookup never fired (forcing the fragile title+desc path — the "location lost during movement" symptom) and `$roomid` was empty. Fix: when the dash form misses, also match `\]\s*\((\d+)\)` after the closing bracket. **Crucial constraint — the flag is OPTIONAL, so id-absence must stay a no-op:** when neither form is present (flag off) OR the id is `(**)`, `roomId` MUST remain `undefined` exactly as before, and the whole downstream pipeline must keep working id-free (MapPanel falls back to `findRoom` title+desc; `$roomid` renders empty; `triggerCtxRef.roomId` only updates when `evt.roomId != null`). Never make a parsed room id load-bearing — it's a bonus signal when present (and the Genie map can't use it at all; its data has no game room ids, pitfall in §19 / B148), never a requirement. The companion id source is `<nav rm='X'/>` (pitfall #46) — when present it agrees with the subtitle; the telescope case shows DR sometimes sends an EMPTY `<nav/>` and relies on the subtitle parens, which is exactly why both subtitle forms must be parsed. **`u`-prefixed parens form (handled since v0.16.1, surfaced during the Lich 5.18 review): when a player has Lich's `display_uid` setting on, Lich REWRITES the native parens form to `[Title] (u12345)` — a `u`-PREFIXED id.** The parens regex is therefore `/\]\s*\(u?(\d+)\)/` (the `u?` is optional, so bare-digit `(56107)` still matches AND `(u12345)` parses to `12345` — the same game uid the native flag emits, so the `lichDb` lookup is identical). Non-numeric parens (`(**)`, `(unknown)`) still miss → `roomId` stays `undefined`, preserving the id-absence no-op. This was PRE-EXISTING (not a 5.18 regression — the `u`-prefix rewrite and the `display_uid` auto-enable at Lich's `xmlparser.rb:501` both predate 5.18); pre-v0.16.1 it silently degraded the Lich Map to the title+desc path for `display_uid`-on players. `display_lichid` instead injects the LICH id INSIDE the brackets as bare digits (`[Title - 500]`), which the dash regex already handles.

66. **Text selection across a virtualized list loses unmounted rows on copy — keep a generous `increaseViewportBy` buffer (B152, v0.11.2).** The story window is react-virtuoso; it mounts only the visible rows plus a small overscan and UNMOUNTS off-screen rows. A browser text selection lives on DOM nodes — when a selected row unmounts (you scroll while drag-selecting), it silently leaves the selection. So the mouseup auto-copy (`getSelection().toString()` → `window.api.writeClipboard` in [GameWindow.tsx](src/renderer/components/GameWindow.tsx)) captures only the rows still mounted at release — "a portion from the end" (the final viewport + overscan). The pragmatic, low-risk fix is `increaseViewportBy={{ top: 3000, bottom: 3000 }}` on the Virtuoso so ~150 rows each side stay mounted, keeping several screenfuls of selection intact through the auto-scroll (rows are memoized + `lines` is capped, so the extra DOM is cheap; scroll math is untouched). **Known ceiling:** a selection scrolling >~3000px past its start still can't be fully captured by a DOM-selection copy — the real ceiling-free fix is to reconstruct the selected text from the `lines` DATA MODEL (capture the anchor line id + char offset at mousedown — before it can unmount — map the focus at mouseup, slice `lines[anchor..focus]`), which was deferred as too intricate for the delicate scroll path. **General rule:** any copy/select/Ctrl-A feature over a virtualized list must either (a) carry a large enough mounted buffer for the expected selection size, or (b) reconstruct from the data model — never trust `getSelection().toString()` to span the full logical list. (Don't "fix" this by un-virtualizing the list — that's the perf foundation of the whole text window.)

67. **A sticky header only lines up with the rows below it if the scroll container reserves the scrollbar gutter — and table rows only stay column-aligned if every row is the same height (v0.11.5, Debug panel).** The Debug panel ([DebugPanel.tsx](src/renderer/components/DebugPanel.tsx) / [debug.css](src/renderer/styles/debug.css)) is a header-over-rows table inside one `overflow-y: auto` scroller (`.debug-scroll`), with the header `position: sticky; top: 0` and rows sharing the SAME `grid-template-columns`. Two traps, both shipped-then-fixed here: **(a)** the header and rows are different children of the scroller, so the moment the list overflows and a vertical scrollbar appears, the *rows* lose scrollbar-width of content box but the *sticky header* doesn't necessarily — columns shift ~15px and stop lining up. Fix: `scrollbar-gutter: stable` on the scroller so the gutter is always reserved and both share one content width. **(b)** the fire-log rows used `align-items: start` + unbounded wrapping (`overflow-wrap: anywhere`) on the matched/detail cells, so a long match made one row 3–4 lines tall while its short cells floated at the top — a ragged, "misaligned" grid even though the templates matched. Fix: `align-items: center` + single-line ellipsis per cell (`white-space:nowrap; overflow:hidden; text-overflow:ellipsis; min-width:0`) with the full value in a `title` tooltip → every row is one line tall and columns read as a real table. **General rule for any header+rows table in a scroll container:** reserve the scrollbar gutter, give the header and rows the *same* column template, and keep rows uniform-height (truncate, don't wrap) unless you deliberately want variable-height rows. Related, same feature: the panel renders in **two modes from one component** — docked bottom strip (`resizable` prop true → drag-handle + per-character height via `scopedKey(character,'debugPanelHeight')`, which round-trips into YAML `state.*` for free per Principle #1) vs. inside a panel zone (`resizable` false → `.debug-panel--fill` fills the zone, no handle, no fixed height). When a component can be hosted in both a fixed-strip and a flex-zone context, gate the fixed/resizable sizing behind a prop rather than baking a height into its root class.

68. **The story window's pin lives in TWO layers — the flex layout must not overflow, AND we (not Virtuoso's `followOutput`) own the bottom-scroll. Both were broken; B153's single-source view was incomplete (v0.11.6, B155).** This is the resolution of a long debugging chain (Sekmeht + Binu) that the earlier B153 (#43) fix only partly addressed. Three distinct bugs, fixed in three distinct places — don't conflate them:
    - **(a) LAYOUT — the flex column overflowed by ~one line (the real "clipped under the vitals bar" root cause).** `.game-main` and `.text-window` ([game.css](src/renderer/styles/game.css)) were `flex: 1` children of flex **columns** but lacked `min-height: 0`. Default `min-height: auto` = content height, so a flex child refuses to shrink below its content and the column runs taller than the window — the bottom (last line / vitals-bar boundary) is clipped. The tell that pinned this after rounds of scroll-math dead ends: **moving the vitals bar top↔bottom shifted the main window by a line** (the overflow flipping ends), and "text feeds beyond its boundary." Fix: `min-height: 0` on both. This is the canonical flexbox scrollable-child trap — **any new flex-column scroll container needs `min-height: 0` on its flex children.** B153 (and B122 before it) treated the clip as purely a scroll-math problem and never found this; with the overflow gone, the clip is gone.
    - **(b) AUTO-FOLLOW — `followOutput` is now OFF; we scroll synchronously.** Virtuoso's `followOutput` aligns the last row to the bottom but UNDER-MEASURES it at fractional heights (the viewport is fractional — vitals/command bars scale by `em`), landing ~one notch short; our correction then snapped it down a frame later — the visible **"scrolled up a notch, then jumps to bottom" two-step** (and once the layout overflow in (a) was fixed, this became visible on EVERY line because the viewport went cleanly fractional). Fix: `followOutput={false}`; when pinned, `totalListHeightChanged` sets `scrollTop = scrollHeight − clientHeight` **SYNCHRONOUSLY (before paint)** so the last line is flush in the same frame the content renders — no short-landing for anything to correct, no jump. A rAF backup only fires if Virtuoso's heights were still settling (estimated rows); if the sync write was exact it's a no-op (no visible motion). `scrollHeight − clientHeight` is the DOM-truth lever (immune to followOutput under-measurement — the one B153 lesson that survived). **Do NOT re-enable `followOutput`** — it and a manual correction will always fight into the two-step.
    - **(c) DISCRETE RELAYOUTS — a settle loop for transitions, a PASSIVE observer for resizes.** Relayouts that reshape the scroller don't settle in one frame (Virtuoso re-measures rows + the flex layout reflows over several frames), so a one-shot correction reads mid-settle. `resnapToBottom()` ([GameWindow.tsx](src/renderer/components/GameWindow.tsx)) re-issues the DOM-truth scroll each frame **until `scrollHeight` stabilizes** (12-frame cap), arming `suppressUntilRef` synchronously and gating on `pinnedRef` throughout (a scrolled-up reader / mid-settle `onWheel` un-pin is never yanked down; rAF tracked in `resnapRafRef`, cancelled on each new resnap + unmount). Wired to the DISCRETE triggers: badge/End (`scrollToBottom`), the font/line-height/large-print effect, an `isActive`-becomes-true effect (tab show — inactive tab is `display:none`→0×0, pitfall #24), a mount-once `window 'focus'`+`document 'visibilitychange'` listener (focus regain after rAF-throttling, guarded `!document.hidden && isActiveRef.current && pinnedRef.current` — Binu's alt-tab case), and **after a replay batch** (`batch.replay === true && pinnedRef.current` in `onGameEvent` — the decouple/split-window path, pitfall #60: a replay fills `lines` in one burst with no follow-up line for `totalListHeightChanged`, and the new BrowserWindow is still settling its first paint; the `isActive` mount effect runs with `lines` empty and the new window's initial focus fires before the listener mounts (B149 race), so this is its own required trigger). **For continuous viewport-height changes** (vitals strip appearing at login — it renders empty ~0px until vitals arrive — compact↔regular toggle, vitals/icon-bar position toggle, window resize) a **`ResizeObserver` on the scroller** re-snaps, but it MUST be passive: gate on an actual integer `clientHeight` change and do a SINGLE bare `scrollTop` write — **never call `resnapToBottom()`/`scrollToIndex()` from it**, because those re-render → nudge the scroller size → re-fire the observer → visible idle **"jumping/scaling fighting itself"** (a ResizeObserver→layout→ResizeObserver loop; the bare `scrollTop` write resizes nothing, so it can't loop).
    - **(d) CONSOLIDATION — `resnapToBottom` + the inline sync+1rAF + the inline ResizeObserver write were merged into ONE primitive `stickToBottom(reindex?)` (v0.11.8, B158).** B155 (b/c above) still left an intermittent "rests one line short, wheel down once to fix it": the per-batch path used a SYNCHRONOUS write + a SINGLE rAF backup, but Virtuoso measures the just-appended last row ASYNCHRONOUSLY (its ResizeObserver fires a frame or two after the row mounts), so `scrollHeight` keeps growing past that single rAF and the correction converged on the short pre-measure bottom. Meanwhile three different correctors (per-batch inline, `resnapToBottom`, the resize-observer write) raced the same target. **`stickToBottom`** ([GameWindow.tsx](src/renderer/components/GameWindow.tsx)) is a self-cancelling rAF **settle loop** that re-asserts `scrollTop = scrollHeight − clientHeight` every frame until `scrollHeight` is **stable for TWO consecutive frames** (or a 20-frame cap) — the two-frame requirement is what guarantees the async measurement grow is always captured. Per-batch (`totalListHeightChanged={() => stickToBottom()}`) keeps the synchronous-first-write-before-paint (flush in the same frame) then runs the loop for the tail; `reindex=true` (badge/End, font change, tab-show, focus-regain, replay) issues ONE `scrollToIndex({index:'LAST'})` first for off-screen targets. The loop writes ONLY `scrollTop` (no re-render), so the scroller `ResizeObserver` now calls `stickToBottom()` directly without the re-render→re-fire loop the old `scrollToIndex` resnap would have caused. Cancel+restart per batch keeps the loop alive across a flood (bottom flush every frame → bursts read as continuous scroll, not chunky jumps). **Trade-off:** during an active flood `suppressUntilRef` is continuously armed, so only `onWheel` (wheel-up) un-pins; scrollbar drag/arrow un-pins once the flood pauses — intended ("stay stuck until the user manually scrolls up").
    - **Rule for the next person:** if the bottom line is *clipped*, suspect LAYOUT first (flex `min-height: 0`, pitfall #43's "scrollable past the API's bottom ⇒ layout not math") — do NOT add more scroll math. If the view *lands short then jumps*, that's `followOutput` fighting a correction — keep it off. If the view *rests one line short intermittently*, that's the async last-row measurement outrunning a one-shot correction — the **settle loop** (`stickToBottom`, two-stable-frames) is the fix, not another bespoke rAF. **There is now exactly ONE auto-scroll primitive — route every new pinned-scroll path through `stickToBottom` (`reindex=true` for a discrete relayout / off-screen target, no-arg for per-batch + continuous resize); do NOT add a fourth mechanism or split it back apart.**

69. **A GameWindow is keyed by `characterId`, NOT `sessionId` — so an in-place reconnect REUSES the same component; per-session local state must reset on the `sessionId` PROP CHANGE, not on a connection-status event (v0.11.6, tab-menu Reconnect).** The session render in [App.tsx](src/renderer/App.tsx) keys each GameWindow `${characterId}:${reloadNonce}`. The tab-menu **Reconnect** ([CharacterTabBar.tsx](src/renderer/components/CharacterTabBar.tsx) → App `handleReconnectTab`) tears down the dead session then re-runs `runConnect`; on success `addSession` REPLACES the record by `characterId` with a fresh `sessionId` (its documented reconnect-in-tab path). Because the key is `characterId`, the GameWindow does **not** remount — it stays mounted and just receives a new `session.sessionId` prop. That's deliberate (preserves scrollback), but it means any per-session LOCAL state in GameWindow (the `dropped`/`disconnecting` flags, and anything gated on them — e.g. `useLichBridge(sessionId, !dropped)`) carries over from the *previous* session and must be explicitly reset. **Do NOT reset it from the `onConnectionStatus` 'Connected' handler** — main emits 'Connected' around when `login()` resolves, which can race AHEAD of the renderer's `sessionIdRef` update, so the event gets filtered out against the stale id (`s.sessionId !== sessionIdRef.current`) and the reset never runs (the v0.11.6 bug: a reconnected tab stayed greyed + Lich bridge stayed off; a remount via "move to another window" looked fine because that IS a fresh mount, which masked it). **The fix is to reset on the `sessionId` change itself** — the effect that maintains `sessionIdRef` compares old vs new and, on an actual change, `setDropped(false)`/`setDisconnecting(false)`. This is deterministic (a React prop-change effect, not a racy IPC event) and a new `sessionId` for the same `characterId` always means a fresh LIVE connection (`addSession` only swaps the id after login succeeds). **General rule:** when a component is keyed by a STABLE identity but receives a CHANGING resource id as a prop (the reconnect-in-place pattern), reset that resource's derived local state on the prop change, and don't depend on an async event whose ordering vs. the prop update isn't guaranteed. Two more notes on the tab menu: it lists only ACTIONABLE options (no greyed rows), ordered window-options-first then the connection toggle LAST below a divider so the destructive Disconnect isn't the top item under the cursor (Binu kept fat-fingering it): "Open in new window" only when >1 char shares the window, "Move to main window" only when `useRoster().isPrimary === false` (a decoupled/secondary window), then Reconnect XOR Disconnect by `status.connected` (divider added only when a window option precedes it). Disconnect uses `window.api.disconnect(sessionId)` DIRECTLY (not the `lichborne:session-action` bridge, which only reaches the *active* GameWindow) so it works on any tab. The "Reconnecting…" spinner is a per-tab indicator driven by an App-owned `reconnectingIds` set (added on reconnect start, removed when `runConnect` settles) — the launcher's connecting overlay isn't visible for a tab reconnect.

70. **Genie Map room-matching mirrors GenieMaps' own client: NAME + EXIT-SET + DESCRIPTION — and the inline `<preset id='roomDesc'>` description must be captured for it to work (v0.11.7, Whistling Wood/Barrows).** Two coupled facts. **(a) DR streams the room description INLINE in `main` as `<preset id='roomDesc'>…</preset>`, NOT as a `<component id='room desc'>`.** The parser tags that segment `preset:'roomdesc'` ([StormFrontParser.ts](src/main/parser/StormFrontParser.ts) preset close) and it flows to the `main` stream — so the ONLY code that wrote `roomState.desc` (the `stream === 'room'` branch in [GameWindow.tsx](src/renderer/components/GameWindow.tsx), fed by `COMPONENT_STREAM['room desc']='room'`) fired only on the rare `<component id='room desc'>` re-emit. Result: `roomState.desc` was empty/stale during normal play, so the Genie matcher's description tiebreaker had nothing to work with and fell through to file order — picking the wrong one of 40+ identically-titled "Whistling Wood, Barrows" nodes. **The fix is renderer-side and batch-ordered:** GameWindow's event loop collects any `preset==='roomdesc'` main-segment text into a batch-local `batchRoomDesc`, then applies `roomUpdates.desc = batchRoomDesc` *after* the loop — so it overrides the B121 `clear-stream 'room'` (`roomUpdates.desc=''`) that lands later in the same batch on a real room entry (the inline desc is emitted BEFORE the `<streamWindow>` that triggers the clear, so an in-loop assignment would be wiped). Do NOT try to fix this in the parser by ordering — the inline-desc-then-streamWindow order makes any emit-time approach race the clear; capture in the renderer and apply last. This also improves the **Lich Map**'s `findRoom` title+desc fallback for free. **(b) The real GenieMaps client (`Node.Compare`/`CardinalCount` in [NodeList.cs](C:/temp/Genie4-Dev/Genie4/Mapper/NodeList.cs)) identifies a room by name + the SET OF EXITS + description, all three** — exits being a strong, always-fresh signal (DR sends the compass every room) that survives a stale description while running. Lichborne ignored exits entirely. Now `roomState.exits` (abbreviated compass tokens) is threaded `GameWindow`/`PanelFrame → MapPanel → GenieMapView` and the pure resolver `resolveGenieRoom` (NOT a memo — see the Architecture note below) resolves an ambiguous title via a ladder: (1) description-EXACT unique over the pool, (2) exit-set EQUALITY unique, (3) description-SUBSTRING unique, (4) graph-adjacency over the FULL pool (preferring an exit-matched neighbour when several qualify), (5) exit-aware conservative-cross-zone-fallback. The exit set excludes only `go`/`climb` arcs (up/down/out DO count — mirrors `CardinalCount`); canonicalize Genie's full words and DR's abbreviations to one token form (`cardinalExitSet`/`liveExitSet`/`exitsEqual`/`exitsSuperset` in [GenieMapView.tsx](src/renderer/components/panels/GenieMapView.tsx)). The two strong signals back each other up — a fresh description nails it outright, the exit set carries it while running — which is what gets the indicator to near-100%. **CRITICAL: exits are used ADDITIVELY, never as a pre-filter that removes candidates before adjacency.** A first cut narrowed the pool by exits before running adjacency; because Genie maps occasionally have imperfect exit data for a room (and DR's live exits can differ for a frame), that EXCLUDED the correct neighbour and broke the adjacency match that worked before exits existed — the marker fell to the conservative hold and stranded ("some room text doesn't match anymore" / "stuck at the Shard healer while hunting elsewhere"). The fix: adjacency runs over the full pool; `exitMatched` only (a) chooses the best blind guess and (b) corroborates a cross-zone move. And the conservative hold (B110) is now **exit-aware** — it COMMITS to the new-zone guess immediately when the guess is exit-corroborated OR when `prev`'s own exits no longer match the live exits (so `prev` provably isn't where you are), and only HOLDS `prev` when exits are unknown or still consistent with `prev`. This kills the false-stall on recall/teleport/walk-through-unmapped while keeping B110's protection against blind file-order teleports. **Architecture (the resolver is now PURE).** The whole thing is a module-level pure function `resolveGenieRoom(pool, normDesc, liveExits, prev, sourceFileToZoneId, staleCount) → { match, staleCount }` (deterministic, no refs, no side effects — testable against real map XML), driven from a SINGLE `useEffect` that owns the breadcrumb (`prevLocationRef`) and the cross-zone hold counter (`staleCountRef`) and advances them in exactly one place after the resolve. The previous design was an impure `useMemo` that **mutated `staleZoneCountRef` and read `prevLocRef` as it computed** — fragile because React may re-invoke a memo and **StrictMode double-invokes it in dev** (`npm start`), so the hold counter double-counted and the cross-zone behavior differed between dev and packaged builds. `currentLocation` is now `useState` set by the effect (one-frame lag, imperceptible for a map). The effect has an **idempotency gate** (`lastResolveRef` compares pool-identity + desc + exits) so StrictMode's double-mount-invoke doesn't advance the refs twice. **Don't move resolution back into a memo, and don't mutate refs mid-render** — pool (the title-matched candidate list) stays a pure memo, but the stateful resolve lives in the effect. `pool`/`roomDesc`/`roomExits` all derive from one `roomState`, so they're coherent per render. **`--stormfront` vs `--genie` is irrelevant** to this: the front-end flag doesn't change the room title/desc/exits we receive; matching on desc+exits works under stormfront. When adding a new map-matching signal, prefer it as another rung in this ladder over replacing the desc/exit conjunction. The Genie map's data has NO Lich/Simu room ids, so it is fundamentally title+desc+exits (the Lich Map matches by roomId first); don't try to add roomId matching to the Genie path.

71. **Critical STATE must not be gated behind `requestAnimationFrame` (or timers) without a hidden-window fallback — Electron throttles/pauses rAF + timers for a minimized/occluded/backgrounded window, which FREEZES any state applied through them (v0.11.7, B157, "idle/minimized loses my map location").** The map indicator kept going stale and stranding on the room the player occupied when the window went idle/minimized — scripted movement didn't move it, but a `LOOK` after restoring the window snapped it to the correct room. Root cause was NOT the matcher: `roomState.title/desc/exits` (everything the matcher reads) is applied through the **rAF room pump** (`roomQueueRef` + `requestAnimationFrame`, pitfall #20), and Electron's `webPreferences.backgroundThrottling` **defaults to `true`**, which throttles rAF and timers to ~1fps (or pauses them) once the window is minimized/occluded. Game events keep arriving over IPC and queue into `roomQueueRef`, but the rAF pump that drains them into `setRoomState` is frozen → room state sticks on the last room → the matcher (correctly) shows the last data it has → `LOOK` after un-minimizing re-runs the pump and snaps. This is a whole CLASS of bug for a client that processes a live stream while backgrounded (multi-session background characters, pitfall #24, are the same shape). **Two-layer fix:** (a) **`backgroundThrottling: false`** on every `BrowserWindow` ([main.ts](src/main/main.ts) `createWindow`) so the renderer stays fully live when minimized — the right call for this app, which is *designed* to keep processing the game stream in the background (socket stays connected; the cost is normal CPU while minimized, which is exactly what a connected MUD client should do); (b) the room pump itself checks **`document.hidden`** and, when hidden, drains the queue **synchronously** (coalesced to the latest state, cancelling any pending rAF) instead of scheduling rAF — so room-state correctness never depends on platform throttling behavior (some OS/Electron versions still pause rAF for a *minimized* window even with throttling off). The visible-window path keeps the one-room-per-frame rAF pump (pitfall #20's anti-skip behavior — which only matters when someone is watching). **General rule: rAF and `setTimeout`/`setInterval` are for COSMETIC pacing (scroll snaps, map animations, smooth camera) — never the sole delivery path for STATE the app must keep current while backgrounded. If you add a new rAF/timer-throttled state pump, gate it on `document.hidden` with a direct-apply fallback, and remember `backgroundThrottling:false` is already keeping the rest of the renderer alive.** When debugging "state goes stale only when the window is idle/minimized/in the background, and a manual refresh fixes it," suspect a throttled rAF/timer feeding that state before suspecting the consumer. **Second instance of this class — the scroll PIN (B191, v0.14.1, Sekmeht):** `stickToBottom`'s rAF settle loop keeps the story window pinned by re-arming `suppressUntilRef` each frame and chasing Virtuoso's async last-row measurement. When the window is OCCLUDED (another app covering Lichborne — NOT minimized; occlusion throttles rAF too), the loop stalls, the +300ms suppress expires before the async measurement grows `scrollHeight`, and the late `scroll` event un-pins the active char via `handleVirtuosoScroll` → a spurious "New Lines" badge that survives the return (`onRefocus` re-snaps only `if (pinnedRef.current)`). Fix wasn't a synchronous drain (like the room pump) but a GUARD: **`handleVirtuosoScroll` returns early when `document.hidden`** — a user can't scroll a hidden window, so any scroll signal there is layout, not intent. The general lesson generalizes: not just "deliver state without rAF while hidden" but also "**don't let rAF-throttle-induced layout events drive user-intent state transitions** (pin/un-pin, focus, selection) while hidden — gate those on `document.hidden` too." Background (`display:none`, 0×0) characters are immune to this whole family by construction; it's an ACTIVE-window-only bug class.

72. **Qt `@Variant` blobs use MINIMAL hex escapes (`\x1`, not `\x01`) — a fixed-width 2-char decoder corrupts every byte after the first single-digit escape (v0.11.9, B159, Frostbite import).** Frostbite stores highlight colours and the `options` bitarray as QSettings-INI `@Variant(...)` byte blobs, byte-escaped like `\0\0\0\x43\x1\xff\xff…`. The shared decoder `parseQtEscapes` ([colorUtils.ts](src/renderer/import/colorUtils.ts)) read **two** chars after `\x` (`s.slice(i+1,i+3)`) and advanced by a fixed **3** — but Qt writes the *minimal* hex for a byte, so a value of 1 is `\x1` (ONE digit), not `\x01`. On a single-digit escape the 2-char slice grabbed the digit **plus the next `\`**, `parseInt` stopped at the `\` (so the VALUE was right, 1), but `i += 3` **overshot past that `\`**, desyncing every subsequent byte. The colour spec byte is `\x1`, so EVERY colour blob desynced right after it → wrong R/G/B (non-null, so it silently produced wrong colours, never an error — undetected since the Frostbite importer first shipped). It only became visible when the v0.11.9 options-decode used the same `parseQtEscapes` to read the bitarray data byte: the desync frequently set bit 3 (`0x08` = "match groups") → the parser flagged most rows `partial` ("a lot of options showing partial" — the reporter's exact symptom). **Fix:** read **1–2 hex digits greedily** (stop at the first non-hex) and advance by however many were consumed — `i++` past `x`, then `while (len<2 && isHex(s[i])) consume`. This is faithful to Qt's own greedy-hex reader and fixes BOTH the colour corruption and the options byte. **General rule: when hand-decoding any escaped byte stream, never assume fixed-width escapes — Qt (and C, and many INI/serializers) emit minimal-width hex/octal, so consume variable-width and advance by the actual count.** The tell for this bug class: values that *parse to plausible-but-wrong* results (not crashes), and corruption that starts right after the first short escape. Verified by bundling the real parser (esbuild) against the reporter's actual file rather than trusting a reimplementation — do that for any "is the parse correct?" question (the pitfall #61b / Iron-rule lesson: check against parsed data).

73. **A Qt QSettings INI array's `size=N` is UNRELIABLE — iterate the indices actually present, not `1..size` (v0.12.0, mute/sub import audit).** Qt writes an array section as `size=N` plus indexed `N\field` keys, and every Frostbite parser here originally looped `for (i=1; i<=size; i++)`. But real exports carry a STALE size: storm-gray `substitutes.ini` declares `size=88` yet contains a fully-formed entry `89\…` (a Main-target sub) — the size-loop **silently dropped entry 89** (and would drop any entry past a stale `size`, or mis-handle a gap). The Iron-rule check (bundle the real parser, count against the file) caught it: 88 parsed vs 89 present. **Fix:** `arrayIndices(section, field)` ([frostbite.ts](src/renderer/import/parsers/frostbite.ts)) scans the section's keys for `^(\d+)\\<field>$` and returns the sorted distinct indices — robust to a wrong `size` AND to index gaps. Applied to **all three** Frostbite array sections (highlights `\value`, ignores `\pattern`, substitutes `\pattern`). The empty-pattern guard (`if (!pattern.trim()) continue`) still skips blanks. **General rule: never trust a serialized count field as the loop bound when the actual records are independently keyed — derive the bound from the records present.** Same class as #72 (don't trust the format's own framing; verify against the bytes). NOTE: editing a source file with a stray null byte via a `latin1` read/write round-trip **double-encodes every multibyte UTF-8 char** (`─`→`â…`) — strip null bytes at the Buffer level (`buf.filter(b=>b!==0)`); to UNDO a latin1-corruption, `Buffer.from(readUtf8, 'latin1')` recovers the original bytes.

74. **Floating-window layout (Windowed Panels / Free Layout, §33) has four geometry traps — all resolved by making geometry DETERMINISTIC + EXPLICIT, recorded after a long tester-iteration (v0.13.0, Sekmeht). Don't reinvent the dead ends.**
   - **(a) NEVER `height: undefined` (auto-height) on a window.** Chrome bars (command/vitals/icon) were first auto-height so the window hugged the bar — it LOOKED right but an auto-height window measures **0-tall on first paint** and only gets real height on a later re-measure, so the icon bar rendered invisible after conversion, then "popped in" at full height and **shoved the whole layout the first time the user clicked anything**. Fix: FIXED height (the conversion bakes `bar + BORDER_PX` into `rect.h` — the measured bar plus the window's own 2px border, since `box-sizing: border-box` folds it into the stored height; it was `bar + TITLE_PX` until v0.18.1, when the header became an overlay that reserves nothing); user-resize changes that EXPLICIT value, which is fine. A window's height must always be a concrete px/fraction, never browser-computed.
   - **(b) `parseFloat(el.style.x) || fallback` reverts a window snapped to the top/left edge (position 0).** Drag/resize write px to `el.style` during the gesture and commit on mouseup by reading them back; `parseFloat("0px") || start` → `0` is falsy → commits the STALE `start`, not 0. So a window flush at the top/left container edge looked docked but its stored rect was wrong, and the next re-render (e.g. starting a resize) "jumped it out." Fix: a 0-safe `numPx()` (`Number.isFinite` guard) in [FloatingWindow.tsx](src/renderer/components/FloatingWindow.tsx). **Any `parseFloat(coord) || fallback` over a position/size is this bug.**
   - **(c) Conversion is a CASCADE, not pixel-for-pixel.** Recreating the panels layout exactly (measure each zone/strip, mint a window there) fights forever — titled windows are bigger than edge-to-edge strips, so they overlap and "scale weirdly" (the bottom command bar constricted the whole layout via a de-overlap/main-shrink pass). Abandoned for `buildWindowsFromCurrentLayout` = default sizes at staggered positions (only chrome HEIGHTS measured). Don't re-attempt measure-and-mint.
   - **(d) Resize handles sit INSIDE the window edges (inset 0), not straddling at negative insets.** With `-3px` insets + the layer's `overflow:hidden`, a window flush at the layer edge had its top/side handles clipped → unreachable ("can't grab the top to resize/move"). Inset-0 handles are always within the layer; title-bar buttons carry `z-index:1` so corner handles don't block them.
   **Meta-lesson:** floating-window geometry breeds emergent layout bugs you can't predict by reading code — the fix is always to make the geometry *deterministic and explicit* (fixed heights, 0-safe coordinate parsing, default-size conversion), not to lean on the browser to compute it.

75. **Map structural colors are force-cascaded from the general palette — a per-theme `--map-*` structural literal is INERT — and overlay text in a tight box must pin `line-height: 1` (B161 / B162, v0.13.1).** Two map/theme traps found together. **(a) The Theme Editor only edits the GENERAL vars** (`--bg-app`, `--text-*`, …), never the specialty `--map-*` set. For years `darkBase` left `--map-bg: var(--bg-app)` cascading, but every OTHER built-in theme PINNED `--map-bg` (and the rest of the map palette) to literals — so editing a built-in theme's App Background moved every surface EXCEPT the map (only the stock **Dark** theme's map followed, because it alone left the vars cascading — that's the diagnostic tell Binu spotted). **Fix: the 12 STRUCTURAL map colors live in `MAP_STRUCTURAL_CASCADE` ([themes.ts](src/renderer/themes.ts)) — one block wired to general vars (`--map-bg`→`var(--bg-app)`, `--map-text`→`var(--text-secondary)`, chrome/border/node-fill/node-stroke/dot→their bg/border/text counterparts) — spread into `darkBase` AND re-applied LAST in BOTH `applyTheme`/`applyCustomTheme`, so a theme's pinned literal can NEVER win.** Consequence: **a structural `--map-*` override in a theme object is now dead code** (force-applied over it); the ~18 built-in themes still carry their old literals (harmless, superseded) — don't add new ones, and don't be confused that they're ignored. Only the SEMANTIC cues stay per-theme and overridable: `--map-arc-*` (directional path lines — need per-background hue/legibility), `--map-current-color` (green you-are-here marker), `--map-select-color`. When adding a new map color: surface/text/border → add it to `MAP_STRUCTURAL_CASCADE` as a `var(--general)`; semantic cue → leave it per-theme. The Lich Map's baked **PNG tiles** are NOT themable this way (image pixels, separate concern). This is the pitfall #34 family (literal that should cascade), but enforced centrally rather than per-theme. **(b) Overlay text centered in a TIGHT box must pin `line-height: 1`.** `.vital-text` ([vitalsbar.css](src/renderer/styles/vitalsbar.css)) had no `line-height`, so it inherited the game-area's (~1.4). The roomy non-compact bar absorbs the extra leading, but the compact bar (`1em`) can't — the taller line-box overflowed it and the baseline shoved the caps/digits optically high ("M: 100%" sat toward the top). Flex `align-items: center` centers the LINE BOX, not the glyphs, so inherited leading on a no-descender string reads as "too high" the moment the container is short. Rule: any absolutely-positioned, flex-centered text overlay on a short bar/chip should set `line-height: 1` so it centers on its own glyph box rather than an inherited multiplier. **(c) The structural cascade only helps things that USE the vars — an OVERLAY drawn over the map is invisible to it (B240, v0.18.1).** `.map-overlay` carried a baked `rgba(10,8,4,0.75)`, so on light themes the map canvas was dark brown while loading and while "waiting for game data", then snapped correct the instant a zone painted — because the SVG *underneath* does use `var(--map-bg)`. **That "wrong until the content renders, right afterwards" signature means a SCRIM/placeholder/empty-state is unthemed while the real content is fine** — look at what's painted ON TOP, not at the content. One rule served four states (Genie loading, Genie waiting, the Lich map-image loader, MapPanel's loader), so a single literal made all four dark-only. Veils should be `color-mix(in srgb, var(--map-bg) N%, transparent)`. **Note map-panel.css still has ~23 such literals** (error overlay, detail panel, further status chips) — expect light-theme reports from them until they're converted.

76. **The Lich Scripts `;listall` auto-poll is gated on a Lich Scripts panel being OPEN — anything Lichborne injects is indistinguishable from typed input to Lich's scripts, so don't poll when nobody's watching (B163, Binu v0.13.1).** The running-scripts list is refreshed by injecting `;listall` from `useLichBridge` ([useLichBridge.ts](src/renderer/hooks/useLichBridge.ts)) every 5s. It was gated only on `connected` and `useLichBridge` is mounted per-session in GameWindow, so it polled for EVERY connected character even with no scripts panel visible. **Why that's harmful, not just wasteful:** Lichborne and Lich share ONE front-end socket, and Lich's `do_client` runs `UpstreamHook.run(client_string)` on every client line BEFORE it checks for `;` commands ([global_defs.rb:2225](C:/temp/lich-dev/lich-5/lib/global_defs.rb#L2225)) — so a script with an upstream hook (dr-scripts automap) sees our `;listall` **byte-for-byte as if the player typed it**. Binu's automap, which records the last command to associate with a room transition, captured a `;listall` that pulsed at the same instant as his movement. **There is NO way to inject a "synthetic" command Lich's scripts won't see** (one socket, no side channel, hooks run first) — the only fix is to not send it. **Fix (Idea A):** a `lichScriptsOpen` memo in GameWindow (a `lichScripts` tab present in an added zone in Static-Panels mode, or in a floating window's tabs in Windowed mode) drives `lichPollRef`; the poll interval reads the ref and injects nothing while closed, and the open transition fires one immediate seed poll (`refresh()`). Panel closed → zero `;listall`. **Don't un-gate this** (e.g. "the panel feels stale on open" — the seed handles that) or move the poll somewhere it runs unconditionally again. The RESPONSE-side intercept (`interceptLine`, pitfall #22) is unchanged — a player-typed `;list` still updates the panel and its output still shows. `useLichBridge` feeds ONLY the `lichScripts` PanelFrame tab (the Lich Dashboard sources scripts separately), so the panel-open check is the complete signal. The "listen to Lich's `active.`/`has exited.` notices to avoid polling even with the panel open" idea was explicitly **deferred** (Sekmeht, don't build it without a fresh ask).

77. **A panel header's BAR HEIGHT, not just its text size, makes it look "giant" — pin the title `line-height: 1` and keep action icons from driving the strip (B164, v0.13.1).** The Lich Scripts "ACTIVE SCRIPTS" header (`.sl-header-title`) shipped at `1.04em` (larger than the game font); shrinking the text to the section-label scale (`0.84em`, cf. `.room-panel-section-label` `0.88em`, with `em` not `px` letter-spacing so it scales) was only HALF the fix. The strip still read 2-3x the text height because the title had **no `line-height`**, so it inherited the game line-height (~1.4-1.5) and ballooned the bar — the **exact pitfall #75(b) trap** (a tight container can't absorb inherited leading). Fix: `line-height: 1` on the title, trim the bar padding (`6px → 2px` vertical), and stop any action icon (the refresh `↻`) from being the tallest child — `font-size: 0.9em`, `line-height: 1`, no vertical padding. The Scripts panel's FOOTER (`.sl-footer`, the "N scripts · …" status strip) had the same disease — `0.96em` text, NO `line-height` — and was actually the taller strip; same fix (`line-height: 1`, smaller metadata font `0.8em`, trimmed padding). **For any panel chrome strip — header OR footer/status: size the text in `em` off the panel root (anchored to `var(--panel-font-size, var(--game-font-size))`, pitfall #58), pin `line-height: 1`, keep buttons/icons at ≤1em with `line-height: 1`, and use minimal (2-3px) vertical padding.** Two smells: a strip's text rendered *bigger* than the body it relates to, OR a strip *bar* much taller than its own text.

78. **DR does NOT push `<right>`/`<left>` hand XML for every action that puts an item in a hand — the parser carries a glance-TEXT fallback; keep its suppression rules intact when touching it (B165, v0.13.2).** JadedSoul's "held item shows as Empty in the hand bar" (right hand more often): the entire in-app path was verified clean against the real parser (bundled `StormFrontParser` fed the exact item incl. quotes/entities/1000-char names — all well-formed shapes parse; renderer/IconBar/replay-snapshot/interceptLine checked too), so the update either never arrived well-formed or was never sent — and the protocol-level gap is REAL: Profanity hard-codes the same inference ([game_text_processor.rb](file:///c:/temp/Profanity/ProfanityFE/lib/game_text_processor.rb), "You glance down at your empty hands." → force both Empty) because custom-verb/event items can land in a hand with no hand tag emitted. **The fallback:** `inferHandsFromGlance` ([StormFrontParser.ts](src/main/parser/StormFrontParser.ts)), called once per `parse()` after the token loop, derives hand events from glance text: the both-hands form, the single-hand forms (the OTHER hand is inferred `Empty` — glance always reports complete state), and the empty-hands form; leading articles stripped (`a bamboo folder…` → `bamboo folder…`) so inferred names read like tag-driven ones. **Five suppression rules, all load-bearing — don't weaken them:** (1) skipped when the same line already emitted a real `hand` event (XML stays authoritative; checked AFTER the token loop, which is why the call site sits after `flushSegments`); (2) never runs when `activeStream !== 'main'` / `streamStack` non-empty / the raw line contains a push/popStream tag (a Lich script echoing glance-shaped text into a panel must not touch the hand bars); (3) patterns are `^`-anchored on the tag-stripped line so quoted speech (`You say, "You are holding…"`) can't match; (4) a cheap `includes()` substring gate runs before any regex/strip work (this fires on every game line — keep the hot path cheap); (5) the single-hand captures reject anything containing another `' in your '` phrase — an unmodeled sentence shape (e.g. a hypothetical left-first ordering) must become a safe NO-OP, not a greedy mis-assignment of the whole tail into one hand. Minor accepted behavior: GameWindow calls `processVariableChange('right'/'left', …)` on every hand event without comparing old value (pre-existing — DR re-sends hand tags on no-change occasions too), so a `$right`/`$left` variable-watch trigger also fires on glance; cooldowns are the user-side lever. Known cosmetic quirk: an inferred name is the glance long form (full tap) vs the tag's short form; the next real tag corrects it. **Support takeaway:** `glance` is now the universal hand-bar re-sync — if a tester's hand bar is wrong AFTER a glance on v0.13.2+, that's a new signal worth a Debug-panel raw-XML capture. The remount-without-replay hole was closed in v0.13.3 (`session:reload` now arms a pitfall-#60 replay, restoring hands and all sticky state) — found while chasing JadedSoul's report, though her clarification (LB restarted several times between import and symptom — a remount desync can't survive a restart) means her actual sightings were most likely the PROTOCOL gap this fallback covers, not the remount. On v0.13.3+ the one remaining signal is a wrong hand bar AFTER a *glance* — that's a new protocol shape worth a Debug raw-XML capture (notably: capture a LOGIN with an item already in hand, to learn whether DR sends hand tags in the login burst at all).

79. **Every "which tabs/streams are visible/watched" aggregation in GameWindow MUST branch on `layoutMode` — the zone arrays are STALE STATE in free mode (B167/B168, v0.13.2).** In Windowed Panels the zone skeleton isn't rendered, but the zone tab arrays (`mainTopTabs`/`topTabs`/`midTabs`/`bottomTabs` + active ids + `*Added` flags) keep their last panels-mode values. Any aggregation built only from zones is therefore wrong in free mode, in BOTH directions: it misses tabs that live in floating windows, and it counts zone tabs that aren't on screen. Three real hits: **(a) `activeIdsRef`** (unread-dot suppression) was zone-only → in free mode the Arrivals dot re-lit even while its tab was visible, and nothing ever cleared it because **(b)** `clearUnread` was wired only to the zone `onActive` handlers — the floating-window `onActiveChange` (in `renderFreeContent`) now clears too (B167, Sekmeht — "unread indicator stays lit in windowed mode"). The same audit found `mainTopActiveId` had been missing from `activeIdsRef` since the Main-Top zone shipped (active Main-Top tab re-marked itself unread). **(c) `watchedStreamsRef`** (STREAM_FALLBACK routing, pitfall #39) was zone-only → in free mode a stream watched only by a window DOUBLE-displayed (fallback also routed it to main), and a stream still in a stale zone array but closed in windows had its fallback BLOCKED — content silently buffered, invisible (B168, found by audit, unreported). **The pattern (use it for any new aggregation):** `layoutMode === 'free' ? <derive from freeWindows[].tabs / freeWindows[].activeId> : <derive from zones gated on *Added>` — exactly what `lichScriptsOpen` (pitfall #76) and `debugOpen` (B166) already do; include `layoutMode` + `freeWindows` in the effect/memo deps. In free mode "visible" = every window's `activeId` (all windows are on screen at once) and "watched" = every tab of every `kind:'panel'` window. **When adding any new per-tab feature (badges, counters, routing, polling gates), grep for these four sites and mirror the branch** — and conversely, do NOT "fix" the stale zone arrays by syncing them to windows; they're deliberately preserved so switching back to Static Panels restores the old layout.

80. **Lich's default `inventory_boxes_off` downstream hook EATS `<right>`/`<left>` hand tags with a greedy regex — Lichborne disarms it by sending Wrayth's `_flag Display Inventory Boxes 1` once per Lich session; don't remove that send, and don't send it on direct-SGE (B169, v0.13.3 — the TRUE root cause of B165).** Lich installs the hook BY DEFAULT for stormfront-style FEs ([main.rb:546](C:/temp/lich-dev/lich-5/lib/main/main.rb)): any server line **starting** with `<container`/`<clearContainer`/`<exposeContainer` gets `gsub(/<(?:container|clearContainer|exposeContainer)[^>]*>|<inv.+\/inv>/, '')` — and the greedy `<inv.+\/inv>` spans from the FIRST `<inv` to the LAST `/inv>` on the line, swallowing anything between two inv blocks, including a hand tag (the exact shape DR emits on a GET from a container when the player's game-side "display all inventory and container windows" account flag is on). Wrayth disarms it at bootstrap with `_flag Display Inventory Boxes 1` (Lich's upstream hook consumes the command — it NEVER reaches DR — removes the eater, and persists player_id in lich.db3 so [xmlparser.rb:604](C:/temp/lich-dev/lich-5/lib/common/xmlparser.rb) auto-removes it at every future login). Lichborne mimics that: [main.ts](src/main/main.ts) `wireSession` sends it ONCE on the first `player-info` event (the `<app>` tag — arrives AFTER `<playerID>`, so the persistence write has a real id), gated on `s.meta?.useLich` + the one-shot `s.invBoxesFixSent`. **Never send it on a direct-SGE connection** — with no Lich to intercept, it goes to DR and flips the player's REAL account flag (changing their Wrayth experience); direct connections have no hook and no bug. The newly-flowing container XML is fully absorbed by the parser (`container`/`clearcontainer`/`exposecontainer` in SILENT_TAGS, `<inv>` capture-discarded, pushStream `inv` deliberately has no main fallback) — zero display change. **Debugging legacy:** this hid as B165's "random items" hand desync (container gets land in the free/right hand → right-hand bias; with the account flag on, the LOGIN dump also rides container lines → "persists through restarts"); only testers with the game-side flag on can reproduce. The parser itself was proven clean by replaying Frostbite's real captured session (`C:/temp/Frostbite-Dev/frostbite/support/mock.xml`, 1225 lines incl. login hand tags) — when a tag-level loss looks impossible in-parser, suspect LICH'S DOWNSTREAM HOOKS next (grep `DownstreamHook.add` in lich-5; `textsubs` and friends rewrite the same stream). The glance fallback (pitfall #78) stays as defense-in-depth for genuine protocol gaps (spell-summoned items).

81. **Scrollback cap-trims use HYSTERESIS via `appendTrimmed()` — never re-introduce a per-batch `slice(-MAX_LINES)` head-trim, and don't reach for Virtuoso's `firstItemIndex` (B171, v0.13.4).** The long-untraceable "text hops at the bottom after a while / pinned hold doesn't last / scroll gets less smooth" was NOT the scroll machinery (pitfall #68's `stickToBottom` architecture is sound) — it was the **MAX_LINES cap**, which is why it only appeared after ~2000 lines and survived every scroll rewrite. Holding `lines` at exactly the cap by trimming the head every batch breaks the pinned hold via two coupled mechanisms (both measured in a post-paint simulation harness replicating GameWindow's machinery verbatim): **(a)** react-virtuoso keys its row-size/offset cache **by INDEX** — `computeItemKey` only stabilizes React DOM reuse, not the measurement cache (verified in the library source) — so an uncompensated head-trim shifts every row's index per batch, staling the cache against variable-height rows; painted content jumps BACKWARD up to ~8 lines (26 jumps/12s at-cap vs zero pre-cap). **(b)** With count constant at the cap (trim N + append N), total list height is ~unchanged, so `totalListHeightChanged` — the ONLY per-batch `stickToBottom` trigger — frequently doesn't fire (~2.0 calls/batch pre-cap → 0.73 at-cap): no correction, no suppress window armed, and a late re-measure trips the 40px deadband → un-pin. **The fix is `appendTrimmed(prev, added)`** ([GameWindow.tsx](src/renderer/components/GameWindow.tsx)): grow freely to `MAX_LINES + TRIM_CHUNK` (2400), then ONE cut back to 2000 — between trims the count grows every batch so the height callback keeps firing, and the rare big cut changes height so much the callback always fires and the existing sync-write + settle loop absorbs it in-frame (0 hops in the harness). ALL `setLines` appends route through this one helper (game batches, hard-cap resume, End/badge re-pin, command echoes — echoes used to micro-shift the view on every keypress at cap). **`firstItemIndex` was tested and rejected** — Virtuoso's compensating `scrollBy` races our raw `scrollTop` writes (more off-bottom painted frames in both tested variants); it's the right tool only for lists where Virtuoso owns the scrolling, which ours deliberately isn't (pitfall #68). Selection-copy (B152) is unaffected: the un-pinned path (where users scroll up and drag-select) never trimmed and still doesn't; pinned trims remove only the oldest head lines, now later and far less often. **General rules:** (a) a capped virtualized list must trim in CHUNKS (hysteresis), never per-append at the exact cap; (b) any new bounded buffer rendered by Virtuoso gets the same shape; (c) when "the view misbehaves only after a long session," suspect the buffer cap engaging before suspecting the scroll math — pre-cap vs at-cap is the diagnostic split (repro harness: `tmp-scroll-repro/`, disposable; `node tmp-scroll-repro/build.mjs; npx electron tmp-scroll-repro/main.js`).

82. **The per-line rule-evaluation hot path has FOUR load-bearing perf structures — keep all four when touching rules, panels, or the event pipeline (B172, v0.13.4; "chunky travel" with a 1,544-highlight imported ruleset).** Real user rulesets are huge and regex-heavy (imports: Genie triggers are always regex, Frostbite alternations become regex), so per-line work is multiplied by rules × segments × panels × batches. The four structures, and the rules they impose:
    - **(a) Every rule-evaluation site gates on `fastLower` first** (mutes and substitutes were the two that did NOT until v0.18.0 — substitutes ran the whole rule list per SEGMENT *and* again over the joined line, ~27µs/line for a 90-rule import, ~9x what the gate costs; both now carry `fastLower` with one lowercase per line) (`lower.includes(cr.fastLower)` before `regex.exec`). `fastLower` comes from ONE decider — `literalGate(mode, pattern)` in [regexLiteral.ts](src/renderer/regexLiteral.ts) — used by BOTH compile sites (`useCompiledHighlights`, `useTriggerEngine`). Regex-mode rules get `extractRegexLiteral`: a CONSERVATIVE walker returning a literal **guaranteed present in any match** (null on top-level alternation; optional atoms excluded; required groups mined recursively; `\xHH`/`\uHHHH` decoded). **Invariant: a wrong literal silently kills a rule; a missed literal only costs speed — when editing the walker, always err toward returning null**, and re-run the `tmp-perf/check-literals.mjs` validation (bundles the real extractor against the real profile, asserts zero gate-vs-regex disagreements). Text-mode gates on the LONGEST TOKEN, not the whole pattern (tokens compile joined by `\s+`, so the full string isn't a guaranteed substring — that was a latent over-gating bug). New rule-evaluation loops MUST copy the gate.
    - **(b) Line-wide match scanning runs ONCE per line** via `computeLineMatchRanges` ([renderSegmentFull.tsx](src/renderer/utils/renderSegmentFull.tsx)); per-segment `renderSegmentFull` calls receive the shared ranges and just intersect (TextLineRow, RoomPanel sections). Don't add a render path that calls `renderSegmentFull` per segment WITHOUT precomputed ranges on multi-segment content — that re-runs the whole ruleset × segment count (the pre-B172 shape).
    - **(c) Panels are memoized; their props must stay referentially stable.** StreamPanel/RoomPanel/ExpPanel/MapPanel are `memo()` — so PanelFrame's `renderPanel` uses module-level `EMPTY_ARRAY`/`EMPTY_SET`/`NOOP` fallbacks (never inline `?? []`/`?? new Set()`/`?? (() => {})`), StreamPanel's `onClear`/`onToggleTimestamp` take the `streamId` as an argument (no per-render `() => f(id)` closures), and the GameWindow callbacks that feed panels are `useCallback`. **The two context provider values (`HighlightsContext`, `ContactsContext`) are `useMemo`'d in GameWindow — never revert them to inline `value={{...}}` literals** (that re-renders every consumer on every render and silently defeats ALL panel memos; it's invisible until a big ruleset makes it expensive). When adding a panel: memo it, check every prop's identity, and remember a consumed context value is part of that check.
    - **(d) Main coalesces event floods** — `scheduleFlush` ([main.ts](src/main/main.ts)) is a leading-edge throttle (`FLUSH_COALESCE_MS` 16ms): immediate when idle, ~one batch per frame during a Lich flood, so the renderer renders once per frame instead of once per socket chunk. Don't revert to bare `setImmediate`, don't make it trailing-only (adds latency to sparse interactive lines), and don't lower it below a frame.
    Diagnostic split for "the client feels chunky during floods": measure whether the cost is per-BATCH (too many renders → d), per-LINE (rule evaluation → a), per-SEGMENT (scan multiplier → b), or per-RENDER (tree breadth → c). The profile-driven numbers that motivated this are in BUGS.md B172.

83. **Never gate the MOUNTED-NESS of stateful children on a live-measured size — a hidden character tab measures 0×0, and "size is 0" must mean "don't paint geometry yet," never "unmount" (B174, v0.13.4).** WindowLayer rendered its FloatingWindows behind `{size.w > 0 && size.h > 0 && ...}` with `size` fed raw from a ResizeObserver. Correct-looking — until a character tab goes `display:none` (pitfall #24): the layer measures 0×0, the observer pushes it, and React UNMOUNTS every floating window of the hidden character. Every tab switch silently destroyed and rebuilt the hidden character's whole windowed UI (map reloading the Lich DB + Genie zones, Virtuoso rebuilding the text — the "everything redraws / blips" report). The pattern that fixes it: **the measure callback ignores 0×0 and keeps the LAST REAL size**, so the render gate degenerates to a first-mount-only "never measured yet" guard; stale px geometry while hidden is harmless (the ancestor is display:none — nothing paints) and the observer re-fires with the true size on re-show. When adding any container that converts fractional/relative child geometry via a measured size (an overlay layer, a future Experiences layer §34, a minimap), apply the same shape. **Companion trap (same bug):** an effect like `useEffect(() => { if (version === 0) return; reload() }, [version])` re-fires on every REMOUNT once version ≥ 1, doubling the mount-time load — guard "changed since mount" with a ref initialized to the mount-time value (`lastRef = useRef(version)`), not with a `=== initialValue` check. **Debugging lesson:** when a remount is observed but every key/provider/conditional reads stable, stop theorizing and drop temporary mount/unmount `console.log`s at the suspect levels — one reproduction beat several hours of static analysis here (the `[diag]` pattern: GameWindow/panel mount+unmount, replay delivery, loader starts).

84. **Constant-size UI must NOT render inside a transform-scaled (or em-rescaled) subtree — counter-scaling it in place is whack-a-mole; render it in a sibling PIXEL-SPACE layer instead (B182, v0.14.0, the Tableau bubble layer).** The Living Tableau scales its figures two ways (a per-figure `transform: scale(--fig-sc)` for depth, plus an `.tableau-scene--large` em-rescale for the amphitheater), and speech bubbles originally rendered INSIDE the figure divs — so they inherited every scaling layer, and each "make the text bigger" fix (em bumps, absolute `var(--game-font-size)`, an inline `scale(1/figScale)` counter-transform) fixed one inheritance path while another kept shrinking them ("bubbles still tiny at >12 people"). The durable fix: bubbles moved OUT of the figure tree into a flat layer in scene-PIXEL space — the component measures its container with a ResizeObserver (**0×0-guarded, pitfall #83** — hidden tabs measure 0×0) and positions each bubble itself, which makes constant sizing true BY CONSTRUCTION and unlocks what in-figure rendering never could: **collision-aware placement** (newest bubble takes the spot nearest its speaker; earlier ones get pushed up; nothing overlaps) with the tail offset (`--tail-dx`) still aiming at the speaker and the speaker's NAME inside the bubble for when it had to drift. Layout-geometry ESTIMATES (char-count × font-size) are fine for SPACING — CSS still does the real text wrapping; a generous estimate is just generous spacing — don't reach for measure-then-relayout passes. When any future surface needs "this child stays readable regardless of how its parent scales" (HUD readouts over the Spar Arena figures, radar labels, X6 captions), start with the sibling-layer pattern, not a counter-transform.

85. **Two auto-detection traps from the B184 guild-badging saga (v0.14.0) — both bit a real tester within the hour.** (a) **A stored preference that rides `TRANSFER_CATEGORIES` is NOT proof of an explicit user choice for THIS character** — Profile Transfer copies the raw scopedKey, so a transferred value is byte-identical to a hand-picked one. The exp panel's Badging first shipped with "stored `focus` wins forever over guild detection," and Sekmeht's cleric was permanently stuck on a Moon Mage badging that Transfer's viewPrefs category had planted — his corrective `info` was *correctly ignored* by the broken rule. The fix: fresh authoritative GAME data (the character's own `info` sheet) OVERRIDES the stored value (`handleFocusChange`, so it persists); a manual pick holds only until the next own-sheet detection. **When auto-detection and a stored preference conflict, ask whether the stored value could have arrived via Transfer/import before granting it permanent supremacy** — "explicit wins forever" is only sound for keys that can ONLY be written by direct user action on that character. (b) **DR's `info` sheet Name field is the FULL TITLED name** — `Name: Soul Reaver Cordio Hawt-Seord, Divine Hammer of Elanthia   Race: Dwarf   Guild: Cleric` — titles BEFORE the first name, hyphenated surname + comma honorific after. Any "is this sheet about our character" check must match the character name as a whole WORD anywhere in the field (escaped, case-insensitive); a first-token compare rejects the player's OWN sheet. Related verbatim-mirroring detail: Lich's `NameRaceGuild` regex (drparser.rb:10) uses GREEDY captures with a trailing `\b\s+` anchor — a lazy port captures "Moon" instead of "Moon Mage". The titled line is a permanent harness case; the `character-guild` event is deliberately NOT behind the §35.6 Experience gate (badging works without Experiences), and `detectedGuild` is deliberately NOT transferable (guild is character identity).

86. **Every command-send closure in GameWindow MUST send through `sessionIdRef.current`, never a captured `session.sessionId` — AND a build + tsc pass does NOT prove a side-effecting call still exists (B189/B190 + a self-inflicted regression, v0.14.1).** Two intertwined lessons from the Morress (macros) / Sekmeht (automapper) "command echoes but nothing happens, and no `>` prompt comes back" reports. **(a) The architecture rule.** A GameWindow is keyed by `characterId`, and a tab Reconnect swaps in a NEW `sessionId` WITHOUT remounting (pitfall #69). So any send closure that *captures* `session.sessionId` goes stale after a reconnect — main's `SEND_COMMAND` handler does `getSession(sessionId)` and **silently `return`s if it misses**, so the command is dropped while the renderer-side echo (`setLines`) still paints `>cmd`. The held-stale closures were: the macro keydown handler (mounted ONCE, `[]` deps — calls `sendCommandSequence` directly), the `sendCommand` `useCallback` (`[]` deps — drives map-walk / room-exit / Quick Send), and the `triggerCallbacks` `useMemo`. Typed commands never broke because the command-bar handler is re-created every render with the live id. **Fix: route ALL of them through `sessionIdRef.current`** (a ref synced every render at the `[session.sessionId]` effect, line ~288) — it reads the live id even from a first-render closure. When you add ANY new command-send path, send via `sessionIdRef.current`, not `session.sessionId`. The diagnostic signature for this whole class: *a command echoes but the game never responds and no prompt returns, while typed commands still work* → a stale session id in a held closure. **(b) The process lesson (the actual time-sink here).** While ADDING the explanatory comment to `sendCommand`, an Edit **replaced the `window.api.sendCommand(...)` line with the comment text** instead of putting the comment above it — leaving the echo but NO send. **`npm run build:renderer` AND `npx tsc --noEmit` both passed**, because a *deleted side-effecting statement* is still perfectly valid code (unlike the dangling-reference case Check-zero already catches). It shipped to the tester and made the bug WORSE (before: dropped only after reconnect; after: never sent at all). The "I verified the fix is in place" grep confirmed the *comment* was present and never re-read the function BODY. **Lesson: after any Edit that touches a function body — ESPECIALLY one that replaces a line with a comment — re-read the WHOLE body, not a grep for the changed token; build + tsc do NOT catch a removed call.**

87. **An absolute SERVER timestamp must be timed against the SERVER's own clock (the `<prompt time>` tag), NEVER the local `Date.now()` — client clock skew is a real, common, invisible-to-us failure mode, and getting this right is a CLIENT-QUALITY bar, not an edge case (B192, v0.14.2).** DR sends `<roundTime value=N>` / `<castTime value=N>` as an absolute Unix-seconds END time, and its OWN current time in every `<prompt time=T>`. The RT/CT bar used to compute `rt = (N*1000 − Date.now())/1000` — comparing the server's clock to the LOCAL clock. Aubrey's PC was ~1–2 min behind the game server (stale NTP), so `rt = realRT + skew`: the bar maxed (`rt`/`rtMax` inflated equally → `rtPct≈100`) and drained `skew` seconds too late. We could never reproduce it (our clocks were fine), which is exactly why this class is dangerous — **a user's wrong clock manifests as "the client is broken," and the symptom is intermittent (NTP re-syncs and drifts) and survives updates (it's not code).** The fix is to anchor to the server: defer the duration calc to the next `<prompt>` and compute `expires = Date.now() + clamp(N − T, 0, 300s)*1000` — duration from the server (`N − T`), the local clock only as the countdown anchor (and the renderer compares against that same local clock, so the skew cancels). `lastPromptTime`/`pendingRtEnd`/`pendingCtEnd` on the parser carry this; reset on login. **Both siblings do exactly this — Frostbite `xmlparserthread.cpp` (`t_to = N − promptTime`, 300000ms cap) and Genie `Game.cs` (`rt = m_iRoundTime − m_iGameTime`); neither touches the local clock.** That convergence is the tell that server-clock anchoring is the *correct* design, and matching a quality client's behavior here is part of *being* a quality client — a visibly-wrong RT bar reads as a broken client even when the bug is the user's PC. **General rule: any feature that compares a server-supplied absolute time to `Date.now()` (timers, "X minutes ago", cooldowns, expiries) is a latent clock-skew bug — anchor it to the server's clock via the prompt time (track the offset once: `serverNow*1000 − Date.now()`), and prefer a server-derived DURATION over a server-vs-local absolute comparison.** The diagnostic signature: *"a timer/bar is wildly wrong only for one user, intermittently, and updates don't fix it"* → check that user's system clock against real time FIRST (10-second confirm), then anchor the code to the server clock so no one's clock can break it again. **The DR Aim Timer (`firingTimer`, v0.14.3) is the THIRD lane on this exact pattern** — `<dialogData id='AimTimerDialog'><timer id='firingTimer' value='N'/>` carries an absolute END time, deferred + anchored on the next `<prompt>` via the same `pendingAimEnd`/`anchoredExpiry` shape (`value='0'` → emit `expires:0` = clear). Any future server-pushed countdown (a new timer dialog, a buff/debuff duration) MUST join this lane — add a `pending*End`, anchor it in the prompt handler, never compute `value*1000 − Date.now()` inline.

88. **A MUTE (and any display-layer line drop) must be applied at the SAME layer that decides prompt emission, or it orphans the trailing `>` — Lichborne fixes this by replaying the parser's prompt-dedup in the renderer AFTER mutes (B194, v0.14.3).** Cherisse muted her mana-regen messages and saw bare `>` prompts pile up (`>` / `>>` / `>>>…`). Root cause is a LAYER MISMATCH: DR fires a `<prompt>` after every server turn, and the parser already suppresses a prompt identical to the last main text (`lastMainText` in [StormFrontParser.ts](src/main/parser/StormFrontParser.ts) `case 'prompt'`, line ~904) — so consecutive identical prompts collapse upstream. But MUTES are a **display-only renderer concern** (DESIGN §31): they run in GameWindow's `applyTextMods` a layer LATER, after the parser already emitted the prompt. A turn of `<regen msg>` + `>` parses as real-text-then-prompt (so the parser correctly emits the `>`); the mute then removes the regen line, leaving an orphaned `>`, and several muted regens in a row pile up because the parser saw distinct real text between each prompt and never deduped them. **Both sibling clients avoid this by construction — the gag/ignore check runs at the DISPLAY layer, before the prompt is committed:** Genie's `PrintTextToWindow` (`Game.cs` ~2938) `return`s on a gag match so the line never reaches the window, and prompt logic is a separate path a gagged line never touches; Frostbite's `MainLogger::onProcess` (`mainlogger.cpp`) runs `if(alter->ignore(text,…)) return;` BEFORE its `prevType==PROMPT` bookkeeping, and models the prompt as a LAZY marker (`>` emitted only right before the next real content), so consecutive prompts collapse and an ignored line never updates the prompt state. **Lichborne's fix keeps mutes in the renderer** (Principle #2 — display concern, must work without Lich, portable, clean import target) and instead **replicates the parser's identical-prompt dedup at the display layer, after the mute pass**: the parser tags each prompt line `prompt: true` ([types.ts](src/shared/types.ts) `StreamTextEvent`/`TextLine`); GameWindow carries it onto the `TextLine` (`mkLine`), and a collapse pass right after `applyTextMods` drops a prompt line whose text equals the previous DISPLAYED main line when both are prompts, tracked across batches via `lastMainLineRef`. **Three constraints make it safe:** (a) it gates on the `prompt` flag, so repeated REAL content (two identical lines with muted content between) is never collapsed; (b) it matches on prompt-text EQUALITY, so statusprompt drift (`H>`→`R>` with no content between) is preserved — only a redundant identical prompt is dropped; (c) it runs whenever `newMain` has content so the cross-batch ref stays current even when nothing is dropped — and because the parser already deduped identical consecutive prompts upstream, this pass is a NO-OP in the no-mute case (it only ever fires when a mute removed the content the parser saw between two prompts). **The raw XML still streams and the Session Log still captures the raw line** (the log records are built from the pre-mute `lineText`, unchanged) — nothing is lost, satisfying "XML must still stream for stability." **General rule: any feature that DROPS a display line (mutes, a future client-side gag, conditional hiding) must account for adjacent state the upstream parser computed assuming that line was present — prompts are the live example; a dropped line that the parser used as a `lastX`-dedup boundary will orphan whatever the parser emitted after it.** Don't try to fix this by moving mute evaluation into the main-process parser (it would break the renderer-side Session Log capture and force all the group-gating/stream-restrict renderer state into main) — collapse at the display layer instead.

89. **Substitutes are a LINE-level rewrite, but DR fragments a STYLED line into multiple segments — a per-segment substitute can't match a regex (esp. one with capture groups) that spans a preset boundary (B195, v0.14.3).** Cherisse's Gweth-thought substitute `^\[(.*)\] Your mind hears (.*) thinking,\s*"<to you>" (.*)` → `[Gweth][$1][$2] to you: $3` never fired on the GAME's own thoughts, though it worked on lnet thoughts. Root cause: a DR thought arrives as TWO segments — `<preset id='thought'>[Personal] Your mind hears Itheron thinking, </preset>` + `"<to you>" "Test"` — and `applySubstitutesToSegments` ([substitutes.ts](src/renderer/substitutes.ts)) ran the regex **per-segment**, so a pattern spanning the boundary matched neither half. lnet thoughts come through Lich as ONE unbroken segment, which is exactly why those "worked" and the native ones didn't. (Highlights had already been fixed for the same fragmentation in B115 — `computeLineMatchRanges` scans the JOINED line and intersects ranges per-segment — but highlights only STYLE ranges, which map back cleanly; substitutes REPLACE text with capture-group reordering, which can't map back, so they were left per-segment.) **Both siblings substitute on the whole plain-line text** (Genie `ParseSubstitutions`, Frostbite `alter->substitute`) and re-emit the result as one styled unit. **Lichborne's fix (in [substitutes.ts](src/renderer/substitutes.ts), keeping substitutes in the renderer per Principle #2):** run the per-segment pass FIRST (an in-segment match — recolor a word, fix a name inside a monsterbold span — keeps every segment's styling, NO regression), then run a JOINED-line pass; if the joined result differs from the per-segment result, the match crossed a boundary, so COLLAPSE the line to ONE segment carrying the joined-substituted text styled like the first non-empty segment (its primary preset, e.g. `thought`/`speech`) — VISUAL fields (`preset`/`bold`/`fg`/`bg`) only, never link fields (`cmd`/`href`), so a reshaped line can't inherit a stray click target. A single-segment line skips the joined pass (no boundary); an unchanged line returns the SAME array reference (caller's identity check skips the rebuild). Verified against the real captured segments by bundling the actual `substitutes.ts` (Iron rule — a heredoc backslash-escaping artifact made the FIRST string-pattern test falsely "fail," so re-test regex logic with regex LITERALS / `String.raw`, not shell-written pattern strings). **WYSIWYG note (and a sharp Iron-rule lesson): users match against the RENDERED text, and rendered === the regex's match text** because (a) the PARSER trims a segment's trailing whitespace (the `<preset>thought, </preset>` span becomes `thought,` — no trailing space) and (b) `.text-line` is `white-space: pre-wrap`, so the joined segment text displays character-for-character. So Cherisse's literal `thinking,"<to you>"` (no space) is CORRECT — it matches what she sees. **I initially gave the WRONG advice (add `\s*`) by reasoning from the RAW XML (which still carries the preset's trailing space `thinking, `) instead of the PARSER OUTPUT (which trims it).** When reasoning about what text a renderer-side regex (highlight/substitute/trigger) sees, the source of truth is the parser's segment output, NOT the raw socket XML — bundle `StormFrontParser` and dump the segments (a 10-line harness settled it; the raw XML had a space the parser removes). **General rule: any display-text transform that REPLACES text (substitutes, a future rewrite rule) must operate on the joined line for correctness; styling preservation is a best-effort layer ON TOP (in-segment keeps spans, cross-segment collapses) — don't ship a per-segment replacer and expect cross-preset regexes to work. Mutes' `match` scope has the same per-segment shape but is lower-risk (it strips, doesn't reorder); revisit it the same way if a tester hits a cross-segment mute.**

90. **localStorage is the per-character rule store and it has a ~5MB ORIGIN quota shared across ALL characters — a bare `setItem` that hits the quota THROWS and silently loses the write; route every rule save through `safeSetItem` (B197, v0.14.4).** A power user with a huge imported ruleset (JadedSoul: Wrayth+Genie → 3436 highlights) × several characters in one localStorage can blow the quota. The symptom is nasty and misleading: a `saveHighlights`/`saveTriggers`/etc. throws `QuotaExceededError`, the delete/edit never persists, and on reopen the OLD list loads back — reads exactly like *"I deleted a rule but it came back / it's duplicating."* **Fix:** all six rule saves (highlights/triggers/macros/aliases/mutes/substitutes) go through `safeSetItem(key, value)` ([characterScope.ts](src/renderer/characterScope.ts)) — a try/catch that, on failure, logs, shows a ONE-SHOT `window.alert` pointing the user to Automation Analytics → "Remove duplicate copies", and returns `false` instead of throwing. **Investigation discipline that applies generally (this was the B197 process):** when a "data gets duplicated / doesn't persist" bug can't be found by reading, PROVE the pure data path with a harness before touching it — `tmp-dedup-repro` bundled the REAL `loadHighlights`/`saveHighlights` + the full `localStorage → YAML (`exportCharacterProfile`/`buildCharacterProfile`) → reload + restart (`importCharacterProfile`)` round-trip with a mock localStorage + in-memory `window.api`, and showed delete is correct and STABLE across reopen+restart (IDs unique via `nanoid`, `exportCharacterProfile` REPLACES `state`, not appends). That RULED OUT the data layer and pointed at quota / the React layer — far better than shipping a speculative "fix." Temporary `[hl-diag]` console logging (HighlightsPanel mount + delete: state count, post-save localStorage count, dup-id count) is the next step to pin a non-reproducible UI-layer bug on a reporter's machine; remove it once confirmed. **General rule: any localStorage write that can grow unboundedly (rule lists, logs, caches) must be quota-guarded; and a "duplication/persistence" bug is a data-path question first (harness it) and a UI question second.**

91. **When hand-decoding a format with a back-reference table (Ruby Marshal's object links), register EXACTLY the entries the reference implementation registers — an extra, missing, or misordered entry silently shifts every later reference to a plausible-but-wrong value (B202, v0.15.1).** [marshalParser.ts](src/main/lichbridge/marshalParser.ts)'s `'I'` (ivar-wrapper) handler pushed its own placeholder into the object table AND let the inner string register itself — but Ruby's `TYPE_IVAR` gets NO table slot (marshal.c registers only the inner value). One extra entry per I-wrapped value meant every subsequent `@` object link resolved one-plus slots early: moonwatch's `UserVars.sun` hash decoded with its `timer` key REPLACED by some other var's name (`"hunting_nemesis":177`, `"almanac_last_use":93` — a different wrong key per blob), which broke the Weather & Moons sun seed and had silently corrupted link-heavy blobs in the Lich Dashboard Variables tab since v0.5.0. The `'U'` branch had the sibling ORDERING bug (registered after reading its payload; Ruby registers before). **Why it hid for so long:** first-occurrence literals decode perfectly — only back-references (repeated/frozen strings, shared objects) mis-resolve, and they resolve to *valid-looking values of the right type*, not errors. This is the #72 family (don't trust your model of a serialization format — verify against real bytes), with a sharper tell: **corruption that varies per blob and substitutes plausible sibling values is a reference-table desync, not a data problem.** The fix protocol: read the reference implementation's registration rules (marshal.c `r_entry` call sites), match them slot-for-slot (inner-registers-not-wrapper; register-before-payload), and verify by re-dumping REAL data (all 22 uservars blobs) rather than synthetic cases — synthetic tests rarely produce enough repeated objects to exercise links.

92. **NEVER bound a TIME window with a LINE count — and never let two features answer the same question from two different sources.** *(v0.16.0, Catch Me Up. The naive log build was reverted then, but **v0.17.1 re-added log-backed Catch Me Up** the right way — `buildCatchupDigest` filters the TIME window in MAIN and returns a compact digest, exactly the architecture this pitfall demands. Both lessons below are LIVE in that code: the window is a time range, never a line cap, and the welcome-back card + catchup share their gather.)* Two bugs, one report (Sekmeht: *"the welcome-back card said 1599 lines but `/ai catchup 11m` said 763 — why don't they match?"*). **(a) The line-cap.** `readLogWindow` asked main for "the last `CATCHUP_LOG_TAIL` (8000) lines of the day-file" and then filtered by timestamp in the renderer. But N is a LINE cap being used to satisfy a TIME window, and the conversion rate is the character's log VOLUME — which varies by orders of magnitude. Measured against the real file: **Agan logs ~820 lines/min** (81k lines/day, mostly `inv`/`spells` state spam), so an 8000-line tail reached back **6.2 minutes**. An `/ai catchup 11m` therefore silently summarized ~6 minutes and reported the truncated count as if it were the whole window — **worst exactly where the log matters most** (a busy character). The fix is architectural, and the codebase already had the right precedent (`session-log:build-export`: *big data never crosses IPC, only the spec and the result*): a **`session-log:read-window(character, fromTs, toTs, maxRows)`** handler that filters **in main** and returns only the matching rows. `maxRows` is a *transport guard applied as a tail*, never the window bound. **When you find yourself converting a time question into a line count, stop — push the time filter down to whoever owns the data.** **(b) Two sources, one question.** The welcome-back card counted the **in-memory buffers** while Catch Me Up read the **log** — so they could never agree, and worse, the memory path was silently *under*-counting because `lines` is capped at `MAX_LINES` (2400) and an 11-minute burst had already trimmed. Both now go through ONE `gatherWindow(from, to)`. **If two surfaces describe the same window, they must share the gather function — a shared *invariant* in a comment is not enough** (this exact invariant WAS written down — "both read through one `collectHistory()`, so they can never disagree" — and then broken three edits later by adding log-sourcing to only one of them). **(c) The skip-list is load-bearing and easy to under-populate:** `spells` was missing from `CATCHUP_SKIP_STREAMS`, so 1,173 lines of clear+rewrite spell-list state per 11 minutes were being fed to the model as events. State-readout streams (`exp`/`inv`/`spells`/`activespells`/`percwindow`/`moonwindow`/`lichscripts`) contribute a stale TABLE, never history — when a new state stream appears, add it. **Diagnostic lesson:** this was unresolvable by reading (I theorized four wrong causes); it took **30 seconds of measuring the real log file** (`node -e` over `%APPDATA%\lichborne\Logs\{Char}\`) to make it obvious. When a discrepancy is a DATA question, go measure the data — don't reason about it.

93. **A floating CHROME window (command/vitals/icon) must NOT auto-size its own height — and any measure→persist loop that rewrites a saved rect is a data-corruption vector, not just a render tweak (v0.17.0, Sekmeht).** The command window read as too tall next to the slim chrome, so an "auto-hug" was added to [FloatingWindow.tsx](src/renderer/components/FloatingWindow.tsx): a `useLayoutEffect` measured the bar's content height and, when it differed from the rendered height, called `onChange({ rect: { …, h } })` to shrink the window to fit. It **built clean, converged in one step in theory, and was WRONG in three ways.** (a) **It's the pitfall #74 family** — chrome height is meant to be explicit/deterministic *precisely because* content measurement is racy (the icon/vitals bar can measure short mid-render before data lands), and the hug shrank the vitals window enough to CLIP the bars ("smashed"). (b) **It PERSISTS.** `onChange` → `updateWindow` → the freeWindows `saveFreeWindows`+`saveProfile` effect, so every (possibly-wrong) measured height was written into the character's saved layout — **per character, as you used each one.** Reverting the *code* could not un-write the *data*; affected characters stayed smashed until a manual delete+re-add / drag-resize / Rebuild-from-panels. (c) **It resized on interaction** — the effect ran on every render, so *moving* a window (which re-renders) re-measured and resized it, which the tester saw as "it's larger until I move it, then it snaps." **Rules that stick:** chrome windows keep an explicit height (the conversion/default size, user-resizable); if a "hug the content" behavior is ever genuinely wanted, it must (i) never shrink below a measured-safe floor, (ii) guard the racy 0/short measurement, and (iii) **NOT write the result back to persisted state on a timer/every-render** — a one-shot correction gated behind an explicit user action at most. The general lesson beyond windows: **an effect that MEASURES the DOM and PERSISTS the result is a feedback+corruption risk — treat "measure→setState→save" with the same suspicion as a render-phase side effect.** And since it never shipped (test-session only), the fix is to tell the affected tester to re-add the windows, NOT to add a migration that would reset every released user's legitimately-sized chrome.

94. **A text `<input>` in the game area inherits `--game-line-height` (the PROSE setting), and the text CARET is as tall as the line box — so pin an explicit `line-height` on any game-area input, especially one with elements pinned to its edges (v0.17.0, B216).** The command bar's `.command-input` set no `line-height`, so it inherited `--game-line-height` (the per-character PROSE line-height, typically 1.4–1.6). The blinking text caret is as tall as the line box, so at a large prose line-height the caret grew tall enough to overlap the **RT/CT/aim timer strips** pinned at the input's top/bottom edges (`.cmd-bar` / `.cmd-chip`, `position:absolute; top/bottom:0`). **Fix:** pin `.command-input { line-height: 1.2 }` (a tight fixed value, independent of the user's prose setting) so the caret stays short at any line-height, and size the input's vertical padding to clear the edge strips (padding ≥ strip height; the two were tuned together — input padding 7px vs 5px bars / 6px chips). **Rule:** game-area inputs should NOT inherit the prose line-height — pin it. The prose setting is for READING multi-line text; an input is a single line and wants a tight, predictable caret. Same font-scaling family as pitfalls #45/#58 (use `em`/`var(--game-font-size)`, not `rem`), but for the vertical/line-box axis. Any future game-area input with edge-pinned decorations (a search box, an inline editor) must pin its line-height and clear its edges the same way. **The clearance is FONT-SIZE-ROBUST by construction** (verified in the v0.17.0 bug check): the input's wrap height scales in lockstep with the caret (both are `1.44·gameFont`), so the strip edges move down exactly as fast as the caret grows — every clearance comparison reduces to fixed-px padding (7px) vs fixed-px strip, a CONSTANT margin at any font (1px chips / 2px bars). Don't "fix" it for large fonts — it can't degrade there. **Caveat kept on radar:** chips-mode margin is only **1px** — positive, but zero slack for a font's ascent/descent metrics or subpixel rounding painting the caret ~1px taller than its box. If a chips-style tester ever reports a hairline caret↔chip touch, the cheap fix is `.cmd-chip` height 6→5px (→2px, matching bars) or `.command-input` padding 7→8px — NOT a bar-mode concern (2px).

95. **`url(#id)` resolves to the FIRST matching id in the WHOLE DOCUMENT — so any SVG def id (`<linearGradient>`/`<radialGradient>`/`<filter>`/`<clipPath>`/`<mask>`) shared across multiple mounted instances of the same component CROSS-HIJACKS; make every def id per-instance-unique with React `useId()` (B217, v0.17.0).** The Moons experience can be mounted more than once at the same time — a floating window AND a panel tab, or (the tester's repro) the SAME character's Moons on one tab while ANOTHER character's Moons sits mounted-but-`display:none` on a background tab (pitfall #24 — hidden ≠ unmounted, its SVG is still in the DOM). Every instance's `<svg>` declared gradients with the SAME literal ids (`lb-ground`, `lb-shade`, `moon-xibar`, `sun`, `glow`, …). An SVG paint reference (`fill="url(#lb-ground)"`) is resolved by document order against the first element with that id — so once a second instance mounted, half the first instance's fills pointed at the second instance's gradient defs; when that second instance was on a `display:none` tab, its gradients still resolved but painted nothing usable, and the first instance's sky/moons/rays/shade went **flat/blank** (the tester's screenshot: switch char1→char2 moonwatch→back to char1 = stripped visuals). **Fix:** `const uid = 'm' + useId().replace(/[^a-zA-Z0-9]/g,'')` once per component, a `gref = (name) => \`url(#${uid}-${name})\`` helper, and every `id="lb-x"` → `id={\`${uid}-x\`}` with every `url(#lb-x)`/`fill={...}` → `gref('x')`. `useId()` is stable across renders and unique per mounted instance, so no two instances collide. **The ONLY safe exception is a def that is byte-identical AND static across every instance** (the Moons `lb-fog` gradient — same stops always, so cross-instance resolution paints the same thing); even then, prefer uniquifying unless there's a reason (e.g. it's referenced from a sibling module-level sub-component that has no access to the parent's `uid`). **General rule: any component that can mount more than once AND uses SVG `url(#…)` references MUST namespace its def ids per instance** — grep a new SVG component for `id="` / `url(#` before shipping. This bug is INVISIBLE with a single instance (the tell is "visuals break only after opening the same feature twice / on a second character") and it's a whole class: gradients, filters, clipPaths, masks, and `<use href>` all resolve by first-in-document id. `useId()` is the fix, not hand-rolled counters (SSR-safe, collision-free, stable).

96. **A scroll listener CANNOT tell a user scroll from a library/layout one — so never un-pin on a single scroll event; require the distance to PERSIST. And react-virtuoso actively scrolls you UP after a scrollback trim (B223, v0.17.1).** The years-long intermittent "the story window un-pins itself, scrolling locks, the 'New Lines' badge appears, and it never recovers" bug. The decisive tester clue was **"the scrollbar thumb quivers — tries to move then snaps back down"**: that's two writers fighting over `scrollTop`. **Mechanism (traced into react-virtuoso's source):** `appendTrimmed` cuts 400 lines in ONE slice (2400 → 2000, recurring every ~400 lines once the buffer is full — pitfall #81). That collapse makes Virtuoso compute `scrollDirection === 'up'`, which is **precisely the gate for its upward-scroll compensation**, while its index-keyed size tree is stale by 400 rows (we deliberately don't pass `firstItemIndex`). Its rAF-batched row re-measures then issue `scrollBy(-k)` — **a scroll nothing in our code suppresses** — and `stickToBottom`'s settle loop slams the bottom back each frame (THE QUIVER). Any compensation landing after that loop exits (2 stable frames) plus the ~250ms suppress tail reached `handleVirtuosoScroll`, whose `dist > 40` branch has **no way to know a human didn't do it** → spurious un-pin. **THE RULE: the un-pin branch must require the >40px distance to still hold ~2 frames later** (`unpinConfirmRef`, a double-rAF re-check that cancels if the distance collapses back). A library compensation is undone within a frame or two; a real user scroll holds forever. This is safe by construction because **wheel-up (`onWheel`) and PageUp/Home set `pinnedRef=false` DIRECTLY and never reach this branch** — only scrollbar drag/arrow/track and touch go through it, and they merely commit 2 frames later. Keep it **source-agnostic**: it must hold whether the mover is Virtuoso, the browser clamping a shrunken scrollHeight, or a stalled-rAF window — do NOT re-write it to depend on identifying the source. **Second half — a spurious un-pin must be able to HEAL:** `onRefocus` used to be gated on `pinnedRef.current`, so once un-pinned it refused to act and the badge survived every return to the window. It now re-pins when `unpinAtRef > lastBlurAtRef` — the un-pin happened **while the window was away**, so it cannot have been the user. **Use that timestamp comparison, NOT "did we observe a user input?"** — detecting a scrollbar-drag `pointerdown` is not something we can rely on, and guessing wrong yanks a reader who HAD scrolled up (the never-un-pinned default of 0 fails the comparison, which is the safe direction). **Two findings that came out of this investigation and change how you read older pitfalls:** (a) **`backgroundThrottling: false` ALSO affects the Page Visibility API** — Electron 43's own typings say so verbatim — so `document.hidden` very likely stays FALSE while minimized/occluded, meaning **the `document.hidden` guard B191 added to `handleVirtuosoScroll` (pitfall #71) is probably inert**, and `visibilitychange` may never fire (the room pump's hidden-drain leans on the same signal). That's WHY the B223 fix was built not to depend on it; if you ever need a real hidden signal, verify it empirically first — don't trust `document.hidden` in this app. (b) **`overflow-anchor: none`, which BUGS.md B20 documented on `.text-window`, is GONE from `src/`** (zero hits) — Chromium scroll anchoring is still neutralized only because react-virtuoso stamps `overflow-anchor: none` on every item element, so the app-level guarantee no longer exists; if you ever stop using Virtuoso for this list, restore it. **Deliberately NOT done (don't "finish" these without a fresh ask):** the root trigger is untouched — the quiver is still visible, because stopping it means changing how the trim interacts with Virtuoso's compensation (risky, and the pin is now safe regardless); and a user-intent gate was designed and HELD precisely because of the unverifiable `pointerdown`-on-scrollbar assumption.

97. **A WOUND and a SCAR are different states — and "healthy" must be derived from the ABSENCE of a known marker, never from a sentinel equality (B224, v0.17.1).** A tester's Injuries panel showed wounds that "stayed persistent since she died", while `HEAL` reported *"no significant injuries"* and full strength. It read exactly like a stale-panel/refresh bug; **it wasn't — the panel was updating fine and mis-reading the protocol.** DR encodes each body part in the `<image name>` inside `<dialogData id='injuries'>`, and per **Lich's parser (`lib/common/xmlparser.rb` ~681-690 — the authority, mirror it VERBATIM like the combat/assess regexes in §32.1):** `Injury<n>` = an ACTIVE wound of rank n; **`Scar<n>` = that wound HEALED (Lich sets `wound = 0`) and left a scar of rank n**; `Nsys<n>` = nerve damage. Lichborne's panel treated **any** name `!== the part id` as a wound and pulled its trailing digit as severity — so `Scar2` rendered as a permanent "Moderate" **wound**. Every row in her screenshot was a SCAR (which is why HEAL disagreed, and why they survived death: scars persist). **Two transferable rules:** (a) when a protocol has multiple KINDS of state in one field, parse the KIND explicitly — never treat "not the healthy sentinel" as one catch-all bad state, and never scrape a severity digit off an unrecognized string; (b) derive HEALTHY from the absence of every known marker (`/^(injury|scar|nsys)(\d)/i` didn't match ⇒ healthy), not from `name === id` — that way an unhurt part clears correctly whichever sentinel DR uses (`head`, `Body`, `Injury0`, …). **Display rule (polish standard #1):** a scar is healed HISTORY, not damage — show it muted/neutral (no wound hue, no fill) and keep "No active wounds." accurate whenever no WOUND is present even if scars remain, or the panel contradicts the game's own `HEAL` output. **Process lesson:** the DESIGN protocol table had documented the name as the part id plus a digit (`"head1"`) and never mentioned scars — the code faithfully implemented a wrong spec, so the docs propagated the bug. When a parser and a tester's report disagree, check the SPEC against the reference implementation (Lich), not just the code against the spec; the DESIGN entry is now corrected. **Two related gaps left UNCHANGED on purpose (want a raw-XML capture around a heal before touching):** the parser only emits `injury-update` when the dialog contains ≥1 `<image>`, so a genuinely EMPTY injuries dialog can never clear the panel; and the renderer REPLACES the whole injury map while Lich MERGES per-part — replace is wrong if DR ever sends deltas, merge is wrong if a part can only be cleared by omission. Don't "fix" either by guessing; one capture settles both. **B422 (v0.19.7) adds one data point short of that capture:** a full captured DR-through-Lich session (the B165 corpus, login burst included) contains ZERO `injuries` dialogs, so an EMPTY part map is the normal reading for an unhurt character rather than "no data yet" — DR sends this dialog when injuries CHANGE. A v0.19.7 empty-state pass briefly made the panel read "Waiting for injury data", which then sat there all session; the affirmative "No active wounds." is back, with the caveat in its tooltip. The two gaps above are unchanged, and the capture is still wanted.

98. **Prompt `>` dedup is TWO layers keyed on "was the LAST thing a prompt" — the parser `emit()` chokepoint + the display collapse — and the command echo merges INLINE into a trailing bare prompt (v0.17.3, Sekmeht).** DR fires a `<prompt>` after every server turn, so raw output is `>`-spammed; a client shows ONE `>` per turn and mutes the rest until real activity. The OLD parser keyed dedup on `lastMainText` (last MAIN-stream text) — wrong, because a move/look/combat flurry pushes room title/exits/players/combat-state to SUB-streams (never main), so the pre- and post-activity `>` compared EQUAL and the post-activity `>` was suppressed (the "no > after a room description" gap), while other turns leaked doubles. **The fix — parser layer:** route EVERY event through one `private emit(e)` chokepoint that sets `lastEmitWasPrompt = (e is a prompt stream-text)`; the `case 'prompt'` emits unless `lastEmitWasPrompt && prompt === lastPromptText` (the *immediately-preceding emitted event* was that exact prompt). So truly back-to-back identical prompts are dropped at the source; ANY non-prompt emit (main OR sub-stream) clears the flag, so the parser never suppresses a `>` after activity. **Display layer (pitfall #88 collapse, GameWindow):** a `>` line is dropped only if the PREVIOUS DISPLAYED MAIN line was an identical prompt — sub-stream/room activity is deliberately NOT a visible break (it never lands in the main scroll), so a room render / combat flurry that fired several `<prompt>`s still collapses to ONE `>`, while the B194 MUTE case (muted MAIN content between prompts) still collapses. **Do NOT reintroduce a "sub-stream activity resets the collapse" break** — it was tried and produced the DOUBLE `>` the user complained about (each of DR's several room/combat prompts got through). Statusprompt drift (`H>`→`R>`, different text) is preserved by the text-equality check. **Inline command echo — `appendEcho(prev, cmd)`:** the typed command merges into a trailing bare prompt (`s>` + `stand` → `s>stand`, reusing the prompt's own `>`, clearing its `prompt` flag, reusing the line id so it updates in place) instead of a standalone `>stand` line (matches Wrayth/Genie/Frostbite); falls back to `>cmd` mid-stream. All three echo sites route through it and then null `lastMainLineRef` (a typed command is a break — the prompt returning after a no-output command must still show, guarding the stale ref the merge leaves). Display only — the Session Log still records the canonical `>cmd`. **The parser `emit()` chokepoint is now load-bearing: EVERY event push goes through `this.emit(e)`, never a raw `this.events.push` (except the one inside emit).** A `sed` that mass-renamed `this.events.push(` → `this.emit(` silently broke `this.events.push(...sceneLineEvents)` into `this.emit(...sceneLineEvents)` — `emit` takes ONE event, so the spread emitted only the first scene event and dropped the rest; build + tsc BOTH passed it (a valid call), caught only by re-reading the body (pitfall #86). When adding a new event-producing path in the parser, emit each event with `this.emit(e)` singly — never spread an array into `emit`.

99. **Clearing an Electron session's COOKIES does NOT clear its HTTP CACHE — and for any token/cookie-paired login, a cached page is a silent auth failure (B229, v0.18.0).** The SimuCoin flow signs in to store.play.net per account, resetting the jar between accounts with `clearStorageData({ storages: ['cookies'] })`. A tester claimed on one account, then the NEXT account reported "authentication failure"; clicking *try again* worked. **The cache is separate storage from cookies**, and the partition was created without `{ cache: false }` — so the second account's GET of the sign-in page could be served **from cache**, handing us a **stale `__RequestVerificationToken` to post against a freshly-issued cookie**. ASP.NET (and every anti-CSRF scheme of this shape) requires the field and the cookie to MATCH, so the sign-in was rejected. It fits every observation: the FIRST account never failed (empty cache at launch), the second did, and the manual retry worked because the failed POST revalidated the page. **The rule: when a login pairs a page-embedded token with a cookie, the token fetch must be UNCACHEABLE — create the partition with `{ cache: false }`, clear cookies AND `clearCache()` in the reset, and send `Cache-Control: no-cache` on the request.** All three, because the failure is invisible from our side (a valid-looking 200 with the wrong token). **Second, generalizable half — pair an unprovable diagnosis with a bounded auto-retry.** The exact cause can't be confirmed without a live multi-account run, so the fix ships WITH a one-shot retry against a fully reset jar (the user's own successful "click try again", automated), which makes the symptom disappear whether or not the theory is complete — the "provably safe regardless of the residual unknown" shape from Principle #10 and the B191 precedent. **Bound it and check the ordering:** exactly ONE extra attempt (a genuinely wrong password must never become a retry storm against a third party's login endpoint), and verify the retried operation can't double-apply a side effect — here `auth-failed` returns BEFORE any claim POST is issued, so a retry can never double-claim. A second failure is then reported as a real credential problem, not a transient one.

100. **A failed READ that degrades to placeholder TEXT becomes a DESTRUCTIVE WRITE the moment an editor lets the user save it — and renderer-built file paths are how the read fails in the first place (v0.18.0 bug check, Lich Dashboard).** The Lich Dashboard built script/profile paths IN THE RENDERER with hardcoded `\` separators (`${lichDir}\\scripts\\custom\\${name}.lic`) and passed the stored path verbatim — which on Linux/macOS is `~`-relative and where `\` is a legal FILENAME character, not a separator. So the whole path was one nonexistent filename, `read-file` returned null, and the editor showed `'(could not read file)'`. Harmless so far — except the **WRITE** handler (`write-lich-script`) builds its path correctly in MAIN (`path.resolve(path.join(lichDirFrom(lichPath), …))`, expandHome'd), and the **Edit button was not gated on read success**. Click Edit → Save and the literal string `(could not read file)` was written over the user's real `.lic`. **Three rules:** (a) **never build a filesystem path in the renderer** — pass the identifier and let main compose it (main owns `expandHome` + `path.join`; the renderer knows neither the home dir nor the separator); (b) **an error PLACEHOLDER must never be editable-and-savable** — track read success explicitly (`readOk`) and disable the edit path, because "could not read" and "the file contains this text" are indistinguishable downstream; (c) **audit read/write path SYMMETRY** — a reader and a writer for the same file that compose their paths differently is the shape that turns a read bug into data loss, and it's invisible on the platform where both happen to work. Related same-sweep miss: `read-file` was the ONE main-side consumer that never got `expandHome` — when you add a cross-cutting normalizer, grep for *every* handler that receives that kind of value, not just the ones you were thinking about.

101. **A per-KEY in-flight guard does not protect a SHARED resource — serialize globally when concurrent runs share one connection, jar, or file (v0.18.0 bug check, SimuCoin).** The SimuCoin runner deduped by ACCOUNT (`inFlight: Map<account, Promise>`), which correctly stops the same account being checked twice — but every run shares ONE Electron cookie partition, and each run clears that jar on entry and signs out + clears again in its `finally`. Two DIFFERENT accounts overlapping (the startup pass on A while the user clicks "Check now" on B — the popover disables buttons per-account, so this is one click away) therefore wrecked each other: B's reset destroyed A's session mid-flight, A then read — and with auto-claim could CLAIM — **B's balance while reporting it under A's name**, and A's sign-out killed B's session so B reported a bogus "sign-in rejected" about a perfectly good password. **The rule: scope the guard to the RESOURCE, not the request.** If runs share global state, the serialization must be global too — a module-level promise chain (the `serializeLichLaunch()` shape in ConnectionManager.ts) or a per-request resource instance (here, a per-account partition name). **Ask of any new concurrent-work guard: what does a second, DIFFERENT-keyed run touch that this one also touches?** Corollary from the same sweep: a UI that disables only the row being worked on is an invitation to trigger exactly this interleave.

102. **A long-running IPC `invoke` gives the renderer NO id to key progress on — mirror progress on a separate channel keyed by something the caller already knows (v0.18.0, connect feedback).** `ipcMain.handle(CH.LOGIN)` resolves at the END of a connect that can take 30+ seconds. `ConnectionManager` was emitting a full commentary the whole time and `wireSession` forwarded it as `connection-status` **keyed by sessionId** — but the renderer doesn't learn the sessionId until the invoke resolves, so every message was dropped and the user watched a bare spinner through the entire wait. Fix: a `connect-progress` channel keyed by **character** (which the caller supplied), sent to `event.sender` only, attached before the try and removed in a `finally`. **The general rule: if work inside an invoke takes long enough to need feedback, the progress channel must be keyed on an identifier that exists BEFORE the invoke resolves** — and check `sender.isDestroyed()` (with no `await` between the guard and the send). Two related lessons from the same change: **(a)** when a status is emitted from a path that runs CONCURRENTLY with another emitter, the renderer keeps only the latest — a notice can be silently overwritten and leave the UI on a stale step (this killed a queue-wait message that looked correct); **(b)** a progress string that only counts (`"(29s)"`) should turn DIAGNOSTIC past the point where the wait stops being normal, naming the likely cause, rather than counting silently to a timeout.

103. **A live-updating status line in a content-sized box makes the box breathe — pin BOTH axes, not one (v0.18.0, connect overlay).** The connecting card showed a step message that changes every few seconds, from "Starting Lich…" to "Still waiting for Lich… (14s) — check your Ruby/Lich paths or antivirus". Being content-sized, the card visibly grew and shrank on every update (Sekmeht: *"very distracting"*). **Pinning only the width trades a horizontal jump for a vertical one** — the long message then wraps to two lines and the card's HEIGHT jumps instead. The fix is a fixed width PLUS a reserved line count on the changing element (`min-height` + `-webkit-line-clamp`), so only the words change. Two riders: verify the longest possible message actually FITS the reserved lines (compute it — chars × glyph width against the measured content width — a clamp that truncates is worse than a jump); and use `font-variant-numeric: tabular-nums` on anything with a ticking counter, or the digits alone re-flow the line. **Any surface that renders changing status text — connect overlays, progress cards, live counters — needs this treatment.**

104. **A rule PRE-FILTER must only ever be too LOOSE, never too tight — and `.` is not a literal (B230, v0.18.0 perf audit).** `extractRegexLiteral` ([regexLiteral.ts](src/renderer/regexLiteral.ts)) mines a lowercase substring that is *guaranteed present in any match*, so the hot path can `includes()` it before running a regex (pitfall #82a). Its walker handled `|`, `(`, `[`, anchors, quantifiers and escapes — but had **no case for `.`**, so the any-character metachar fell through to the literal-atom branch and was appended as a literal dot. `joins the .+` produced the gate `"joins the ."`, which demands a real period the match never has to contain, so the rule **silently never fired** — no error, still shown as enabled. That gate feeds BOTH highlights and triggers, so one walker bug kills rules in two subsystems, and it shipped from v0.13.4 until v0.18.0 with six of one tester's highlights dead. **The invariant, which the file's own header states and which this violated: err toward returning `null`. A MISSING literal costs only speed; a WRONG one breaks the rule.** Any metachar that can match something *other than itself* (`.`, a class, an alternation, an optional atom) must END the run, never extend it. **When you touch this walker, re-run the agreement check** (`tmp-perf/check-literals.mjs` — machine-local, rebuild from B230's notes if missing): it bundles the REAL module, loads every rule from the profiles on disk, and asserts that no gate rejects a line its own regex matches. It found this bug; a unit test with hand-written patterns would not have, because the dead patterns came from real imported rulesets.

105. **Volatile TRACKING data must not share an identity with RENDER data (v0.18.0 perf audit).** Contacts carry both: `name`/`templateId` decide how text is painted, while `lastSeen`/`lastRoom`/`encounterCount`/`timeSpentMs` are presence stats rewritten on **every room change**. Because both lived in one `contacts` array, a tracking write minted a fresh array identity that invalidated `nameRegex`, the contacts CONTEXT, and through it every mounted `TextLineRow` — each then re-running the whole ruleset. With ~350 rows mounted (the B152 overscan) and a big imported ruleset that measured as a **~30-40ms hitch per room transition**, worst exactly where you feel it: walking through a busy town. **Fix shape:** derive a `contactsRenderKey` folding ONLY the render-relevant fields, and key the render-facing array off it (`useMemo(() => contacts, [contactsRenderKey])`) so its identity survives tracking churn. **Two traps when doing this:** (a) find EVERY consumer — the context was obvious, but the main window's `TextLineRow` takes `contacts` as a direct PROP, and leaving that one on the volatile array defeats the whole fix for the main scroll while appearing to work in the stream panels (the Living Tableau was a third); (b) leave surfaces that genuinely want fresh tracking data — the contact POPOVER — reading the live state, or you fix a hitch by making "last seen" stale. Generalizes to any store mixing hot bookkeeping with render inputs.

106. **A CALLBACK in a big memo's dep array is a silent invalidation bomb — if it closes over per-tick state, the memo never holds (B231, v0.18.0).** `nodeRects` ([GenieMapView.tsx](src/renderer/components/panels/GenieMapView.tsx)) memoizes ~1057 room-marker `<g>` groups on `[visibleNodes, isStubNode, onNodeClick, onNodeContextMenu, onNodeHoverEnter, onNodeHoverLeave]`. Five of those are stable; `onNodeContextMenu` (right-click-to-walk) closed over `currentLocation`, so it got a **new identity on every room change** and the entire node layer was re-created on every step the player took — **8.92ms of React commit per room vs 0.34ms when the memo holds** (React Profiler, real node count). The memo LOOKED correct: the data deps (`visibleNodes`) were right, and nothing in the diff hints that a handler is the churning one. **The rule: when a memo is expensive, every CALLBACK in its deps must have a stable identity — put the callback's changing inputs in a latest-closure ref (pitfall #31) and give the callback `[]` deps.** Audit this whenever you (a) add a handler to a memoized list's deps, or (b) add a new dep to an existing handler that ticks per line/room/frame — a `useCallback` dep list is where this leaks in, and it degrades silently (correct output, quietly 25x the cost). **The tell:** a surface that is smooth when idle but hitches on a state change that shouldn't affect what's drawn. **Sibling smell in the same file:** a memo can also cover only HALF the work — `arcSegs` memoized the segment arrays but the `.join('')` that turns them into path `d` strings ran inline twice per render (~0.15ms + 78KB of garbage each time) for byte-identical output. **Memoize the value you actually hand to React, not the intermediate.** Both are measured by `tmp-map-perf/` (gitignored; a React-Profiler bench at real zone density — rebuild from B231 if wiped).

107. **An unqualified `:hover` rule OUTRANKS a state class — so a "selected/active" style can be silently replaced the moment the cursor touches it (v0.18.0, character tabs).** `.character-tab:hover` is specificity (0,2,0); `.character-tab--active` is (0,1,0). Whichever you write second in the file is irrelevant — hover simply wins. This hid for years because the two rules happened to set the SAME `background: var(--bg-base)`, which was itself the real bug: the active character tab and a merely-hovered one were pixel-identical, leaving "which character am I on" resting on `font-weight: 600` alone. The moment the active state got a real accent chip, the latent specificity problem would have eaten it on hover. **The fix is to EXCLUDE, not to escalate:** `.character-tab:not(.character-tab--active):hover` — hover can no longer match an active element at all, which is clearer than an `--active:hover` override and cannot be defeated by a later edit. **Two rules that generalize:** (a) any hover style paired with a selected/active/current state must exclude that state in the selector, and (b) **a state and its hover must never resolve to the same declaration** — if `:hover` and `--active` paint identically, the state is not communicated, and no amount of font-weight rescues it. Worth auditing wherever a list has a current item: tabs, panel tab strips, rule lists, the theme picker. **v0.19.7 (B340): a UI/UX sweep found about 25 more instances** — panel tabs, every rule-list row, the Automations/Contacts chip tabs, the Analytics and scope ON buttons, map chips, pickers and pills — all fixed in one pass. The lesson is that the pattern gets copied faster than it gets fixed: **when you ADD a state class (`--active`, `--selected`, `--on`, `--open`), grep the same file for that element's `:hover` rules in the same edit.**

108. **A scripted source edit must pass a FUNCTION replacement — JS `String.replace()` expands `$&`, `$1`, `` $` `` and `$'` INSIDE the replacement string, silently corrupting the code it inserts (v0.18.0).** Extracting the contact-name matcher into a helper was done with a script whose replacement text contained the standard regex-escape idiom `'\\$&'`. `String.replace(old, new)` treats `$&` in `new` as "the matched substring", so what landed in the file was `name.replace(/[.*+?^${}()|[\]\\]/g, "\\let lineId = 0")` — the escaper substituted a chunk of unrelated source for every regex metacharacter. **It compiled, `tsc` passed, and both builds were green**, because it is a perfectly valid string; the only symptom was that any contact whose name contains a metacharacter silently stopped matching. **Rules:** (a) when a script rewrites source, use a function replacement (`s.replace(old, () => next)`) or an index-based splice — never a plain string, and never trust that the inserted text is inert; (b) after ANY scripted edit, re-read the touched region (pitfall #86's rule, which is what caught this) and diff against `git show HEAD:<file>` when the region predates the session — that comparison is what identified the intended `'\\$&'`; (c) be most suspicious where the inserted text itself contains `$`, backslashes, or regex source, which is exactly the code most likely to be extracted into a helper. This one never shipped — it was found by the pre-merge bug check, which is the argument for running it even when the change "was just moving code".

109. **A popover whose LENGTH depends on user data must be bounded AND scrollable — and `overflow: hidden` on the root is still PROGRAMMATICALLY scrollable, so an oversized one can drag the whole app off-screen (B235, v0.18.1).** The SimuCoin popover rendered one block per account, each carrying a full 6-line consent disclosure, with no `max-height` and no scroller; for a tester with 7 accounts it stood several screens tall. Being unreachable was the MILD half. The severe half: `html, body, #root` are `overflow: hidden` ([global.css](src/renderer/styles/global.css)), and **that stops USER scrolling while leaving the element a scroll container the BROWSER can still scroll** — so scrolling a clicked/focused row into view scrolled the ROOT, and the entire UI slid off-screen with no scrollbar to bring it back (the reporter recovered by clicking something near the top, which scrolled it back — that recovery is the diagnostic signature). **Three rules:** (a) any popover / menu / dropdown whose row count comes from USER DATA (accounts, characters, rules, saved searches) needs `max-height` + `overflow-y: auto`; a viewport-relative ceiling like `70vh` always clears the bar above it. (b) Add `overscroll-behavior: contain` so a scroll that reaches the popover's end can't chain out to that root. (c) **Better still, make the surface's size independent of the data** — what actually settled this was reducing the popover to one summary line plus fixed actions and moving the per-account list into Settings, because "bounded and scrollable" still reads as *inconsistent* when it's two rows for one user and full-height for another (Sekmeht: *"there could still be a ton of listed accounts and it cause the window to be inconsistent"*). A modal built for lists is the right home for a list; a popover is the right home for one action. The sibling `.app-bar-more-menu` is fixed-length today so it's fine, but it has no ceiling either — give it one if it ever becomes data-driven. **WIDER TRIGGER (B313, v0.19.5): "user data" was too narrow — a popover sized by a REGISTRY count inside a resizable window fails identically.** The Experiences ⚙ (`.exp-inst-options`) had no `max-height` while BOTH its hosts (`.fl-body`, `.panel-frame-body`) are `overflow: hidden`, so in a short window it was simply CLIPPED — top options unreachable, no scrollbar, and nothing on screen saying they existed. Its height is the option count, and **Moons declares 17**, so that Experience had shipped with the defect since v0.15.1. The count's SOURCE is irrelevant — user data, a registry, a feature list — what matters is that the length is variable and the container is not. Bound every popover: `max-height` against the positioned host + `overflow-y: auto` + `overscroll-behavior: contain`. And note the two failure MODES differ by host: an `overflow: hidden` ancestor CLIPS silently (this one), while a scrollable root instead lets an oversized popover drag the app off-screen (B235) — both are the same missing ceiling.

110. **An imperative handle called from a PARENT effect closes over pre-measure state — measure LIVE inside the method, never read measurement STATE (B236, v0.18.1).** The Lich Dashboard's view measures the `<pre>`'s line-height into `lineMetrics` state, and `scrollViewToLine` read that state behind `if (lineMetrics.lineHeight <= 0) return`. The parent restores the last search hit from an effect keyed on `isEditing`, which on a mode switch runs in the SAME commit as the freshly-mounted child. React runs CHILD effects before PARENT effects, so the child's measuring effect HAS already run — but it only SCHEDULED a state update, and the `useImperativeHandle` object the parent holds was built during the render where that state was still `{0, 0}` (a new handle installs on the NEXT commit). The guard tripped and the scroll was skipped **silently**: leaving Edit — including after every save — set the highlight but never scrolled to it. **The rule: an imperative method a parent may call immediately after mount must derive layout facts at CALL TIME (`getComputedStyle` / `getBoundingClientRect`), not from state a sibling effect populates.** Reading state there is only safe when the caller is guaranteed to be a LATER commit, which "parent effect on the same mount" is not. Two riders: an early-return guard on a measured value makes this failure INVISIBLE (prefer measuring over bailing), and a `ResizeObserver` re-measure is the durable backstop for the whole class — it self-corrects any post-mount box change without your having to enumerate the triggers. Keep such a re-measure IDENTITY-PRESERVING (`setState(prev => equal ? prev : next)`) so it cannot loop, and note it is NOT the pitfall-#93 measure→persist trap so long as nothing is written to the profile.

111. **A property added to a SHARED multi-selector block is silently undone by a later single-selector rule of EQUAL specificity — grep the individual rules for that property before you trust the grouped one (v0.18.1, the free-layout header overlay).** Making the floating-window header an overlay meant adding `position: absolute` to a grouped `.fl-titlebar, .fl-grip { … }` block near the top of [free-layout.css](src/renderer/styles/free-layout.css). `.fl-titlebar`'s own rule further down sets no `position`, so it took the override. **`.fl-grip`'s own rule ended with `position: relative`** (it was there so the grip's `::after` dash had a containing block) — same specificity (0,1,0), later in source order, so it WON and the grip stayed in flow, still reserving ~10px. Result: the fix appeared to work in testing (title-bar windows) and did nothing at all for the case that actually mattered — chrome bars, which users collapse to a grip — so the tester's original complaint came back unchanged and cost a full round-trip. **Three rules:** (a) when you add a declaration to a grouped selector, `grep` that property against every member selector — CSS has no "override" warning and the build cannot help you; (b) prefer deleting the conflicting declaration to escalating specificity, and leave a comment saying why it must not return (here: `position: relative` is unnecessary, because an absolutely-positioned element is itself a containing block for its absolute descendants); (c) **verify by reading the resulting cascade in source order, not by rebuilding** — this is pitfall #86's "a green build proves it compiles, never that it does the thing" in CSS form, and CSS is worse because there is no type checker at all. Sibling of #107 (specificity), but a distinct mechanism: that one is UNEQUAL specificity, this one is equal specificity decided by source order.

112. **Inserting into a STRUCTURED config file (YAML/JSON) by anchoring INSIDE a block splits that block — and nothing compiles it, so nothing tells you (v0.18.1, the release workflow).** Adding the mac signature-verify step to [release.yml](.github/workflows/release.yml) meant inserting after the packaging step. The Edit anchored on `GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}` — the FIRST entry of that step's `env:` map — so everything after it in the map (a comment plus `CSC_IDENTITY_AUTO_DISCOVERY: 'false'`) was orphaned INTO the new step's `run:` script. **The failure was silent in two different ways at once.** The visible error, `CSC_IDENTITY_AUTO_DISCOVERY: command not found` (exit 127), points at a nonsense shell command and says nothing about structure — and it fired only *after* the whole build had already succeeded and uploaded. The REAL damage never errored at all: the env var was simply gone, so electron-builder resumed hunting the runner's keychain for a signing identity (`allIdentities= 0 identities found`) when the entire point of that variable is that it must never look. On a runner that HAD an identity it would have used it. **Three rules.** **(a)** When inserting into a structured file, anchor on the **END of the block you're inserting after**, or on the **START of the next sibling** — never on a line inside a multi-entry block; the same instinct that puts a new pitfall in the wrong place (see the note at the top of this list) puts a new YAML step in the middle of someone's `env:`. **(b)** Re-read the resulting REGION, not just your inserted text — pitfall #86's rule, which matters *more* here because there is no typechecker and no build step to catch it. **(c) VALIDATE SEMANTICS, NOT SYNTAX.** I ran a YAML parse check on the broken file and it passed, because the file *was* valid YAML — the env var had merely become a line of shell. A config check must assert the PROPERTIES you depend on: *this step still carries these env keys*, *no `run:` block contains an env-style `^[A-Z_]+:` line*, *this step is still gated by that `if:`*. That check now lives in the scratchpad recipe and catches exactly this; "it parses" proves nothing about a config file. Applies equally to package.json (`build` config), tsconfig, and electron-builder settings.


113. **A selector omitted from a shared metrics group is SILENT — the odd one out renders with browser defaults and nothing warns you (B244 / the account-header Remove button, v0.18.2).** Two instances in one release. The wizard footer has `.wiz-btn-back, .wiz-btn-cancel, .wiz-btn-next, .wiz-btn-finish { border-radius; padding; font-family: inherit; … }`; `.wiz-btn-lich-setup` was added later and never joined, so it drew with **no radius, no padding, and the BROWSER'S default UI font** beside four buttons that had all three. Then the new `.launcher-account-remove` got its own block and immediately diverged the same way — it omitted `align-self: center`, and because `.launcher-account-header` is `align-items: stretch` it grew to the full header height and towered over Refresh. **Both were invisible to every tool**: the build can't see it, `tsc` can't see it, and the button still *works* — only a human comparing two buttons notices. **The rule: sibling controls share ONE metrics rule and differ only in their state/hover declarations.** **The strongest form of this is to name BOTH selectors on the ORIGINAL rule rather than write a parallel block anywhere else.** v0.19.0's Overview input bar was built from the command bar's *recipe* — same tokens, values copied — and read visibly wrong beside it (flat instead of the `--bg-sunken → --bg-base` gradient, `6px` padding instead of `3px`, a `1em` input instead of `1.2em`, and the border on the INPUT rather than on a wrapper with `:focus-within`). The fix was to add `.ov-inputbar*` to `.command-bar` / `.cmd-input-wrap` / `.command-input` / `::placeholder` in [game.css](src/renderer/styles/game.css) and DELETE the copies, so retuning the game bar moves both. **Two riders when you do this:** the borrowing file must then restate NOTHING shared (an equal-specificity rule there wins or loses on bundle order — pitfall #111), and check what the shared rule does NOT set: `.command-bar` sets no `line-height`, so every child of the borrowing bar must pin its own or it inherits the PROSE line-height and balloons the strip (pitfall #77). When you add a button to an existing row, add its selector to that row's group in the same edit rather than writing a parallel block; when you add a property to a grouped selector, `grep` it against every member (that's #111, the equal-specificity sibling of this). Two riders learned here: `align-self` is load-bearing whenever the container is `align-items: stretch`, and a button's own `line-height: 1` is what keeps a deliberately-smaller font from also making it a different HEIGHT.
    - **THE SAME AFFORDANCE UNDER TWO CLASS PREFIXES IS THE OTHER WAY IN (v0.18.6, Sekmeht).** The launcher's Favorites heartbeat was written as `.launcher-section--favorites .launcher-card-favorite--on`, which is correct — for CHARACTERS. Teams can be pinned to Favorites too (F97) and their heart is a different prefix (`.launcher-team-fav--on`, because team ROWS and character TILES are separate components), so a pinned team sat in the same list with a dead, un-beating heart. Nothing is broken, nothing warns, and it only reads as wrong when the two are side by side — which is exactly when a user sees it. **When one user-facing affordance is implemented by two components, every rule ABOUT that affordance must name both selectors, in ONE rule, so the beat/colour/size can't be retuned for one and silently left on the other.** The check is to grep by the AFFORDANCE (`favorite`, `heart`) rather than by the class you happen to be editing — a class-name grep is precisely what misses the other prefix. Note this is the inverse of #55's `.tp-` collision: that was two components sharing ONE prefix by accident, this is one concept split across TWO prefixes by design. Both fail silently; both are found by asking "who else renders this same thing?".

114. **`preventDefault()` in a window's `close` handler CANCELS an in-progress `app.quit()` — if your shutdown is async, it must re-issue the quit itself (B245, v0.18.2, macOS).** Lichborne holds the primary window open to drain sessions and flush profile saves: `close` → `e.preventDefault()` → `runAppShutdown()` → destroy windows. That preventDefault silently aborts whatever quit was underway, so **⌘Q, File → Quit, the dock menu and the red X all failed to quit** — the app sat with zero windows and Force Quit as the only exit. **Windows and Linux only ever recovered by ACCIDENT**, through the boilerplate `app.on('window-all-closed', () => { if (process.platform !== 'darwin') app.quit() })`; macOS deliberately skips that branch by platform convention, which is exactly why the bug was invisible on the mature platform for the app's entire life. The fix is to `app.quit()` at the end of the drain, making `window-all-closed` a redundant safety net rather than the mechanism. **Generalises past quitting: any handler that defers a lifecycle event by preventing it OWNS completing that event** — don't leave the completion to a platform-conditional hook you didn't write. And when auditing lifecycle code, read the `!== 'darwin'` guards as *"this is the only thing driving the behaviour on two of three platforms"*, not as a cosmetic platform nicety. **Three later lifecycle riders (v0.19.7):** **(a) B353** — on quit Electron sends `close` to EVERY window, so a decoupled window used to log its character out before the primary's confirm could be cancelled; `before-quit` now sets `quitInProgress`, and a secondary window's close handler defers to the primary's app-scope confirm while it is set. The flag clears on Cancel and SELF-HEALS after `QUIT_CLOSE_PASS_MS` (3s) — a flag that could stick would leave a window's ✕ doing nothing, which is worse than the bug. **(b) B354** — off Windows, `powerMonitor.on('shutdown')` marks the quit consented and runs the drain with no prompt, calling `preventDefault()` so the OS waits; a dialog during a system restart is a hang the user can't answer. **(c)** the macOS app menu's Quit is a CLICK item calling `app.quit()`, not the `quit` role — the role goes out as the native `terminate:`, which (per Electron's source, unverified on a Mac) may reach the shutdown path and skip the multi-character confirm. The Dock's Quit may still arrive that way.

115. **On macOS and Linux the platform-specific CODE is usually fine — it's the DEFAULTS and the LIFECYCLE that assume Windows (the v0.18.2 macOS pass).** All three findings from our first Mac tester had this shape, and none was a missing platform branch. The Lich LAUNCH path is genuinely cross-platform (`resolveRubyw` only rewrites a path ending in `ruby.exe` so a Mac interpreter passes through verbatim; `expandHome` handles `~`; no `windowsHide`, no shell) — **Lich can run on macOS**; what stops it is that `DEFAULT_RUBY` is deliberately EMPTY there (a plausible `/usr/bin/ruby` would pin users to a Ruby too old for Lich 5.18+, which needs Ruby 4). So every non-Windows install lands in a state Windows testers never see, and the failures were unreadable: a raw `spawn ENOENT`, and a Connect that opened the "Add account" wizard with no explanation because the password wasn't saved. **When triaging a Mac/Linux report, ask in this order: (1) what does this default to on a machine with nothing installed? (2) does the failure SAY what's missing? (3) does the lifecycle hook actually run on this platform?** — before looking for absent platform code. Two corollaries. **Never silently switch a user to a degraded mode to route around missing config**: defaulting a Lich-less machine to DIRECT was tried and reverted, because DIRECT bypasses Lich entirely and would have quietly stripped scripts/maps/vars from any Lich player whose install sits somewhere non-default (Sekmeht). Fail legibly instead — a pre-flight check naming Lich Setup. **And platform-specific ADVICE must follow the platform**: the "password saving is unavailable" notice named GNOME Keyring and KWallet on every OS, which is nonsense on macOS.

116. **A CSS length that isn't representable on the browser's layout grid makes `n × length` DRIFT — pin any value you do arithmetic with to a whole pixel (B249, v0.18.3).** The Lich Dashboard search overlay positions itself as `paddingTop + lineIndex * getComputedStyle(pre).lineHeight`. All four participating elements were correctly pinned to the same absolute `1.3rem` (pitfall #62's fix), so it read as airtight — but **`1.3rem` = 20.8px, and Chromium lays out on a 1/64px LayoutUnit grid where 20.8 is not representable** (20.8 × 64 = 1331.2). Layout therefore uses 1331/64 = 20.796875px while `getComputedStyle` still reports `"20.8px"`, and that 0.003125px/line discrepancy ACCUMULATES: 0.6px at line 200, **~3.4px by line 1083**. **The diagnostic signature is the important part: an offset that is invisible in short content and grows with distance is ACCUMULATION, not a mismatch** — it was reported repeatedly and never reproduced because nobody scrolled deep enough, and the pair of clues that cracked it was "fine in the Ruby editor (short scripts), wrong in the YAML profile (1105 lines)". So: **if you multiply a CSS length by an index, make the length an integer px** (computed == used, exact at any n), or measure the real geometry instead of trusting the computed value. Applies to any gutter/overlay/virtual-list arithmetic, not just this one. **And the length you multiply must be the one actually RENDERED, not one you declared nearby.** v0.19.1 (B277) capped an Overview feed line at `calc(3 * 1.3em)` using the container's line-height — while `.text-line` sets its OWN `line-height: var(--game-line-height)` (the user's prose setting, 1.4–1.6). A 3.9em box against ~1.5em rows is **2.6 rows**: two whole rows and a visible sliver of a third, with the count sliding as the user changed a setting that looked unrelated. **Before doing arithmetic in rows, check what the CHILD declares** — a child that re-declares `font-size` or `line-height` (as `.text-line` does, for the F31 per-panel override) silently invalidates any `em` maths done in the parent's terms. The durable fix was to stop counting rows at all and let the container clip, which needs no arithmetic to stay correct. Two process notes from the hunt: `getComputedStyle` returns the SPECIFIED value, not the value layout actually used — they are not the same number and nothing warns you; and the Electron harness built to measure this directly failed because Electron could not resolve an app directory containing `--` (the scratchpad path), throwing error dialogs at the tester before it was abandoned — so the mechanism here is INFERRED from arithmetic, and the fix was chosen to be safe either way.

117. **A GUI-launched app inherits no shell environment — that means no `LANG`, not just no `PATH` (B250, v0.18.3, Zithri).** We already knew a Finder/dock-launched app has no shell `PATH` (it is why `resolveRubyw` probes rbenv shims as explicit paths). The same hole costs an encoding: we spawned Lich with no `env` at all, so it inherited Electron's, which on macOS has no `LANG`. **Ruby derives `Encoding.default_external` from the locale**, so it read every `.lic` as US-ASCII, and Lich's `script.rb` then regex-matches each line — one non-ASCII byte anywhere in the file (a curly quote, an em dash, an accented name in a COMMENT) raises `invalid byte sequence in US-ASCII` before the script runs. It presents as a Lichborne incompatibility, because the same script works from a terminal and in Frostbite (Qt calls `setlocale()` at startup, so the Ruby it spawns inherits a locale). **When a spawned child behaves differently under Lichborne than from a shell, enumerate what the shell would have provided** — `PATH`, `LANG`/`LC_ALL`, `HOME`, `TMPDIR` — before suspecting the child. Set defaults only when ABSENT (a deliberate locale must be respected) and only where the platform has that convention: Windows Ruby derives its default from the code page and has no `LANG`, which is exactly why this never showed there.

118. **A PORTALED popover escapes its opener's stacking context — its z-index must beat the MODAL tier on its own (B251, v0.18.3).** `ContextMenu` renders through `createPortal(..., document.body)`, so being "inside" a modal in the React tree says nothing about paint order. At `z-index: 500` it lost to every modal in the app (wizard 600, AI-consent 620, add-character **1000**, connecting overlay 1500, Quick Send 2000): every ⋯ menu opened from the compact launcher **opened correctly and painted underneath the modal's scrim**, which reads as a dead button, not a layering bug — Sekmeht reported it as "the ellipsis box doesn't work" while the same code worked fine in the full launcher, where there is no modal above it. **The rule: a portaled popover sits ABOVE the modal tier (`.ctx-menu` is 2100), and any NEW modal must stay below it.** Two riders. **(a)** The tell for this class is *"the control works in one place and silently does nothing in another"* — check what is painted over it before you touch the handler; the click, the state and the portal were all correct here. **(b)** Don't raise every popover reflexively: `ContactPopover` and `ModeSwitcher` are also portaled at 500 and were deliberately LEFT there, because both render only in the game area (z ≤ 61) where nothing can cover them, and lifting them above the modal tier would invite the inverse bug — a stale popover floating over a modal opened after it. Raise a popover when something can actually cover it, not on principle. **(c) The same trap hit portaled DIALOGS (B322, v0.19.7).** `.cne-backdrop` — the chrome behind Edit Profile, Attach, Team Login and the team editor — sat at 100, and all four are portaled, so when opened from the + tab's compact launcher they painted UNDER its 1000 scrim. It was first fixed for Attach alone with a per-dialog override, with Edit Profile recorded as a latent copy of the same bug; when v0.19.7 put Teams into the compact launcher, the fix moved to the one shared rule (`.cne-backdrop` → 1600). **When several dialogs share a chrome and one is found under a modal, fix the shared rule** — the others have the same bug whether or not anyone has clicked them there yet. **(d) The tier map as of v0.19.7 (B329/B341/B374/B403):**
    - floating windows ≤ 61
    - the Overview 65
    - app bar 70
    - per-session dialogs 100–400 (the Maps overlay 250)
    - popovers portaled from those dialogs 450 (`.gp-menu`, the Macros "$" menu)
    - game-area popovers 500 (ContactPopover, ModeSwitcher, the panel + menu, which B403 moved down from 9999)
    - wizard 600 (AI Consent sits at 620 for reasons nobody wrote down; don't copy it)
    - + modal 1000
    - connect overlay 1500 (the Team Login progress overlay too; B374 removed its inline 9000)
    - `.cne-backdrop` 1600
    - Quick Send 2000
    - **About 2010**
    - **toasts 2050**
    - **the confirm tier 2080** (ConfirmHost)
    - `.ctx-menu` 2100
    - Quit confirm and the shutdown overlay 10000

    Two survivors sit outside the map and are benign: `.app-bar-more-menu` (9999, but inside `.app-bar`'s own stacking context at 70, so it is internal) and `.map-tooltip` (9999, `pointer-events: none`, rendered only while hovering a map node). Neither is a precedent — a new layer goes in the map. Toasts sit above every modal because a toast is usually raised BY one (the quota warning, "moved to All Characters"), and at the old 200 they tied with Automations and lost on DOM order. About sits above Quick Send because the native menu can open it over anything. A new layer goes into this map deliberately, not at a round number that looks free.

119. **On Linux a packaged app's own path is NOT where the user installed it — an AppImage runs from a temp mount (B252, v0.18.3).** `app.getPath('exe')` inside an AppImage returns something like `/tmp/.mount_LichbAbc/usr/bin/lichborne`: a transient squashfs mount that is unmounted when the app quits. Anything that shows, opens, or writes next to "the install directory" is therefore wrong on Linux while being correct on Windows and macOS — File → Open Installation Directory opened a folder that ceases to exist. **Electron's AppImage launcher exports `$APPIMAGE` with the real path to the `.AppImage` file**, so that is the value to use, falling back to the exe's directory elsewhere (the variable is unset on every other platform and on a non-AppImage Linux build, which makes the fallback the natural default). The same caution applies to any future "portable mode", sidecar file, or log written relative to the executable: **userData is the only writable location that means the same thing on all three platforms.** Found by audit rather than by a tester — this class of bug only shows up when someone is asked to go look at their install.

120. **A STREAM boundary is not a LINE boundary — DR splits one sentence across consecutive `pushStream`/`popStream` blocks on a single physical line (B256, v0.18.4).** Viewing another room (shadewatch mirror, arena view, distant gaze) produces `...shatters into a thousand<popStream/><pushStream id="familiar"/> small projectiles!` — one sentence, two blocks, no newline between them. `StormFrontParser` flushed on every push and pop, and in this architecture **a flush emits a `stream-text` event, which the renderer draws as a discrete LINE** — so remote text arrived shredded mid-word, and it looked like random newline injection. Flushing is now deferred to **`claimPending`**, which fires only when the target stream genuinely CHANGES: a same-stream pop-then-push merges, a line spanning two real streams still splits (and each half is filed under the stream it was written to, via `pendingStream`), and the end-of-line flush closes the line either way. **The sibling clients are the evidence this is the right model, and they are why the bug existed:** Profanity flushes on both open and close exactly as we did, but its flush WRITES CHARACTERS to a window, so two flushes just append to the same line; Frostbite caches across chunks while inside a stream and re-joins the block; Genie switches output window on push and restores on pop. **None of them treats a stream transition as a line terminator.** So when adapting a technique from those clients, check what their equivalent operation COSTS: ours emits a line where theirs writes text, and that difference is the whole bug. Never re-add an unconditional flush to `pushstream`/`popstream`, and remember every `pendingSegments.push` must be preceded by `claimPending()` or its content is filed under the wrong stream.

121. **A store's `coerce`/normalize function that REBUILDS each entry runs on the way OUT as well as in — so any field it fails to copy is DESTROYED, not merely ignored (F97, v0.18.4).** `saveBulkSets` calls `coerce(sets)` before writing, and `coerce` constructs a fresh `{ name, characters }` per entry. Adding `favorite` and `notes` to `BulkSet` therefore did nothing until `coerce` was taught to copy them: the fields would have been dropped on the very next save of ANY team, which reads as "my notes randomly vanish" rather than as a missing feature. **Two riders, both real here.** *(a)* The same hazard lives in every partial-update helper: `upsertBulkSet` replaced the matched entry WHOLESALE, so Team Login — which knows only the name and the roster — would have wiped the notes and the favourite pin on every re-save; it now merges (`{ ...existing, ...incoming }`). *(b)* The TYPE has to follow, or the next person re-introduces it: `SharedProfile.bulkSets` was an inline `{ name, characters }[]`, so the round-trip survived only by structural-typing accident and nothing would have flagged a rebuild that dropped the rest. **When you add a field to a persisted record, grep for every function that CONSTRUCTS that record rather than spreading it** — coercers, upserts, importers, migrations — and prefer a test that saves, loads and asserts the new field (the rules harness went 34 to 42 cases for exactly this).
    - **Third instance, and the one that shows why `tsc` will never save you here (B279, v0.19.1).** `ContactTemplate` gained `effect` / `glowColor`; the editor offered them, `saveContactTemplates` persisted them raw — and `normalizeTemplate` never copied them, so every load stripped the effect and the panel then wrote the stripped record back, DESTROYING it on disk. The symptom read as "effects don't work" rather than "my setting vanished", because COLOUR survived: the rebuild copies that one. **Both new fields were OPTIONAL, and that is the whole trap** — optionality is exactly what makes a field safe to add to a type and invisible to omit from a rebuild, so the compiler is structurally incapable of catching it (a missing REQUIRED field is a compile error; a missing optional one is silence). Three occurrences now (SimuCoin's config coerce, the Overview's restated enum defaults, this), so treat it as the default expectation rather than a curiosity: **when you add an optional field to a persisted record, the rebuild and a round-trip TEST are part of the same change.** The harness guard is the only mechanism here that actually holds — save it, load it, assert it survived, and assert an absent value stays absent.
    - **The same function is also where a DEFAULT quietly stops meaning anything.** A coercer that RESTATES its fallback — `sort: o.sort === 'tab' ? 'tab' : 'attention'` — has hardcoded a second copy of the default, so changing `DEFAULT_*` has **no effect** on any stored value that is missing or invalid: the source says one thing and the app does another, with nothing to flag the disagreement. v0.19.0 hit this changing the Overview's default card order; the field validated fine and the new default simply never applied. **Write the fallback as `DEFAULT_X.field`, never a repeated literal** — validate the shape, then defer to the one default (`o.sort === 'tab' || o.sort === 'attention' ? o.sort : DEFAULT_OVERVIEW_OPTIONS.sort`). Numeric/boolean helpers that TAKE the default as a parameter (`num(o.x, DEFAULT.x, lo, hi)`) already have this property, which is why only the hand-written enum branches drifted.

122. **A guard flag must be reset by the function it guards, not by one of its callers (B261, v0.18.4).** Cancelling a connect sets `pendingCancelledRef`, and `runConnect` checks it at three points so a cancelled login is torn down rather than added as a tab. The reset lived in `handleCardConnect`, which is only ONE of four paths into `runConnect` — the tab menu's Reconnect and both attempts of the account-conflict resolve go straight there. So after any cancelled connect, the stale `true` made the next connect return instantly and silently: a Reconnect that spun and did nothing, with no error to explain it. Moving the reset to the top of `runConnect` makes every entry path correct BY CONSTRUCTION, and it is safe because cancellation during that attempt is necessarily set after the line runs. **The general rule: state that scopes to one INVOCATION should be initialised at the start of that invocation. Initialising it at a call site is a promise every future call site has to keep, and `grep` is the only thing enforcing it** — so when you add a guard flag, count the callers of the guarded function before deciding where to reset.

123. **A placement pass that CLAMPS when it runs out of room re-creates the very collision it exists to prevent — drop instead, but never drop the newest (B258, v0.18.4).** The Tableau walks a speech bubble upward until it stops overlapping, then did `top = Math.max(topLimit, bottom - h)`. That clamp shoves the bubble back DOWN into the space the loop just spent twelve iterations escaping, and it recorded the CLAMPED rect in `placed` while the loop had tested the UNCLAMPED one, so every later bubble was measured against a rectangle that was not where the previous bubble actually went. In a quiet room you never see it; in a busy one — precisely when bubbles stack — the newest pile back onto the older ones. **Dropping is the right resolution when the list is sorted by importance** (newest-first here, so whatever runs out of room is the stalest thing on screen, which was already unreadable) — and it also lets an arbitrary cap go, since "show what fits" self-adjusts to the panel and the font. **But an unconditional drop is its own regression:** in a short panel-tab stage even ONE bubble may not fit, and showing no speech at all is worse than the overlap. So the first item placed always clamps and renders; only subsequent ones drop. Any collision layout that can exhaust its space needs both halves of this — a drop policy AND a guaranteed-visible element.

124. **An "always on top" constant must be DERIVED from the threshold it has to clear, never guessed — and never expressed as an offset (F96, v0.18.4).** The Debug window is lifted above ordinary floating windows at render time. The first cut was `DEBUG_Z + win.z` with `DEBUG_Z = 10_000`, which looks generous until you notice `WindowLayer.focus()` renormalizes stored `z` back to 1..N only when it passes **100,000** — click-to-front increments `z` on every focus, so an ordinary window can legitimately reach 15,000 and outrank Debug. The failure is invisible for a long time and then reproduces the exact complaint the feature was built to fix ("it disappeared behind the game window"). Fixed by exporting `Z_RENORMALIZE_CEILING` from `freeLayout.ts`, consuming it in `WindowLayer`, and defining `DEBUG_Z = Z_RENORMALIZE_CEILING + 1` as an ABSOLUTE value — one contract, one place, and no arithmetic that can be outgrown. Same reasoning applies to any "must sit above X" constant (modal tiers, toast z, overlay insets): if two files each hold a number and only their RELATIONSHIP matters, export one and compute the other.

125. **A bare symbol glyph is a PLATFORM-DEPENDENT rendering decision — on macOS it can arrive as a colour emoji that ignores `color` (B262, v0.18.4).** Unicode splits these: a codepoint with `Emoji=Yes, Emoji_Presentation=No` (⚔ U+2694, ⚙ U+2699, ⚡ U+26A1, ⚠ U+26A0 and most of the Miscellaneous Symbols block) *defaults* to text presentation per spec, but the font cascade decides in practice. Windows resolves them monochrome out of Segoe UI Symbol; **macOS can resolve them through Apple Color Emoji**, and a colour glyph ignores the `color` property entirely — so an icon themed to sit in an accent band arrives in full colour and stops matching, which is B260's symptom reached by a different route. **Append U+FE0E (VARIATION SELECTOR-15, TEXT presentation) to force the monochrome form.** Do NOT confuse it with U+FE0F, its emoji-presentation opposite — that is the one to avoid, and they differ by a single digit. Three riders: **(a)** VS15 is best-effort, not a guarantee — it asks for a text glyph and still needs a font that HAS one, so if a platform still shows colour the answer is a codepoint that is not emoji-classified at all (U+2691 ⚑, U+2630 ☰, U+2756 ❖), not more selectors; **(b)** the inverse is a legitimate choice — the Moons season icons carry U+FE0F on purpose because they WANT colour, so audit for intent rather than blanket-converting; **(c)** you cannot verify this from the wrong OS, so treat a sweep of existing glyphs as needing a screenshot from a tester on that platform first (26 bare ones were found and deliberately left for exactly that reason). The tell to watch for in a report: *"the icon is the wrong colour"* or *"it doesn't match the rest of the header"* from one platform only.

126. **A `*` in a selector whose class TOGGLES over a large subtree is a full-subtree style recalculation, every toggle — enumerate the classes instead (v0.18.5, Sekmeht).** [map-panel.css](src/renderer/styles/map-panel.css) froze map animations with `.genie-pan-dragging *` and `.genie-anim-off *`. Blink builds a style INVALIDATION SET per selector: `.a .b` records *"when `.a` toggles, recalc descendants carrying `.b`"*, while `.a *` records *"recalc EVERY descendant."* The pan group holds ~1057 room groups (each a `<g>` + rect + label) plus arcs and auras — on the order of 3–5k elements — so each toggle recalculated the lot. `genie-pan-dragging` is applied on `isDragging || inMotion`, and **`inMotion` flips true on every room change**, so walking toggled it continuously. Style recalc runs on the renderer's MAIN THREAD, the one React, Virtuoso and the whole game-text pipeline share, so the map was stealing frames from the text window. Enumerating the 18 animated classes cut the invalidation set to the elements that actually carry them; behaviour is byte-identical in both modes (verified by script: 18 animated classes, `anim-off` covers all 18, the transient freeze covers 16, exempting exactly the ping and the heartbeat).
    - **The DIAGNOSTIC SIGNATURE is the most reusable part: a display toggle on one subsystem changing the responsiveness of an UNRELATED one means main-thread contention, not a bug in either.** This surfaced as *"turning off Genie Map Animations makes the CONSOLE less sluggish and text flow better"* — which sounds impossible until you remember there is one main thread. Chase it as contention; do NOT go looking for a defect in the text pipeline. Corollary for triage: `mapAnimations` is a PER-CHARACTER setting ([settings.ts](src/renderer/settings.ts)), so turning it off on one character leaves every other tab paying full price — a tester reporting "I turned it off and it's still slow" is usually describing that, not a failed fix.
    - **The comment was the bug.** The original rule justified its wildcard as *"only applies while the dragging class is present (briefly during pan); it's not a global cost."* That was TRUE when written and was silently invalidated when `inMotion` widened the class to all movement. Nobody re-read the comment, because nothing forces you to — this is the pitfall #111/#113 family (invisible to build, tsc and review). **When you widen WHEN a class/flag applies, re-read every rule that justified its cost by assuming it was rare.**
    - **(a) Express an exemption by OMISSION from the enumerated list, not by a counter-rule.** The ping and heartbeat each needed `animation-play-state: running !important` to win back against the wildcard; enumerating deletes both counter-rules, and an exemption that cannot be out-specified is one fewer specificity duel to lose later (pitfall #107/#111).
    - **(b) Build the class list BY SCRIPT, not by eye.** The four `genie-mote-drift-*` classes declare their animation through LONGHAND (`animation-name` / `animation-duration`), so a `grep "animation:"` sweep misses them and would have silently left four effects running straight through the freeze. Parse the rule blocks for any `animation`/`animation-name` declaration, and after editing, assert coverage programmatically (animated set ⊆ frozen set) rather than trusting the diff.
    - **(c) Keep the PERSISTENT list comprehensive even where entries are inert today.** When `genie-anim-off` is active `showEffects` is false, so most of those classes are never mounted — but `genie-heartbeat` and `genie-aura-fire` already render OUTSIDE that gate over the uncapped `visibleNodes`, and the next effect to do so would silently escape a list pruned to "what is mounted right now." Correct-by-construction beats minimal.

127. **Two handlers that answer the SAME question must share one source of truth — otherwise a feature can work by ACCIDENT and die the day you harden the other one (B265, v0.18.5).** Electron has two permission hooks: `setPermissionRequestHandler` (asked when code calls `requestPermission()`) and `setPermissionCheckHandler` (asked for status reads — `navigator.permissions.query`, **and `Notification.permission`**). Ours disagreed: the request handler allowed only `local-fonts`/`midi`, while the check handler was `() => true`. So `Notification.permission` read `granted` and the trigger `notify` action worked — while `Notification.requestPermission()` resolved `denied` in the same session. The feature depended entirely on the blanket-true check handler, and the OBVIOUS security hardening (mirror the request allowlist into the check handler) silently kills it. Fix: one `ALLOWED_PERMISSIONS` set consumed by both. **The general rule: when two code paths can answer the same question, give them ONE list and make the redundancy structural, not coincidental** **The RENDER-layer form of this is a PREVIEW (B281, v0.19.1):** a preview and the real thing are two code paths answering "what does this look like", and the Contacts previews built a plain `style={{ color }}` while the game renderer resolved the template's text effect — so a rainbow template previewed as flat colour and you only discovered the effect by meeting that contact in play, which is precisely what a preview exists to prevent. **The fix is a shared BUILDER (`paintContactText`), not a second correct copy** — two copies is how they drifted in the first place. Ask it of any preview, swatch, sample or live-style widget: does it call the same function the real surface calls, or does it merely resemble it today? — the same instinct as pitfall #124 (derive the constant, don't restate it) and #92 (two surfaces answering one question must share the gather).
    - **v0.19.0 proved the sharp edge of this.** The IPC channel map existed in THREE copies: `shared/types.ts`'s exported `IPC`, plus private `const CH` objects in `main.ts` and `preload.ts`. A new channel went into two of them. `CH.SEND_USER_TEXT` in main was therefore `undefined`, so `ipcMain.on(undefined, handler)` registered on a channel literally named **"undefined"** while the renderer published on `send-user-text` — **every Quick Send and every Overview send silently reached nobody**. Note what did NOT catch it: both builds passed (valid code), the renderer `tsc` passed (it does not include `src/main` — see Check zero), and every individual site reads perfectly. **Three durable lessons.** *(a)* `ipcMain.on`/`webContents.send` take a plain `string`, so an `undefined` channel is a silent no-op at BOTH ends — no runtime error in either process. *(b)* When a constant map is duplicated, adding a key is a change you must make N times and only ONE of them is where you are looking; the fix is to DELETE the copies (both private maps are now just `= IPC`), not to remember harder. *(c)* **The diagnostic signature is worth memorising: a feature that fails COMPLETELY and SILENTLY across every target, with no error anywhere, is a WIRING break — grep the BUILT output for the channel/route string before re-reading the logic.** `grep -c send-user-text dist/main/main.js` returned **0** while preload and the renderer bundle both had it, which located the bug in one command after a long stretch of theorising about alias resolution.
    - **The process half is the more valuable half. I diagnosed this as "notify can never fire", which was WRONG, and only a harness caught it before I "fixed" working code.** A permission bug is invisible to the build, to `tsc`, and to reading — the request handler genuinely does deny `notifications`, so the code reads exactly like a bug. **Before changing code to fix a runtime-behaviour claim you have not observed, reproduce the behaviour.** A ~40-line throwaway Electron app that sets the real handlers, loads a page and prints `Notification.permission` / `requestPermission()` settled it in one run — and by parameterising the handler config it also proved the *latent* trap (allowlist without `notifications` → `denied`), which is what justified the change at all. This is Principle #10 in its literal form: the grounding was cheap and available, so a guess was never acceptable.
    - **Two harness gotchas worth reusing.** *(a)* Put a throwaway Electron app under `tmp-*/` in the REPO, not the scratchpad — the scratchpad path contains `--`, which Electron cannot resolve as an app directory (pitfall #116). *(b)* A page script racing `executeJavaScript` will report nothing: have the page park its result on `window.__result` and poll for it, rather than assuming your injected callback is installed before the page finishes.

128. **When MAIN blocks a lifecycle event on a RENDERER answer, the renderer can die after agreeing to answer — and a liveness timeout alone will not save you (F99, v0.18.6).** The close confirmation asks the renderer to show a themed modal and holds the window open until it replies. The guard was an ack timeout: no ack in 2s ⇒ fall back to a native dialog. That is necessary and **not sufficient**. Once acked, the timer deliberately stands down (so a slow human decision never stacks a second dialog), which means the promise then settles ONLY on a response — so a **Ctrl+R reload** (a reserved accelerator that stays available) or a render-process crash *after* the ack leaves it pending forever, `confirmingClose` never clears, every later close short-circuits on it, and **the app can never be quit**. Precisely the failure the fallback existed to prevent, re-entered through a different door. Fix: also reject on `win.once('closed')`, `webContents.on('render-process-gone')` and a main-frame document swap, all routing to the same native fallback. **The rule: enumerate every way the counterparty can disappear — not just "it never answered", but "it agreed to answer and then vanished" — and give each one the same escape hatch.**
    - **(a) A same-document navigation is not a death.** `did-start-navigation` also fires for in-page navigations; treating those as "renderer gone" would stack a native dialog on top of a perfectly good themed one. Filter on `isMainFrame && !isSameDocument`.
    - **(b) The fallback must tolerate the corpse.** The themed path can reject *because* the window was destroyed, so the native fallback would then try to parent a dialog to a destroyed window — guard it and resolve "don't proceed", letting the teardown already in flight own the outcome.
    - **(c) Send the ack from the code that RECEIVES the request, not from the modal.** It is a liveness signal, not a paint signal — if the modal's own render threw, you still want main to know the renderer is alive rather than silently falling back.
    - **(d) The dialog must out-z everything, and be ASYNC.** Main is blocked on it, so anything that can paint over it is a hang the user cannot resolve (context menus sit at 2100 and everything else below them — the tier map is in pitfall #118(d); this is 10000). And `showMessageBoxSync` would block main — which owns every session socket — stalling the game for every character while the dialog sits open.

129. **A `useSyncExternalStore` `getSnapshot` MUST return a CACHED value — rebuilding a derived array inside the getter is an infinite render loop, not a perf nit (v0.19.0 Views).** React calls `getSnapshot` on every render and compares with `Object.is`; a getter that does `[...map.values()]` returns a new array identity each time, so React concludes the store changed, re-renders, calls it again, and warns *"The result of getSnapshot should be cached to avoid an infinite loop."* [overviewStore.ts](src/renderer/overviewStore.ts) keeps `let digestSnapshot` reassigned ONLY inside its coalesced `flush()`, and the getter is a bare `return digestSnapshot`. **This is the FIRST `useSyncExternalStore` in the codebase** (every other cross-component store here is a CustomEvent + `useState`, the `toasts.ts` shape), so there was no local precedent to copy — if you add a second one, copy this one's cached-snapshot discipline, and remember the same rule binds any DERIVED slice (a `.filter()` in the getter is the same bug). Object-valued slices need it too: the store's `options` object is replaced only when a value actually differs (`optionsEqual`), so consumers don't re-render on a no-op write.

130. **Gate a store payload with a KEY LOOP over a frozen key list, never a hand-written `&&` chain (v0.19.0 Views).** `SessionsContext.updateStatus` ([SessionsContext.tsx:165-180](src/renderer/SessionsContext.tsx#L165-L180)) compares its 16 fields with a literal `&&` chain — which works, and is exactly the shape that fails SILENTLY the day someone adds a 17th field to `SessionStatus` and forgets the comparison: the value is written, no listed field changed, the bail returns `prev`, and the new field never re-renders anything. Nothing warns; it looks like a stale-UI bug months later. [overviewStore.ts](src/renderer/overviewStore.ts)'s `CharacterDigest` gate instead iterates a frozen `DIGEST_KEYS` list and compares `a[k] !== b[k]` — and (B298 correction, v0.19.2) the list is derived from a **`Record<keyof CharacterDigest, true>` companion object**, because that is what actually makes omission a compile error: a bare `[...] as const` list only types its entries as a SUBSET of the keys, so a forgotten field compiled clean. Use the Record shape for any new key list of this kind — a missing key AND a stale extra key both fail to compile. Two riders: every field in such a payload must be a SCALAR (an object or array fails `!==` every time and defeats the gate — the digest serialises its flag list to a string for exactly this reason), and the same "make omission a type error" instinct applies to any coerce/rebuild function (pitfall #121). Worth converting `SessionStatus` to this shape opportunistically; it was deliberately left untouched by the Views work so a new feature couldn't destabilise the character tab strip.
131. **An OVERLAY that deliberately leaves the surface beneath it laid out also leaves that surface FOCUSABLE — a covered input still takes keystrokes, and Enter still submits (v0.19.0 bug check).** The Overview is `position:absolute; inset:0` over the session shells, which stay laid out on purpose so the active character's virtualised scrollback keeps measuring (returning to Session view then needs no re-snap). The cost is that the command bar underneath is not `display:none` — it is a perfectly good focus target the user cannot see. `App` blurs it on the transition into the view, and GameWindow's document `keydown` early-returns while it is open, and **neither is sufficient**, because a document-level listener does not control where the BROWSER routes a key: whatever holds focus gets it. Three ways focus got back in: **(a)** a GameWindow MOUNTING while the view is open (connecting a character, or moving one into this window) — `autoFocus` plus two mount effects, none view-aware; **(b)** the cursor-marker (`@`) path in `sendCommandSequence`, newly reachable because v0.19.0 routes the Overview bar and Quick Send through `dispatchUserText`; **(c)** the tab-switch refocus, which was already guarded. The symptom is the nastiest kind — typing produces nothing visible and Enter sends it to the game, i.e. the same "invisible input" class as B199. **Rules: (1)** funnel every programmatic focus of a coverable field through ONE guarded helper (`focusCommandInput`) so a new call site cannot forget, and gate the mount-time `autoFocus` ATTRIBUTE on the same condition (it is not a call and cannot go through the helper); **(2)** a blur-on-enter effect must have a **focus-on-leave** counterpart, or the view returns with focus nowhere and the user has to click before typing; **(3)** for a type-anywhere listener on an app-level surface, guard on the **LOCATION** of `document.activeElement` (an allowlist: body/null, or inside this surface) rather than a denylist of tag names — the app bar sits ABOVE the overlay, so a modal is one click away, and a modal holding focus on a plain `<button>` passes every tag check while still being somewhere a key must not be stolen from. GameWindow's F60 uses `anyModalOpenRef` for this, which is per-session state an app-level surface cannot reach; the location allowlist gets there without it and stays correct for the next modal added.

132. **Widening a piece of state's SCOPE silently removes whatever its narrower LIFETIME was doing for you (B284, v0.19.1).** The Overview's input-bar target lived in component `useState`, and the bar is rendered `{open && <OverviewInputBar/>}` — so leaving the view unmounted it and the target reset to "all characters" for free. Moving it into a module store (so cards could select it) was correct for the feature and quietly deleted that reset: module state persists, so re-entry restored the last target — the opposite of the documented behaviour, and a straight test failure. **Nothing changed at the place that depended on it, and nothing documented it**, because it was never code — it was a property of where the state lived. **When you lift state to a wider scope (component → module, local → context, memory → disk), enumerate what the OLD lifetime guaranteed** — reset on unmount, clearing on navigation, dying with the session, being per-window — and re-create each one explicitly. The reset belongs wherever the transition happens (here `setViewMode`, so it holds for the toggle, the menu action and `/view` alike), not in whichever component happened to own it before. Same family as pitfall #121 (a rebuild destroys what it does not name) — both are losses you cannot see at the call site.

133. **Every stream needs a DELIBERATE routing decision, and "watched" means a tab EXISTS — not that it is showing (B306 + B307, v0.19.3).** Two reports in one day, same symptom ("nothing came out"), two different holes in the stream model.
    - **(a) A stream with no decision is silently buffered.** The stream-text case ([GameWindow.tsx](src/renderer/components/GameWindow.tsx) `STREAM_FALLBACK` lookup) shows an UNWATCHED stream in main ONLY if the table has an entry for it; otherwise its lines go into `streamLines[id]`, which nothing displays until someone adds a panel. That is correct for STATE streams (spells/inv/group/moonWindow — clear-and-rewrite tables that would spam main) and for NATIVELY-DUPLICATED ones (talk/whispers/`ooc` — DR sends an outside-the-block copy, so a fallback double-prints, B136/pitfall #49). It is a BUG for any narrative stream that has neither property — `shopWindow` (B306, JadedSoul) sat there for the feature's whole life: `shop` printed nothing unless a Shopping panel happened to be open. **The rule: when a new DR stream id turns up (a discovered panel with a game-supplied title is the tell), classify it explicitly — fallback / state / native-dup / alias — and record the decision in DESIGN's stream table.** Grounding order: the report itself (blank output = no native copy), then the sibling clients (Frostbite's `xmlparserthread.cpp` id branches, Profanity's known-stream regex), then a Debug raw-XML capture. `chatter` is the one id still undecided (needs a capture).
    - **(b) Existence ≠ visibility.** `watchedStreamsRef` answers "does a tab for this id exist in a rendered surface" (correct for routing game text into panels — a background tab gets an unread dot, like every stream client). It is the WRONG question for "should client-generated output go to the panel or the game window": Catch Me Up keyed on it, so a background lbAI tab swallowed the recap with only an unread dot to show, which from the game window read as "closed, and nothing came out" (B307, Sekmeht). The visibility question is `activeIdsRef` — and note that set was itself UNGATED on the zones' `*Added` flags in panels mode (a removed zone's stale activeId counted as visible, pitfall #39's shape), fixed in the same change. Any future "is this on screen?" decision uses `watched && active`; any new zone/tab aggregation gates on `*Added` and branches on `layoutMode` (pitfall #79).

134. **A boolean FLAG and a human-readable MESSAGE travelling together is a trap — never gate behaviour on the prose (PR #2, v0.19.4).** `ConnectionStatus` carries both `connected: boolean` and `message: string`, and GameWindow's handler cleared its `dropped` flags only on `s.connected && s.message === 'Connected'`. That made the FLAG decorative and the STRING load-bearing: the moment a new connect path sent different wording — attach mode's `'Attached'` / `'Re-attached'` — the tab stayed greyed out and the toolbar kept offering Login while game text streamed happily into that same window. Nothing errors; the feature simply looks half-broken. **Keyed on `s.connected` alone it is correct by construction**, because main sends `connected: true` only from a genuine connect (every progress line goes out `connected: false`) — and the message is then free to say something useful, which the session log records. **The rule: when a payload pairs a state flag with a display string, branch on the flag and treat the string as presentation.** A string comparison silently converts every future caller into a bug, and the compiler cannot see it. Note the deliberate ASYMMETRY here: the mirror-image `!s.connected && s.message === 'Disconnected'` check was LEFT alone, because there the string does real work — it separates a final drop from the progress lines that precede a successful connect. So the test is not "never compare a message" but "is the string carrying information the flag already carries?"

135. **A state RESYNC is not the live stream — the same tag can carry its data in different FIELDS (PR #2, v0.19.4).** Lich's detachable-client init pushes `<progressBar id='health' value='0' text='health 100/'/>`: the numbers live in `text` and `value` is hardcoded to `0`. The parser had only ever seen the LIVE shape, where `value` is authoritative — so attaching painted every vital bar at ZERO, which is worse than blank because a zeroed bar looks like live data and reads as "one hit from dead". **When you add a replay/resync/handoff path, do not assume the existing parser branch generalizes — read what the SOURCE actually emits for that path.** Two riders learned here: **(a)** gate the new interpretation as narrowly as the evidence supports (`value === 0` only, so live bars take exactly the path they always did) rather than rewriting the shared branch; **(b)** the resync's own text was DR-specific — `health 100/` with no maximum, because Lich derives max by scanning DR's percentage bar text and finds one number, while GemStone sends `health 100/100`. A `(\d+)/(\d+)` pair regex parses GS perfectly and silently never matches DR, which is the shape of bug that ships to one game and not the other. Full protocol citations: Knowledge.md §19.

136. **A scripted insert into a document with MORE THAN ONE table lands in the first one — anchor on the SECTION, never on a separator or header shape (v0.19.4 doc pass).** Adding two resolved bugs to BUGS.md, the insert anchored on `^\|---\|---\|---\|---\|` and took the first match — which is the **Open Bugs** table's separator, not **Resolved**'s (BUGS.md has five such separators). Both rows landed under Open, carrying the Resolved table's column shape, so `v0.19.4` rendered under the "Reporter" heading. Nothing errors: markdown renders a wrong-but-well-formed row perfectly, and the file still parses. **The rule: for a multi-section document, find the SECTION heading first and the anchor within it** (`awk '/^## Resolved/{r=1} r&&/^\|---/{print NR; exit}'`), and after any table edit **assert the row's column count matches its header's** (`awk -F'|' '{print NF}'`) plus which section it landed in. This is the pitfall #112 family (anchoring inside a structure you didn't mean to target) specialised to prose files, and it is *easier* to hit there than in YAML because there is no parser to complain — the only detector is reading the result. **Rider, same session, same class:** a `perl -pe` replacement of `\$119.` was read as capture group **$119** (undefined → empty), silently deleting the line it was meant to follow in the Knowledge.md TOC. When a `$N` backreference is followed by digits, brace it (`${1}19`) — and re-read the surrounding lines, not just the inserted one.

137. **`dragend` never fires on an element that UNMOUNTS mid-drag — so any drag state it was supposed to clear gets stuck (B311, v0.19.5).** HTML5 delivers `dragend` to the SOURCE node. If the drop causes that node to be removed — here a cross-frame tab adoption removing the tab from the source frame's list — the browser fires `dragend` on a detached node and React's synthetic handler never runs. The source frame's `dragTabId` then stayed set **forever**. **When a drag can end by the dragged element being moved/removed from the component that started it, `onDragEnd` is not a reliable clear — add a state-derived reset** (`if (dragId && !items.some(i => i.id === dragId)) clear()`), which is exact because the component already re-renders with the new list. **The second, more general half is why this was severe rather than cosmetic: an effect with NO dep array whose only guard is a state flag is exactly as safe as that flag's worst clearing path.** The F46 FLIP animation effect runs after every render, PanelFrame re-renders on every game batch, and its sole gate was `dragTabId === null` — so a stuck flag turned it into a permanent per-batch hot loop doing forced reflows (`getBoundingClientRect`) plus style writes. It also **self-sustained**, because it measures positions while its own 120ms transition is still running: the intermediate value gets stored, the next render sees a >1px delta, and it re-animates. Two rules: a no-deps effect gated on a flag needs that flag to be unstickable, and **never store a measurement taken mid-transition as the baseline for the next comparison** (measure before the animation starts, or exclude transitioning elements). Symptom shape worth recognising: tabs "blinking in and out" plus an unrelated subsystem's FPS collapsing — that second part is pitfall #126's main-thread contention signature, and it is the clue that the cost is per-render forced layout rather than anything wrong with the subsystem that looks broken.

138. **A conditionally-rendered child in an `overflow-x: auto` strip can TOGGLE a scrollbar, and that scrollbar's height is taken out of the `flex: 1` sibling below it — reflowing everything (B312, v0.19.5).** The panel tab strip's unread dot is `{isUnread && <span/>}` with `width: 6px; flex-shrink: 0`, so a stream tab going unread/read changes its width. `.panel-tab-list` has CONTENT-DRIVEN height, so once the tabs crossed the overflow threshold the horizontal scrollbar appeared and ADDED its height to the strip; `.panel-frame-body` is `flex: 1`, so the body lost exactly that much and every line of game text reflowed — repeatedly, as unread states churned during play. **The symptom does not look like a layout bug:** the reported form was text "quivering" and HIGHLIGHTED runs appearing to bold and revert, because a highlight's filled background makes a subpixel re-raster far more visible than on plain text — it reads as a font/effect problem, and the reporter used no text effects at all. It was also **unreproducible for years**, because it needs the strip sitting right AT the threshold, which is panel width × tab count × label length × font size — change any one and it stops. **Rules: (a)** any strip whose height feeds a `flex: 1` sibling must reserve its scrollbar gutter (`overflow-x: scroll`, since `scrollbar-gutter` does NOT apply to a horizontal bar) rather than letting it toggle; **(b)** treat *any* conditionally-rendered element inside an overflow container as a potential layout oscillator — a badge, a dot, a count, a close button that appears on hover; **(c)** when a text-rendering complaint has no text-rendering cause, suspect a sub-pixel reflow of the container. **And the compounding half, which is pitfall #67's cousin:** `scrollbar-width: thin` was set on that list, which per **B178** DISABLES all `::-webkit-scrollbar` styling in modern Chromium — so the `height: 3px` rule beneath it had been inert and the real bar was Chromium's native ~11px, making every toggle a large jump. **Setting `scrollbar-width` or `scrollbar-color` silently voids every `::-webkit-scrollbar` rule for that element; the two cannot be mixed.** Grep for that pair whenever a scrollbar "doesn't follow the CSS" or is unexpectedly thick.

139. **A SELF-RETIRING timer must wake for every kind of state that ages, not just the obvious one — enumerate the wake-ups from "when does the display next change?", never from "when does the main thing expire?" (B315, v0.19.6).** The Spell Monitor's 1 Hz clock is deliberately self-clearing (B292 — an interval that never clears is a session-long cost), armed from a `soonest` derived over the effects that are counting down. That was correct while countdowns were the only thing that aged. The moment a SECOND kind of ageing state arrived — spent cells with a one-roisan grace — the derivation silently under-covered it: once the last countdown ran out the interval cleared, `now` froze, and because the grace is measured against `now` the greyed cell stranded until something else forced a re-render. **A frozen clock is invisible in the common case and worst in the quiet one:** here the very scenario that retires the clock (your last buff drops, nothing else is up) is also the one where nothing is left to cause the repaint that would have rescued it, so the bug only appears when the app is otherwise doing nothing — the hardest state to notice and the least likely to be reproduced by exercising the feature. **The rule: derive the wake-up from the QUESTION "what is the next moment the rendered output differs?", and take the union over every ageing source** (here: live anchors ∪ `endedAt + TTL`, the latter gated on the layer that renders them, since state nobody draws has nothing to expire). **The tell during review is a mismatch between the filter that RENDERS and the set that TICKS:** `liveSpellEffects` filtered on two time-based conditions while `soonest` bid on one — any render-time predicate that reads the clock needs a corresponding bid, or it can never fire. Two riders: keep the strictly-future filter (`> now`) on every bid, since the interval self-clears at `soonest` and a moment that stayed in the value would leave the dep unchanged and freeze everything behind it; and remember this generalises past intervals to any derived wake-up — a `setTimeout` chain, a scheduled re-render, an animation frame armed from "the next deadline".

140. **A value that changes far more often than the props bag it would ride should be passed as a REF, not a prop — a ref object's identity is stable, so it reaches its one consumer without re-rendering that consumer's siblings (v0.19.6, the Spell Monitor's feed readout).** `renderExperienceContent` passes the SAME prop list to every Experience, so a new prop is not scoped to the component that wants it: adding `spellsPulse: number`, which ticks on every DR repaint, would have re-rendered Moons (0.38ms) and the Tableau on every pulse — silently undoing exactly what `deriveSpellState`'s delta gate exists to buy (pitfall #82c). Passing `spellsPulse: { current: SpellPulse }` instead costs the siblings nothing, because the object identity never changes and their memo comparison always passes. **The technique has one real cost, and you must pay it deliberately: a ref write triggers no render, so the CONSUMER has to own a clock** — here the component's existing 1 Hz tick, which is why the readout keeps that interval alive (and is a ⚙ layer you can switch off). **Reading `.current` during render is a smell that is acceptable only under specific conditions**: the value must be DISPLAY-ONLY (nothing derives state from it, no ref is mutated mid-render — that mutation is what made pitfall #70's memo double-count under StrictMode), and the component must re-render often enough that the reading cannot sit stale. If either fails, use a module store with `useSyncExternalStore` (pitfall #129, the Overview precedent) so only leaf subscribers re-render. **The diagnostic question when adding any prop to a SHARED props bag: who else receives this, and how often does it change?** — a per-tick value on a bag shared by three memo'd components is three components' worth of waste that nothing warns you about. Same family as pitfall #105 (volatile tracking data must not share an identity with render data), applied across a prop boundary rather than within one array.

141. **Esc must close exactly ONE dialog — the topmost — so every dialog registers through `useEscapeClose`, and every OTHER Esc consumer must `preventDefault` (B341, v0.19.7).** Dialogs used to each bind their own `document` keydown. Some didn't stop propagation, so one Esc closed a dialog AND whatever sat under it; many had no Esc at all. [useEscapeClose.ts](src/renderer/hooks/useEscapeClose.ts) is a mount-ordered stack plus ONE `window` keydown listener in the BUBBLE phase. Window-bubble is the last stop a keydown makes, so everything nearer the target gets first refusal: the Quit confirmation's document-capture handler (z 10000, must always win), a search box clearing its text, the slash palette, a context menu or dropdown inside the dialog. **The contract for new code: (a)** a dialog calls `useEscapeClose(onClose)` in a component that MOUNTS when it opens — never `enabled: isOpen` in a long-lived parent, because stack order IS mount order and a parent mounted at startup would sit beneath every dialog opened after it, so Esc would close the wrong one; **(b)** anything else that handles Escape must call `preventDefault()` when it consumes the key, or the dialog under it closes too — the hook bails on `defaultPrevented`; **(c)** an INLINE (non-portaled) dialog passes `ref`, because a hidden character tab stays mounted (pitfall #24) and an entry with no layout box must be skipped rather than closed out of sight; **(d)** `enabled: false` means "not a dialog right now" and lets Esc FALL THROUGH to the dialog beneath — to swallow Esc while a dialog is busy (a save, an attach), keep it enabled and pass a close that does nothing while busy, mirroring a disabled ✕. For an inline App-level dialog with no component of its own, render `<EscToClose onClose={…} />` ([App.tsx](src/renderer/App.tsx)) inside it — it renders nothing and registers while mounted. Esc does exactly what the dialog's ✕ does; a search field clears its text first and lets Esc through only when empty. **Diagnostic signature:** "Esc closed two things" means an Esc handler that didn't `preventDefault`; "Esc closed the dialog behind the one I was looking at" means a hook registered in a component that mounted too early. **(e) The stack also owns FOCUS (v0.19.7, B375/B376).**
    - Opening the first live dialog blurs a covered command input (`.command-input`, `.ov-inputbar-input`), so Enter can't reach the game from under a dialog.
    - Closing the last one calls the home focus App registers through ConfirmHost (`setDialogHomeFocus`: the Overview's input bar in the Overview, else the active command bar). It only acts when focus was left nowhere or sits in the app bar.
    - A dialog therefore needs no focus-restore code of its own. Quick Send's `closeQuickSend` predates this and stays compatible: its rAF runs first, so the stack then finds focus already placed and does nothing.

142. **A clickable element that isn't a `<button>` is invisible to the keyboard — make it a button, or spread `pressable()` onto it (B335, v0.19.7).** A `div onClick` has no tab stop and ignores Enter/Space. Worse, a `<label>` wrapping a clickable div forwards clicks to NOTHING — a label forwards only to labelable elements (input, button, select, textarea…) — so a switch built that way ignores clicks on its own text. [pressable.ts](src/renderer/utils/pressable.ts) spreads `role`, `tabIndex`, `aria-pressed`/`aria-selected` and an Enter/Space handler; it ignores keys whose target is a NESTED control (`e.target !== e.currentTarget`), so a row's own ✕ keeps its Enter. The global `:focus-visible` ring in [global.css](src/renderer/styles/global.css) already styles `[role=button]`, `[role=tab]` and `[tabindex]`, so no per-component focus CSS is needed — unless the element's own rule sets `outline: none`, which silently removes the ring. **Prefer a real `<button type="button">` whenever the markup allows** (reset its chrome in CSS); pressable is for rows, cards and tabs whose layout a button would disturb. Same class as UX #8: a control a user can't reach is as unfinished as one that doesn't explain itself.

143. **On macOS, Option REWRITES `e.key` — so a key-combo recorder that reads `e.key` produces a different string on a Mac than on every other platform (B347, v0.19.7).** Option is the Mac's compose key: Option+T reports `e.key === '†'`, Option+Shift+1 reports `'⁄'`. `formatKeyCombo` used `e.key`, so a Mac recorded `Alt+†` while Windows, Linux, and every Genie/Frostbite import record `Alt+T` — imported and transferred `Alt+` macros never fired on a Mac, and a macro recorded there was unusable anywhere else. **Name the key from `e.code` (the PHYSICAL key) when Option is held on a Mac**: letters from `/^Key([A-Z])$/`, digits and punctuation through `MAC_OPTION_CODE_MAP` ([macros.ts](src/renderer/macros.ts)), which stores `[unshifted, shifted]` US characters so Option+Shift+1 names `Alt+Shift+!` — the string Windows records. Three riders: **(a)** keep the platform OUT of the pure module — `formatKeyCombo(e, { mac })` / `matchKeyCombo(combo, e, { mac })` take an option, and callers pass `IS_MAC`, so tmp-cmd-harness can exercise both platforms (23 cases); **(b)** matching on a Mac ALSO accepts the legacy `e.key` form, so macros already recorded as `Alt+†` keep firing — never strand data an older build wrote (pre-merge check #3); **(c)** `e.code` is layout-INDEPENDENT (it names the US position), which is exactly right for matching a recorded chord and exactly wrong for displaying a typed character — use it for key IDENTITY, never for text. What Chromium on macOS actually reports in `e.code` under Option is still unverified from Windows; the harness locks the mapping, a Mac tester confirms the input.

144. **A stylesheet's position in the IMPORT GRAPH is its cascade priority — a base layer that components refine must be imported before App in main.tsx, never from global.css (v0.19.7, the shared ui.css primitives).** Vite emits CSS in the order modules are imported. main.tsx imports `App` BEFORE `./styles/global.css`, so every component stylesheet App pulls in lands first and global.css lands last. global.css therefore wins every equal-specificity tie against a component rule, and anything it `@import`s inherits that late position. ui.css was first wired in as an `@import` from global.css. That would have let `.ui-btn`'s padding and font-size override every component that took the class for chrome and then adjusted its own layout: the reverse of a base layer, and invisible to the build and to tsc. It is now imported on its own line in main.tsx ABOVE `import App`. **Rules:**
    - **(a)** A stylesheet meant to be REFINED by component rules goes before App in main.tsx. One meant to OVERRIDE them (global.css's long-standing role) goes after.
    - **(b)** Never reorder those two existing lines. Moving global.css above App would silently flip every tie it currently wins.
    - **(c)** When a rule loses to one it should beat at equal specificity, work out which file comes first before touching selectors.

    This is pitfall #111's source-order mechanism, applied across files instead of within one. **Verified against a real build:** the renderer build inlines all CSS into the JS bundle (there is no separate .css asset), and in the v0.19.7 bundle `.ui-btn{` sits at offset ~600, about.css at ~45k, the Contacts rules at ~180k and global.css's `--modal-w-standard` at ~300k. That is ui.css, then the components, then global.css. To re-check, search `dist/renderer/assets/index-*.js` for one selector from each file.

145. **A native tooltip can't change while it's showing — a `title` built from a ticking value BLINKS (B412, v0.19.7).** Hover text in Lichborne is the browser's `title` tooltip. When an element's `title` changes while the pointer rests on it, Chromium hides the tooltip and makes you wait out the hover delay again. So a title that embeds a live value disappears every time the value ticks.
    - **The instances:** the Spell Monitor's spent cell carried its clear-out countdown in its title ("…clears from the grid in 34s") and blinked every second. The Moons ⟳ listed "12s ago" ages, and blinked on its 2-second re-render.
    - **Why it was misread:** it looked like a timer-strip bug, because the RT chips refresh on the same one-second beat. Sekmeht's own test isolated it: only ENDED cards blinked.
    - **The rule:** a title says what a value MEANS, never the value itself when that value changes on a clock. Put the live number in the element. If the tooltip needs a time, give an absolute clock time ("14:05"), which holds still.
    - **Not a violation:** a title that changes because the underlying FACT changed (a combat gauge, what's in your hands). That's news, not a tick.
    - **Auditing:** grep `title=` for anything built from `now`, `Date.now()`, `formatAgo` or a countdown.

146. **A module-level flag written by a BUBBLING handler is overwritten by every ancestor sharing that handler — record the EVENT's identity, not a per-instance verdict (B420, v0.19.7).** [backdropClose.ts](src/renderer/utils/backdropClose.ts) kept `downOnBackdrop = e.target === e.currentTarget` so a dialog closes only when the press AND the click both landed on its own backdrop (never a text-drag that ended there). That verdict is per-INSTANCE, and a dialog can be a React child of another dialog's backdrop (Lich Setup inside Settings, the wizard's account prompt): one mousedown then runs BOTH handlers, the inner setting `true` and the outer immediately setting `false`, so clicking outside the inner dialog did nothing.
    - **The fix:** store `downTarget = e.target`. The target is the same for every handler the event passes through, so the assignment is idempotent, and each backdrop's click check (`downTarget === e.currentTarget && e.target === e.currentTarget`) answers only for itself.
    - **The general rule:** when a shared helper keeps state ACROSS events and the same event can pass through several instances of it, store a fact about the event (its target, its timestamp), never a conclusion that only one instance can be right about.
    - **Why it surfaced now:** it was latent for as long as nested dialogs existed, and invisible while the inner scrim was opaque — you couldn't see the dialog you were failing to click. B405's transparent nested backdrop made the dead click obvious. **A long-standing bug whose SYMPTOM was hidden by another bug is normal; expect a fix to surface its neighbours.**

147. **`resolveEffect`'s `colorReplacing: false` is a CONTRACT — the caller MUST supply the colour, and a run rendered bare inherits whatever the host happens to pass down (B427, v0.19.7).** The effect system splits into two families, and only one of them paints its own glyphs. `FX_COLOR_REPLACING` is exactly `shimmer, rainbow, gold, gradient, fire, frost` — those clip a background to the text, so a caller must SKIP its inline colour or the two fight. **Everything else — glow, pulse, neon, wave, bounce — keeps the caller's colour, which means the caller has to HAVE one.** `paintContactText` honours this (`...(fx.colorReplacing ? {} : { color })`); the new wordmark painter did not. It branched on `perLetter || colorReplacing` and rendered a bare run for both, so wave and bounce lost the brand's `.toolbar-title-*` spans and inherited `--text-primary` from the document — plain white, on every theme.
    - **The two properties are INDEPENDENT and must be handled separately.** `perLetter` is about DOM SHAPE (wave/bounce need `.hl-fx-ch` spans staggered by `--i`); `colorReplacing` is about WHO OWNS THE COLOUR. Folding them into one branch is what produced the bug — wave is per-letter AND colour-keeping, a combination the single branch could not express.
    - **The file's own header comment lists neon as colour-replacing; the CODE does not.** `FX_COLOR_REPLACING` is the authority (Iron rule — when a comment and the code disagree, the code wins and the comment is the bug). Read the Set, don't trust the prose.
    - **When a per-letter effect must be split across several coloured elements, carry the stagger with `effectContent`'s `startIndex`** rather than re-implementing the letter split — a second copy of that loop is the pitfall #127 drift risk, and an un-offset second span restarts the wave at zero mid-word.
    - **Diagnostic signature:** an effect that renders in the *default text colour* rather than the one you configured means a caller skipped the colour it was supposed to apply — look at the caller's branch, not at the effect CSS.

---

## Proactive bug-checking

The goal is to find bugs before testers do — both new bugs the current change might introduce, and **latent bugs from prior versions that are still living in the current code**. Lichborne is single-developer with a small tester pool; every bug we ship is one a tester has to find and report, costing them and us time. Two complementary practices.

### Iron rule: read the actual code, not memory or docs

Every claim you make about how the code behaves must come from reading the file you're claiming about, IN THIS SESSION. Not from CLAUDE.md, not from pitfalls, not from a Tracker.md entry describing the code, not from "I remember how this works." Documentation describes the code's intent and history; only the source describes what it currently does. The B114 false-positive that prompted this rule (v0.8.7 retroactive sweep): pitfall #33 described the theme-overlay re-apply hook pattern, I worked from that description, claimed a multi-character bug existed, and was wrong — the actual effect at [GameWindow.tsx:954-973](src/renderer/components/GameWindow.tsx#L954-L973) had `isActive` in its deps and re-applied overlays directly on tab switch, which the pitfall didn't mention because the pitfall was scoped to a different scenario. Acting on the false positive would have shipped a redundant fix and possibly introduced a real new bug. The fix-cost of a false positive is higher than the fix-cost of doing the read.

**Verification protocol** when claiming a bug exists or doesn't exist:
- Grep for the specific function / state / effect you're reasoning about.
- Read the relevant lines yourself (Read tool with line offsets, not just the grep snippet — grep context can mislead by hiding deps arrays, guard clauses, or cleanup returns).
- Confirm the deps array of any `useEffect` you reason about — pitfalls describe patterns, not necessarily the current deps list.
- Confirm the cleanup function — pitfalls describe what's supposed to happen, not necessarily what does.
- If a pitfall and the current code disagree, **the code wins** — and the pitfall needs updating.

This rule has no "n/a." Every retroactive sweep claim, every "this is safe because…" assertion, every "no concerns" sign-off must be backed by a read in this session. The cost is 30 seconds of grep + read per claim; the benefit is not shipping false-positive fixes (and not missing real bugs that contradict your memory of how something works).

### Investigating a reported bug — build a fact chain, don't pattern-match a fix

When a tester reports a bug, the job is a **real, hard bug-check of the actual code** — not "this smells like the last bug, ship a plausible patch." The discipline that worked for B189/B190/B191 (and that you should repeat):

1. **Reproduce the symptom precisely, in the tester's words, and note what DOESN'T happen.** "Active char only, not background tabs" / "echoes but no `>` prompt" / "only after tabbing away, not minimizing" — each negative is a clue that *eliminates* whole hypotheses. Write down the exact discriminator before touching code.
2. **Trace it end-to-end by SCANNING THE CODE, link by link** (grep → read the real lines, Iron rule). Build an explicit chain: source → … → effect. Each link is either **confirmed from code** (you read it this session) or **an inference** — and you must label which. B191's chain was 5 confirmed code links + ONE labelled inference (occlusion ⇒ `document.hidden`, which the room pump already relies on). Never present an inference as a fact.
3. **Decide how the cause is knowable:**
   - **Glaringly obvious from the read** (a deleted call, a wrong var, a missing gate) → fix it, then re-read the whole function body (pitfall #86 — a green build is not proof the behaviour survived).
   - **Logic/resolution bug** (which string, which branch, which match) → confirm with the **harness** (`tmp-cmd-harness` / `tmp-scene-harness`), or add a case to it; if there's no harness for that logic, that's a signal to build one.
   - **Timing / environment / lifecycle-dependent** (rAF throttle while occluded, reconnect-in-place, replay race, async measurement) → you usually **cannot** prove it statically. Either **simulate the condition** (a `tmp-*` repro like `tmp-scroll-repro`, or reason from an existing mechanism the codebase already trusts for that condition — e.g. the room pump's `document.hidden` branch *is* the evidence that occlusion throttles rAF), or **drop a temporary `[diag]` log** at the suspect link and have the tester reproduce (pitfall #83's lesson: one real reproduction beats hours of static analysis).
4. **Prefer a fix that is provably safe regardless of the residual uncertainty.** B191's guard (`document.hidden` → skip un-pin) can only PREVENT a state transition that's impossible-by-definition while hidden, so it cannot regress the visible path even if the exact trigger were mis-identified. When you can find such a fix, the one unproven inference stops mattering.
5. **State the residual honestly.** Name the single thing you did NOT prove, say what would confirm it (the diagnostic), and ship the safe fix anyway. Don't pretend certainty you don't have, and don't withhold a safe fix waiting for certainty you can't cheaply get.

The anti-pattern this guards against: the v0.14.1 macro "fix" that pattern-matched "stale sessionId" (a real latent issue) onto a symptom whose *actual* immediate cause turned out to be a deleted send line — and a comment-edit regression then made it worse. Chain-of-evidence + re-read-the-body would have caught it before the tester did.

### Pre-merge checks

**Check zero (mechanical, ALWAYS): `npm run build:main` + `npm run build:renderer` + `npx tsc --noEmit` + `npx tsc --noEmit -p tsconfig.main.json`.** **BOTH tsc invocations — this is not optional and the second one was missing until v0.19.0.** The default `tsconfig.json` includes ONLY `["src/renderer", "src/shared"]`, so a bare `npx tsc --noEmit` never looked at `src/main` at all: every socket, IPC handler, session, Lich-launch and profile path in the main process was outside Check zero for the project's whole life. It shipped a real bug the day it mattered — `ipcMain.on(CH.SEND_USER_TEXT, …)` against a private `CH` map that had no such key, so the handler registered on a channel literally named "undefined" and every Quick Send silently reached nobody (see pitfall #127's v0.19.0 entry). `tsconfig.main.json` catches it instantly. **Baseline: BOTH configs are CLEAN (0) as of v0.19.7** — the main config's one long-standing error (`main.ts`'s `sceneParser.derive(filtered)`) was fixed by B365 with an explicit `GameEvent[]` annotation; its old description here blamed `Array.filter` narrowing, which was wrong — the filter narrowed fine, the scene events pushed onto the result didn't fit. ANY error in either config is yours, and CI runs both on Windows AND Ubuntu. Vite/esbuild only TRANSFORM TypeScript — they do **not** typecheck, so a reference to a deleted variable sails through both builds and crashes at runtime (B166's first draft shipped a dangling `debugDockHeight` through two "successful" builds; only `tsc` caught it). **`noUnusedLocals` is ON as of v0.18.1** (tsconfig.json) — it earned its place by catching **B239**, where a `useState` SETTER was never called, so the flag it gated (`readOk`) sat at its initial `false` and the Lich Dashboard's Scripts **Edit button was permanently disabled** for a whole release. *A state setter reported as unused means whatever it gates is stuck at its initial value* — that class is invisible to the build and looks correct in review. The project was made clean (7 dead imports removed) before enabling it, so it starts at zero. `noUnusedParameters` is deliberately still OFF: its 4 findings are unused PROPS, which means changing call sites — a separate cleanup, not a bug class. `tsc` is now CLEAN (0 errors) — the 2 long-standing `HTMLElement | Window` errors in GameWindow's Virtuoso scroller typing were fixed in v0.17.1 (they were real un-narrowed-union holes — `el.clientHeight` / `ro.observe(el)` used outside the `HTMLElement` guard; fixed by widening the `scrollerRef` guard from `if (el)` to `if (el instanceof HTMLElement)`, which our scroller always is). ANY tsc error is now yours — there is no baseline noise to look past. Especially mandatory after REVERTING or REWORKING a change mid-session (that's the dangling-reference window). **But Check zero is NOT sufficient: build + tsc do NOT catch a DELETED side-effecting statement** — removing a `window.api.sendCommand(...)` / a `setState` / an effect's `return` cleanup leaves perfectly valid code, so both pass green while the behavior is silently gone (v0.14.1: an Edit replaced `sendCommand`'s send call with a comment; both builds passed; the command echoed but never sent — pitfall #86). **After any Edit that touches a function body — especially one that replaces a line with a comment — RE-READ the whole body, not a grep for the token you changed.** A green build proves it compiles, never that it still *does* the thing.

Before declaring a change done, run through these five questions. ~30 seconds per change, catches the classes of bug this codebase has actually shipped (B114 cross-tab overlay drift, B121 cross-feature state writes, the kind of stale-state-vs-new-code mismatches that come from incremental migrations). The right answer to any of these can be "n/a" — the point is to have considered it.

1. **What state does this write that other code reads?** List every var / ref / state your change writes. For each, grep one consumer and read it. Cross-feature state is where the bug class lives: B121 cleared `roomState.desc` and three other subsystems (Room panel, Lich Map findRoom, Genie Map resolver, contacts effect) read it. The pre-check found the same-batch behavior was already absorbed by existing protections (B109 timer, F34 early-return); the after-the-fact discovery would have shipped as B12X. If your change writes nothing other code reads, the answer is "n/a" — say so.

2. **What cleanup runs on unmount vs. on hide?** Tabs in Lichborne don't unmount when you switch — they go `display:none` (pitfall #24). So `useEffect` cleanup only fires on real unmount (close session), not on hide. Anything that should re-fire on *show* (re-apply per-character settings, refresh layout-dependent measurements, reset transient state) needs an explicit activeId-watcher in App.tsx, NOT just a mount-effect in GameWindow. B114 shipped with this gap: theme-overlay re-apply only ran on theme change, not on tab switch, so two characters with different accessibility overlays bleed into each other. If your change relies on a mount/unmount lifecycle, ask "does this need to fire on show/hide too?"

3. **Does the current code handle state written by older versions of itself?** YAML profile files, localStorage entries, the genie parse cache, the panel layout state, custom themes — all of these were written by some prior Lichborne version a user already ran. When your change reads any of this data, what happens if the field is missing (older write), has a different shape (intermediate refactor), or has a value the new code doesn't expect? **Backward compatibility we care about runs in one direction only**: current code reading prior-version state. We do NOT care about older clients reading newer files — testers run the current build, not v0.8.4. The right answers: (a) treat missing fields as their type default (most optional YAML), (b) bump GENIE_CACHE_VERSION when the cached shape changes (pitfall #16), (c) write an explicit migration in `profile-migrations.ts` by SOURCE version when a field is renamed / restructured. The wrong answer is reading stale state and crashing / silently dropping data the user previously saved.

4. **Does this change the `CharacterProfile` or `SharedProfile` shape — and if so, where's the upgrade guardrail?** This is the mandatory profile-safety check; a user's whole setup lives in `profiles/*.yaml` + `_shared.yaml`, and corrupting it on upgrade is the worst bug class we can ship (pre-v0.6.4 wiped everyone's profiles, principle #8). **Always ask it, even when the answer is "no change."** Walk it explicitly:
   - **Did I add/rename/restructure a field on `CharacterProfile` or `SharedProfile` ([profile-types.ts](src/renderer/profile-types.ts)), or change what a `state.*` suffix means?** Adding a NEW *optional* field is non-breaking — it loads as `undefined` on old profiles and needs no migration (just a sensible default at the read site). Renaming, removing, retyping, or changing the meaning of an existing field IS breaking.
   - **For a breaking change**: bump the version constant (`SHARED_PROFILE_VERSION` / `CHARACTER_PROFILE_VERSION` in [profile.ts](src/renderer/profile.ts)) AND register a migration in [profile-migrations.ts](src/renderer/profile-migrations.ts) keyed by the SOURCE version. Migrations must be PURE (the file isn't rewritten unless every step succeeds). The forward guardrail already exists for free: `runMigrations` returns `null` for a future-version file so a downgrade preserves it on disk rather than clobbering it — don't defeat that.
   - **For a NEW sidecar file/format** (like the v0.10.0 `.lb.yaml` Transfer bundle, or the genie cache): give it its OWN `formatVersion` and reject newer-than-known on read (`parseProfileExport` rejects `formatVersion > current`; genie cache bumps `GENIE_CACHE_VERSION`). New folders (e.g. `Exports/`) need no migration but must be created on demand (`ensureExportsDir`).
   - **For a new per-character setting**: it's a new `state.*` suffix — non-breaking by construction (the dynamic `state:` map round-trips it), but decide whether it belongs in the Profile Transfer allowlist (`TRANSFER_CATEGORIES`, pitfall #56) and the `_shared.yaml` vs per-character split.
   The output is one line: "no profile-shape change" OR "added optional field X (default Y)" OR "breaking: bumped to vN + migration registered." Never leave it unstated — a silent schema drift is how a profile-eating bug ships.

5. **Does this change add or alter a user-facing capability — and is its slash-command surface in lockstep?** (Principle #11.) Walk it explicitly:
   - **New capability**: decide its `/command` (or record "no command because X" in the feature's DESIGN section) — new rule types get the full `add`/`remove`/`list`/`edit` verb set + `openRuleId` plumbing; toggles get the `/timestamps` shape; new completable values extend `SlashLiveData`.
   - **Changed capability**: `grep slashCommands.ts` for the feature's noun. Update executors, options, hints, examples, summary formatters, and error messages to match the new behavior — stale command help is a shipped bug, same class as a stale tooltip. Re-run the `tmp-*` slash harness cases if the resolution logic moved.
   - **Removed/renamed capability**: remove the entry (fail-closed covers users' muscle memory with a `/help` hint) or keep the old noun as a `nounAliases` alias.
   The output is one line: "slash surface: added /X" OR "updated /X entries for the new option" OR "n/a — no user-facing capability change" OR "no command by design: <reason>, recorded in DESIGN §N."

These are not a substitute for testing — they're a substitute for the bug report you'd otherwise get next week. The right output is either "no concerns" + a one-line note in the commit message, or a tracked issue if a concern surfaces that's out of scope for the current change.

### Retroactive sweeps at version boundaries

When cutting a new version (or whenever there's a natural pause), do a focused read of the last 3–5 versions' Tracker entries with a different question: **not "did the change work" but "what could a tester still hit that hasn't been reported yet?"** The pattern that ships dormant bugs in this codebase:

- A new feature (Fxx) writes state or uses an effect lifecycle in a way that's correct for its own scope but interacts unexpectedly with an OLDER subsystem the author wasn't focused on.
- The bug is silent — no exception, no obvious wrong rendering. Just a corner case where state drifts (B114), focus jumps unexpectedly (F32 edge cases), or stats accumulate against the wrong target.
- It takes a specific user configuration to trigger: multi-character + accessibility overlay (B114), Ctrl+Tab from a modal text input (F32), Lich script with both `<streamWindow>` and `<pushStream>` for the same id (B123).

For each recent feature, ask the five pre-merge questions retroactively (incl. #5: does its slash-command surface exist and still match the feature's current behavior?). Read the consumers of any state it writes. Verify the cleanup story on show/hide. Check whether older saved state still loads correctly. Don't refactor on speculation — log a real concern with a repro path, or call it clean. The retroactive sweep done for v0.8.7 found B114 (real, fixed in v0.8.7) and noted F32 edge cases (deferred, not fix-worthy without a tester report). That's the right output shape: distinguish "real bug worth fixing" from "future-feature concern" from "minor papercut without a clean fix" and don't conflate them.

**When to do this**: at the cut of every minor version (v0.x.0 → v0.x+1.0) at minimum, and ideally before each patch release when a meaningful feature shipped recently. The cost is one focused review session; the alternative is a tester finding it cold and the trust hit that comes with shipping obvious-in-retrospect bugs.

### Periodic Lich upstream review (a standing exercise)

**From time to time, review the latest Lich against Lichborne** (Sekmeht, 2026-09-14): whenever Lich cuts a release, and otherwise every few weeks. There are two goals, equally important. **(1) Confirm nothing broke** — usually nothing has. **(2) Find new Lich features Lichborne could surface or build on** — this is a source of feature ideas, not just a risk check. The local checkout is `c:\temp\lich-dev\lich-5`; Sekmeht pulls it, so check `git log -1` rather than assuming it's current.

1. **Find the delta.** The last reviewed version is the newest dated entry in Knowledge.md §17. Run `git log <that-tag>..HEAD --no-merges`, read `CHANGELOG.md`, then `git diff --stat <tag> HEAD -- lib lich.rbw ':!lib/gemstone'`. `lib/gemstone/` and `(gs)`-scoped commits never reach a DR front-end, so skim their titles only.
2. **Walk the integration checklist by reading each diff, never by inferring from commit titles:**
   - `lib/main/argv_options.rb`: `--stormfront` → `$frontend`
   - `lib/main/main.rb`: the force-mode listener, the two-line key + client-ID handshake, `inventory_boxes_off`
   - `lib/games.rb` and `lib/util/util.rb`: anything that rewrites the downstream stream (hooks, quiet filters)
   - `lib/common/xmlparser.rb`: the assess / percWindow / injury rules we mirror verbatim
   - `lib/dragonrealms/drinfomon/drparser.rb`: the Position / Balance / NameRaceGuild rules we mirror verbatim
   - `lib/common/vars.rb`: Marshal format and `Vars.save`
   - `lib/common/script.rb`: the `;listall` output format
   - `lib/common/frontend*.rb`: the capability registry, and saga
   - `lib/common/map/`: the map JSON shape
   - `lib/main/detachable_client_target.rb` and ActiveSessions: Attach mode
3. **For anything new that can reach the front-end socket, confirm what Lichborne's parser does with it** — a tag-only line, attributes-only XML, a new stream id (pitfall #133). Reason from `StormFrontParser`'s actual code, per the Iron rule.
4. **Then look for opportunities:** new Lich APIs, feeds, server features, or CLI flags a front-end could use. New files under the Lich repo's `docs/` often explain them.
5. **Record it:** add a dated Knowledge.md §17 entry (the delta, Lichborne impact, tester-support notes, `file:line` citations), write new verified facts up as their own Knowledge.md subsection, and add one "Re-verified against Lich X" line to the Lich version compatibility section above. Bring feature ideas to Sekmeht as proposals rather than building them; change code only if something actually broke.

---

## Commands

```
npm install              # one-time
npm start                # build main + renderer, launch Electron
npm run build            # build only, no launch
npm run build:main       # esbuild → dist/main/main.js, preload.js
npm run build:renderer   # vite → dist/renderer
npm run dev:renderer     # vite dev server on :5173 (NODE_ENV=development)
npm run dist             # build + electron-builder NSIS installer → release/
```

**NEVER run a build while someone is launching or testing the app.** `build:renderer` deletes and rewrites `dist/renderer`, and `build:main` rewrites `dist/main/main.js`; an `npm start` that reads either mid-write loads a truncated bundle and the process **dies instantly with no useful error** — it looks exactly like a code bug. This burned two debugging cycles on 2026-07-28 (Sekmeht: *"Lichborne started then died immediately"*, then a Crashpad `not connected` line), and both times it resolved with no code change, which is the tell. **Both `npx tsc --noEmit` invocations write nothing** (the default config and `-p tsconfig.main.json`), so use them for verification while a tester is running; save real builds for when they're not. Corollary for triage: `crashpad_client_win.cc … not connected` is emitted DURING an abnormal exit — it says the app died, never why, and is often benign noise. Get `node_modules\electron\dist\electron.exe . 2>&1 | Tee-Object` with the build already finished before diagnosing anything.

No unit-test framework — verify changes by running `npm start` and exercising the UI, guided by the current version's `testplans/` checklist (whose `[auto]`-tagged items are locked by the harnesses below — run them before packaging). **Four machine-local regression harnesses exist for the trickiest pure logic** — the two below, **`tmp-moon-harness`** (`node tmp-moon-harness/run.mjs`, 160 cases over the Moons parsing/orbital logic), plus **`tmp-rules-harness`** (`node tmp-rules-harness/run.mjs`), which bundles the REAL rule/profile modules with Node stubs (localStorage / document / window.api) and locks the v0.15.2 global-rules stack: ruleIdentity keys, `asGlobalRules` + the `_global` virtual-scope store, `applyProfileImport`'s global-awareness (merge/idempotency/global-skip/Replace-never-wipes-globals), the `_shared.yaml` bridge round-trip, and `planReconnect` (F62's account law — extracted to [reconnectPlan.ts](src/renderer/reconnectPlan.ts) precisely so it's harnessable). Run it after touching ruleIdentity/profileTransfer/profile shared-lists/reconnect logic. The older two (gitignored under `tmp-*/` because they embed verbatim corpus/player data; rebuild from each one's README/recipe if wiped): **`tmp-scene-harness`** (SceneParser capturers vs the real captured session — recipe in DESIGN §35.4) and **`tmp-cmd-harness`** (`node tmp-cmd-harness/run.mjs`) which bundles the real `macros.ts` and asserts the command-RESOLUTION core that feeds the send path — `resolveMacro` / `resolveAlias` / `parseCursorMarker` / `matchKeyCombo` (incl. the numpad mapping behind Morress's `Num+`→LOOK) / `interpolate` / `getMacroToken`. Neither harness reaches the React component send call itself (pitfall #86 — that's caught by re-reading the body); they lock the testable logic underneath. Run `tmp-cmd-harness` after touching macro/alias/cursor resolution or key-combo matching.

**Append harness cases ABOVE the summary — `tmp-rules-harness/test.ts` ends with `console.log(...)` followed by `process.exit(fail ? 1 : 0)`, so anything added after those lines NEVER RUNS and the count comes back unchanged.** That happened writing B279's guard: seven new cases reported 117 exactly as before, which reads as "already covered" rather than "never executed", and a FAILING case there would have been silently green. **The tell is a case count that does not move after you added cases** — check the insertion point before believing the number. More generally: a guard that cannot fail loudly is worse than no guard, because it is also a claim that the thing is tested.

Releases ship via `node publish.mjs` (needs `$env:GH_TOKEN`). Cleans `release/`, builds, **PRE-CREATES the draft release**, packages, uploads, verifies, patches release body from `release-notes.md`, prints the draft URL. **Do NOT create git tags manually** — the tag is only created when the draft is published (a pre-created DRAFT does not tag). The script **refuses to publish if release-notes.md doesn't mention the current version** and warns on a dirty/unpushed git tree. **The pre-create step is load-bearing (Sekmeht hit this):** electron-builder's GitHub publisher creates the release lazily on the FIRST artifact upload and uploads in PARALLEL — two uploads can race the creation, and GitHub allows duplicate drafts on one tag, splitting artifacts across TWO draft releases (the "blockmap in its own release" bug). Pre-creating makes every upload target one release; the script also post-verifies (deletes empty duplicate drafts, refuses on asset-bearing duplicates, and checks the exe/blockmap/latest.yml set is complete before you publish).

**Releases are cut from GitHub Actions (added 2026-07-26; full 3-OS matrix enabled for v0.18.0):** `.github/workflows/release.yml` is a **manual-only** workflow (Actions tab → Release → Run workflow — plain commits never build) that mirrors publish.mjs: a `prepare` job validates release-notes.md + pre-creates the ONE draft (`.github/scripts/release-prepare.mjs`), a per-OS `build` matrix (windows-latest NSIS / ubuntu-latest AppImage / macos-latest dmg+zip arm64 UNSIGNED — `CSC_IDENTITY_AUTO_DISCOVERY=false`) packages + uploads into it, and a `verify` job asserts the full artifact set + refreshes the notes (`.github/scripts/release-verify.mjs`). The draft/Publish/tag semantics are IDENTICAL to publish.mjs (tag only mints on Publish; never tag manually), and **publish.mjs remains the untouched local Windows-only fallback**. **`.github/scripts/check-workflow.mjs` now does this for you and CI RUNS IT on every push** (11 assertions; run it locally too after touching release.yml or the build config). **After ANY edit to release.yml, validate it SEMANTICALLY, not by parsing** — a YAML syntax check passes on a workflow whose env var has been orphaned into a shell script (pitfall #112, which cost a failed mac release and silently re-enabled keychain hunting). Assert the properties: the packaging step still carries `GH_TOKEN` AND `CSC_IDENTITY_AUTO_DISCOVERY`, no `run:` block contains an env-style `^[A-Z_]+:` line, and the signature step is still `macos-latest`-gated. Two lockstep rules: (a) a matrix entry and its `EXPECTED_PLATFORMS` token (verify job env) move TOGETHER — or verify passes on a release silently missing a platform; (b) if the expected artifact names change (artifactName config in package.json build.mac/build.linux, new targets), update `PLATFORM_ARTIFACTS` in release-verify.mjs AND the `expected` list in publish.mjs — they encode the same contract in two places. **The Linux artifact is deliberately VERSION-LESS — `Lichborne.AppImage` (B357, v0.19.7); don't "restore" the version for symmetry.** electron-updater's `AppImageUpdater.doInstall` replaces a version-less AppImage in place, but for a versioned one it deletes the running file and moves the update to the NEW versioned name — so every update broke the user's desktop shortcuts and dock favourites. **Top-level `desktopName: "lichborne.desktop"` plus `linux.syncDesktopName: true`** (B363) name the AppImage's `.desktop` file and give Electron's Linux app_id / WM_CLASS the same name, so running windows group under the launcher entry; both are ignored on Windows and macOS. When the Apple cert ever lands: add cert+notarytool secrets to the mac job, drop the CSC env, and un-gate the darwin updater in main.ts. `.github/workflows/ci.yml` separately runs Check zero (both builds + BOTH tsc configs) on every push/PR to main, PLUS `check-workflow.mjs`, on a **Windows + Ubuntu matrix** (`fail-fast: false`) since v0.19.7 (B365) — Ubuntu's case-sensitive filesystem catches a wrong-case import that Windows forgives, and it is the only place main-process code is compiled on Linux before a release; it never packages or touches releases. **`release-prepare.mjs` refuses a tag that is already PUBLISHED** (v0.18.2) — it only ever filtered for DRAFTS, so re-running Release without bumping package.json would mint a second draft on a released tag; it now names the published release and tells you to bump. `build/icon.png` (512, upscaled from the ico's 256 frame) feeds the mac/linux icons — replacing it with a crisp ≥512 source is an open polish item.

**Packaging rules (v0.15.0 hygiene pass):**
- **Check for platform updates periodically (a standing recommendation — do it at version boundaries or every few weeks, Sekmeht 2026-07-21).** Run **`npm outdated`** and read it in three tiers: **(1) within-major "Wanted" bumps** (e.g. electron 43.0→43.2, `@electron/rebuild`, `@types/node` patch, `react-virtuoso`) are low-risk security/bugfix updates — take them, then re-run Check zero (build:main + build:renderer + tsc) and a launch smoke; an Electron patch inside the SAME major keeps the bundled Node/Chrome line, so no esbuild-target/@types/node change and (same ABI) usually no rebuild, though `npx electron-rebuild -f -w better-sqlite3` is the safe habit. **(2) Held majors** — **React 19, Vite 8/`@vitejs/plugin-react` 6, TypeScript 7, better-sqlite3 13, `@types/node` 26** are deliberate, breaking migrations; do NOT bump them casually — each is its own dedicated effort with its own testing. **(3) An Electron MAJOR** (43→44…) is the FOUR-things-in-lockstep ritual below. Record the check + what you took in the version's notes so the next person sees the cadence. **Verified baseline (2026-09-15, v0.19.7): Electron 43.7.0 = Node 24.21.0 / Chrome 150 line / ABI 148; esbuild 0.28.2 + `node24` target + `@types/node 24.13.4` aligned; `dependencies` = `better-sqlite3` only.** The 43.4.1 → 43.7.0 bump again kept ABI 148, and a direct better-sqlite3 load plus a real query passed under the new runtime with no rebuild. Gotcha (a) below recurred: `npm install -D electron@43.7.0` also skipped the postinstall, so it isn't only `npm update`. Always check `dist/version` after touching electron. Both within-major Electron bumps so far (43.0→43.2, 43.2→43.4.1) kept ABI 148, so better-sqlite3 needed NO rebuild — verified each time by loading it under the new runtime and running a real query rather than by assuming. Prefer that check over a reflexive `electron-rebuild -f`: a forced rebuild of a working native module is itself a risk, and the load test tells you in seconds whether it is needed. **Two operational gotchas from the 2026-08-23 pass:** (a) `npm update electron` can swap the package WITHOUT running its postinstall — `node_modules/electron/dist/` is then empty and the next `npm start` dies unreadably; check `dist/version` after any electron bump and run `node node_modules/electron/install.js` if it's missing. (b) electron.exe is a GUI-subsystem binary, so its stdout may never reach a PowerShell pipeline even under `ELECTRON_RUN_AS_NODE` — have a diag script write its result to a file and read that, instead of trusting the console.
- **`dependencies` = TRUE runtime externals ONLY** (currently just `better-sqlite3`). Everything else is bundled by esbuild (main — see `external` in [build-main.mjs](build-main.mjs), the source of truth) or Vite (renderer) and belongs in `devDependencies` — a bundled package listed in `dependencies` ships a SECOND, dead copy in the asar. Adding a new npm package? devDependencies unless it's native/unbundleable, in which case also add it to esbuild's `external`.
- **Electron upgrades move FOUR things in lockstep:** `electron`, `better-sqlite3` (new ABI → may need its own major), esbuild `target` in build-main.mjs (match the bundled Node: `ELECTRON_RUN_AS_NODE=1 electron -p process.versions.node`), and `@types/node`. Then `npx electron-rebuild -f -w better-sqlite3`, and run the verification ladder: builds → tsc → launch smoke → a DIRECT better-sqlite3 in-Electron diag (it's lazy-loaded in [sqliteReader.ts](src/main/lichbridge/sqliteReader.ts), so a launch alone doesn't prove the ABI) → `npm run dist`. Pre-flight grep for the release-notes' breaking-changes APIs before starting.
- **`ELECTRON_RUN_AS_NODE` trap (cost a phantom bug hunt in the v0.15.0 upgrade):** with that env var set, electron.exe runs as plain Node — `require('electron')` then resolves the npm package (a path STRING; `ipcMain`/`app` undefined) and the app "crashes at startup" in a way that perfectly impersonates an Electron breaking change. In this dev environment the var can LEAK between shell invocations after a version probe — clear it in the SAME call as any electron launch (`Remove-Item Env:\ELECTRON_RUN_AS_NODE`), and when a fresh Electron "breaks" at `require('electron')`, check the environment before the code.
- **App icon:** `build/icon.ico` (multi-size) is auto-detected from buildResources — exe/installer/shortcuts need NO config. `app.setAppUserModelId('com.lichborne.app')` in main.ts is the matching taskbar identity; keep it equal to `build.appId`.

- **The repo is moving to the Elanthia-Online org — the updater checks BOTH homes (DESIGN §18.4.1).** `checkForUpdatesDualFeed()` ([main.ts](src/main/main.ts)) tries `elanthia-online/Lichborne` first and falls back to `SekmehtDR/Lichborne`; before the transfer the new-home probe fails silently (the `updaterProbing` flag suppresses the updater-log ERROR for non-final feeds — keep that gate). A feed that answers is authoritative, so never pre-create a partial repo at the org. At transfer time, follow §18.4.1's Phase-3 cut-over checklist (every hardcoded `SekmehtDR` URL — package.json publish, build/app-update.yml, publish.mjs, the Actions scripts' local fallbacks, Help-menu links, credits.ts, AboutModal, README, User Guide, DESIGN §18.2, release-notes) and pre-flight the org's Actions "Read and write" workflow permission. **After the transfer, never re-create a repo named `Lichborne` under SekmehtDR** — it severs GitHub's redirect, stranding exactly the pre-dual-feed installs that can't be patched remotely.
- **Per major version: rotate the Discord invite link — now in TWO places, keep them in sync.** The invite URL is hardcoded (a) in the native menu's **Help → Discord** item in `setupMenu()` ([main.ts](src/main/main.ts) — search `discord.gg`) and (b) as `DISCORD_URL` in [credits.ts](src/renderer/credits.ts) (the About modal's "Join our Discord" link, v0.17.0). For each major version, generate a fresh invite and update **both** so the link can't go stale/expire on shipped builds (and so we can tell which version a join came from). Update both literals, rebuild, and re-check the link opens from both the menu and the About dialog. (Main and renderer are separate processes, so they can't share one const without a shared-module refactor — hence the two literals.)
- **Keep the Help → About Lichborne credits current — inspect them every release.** As of v0.17.0 the About dialog is a **themed React modal** ([AboutModal.tsx](src/renderer/components/AboutModal.tsx) + [about.css](src/renderer/styles/about.css)), NOT the old native message box — opened via the menu-action bridge (`'about'` in [menuActions.ts](src/shared/menuActions.ts) → `runAppActionRef` in [App.tsx](src/renderer/App.tsx) → `setShowAbout`); the native menu item just `sendMenuAction('about')`s to the focused window. **Credits are the single source of truth in [credits.ts](src/renderer/credits.ts)** — three exports: **`DEVELOPERS`** (`['Sekmeht', 'Binu']`), **`CONTRIBUTORS`** (people who've filed logged reports/fixes), **`TESTERS`** (the rest of the pool — no logged reports yet) — plus `ABOUT_BLURB` + `REPO_URL` + `DISCORD_URL` (the "Join our Discord" link — mirror of the menu invite, see the Discord-rotation rule). **Rules:** (a) the version shown comes from `window.api.getAppVersion()` → main's `get-app-version` handler → `app.getVersion()` — DYNAMIC (reads `package.json`), auto-tracks every release; never hardcode it. (b) When a new person joins the pool, **add them to `TESTERS`**; promote them into `CONTRIBUTORS` once they've filed a logged bug/fix — this two-tier split is a first-class courtesy, not an afterthought (treat "is everyone credited in the right tier?" as part of the release check). (c) **Both lists are ordered by contribution volume** (count logged mentions in BUGS.md, combining aliases — see below); re-sort when the counts shift. (d) Identity notes to honor: **Rakkor ≡ TheTargonian** (list once as "Rakkor", counts combined); ****Illiahanna** is the renamed **JadedSoul** ≡ Jaded (one person — BUGS.md still uses the old handles, so count those toward Illiahanna)** (combined); **Elore ≡ Cherisse ≡ the renamed Aubrey** (ONE person, counts combined → 3rd, list as "Elore"); **Zithri ≡ ohbeanz** (the Discord handle — BUGS.md/Tracker log B238 under "ohbeanz", so count those toward Zithri); **Binu is a co-CREATOR** (in `DEVELOPERS`, never in a credits list). (e) It's now **themed** — style it via `about.css` with theme vars / `color-mix` and eyeball it on light themes (Principle #4); don't revert it to a native message box. When someone is promoted (tester → contributor → creator) or two handles turn out to be one person, reconcile `credits.ts` so nobody is double-listed, mis-tiered, or miscredited.
- **About easter-egg MIDI player (v0.17.0) — self-contained, zero deps.** The About modal can play a bundled `.mid` (drop it at `src/renderer/public/about-theme.mid`; Vite copies it to `dist/renderer/`, loaded via XHR arraybuffer — `fetch` of a file:// resource is unreliable in Electron). [midiPlayer.ts](src/renderer/midiPlayer.ts) is a hand-rolled Standard-MIDI-File parser (`parseMidi` → both extracted `notes` AND the raw timed `events`) + two `MidiPlayback`s: **`MidiOutPlayer`** routes the raw events to the OS synth via **Web MIDI** (the Microsoft GS Wavetable Synth — sounds like Windows Media Player; needs the `'midi'` permission, added to main's `setPermissionRequestHandler`), and **`MidiSynth`** is a Web-Audio oscillator FALLBACK when no MIDI output exists. `createMidiPlayer` picks the best. Muted by default (the AudioContext / MIDI access only spin up on the first un-mute — a user gesture). **Do NOT `import` a `.mid` via Vite** (a missing file breaks the build) — reference it by runtime path in `public/`. **`.mid` CANNOT go in an `<audio>` element** (Chromium has no MIDI synth) — that's the whole reason for this. **Two reusable gotchas this surfaced:** (1) **Web MIDI messages sent with future timestamps CANNOT be unsent** — so bounding audio on Mute/close needs a SHORT lookahead + suppressing only real note-ONs (velocity>0, keep note-offs flowing) + an All-Notes-Off sent BOTH immediately AND scheduled past the lookahead window (else a note drones after the About window closes). (2) **Never put side effects inside a `setState(prev => …)` updater** — StrictMode double-invokes updaters in dev; compute the next value from current state and do the side effect outside (the `toggleMusic` fix).

**Merging an EXTERNAL PR — the release ritual (first done for PR #2, v0.19.4).** Now that the project is moving to the Elanthia-Online org, outside PRs stop being a one-off. A contributed PR arrives as **source only** — none of the project's conventions come with it — so merging is a release-shaped job, not a `git merge`. In order:

1. **Verify the base before touching anything.** `gh pr view N` + `git merge-base --is-ancestor <main> <pr-head>`. A PR cut from an older main needs a rebase decision BEFORE the merge, not after.
2. **`git merge --no-ff`, never squash, for an outside contribution.** The commits and their reasoning are the contributor's record. **Check the AUTHOR field** (`git log --format='%an <%ae>'`): an AI-assisted PR is often authored `Claude <noreply@anthropic.com>`, so the HUMAN's name appears nowhere in git history — put it in the merge commit message explicitly, because that becomes the only place git records it.
3. **CREDIT THE AUTHOR IN `release-notes.md` BY NAME** (Sekmeht, 2026-09-02) — in the feature's own section, not a trailing thanks line. That file is the GitHub release body, i.e. the one place the community reads. Also promote a first-time code contributor into `CONTRIBUTORS` in [credits.ts](src/renderer/credits.ts) (the two-tier rule in the About-credits section), and title the Tracker entry with their name.
4. **Run the FIVE pre-merge checks against the merged result** — the contributor didn't. #5 (the slash-command surface) is the one most often missing: decide the `/command`, or record the "no command because X" decision in the feature's DESIGN section. Both outcomes are fine; skipping the question silently is not.
5. **Write the whole doc set yourself** — Tracker, DESIGN section, CLAUDE.md (a new subsystem is a non-trivial architectural change), Knowledge.md if the PR verified Lich/DR internals, the User Guide if it changes what a user does, README if a newcomer would care, BUGS.md for anything it fixed or exposed, and the version's test plan. **Verify the PR's claims against the real source rather than transcribing its description** — PR #2's Lich citations all held, and checking them surfaced one fact the PR never mentioned.
6. **Clear `(draft)` / `(WIP)` markers** from comments the PR shipped. It's a release now.
7. **Bump the version and commit the release work BEFORE pushing.** The push is what closes the PR (see below), so pushing the merge alone leaves `origin/main` briefly carrying the feature while declaring the previous version with the previous release notes.

**A PR closes on PUSH, by reachability — do NOT run `gh pr merge`.** GitHub marks a PR *Merged* when its head commit becomes reachable from the base branch on the remote, so a local `--no-ff` merge plus `git push origin main` closes it automatically and correctly. `gh pr merge` would merge work already merged. (Pushing is Sekmeht's — see Operational guardrails.)
- **Experience ⚙ layers can DEFAULT OFF via `defaultHidden` — but respect it in three places or they disagree (v0.17.0).** An `ExperienceOptionDef` may set `defaultHidden: true` (Moons countdowns/names — the orrery pill covers them). The `hidden` map stores only EXPLICIT choices, so the default applies when the key is absent, and it must be honored identically by: (a) the **component gating** (`hidden?.x === false` = shown-only-if-explicitly-on, matching `optionShown`), (b) the **⚙ checkbox** `checked={optionShown(hidden, opt)}` in BOTH ExperienceLayer (floating) and PanelFrame (tab), and (c) the **toggle**, which must store the EXPLICIT value (`onSetExperienceOption(expId, optId, hidden)`, not a blind `!hidden?.[optId]` flip) and SEED new instances with `defaultHiddenMap(def)` so the default PERSISTS to the profile. `optionShown` / `defaultHiddenMap` in [experiences.ts](src/renderer/experiences.ts) are the single source — use them, don't re-derive. (This is why a blind-flip toggle can't support a default-off layer: from the absent-key default, one flip would set `true` (hide) again, never showing it.)

---

## Documentation discipline

**"Update documentation"** means: **BUGS.md, DESIGN.md, Tracker.md, README.md, CLAUDE.md, the current version's test plan, AND — as needed — three CONDITIONAL docs: AINOTICE.md (whenever the change touched AI processing), Knowledge.md (whenever the change surfaced new VERIFIED Lich-internals / DR-protocol knowledge), and Lichborne-User-Guide.md (when terminology/feature-names/locations change — NOT for every fix)** (CLAUDE.md added to the list 2026-05-17, the per-version test plan 2026-07-10, AINOTICE.md + Knowledge.md + Lichborne-User-Guide.md all 2026-07-23, all at Sekmeht's ask).

**The two conditional docs — ALWAYS ASSESS both, update only when they apply:** AINOTICE.md is user-facing AI privacy; Knowledge.md is the internal Lich/DR knowledge base. **Knowledge.md is where a hard-won, VERIFIED fact about how Lich or DragonRealms *actually works* gets recorded so it isn't re-derived** — the Marshal reference-table registration rules (B202/#91), a Lich downstream-hook that rewrites the stream (`inventory_boxes_off` / `textsubs`, #80), how Lich rewrites the room-id subtitle (`display_uid`/`display_lichid`, #65), a `drinfomon` parse rule or verified game constant we mirror (the `drbanking.rb` coin ratios, the `xmlparser.rb` injury/scar/assess encodings, the combat ladder), or a new Lich-version compatibility finding (5.18/5.19). It captures the LICH/DR *fact* (what the source does, with a `file:line` citation), NOT the Lichborne implementation (that's CLAUDE.md/DESIGN). When a bug-fix or feature is grounded in reading Lich's source or DR's protocol, ask "did I learn a durable Lich/DR fact worth adding to Knowledge.md?" — the pitfall goes in CLAUDE.md, the underlying Lich fact goes in Knowledge.md. Skip it (n/a) when nothing new about Lich/DR internals was learned.

**AINOTICE.md is the AI-processing & privacy notice (user-facing).** It is the one place that explains how Lichborne uses AI, what data goes to the LLM provider, and — critically — that **PII / credentials are NOT sent** (the redaction guardrail: `redactForAI` in [src/shared/redact.ts](src/shared/redact.ts) scrubs the account PIN output, credentials, and account username from the AI copy while the log on disk stays pristine). **When you update documentation, ASSESS whether AINOTICE.md needs it — and it does whenever the change affects: what AI features exist, what data an AI feature reads or sends, when/where it sends, the redaction rules, key storage, or the AI-never-issues-commands guardrail.** ONLY AI-related processing/privacy information belongs in AINOTICE.md — not general features. A new AI feature (or a change to an existing one's data flow) is not "done" until AINOTICE.md reflects it, same bar as the slash-command registry (Principle #11).

| File | Role | When to update |
|---|---|---|
| **CLAUDE.md** (this) | AI-facing guide. Behavior, conventions, pitfalls. | When a subsystem's *shape* changes, when a new pattern emerges, when a pitfall is discovered. Keep tight. |
| **DESIGN.md** | Technical spec, source of truth for *how* things work. | *Before* implementing significant UI / architecture changes. It's a spec, not a journal. |
| **Tracker.md** | Version history. Brief why/how per release. | When a release ships. Reverse-chronological. |
| **README.md** | **The repo's front door** — the first thing a stranger sees. Answers three questions and stops: *what is it*, *why is it different*, *what does it bring me*. Friendly, skimmable, honest — **never salesy or hype-driven**. Getting started is **download a release**, not build from source. | When something a NEWCOMER would want to know changes. Most releases need no edit — feature detail belongs in the User Guide. |
| **BUGS.md** | Tester-reported bug ledger (B-numbers). | Open / Resolved tables, with full root-cause notes on resolution. |
| **AINOTICE.md** | User-facing AI-processing & privacy notice. **Only AI data-flow / privacy content** (what's sent, redaction, keys, guardrails). | Whenever a change touches AI processing: a new/changed AI feature, what data it reads/sends, redaction rules, key storage, or the never-issues-commands guardrail. Committed (unlike the test plan). |
| **Knowledge.md** | Internal **Lich / DragonRealms knowledge base**. Verified facts about how Lich5 and DR *actually work* — `lich.db3` schema, Marshal format, feature flags, connection/frontend modes, `drinfomon` parse rules & game constants, and a **Lich version log** (what changed in each Lich release, what it means for Lichborne, and why any fix matters). Cite `file:line` in the source. NOT Lichborne implementation (that's CLAUDE.md/DESIGN). | As needed — whenever a fix/feature surfaced a new VERIFIED Lich/DR fact (a Marshal/hook/room-id/parse detail), OR a new Lich version was reviewed (record the delta + tester impact). Committed. |
| **Lichborne-User-Guide.md** | User-facing **primer / guide** — what Lichborne is, getting started, connecting, a feature tour (what each does + how to configure at a high level), the **With-Lich vs Without-Lich feature matrix**, and appendices (slash reference, Lich-vs-direct, where settings live, troubleshooting). Written for a total newcomer; friendly, concise, GitHub-hosted with internal anchor links. **NOT a changelog** (Sekmeht 2026-07-23: *"we don't need to capture a running list of updates"*). | Do NOT log every fix/feature. DO update when: **terminology changes**, a **feature is renamed**, or something is **retired / moved to a new location** — keep the evergreen tour accurate. Two snapshot sections ARE swapped per release (not appended): "What's New in the Latest Version" (mirrors release-notes' current version) and "On the Horizon (Roadmap)". Committed. |
| **testplans/test-plan-v{version}.md** | The version's manual verification checklist — every feature/fix that lands gets its Do → Expect items the same day it lands, so the pre-packaging feel pass is never reconstructed from memory. **Gitignored (Sekmeht, 2026-07-10) — machine-local like the `tmp-*` harnesses it feeds, not committed.** | Grows WITH the version (each feature/fix adds its items when it ships, not at the end); run before packaging; a NEW file is started when a new version begins (the old one stays as the local record of what was verified). Ordered by risk. Items whose PURE logic is harness-locked carry an `[auto]` tag; the rest is the manual pass. Mine pure-logic items into the `tmp-*` harnesses — the plan doubles as the harness-case backlog. |
| **release-notes.md** | GitHub release body. User-readable changelog. | Separate from Tracker.md (dev-facing). **Holds ONLY the version currently in development — nothing else, ever** (Sekmeht, 2026-07-10; re-stated 2026-09-06 after I drifted twice in one session). When you bump the version, **REPLACE the file's whole contents** with the new version's section — do not prepend to what is there, which is the drift that keeps happening because prepending *looks* like the tidy thing to do. No running trail: the full history lives in Tracker.md and in the published GitHub releases. **Do NOT try to infer whether a past version shipped** — local tags are stale/partial and the file's own prose is not evidence (v0.19.6 was nearly written to re-announce the whole Spell Monitor because a leftover line claimed v0.19.4 "was never published"; both had in fact shipped). If it is already in the file from a previous version, it has shipped: cut it. If you genuinely need to know, ASK. |

**Writing README.md (the front door, standards set 2026-07-30 by Sekmeht).** It is read by someone who has just landed on the repo and does not yet know what this is. That audience sets every rule below.

- **Three questions, in order: what is it · why is it different · what does it bring me.** Then how to get it. Nothing else earns space above the fold.
- **Skimmable beats complete.** A bullet is one or two sentences. If a feature needs a paragraph to explain, that paragraph belongs in [Lichborne-User-Guide.md](Lichborne-User-Guide.md) and the README gets one line plus a link. (The pre-v0.18.3 README had single bullets over a thousand words long — exhaustively accurate and completely unreadable. Length was the bug.)
- **Not salesy, not hype.** State what it does and let it stand. No superlatives, no "revolutionary", no exclamation marks stacked on features. Being honest about alpha status, platform betas and known limits is part of the tone, not a caveat bolted on the end.
- **Getting started = DOWNLOAD A RELEASE.** Almost nobody reading this wants to compile anything. Build commands, prerequisites, project structure and the publish flow are DEVELOPER material and do not belong in the main flow — they live in this file (Commands / Packaging) and [DESIGN.md](DESIGN.md). A short pointer at the very bottom for would-be contributors is the most the README should carry.
- **Link, don't duplicate.** The User Guide covers features in depth, AINOTICE.md covers AI privacy, Knowledge.md covers Lich/DR internals. The README's job is to get someone to the right door, not to be all of them.
- **Screenshots earn their place** — a client is a visual product, and one good image says more than a paragraph of prose about panels.

---

## Operational guardrails

- **Never `git commit` without explicit user approval, and NEVER `git push` at all.** Asking before a commit is mandatory. **Pushing is Sekmeht's, always** (2026-07-28: *"Unless specifically asked, I'll be manually pushing to the repo"*) — do it only if he asks in that message, and don't offer it as the obvious next step after committing. The asymmetry is deliberate: a commit is local and reversible, whereas a push publishes the work AND is what a release run checks out, so the timing is his call. There are no git hooks in this repo (verified 2026-07-28), so a commit genuinely stays local. Committing straight to `main` IS the convention here — don't branch unless asked. Report a commit as "committed, unpushed" and stop.
- **Windows + PowerShell** is the host shell. Use PowerShell syntax (`$null`, `$env:VAR`). Bash available for POSIX scripts.
- `profiles/` is gitignored; never commit user data captured during testing.
- `passwords.json` lives in `app.getPath('userData')`, encrypted with Electron `safeStorage` (Windows DPAPI). Don't move it into the repo tree.
- **Every source file opens with an ORIENTATION HEADER — and a new file ships with one (v0.19.2 comments pass, for the community handover).** All 171 `.ts/.tsx` files under `src/` carry a top-of-file `//` block in one voice: first line `// <Subject> — <one-line role> (version / DESIGN § where the file's own comments state them)`, a blank `//`, then 4–15 lines (longer only for the hubs — main.ts, GameWindow, StormFrontParser) on what the file OWNS, how it CONNECTS (who calls it, what it feeds), and the INVARIANTS a developer must not break, pointing INTO the file's own `── section ──` dividers rather than duplicating them. Read [overviewStore.ts](src/renderer/overviewStore.ts), [main.ts](src/main/main.ts) and [StormFrontParser.ts](src/main/parser/StormFrontParser.ts) for the reference voice. **The accuracy bar is the whole point:** every claim must be verifiable from the file's code or its existing inline comments; hoist a pitfall #N / DESIGN §N / B-number only where the file or the project docs already state it; when unsure, say LESS — a wrong orientation comment in public code is worse than none (pitfall #126 was literally a stale comment). Eleven files keep their block BELOW the imports (AboutModal, AIConsentModal, AppBar, ExperienceLayer, ExperienceShelf, FloatingWindow, LichSetupDialog, QuickSend, ResizeDivider, SimuCoinButton, WindowLayer) — a valid placement; don't add a second block above them. Inventory check: a file whose line 1 is not `//`/`/*` AND isn't one of those eleven is missing its header.
- Prefer fixing root causes over adding fallbacks.
- **When proposing a feature, check the principles first.** If it duplicates Lich functionality, say so and ask. If it lives in the display/configuration/profile space, propose freely.
- **Native menu & hotkeys — "never touch" rules (PERMANENT).** These protect a consistent, Windows-native experience; treat them as invariants, not redesign-phase rules:
  - **Never override an Electron-reserved chord with a Lichborne action.** The built-in **role** accelerators stay exactly as-is — don't rebind them and never assign any of them to a Lichborne feature: `Ctrl+C/V/X/Z/Y/A` (clipboard / undo / redo / select-all), `Ctrl+R` & `Ctrl+Shift+R` (reload), `Ctrl+0` / `Ctrl+±` (zoom), `Ctrl+M` (minimize), `Ctrl+W` (close), `Ctrl+Q` (quit), `F11` (fullscreen), `Ctrl+Shift+I` / `F12` (devtools). Overriding any of these makes Lichborne behave unlike every other Windows app.
  - **Never touch the pre-existing in-app Lichborne chords.** `Ctrl+Shift+Enter` (QuickSend), `Ctrl+Tab` (next character), `Ctrl+1–9` (jump to character N, 1-indexed — `Ctrl+1` = first char) are handled in [App.tsx](src/renderer/App.tsx) and must keep working unchanged.
- **Native menu — hotkey policy (PERMANENT).** `setupMenu()` in [main.ts](src/main/main.ts) is the full File·Edit·View·Tools·Lich·Window·Help tree (shipped v0.10.0; Window menu gained the Move-to-New/Main-Window decouple items in v0.11.0). Policy: **(a) do NOT assign keyboard accelerators to native menu items** — Lichborne items are **click-only**; the final hotkey set is still deferred until agreed (don't invent shortcuts). **(b)** Where a native menu item maps to one of the pre-existing in-app chords above, *display* it as a hint via `registerAccelerator: false` (so App.tsx stays the handler — no double-fire) **unless** the chord is Electron-reserved. Lichborne menu clicks route through the `menu-action` IPC → `lichborne:session-action` DOM-event bridge ([src/shared/menuActions.ts](src/shared/menuActions.ts)); `sendMenuAction` targets the **FOCUSED** window — the active GameWindow there handles session actions, App handles app-level ones.
